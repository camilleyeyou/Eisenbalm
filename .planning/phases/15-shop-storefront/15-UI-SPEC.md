---
phase: 15
slug: shop-storefront
status: draft
shadcn_initialized: true
preset: "existing — components.json from Phase 2; shadcn Button + Tooltip primitives already installed"
created: 2026-05-28
---

# Phase 15 — UI Design Contract: Shop Storefront — Rich Product Page

> Visual and interaction contract for the long-scroll `/shop` product page.
> Generated by gsd-ui-researcher. Consumed by gsd-planner and gsd-executor verbatim.

---

## Voice Register Guardrails (executor: read this first)

**On `/shop`, copy uses the lip balm's "Stop. Breathe. Balm." register.**

Rules for every copy element on this page:
- Short declarative sentences. No subordinate clauses.
- Meditative, deliberate, slightly ceremonial. Not hushed-spa. Not clinical. Not tech-marketing.
- Do NOT use the Dispatch's dry-charity-editorial voice here (that voice belongs to `/issue/*`).
- Do NOT mention AI in body copy. The tagline does it exactly once, intentionally. That's enough.
- Do NOT use exclamation marks, superlatives ("most luxurious"), or competitive framing ("unlike other lip balms").
- "Release 001" is a BRAND FACT (a designation, like a product edition). It is NOT a scarcity claim, urgency mechanic, or "limited edition" signal. Never write "limited," "only X left," "act now," or similar.

**Pattern-match examples in the correct register:**

```
CORRECT: "Petrolatum-free. No synthetics. No parabens. No petroleum derivatives."
CORRECT: "One product. One weekly charity. One hundred percent of proceeds."
CORRECT: "Applied once. Lasts."
WRONG:   "Our premium formula gives you the best lip protection available!"
WRONG:   "Hurry — limited quantities of Release 001 remain."
WRONG:   "Powered by advanced beeswax technology for lips that glow."
```

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn (existing) | Phase 2 `components.json`; Button + Tooltip installed |
| Preset | Existing install — no re-init needed | Phase 2 decision |
| Component library | Radix (via shadcn Button primitive) | Inherited |
| Icon library | lucide-react ^1.14.0 (pinned) | Phase 2 |
| Fonts | Cormorant Garamond (display) + Lora (body) + Inter (UI) | Phase 10 FONT_WHITELIST — LOCKED |

**No new fonts. No new npm dependencies. No CDN scripts.**
FONT_WHITELIST in `apps/web/lib/theme.ts` is byte-unchanged.

---

## Spacing Scale

All values are multiples of 4. Inherited from the project-wide 8-point grid.

| Token | Value | Usage on /shop |
|-------|-------|----------------|
| xs | 4px | Inline tag/pill gaps |
| sm | 8px | Feature-cluster internal padding |
| md | 16px | Paragraph spacing, label-to-headline gap |
| lg | 24px | Feature card padding |
| xl | 32px | Section-internal spacing |
| 2xl | 48px | Ornament-divider margin-block (`.ornament-divider` already defines `margin-block: 2.5rem`) |
| 3xl | 64px | Section top/bottom padding |
| 4xl | 96px | Hero top padding |

Exceptions:
- Hero section top padding: 96px desktop / 64px mobile (intentional magazine-scale breathing room).
- Touch-target minimum: 44px height on all interactive elements (enforced — not an exception, a hard floor).
- `.ornament-divider` margin-block is already 2.5rem (40px) — leave as defined in globals.css.

---

## Typography

All sizes reference existing CSS variables from Phase 10. No new sizes are introduced.

### Canonical 4-Tier System

There are EXACTLY 4 type-size tiers on this page. No text element may use a size outside this system.

| Tier | Name | Size | Weight(s) | Line Height | Font | When to use |
|------|------|------|-----------|-------------|------|-------------|
| T1 | Label / UI | `12px` | 500 (button), 600 (eyebrow) | 1.5 | Inter (`--font-ui`) | `.eyebrow` labels, UI button text, chevron size |
| T2 | Secondary | `16px` | 400, 600 | 1.4–1.65 | Lora (`--font-body`) | FAQ questions (600), FAQ answers (400), feature card body, edition sub-notes, footer outro prose, any secondary text that needs quieter weight — use `--color-text-mute` for tone, not a new size |
| T3 | Body | `18px` | 400 | 1.65 | Lora (`--font-body`) | All prose blocks (positioning paragraph, ingredient story, charity supporting line) |
| T4 | Display | `clamp(28px, 4vw, 72px)` | 400 | 1.1–1.3 | Cormorant Garamond (`--font-display`) | Hero h1 (`Stop. Breathe. Balm.`), hero sub-tagline (italic, upper midrange of scale), section headlines, product name (`#shop-buy` h2), price/edition callout, dynamic charity line, `#shop-features` card headlines |

**Tier 4 detail — how elements share one clamp scale:**
The `clamp(28px, 4vw, 72px)` formula is a single continuous scale. Visually different display levels are achieved through a single property change — not separate size values:
- Hero h1 (`Stop. Breathe. Balm.`): `clamp(28px, 4vw, 72px)` — sits at the upper bound at wide viewports
- Hero sub-tagline: `clamp(28px, 4vw, 72px)` at a narrower container width (`max-w-xl`) + italic — visually smaller by context, not by a different clamp
- Section headlines (e.g., `The formula.`): `font-display text-[clamp(28px,4vw,72px)]` — used inside `prose-measure` (68ch), which naturally constrains the visual scale relative to the full-bleed h1
- Product name / charity line / price callout: same tier, same class; size variation comes from container width, not a new value

**Forbidden:** Do not introduce `clamp(22px,2.8vw,32px)`, `clamp(24px,3.5vw,36px)`, `clamp(28px,4vw,42px)`, standalone `22px`, standalone `14px`, standalone `28px` as a separate value, or any other size outside the 4 tiers above. All previously specced display variants merge into T4.

**Typography contract:**
- Exactly 4 declared size tiers in use: T1 `12px`, T2 `16px`, T3 `18px`, T4 `clamp(28px,4vw,72px)`.
- Exactly 2 weights per face: display 400 only; body 400 + 600 (FAQ questions); UI 500 + 600.
- `.prose-measure` (max-width: 68ch, margins auto) wraps ALL body prose blocks on this page.
- `.drop-cap` is applied to `#shop-positioning` (the first body paragraph after the hero) — the drop-cap reads as the opening of the product's story, parallel to the lead-section drop cap on issue pages.
- `.ornament-divider` (❦ FLEURON) appears between: `#shop-positioning` → `#shop-features`, `#shop-features` → `#shop-ingredient-story`, `#shop-ingredient-story` → `#shop-charity`, `#shop-charity` → `#shop-buy`, `#shop-buy` → `#shop-faq`.
- `.eyebrow` appears on every section label (above each section headline).

---

## Color

All tokens reference `--color-*` CSS variables from `apps/web/app/globals.css :root` (Phase 14 warm-paper light palette). **No hardcoded hex values anywhere on this page.**

| Role | Token | Hex (Phase 14) | Usage on /shop |
|------|-------|----------------|----------------|
| Dominant surface (60%) | `--color-bg` | `#FAFAF8` | Page background |
| Secondary surface (30%) | `--color-surface` / `--color-card` | `#F2EFE9` / `#EDE9E1` | Feature cards, FAQ panel, ingredient-story panel |
| Accent — decorative (10%) | `--color-primary` | `#CDA434` | Drop cap, ornament-divider color, feature card top-border accent, `.eyebrow` tint |
| Text — primary | `--color-text` | `#1A1A1A` | All body text, headlines |
| Text — secondary | `--color-text-dim` | `#595047` | Sub-tagline, caption-level text, FAQ answer text |
| Text — muted | `--color-text-mute` | `#706860` | `.eyebrow` (opacity 0.6 already baked in class) |
| Gold as text (AA-safe) | `--color-primary-text` | `#7A5C0E` | Any gold text at normal body size (5.97:1 on `#FAFAF8`) |
| Rust as text (AA-safe) | `--color-accent-text` | `#9B3015` | Any rust/accent text at normal body size |
| Hairlines | `--color-line` / `--color-line-strong` | `color-mix(...)` | Borders between feature cards, FAQ dividers |

**Accent (`--color-primary`) is reserved for:**
- `.drop-cap` first letter (via the existing `.drop-cap` utility)
- `.ornament-divider` color (opacity 0.5 via utility)
- Feature card top-border accent stripe (2px top border on each feature card)
- `.eyebrow` label color (via the class — opacity 0.6)
- The edition badge (`Release 001`) border/accent treatment

**Accent is NOT used for:**
- Button backgrounds (the BuyButton uses the shadcn Button primitive with `--primary` → `--color-primary` for background fill — which is gold-as-fill, decorative, acceptable)
- Body text (use `--color-primary-text` for any gold at text size)

**WCAG AA contract (enforced by Phase 14 `theme-aa-tones.test.ts`):**
- All body text: `--color-text` (#1A1A1A) on `--color-bg` (#FAFAF8) — 17.3:1, AAA
- `.eyebrow` at opacity 0.6: rendered `~#6B6B6B` on `#FAFAF8` — passes AA (4.81:1)
- `--color-text-dim` on `--color-bg` — 7.55:1, AAA
- Gold text: ONLY via `--color-primary-text` (#7A5C0E, 5.97:1), never raw `--color-primary`

**Arbitrary-value CSS variable pattern:** When applying `--color-surface` (or any CSS variable) as a Tailwind background class, use the intentional codebase pattern `bg-[color:var(--color-surface)]`. Do NOT substitute a semantic class like `bg-surface` — even if an `@theme` alias exists, the arbitrary-value form is the established project pattern and the executor must match it exactly.

---

## Copywriting Contract

| Element | Copy | Status |
|---------|------|--------|
| Page `<title>` | `Shop — The Eisenbalm Dispatch` | Locked |
| Page meta description | `Jesse A. Eisenbalm lip balm. 100% of proceeds go to the featured charity.` | Locked (from Phase 8) |
| Primary CTA label | `Buy the lip balm` | Locked (BuyButton text; byte-unchanged) |
| Hero tagline (h1) | `Stop. Breathe. Balm.` | Locked verbatim |
| Hero sub-tagline | `A human-only ritual for an AI-everywhere world.` | Locked verbatim |
| Price display | `$8.99` | TODO(Andrew): confirm final price before launch |
| Edition designation | `Release 001 · hand-numbered` | TODO(Andrew): confirm edition number before launch |
| Positioning headline | `The formula.` | Assumption (editorial call — short, declarative) |
| Positioning paragraph (body) | See `#shop-positioning` section below for full draft copy | Draft in voice |
| Feature 1 label | `BEESWAX FORMULA` | Locked |
| Feature 1 body | `Premium beeswax base. Petrolatum-free. No synthetics, no parabens, no petroleum derivatives.` | Draft |
| Feature 2 label | `100% TO CHARITY` | Locked |
| Feature 2 body | `Every dollar from every sale goes to the week's featured charity. No overhead. No administrative cut.` | Draft |
| Feature 3 label | `RELEASE 001` | Locked; TODO(Andrew): confirm edition number |
| Feature 3 body | `Hand-numbered. The first edition. Each tube is marked at the manufacturing step.` | Draft; TODO(Andrew): confirm hand-numbering process |
| Ingredient story headline | `What's in it. What isn't.` | Assumption (editorial call) |
| Ingredient story body | See `#shop-ingredient-story` section below | Draft |
| Charity callout intro | `This week's proceeds benefit [charityName].` | Dynamic from Sanity; fallback: `Proceeds go to our featured charity each week.` |
| Charity callout body | `One product. One weekly charity. One hundred percent of proceeds.` | Locked (below the dynamic line) |
| Product section eyebrow | `THE BALM` | Assumption |
| Buy section sub-copy | `$8.99 · Release 001 · ships continental US` | TODO(Andrew): confirm price + edition + shipping details |
| FAQ section headline | `Questions.` | Assumption (declarative, fits register) |
| FAQ empty state (no charity loaded) | `Proceeds go to our featured charity each week.` | Locked (Phase 8 fallback) |
| Footer CTA outro | `One product. This week's charity needs it.` | Assumption — or omit if Andrew prefers; see `#shop-footer-cta` — TODO(Andrew): voice-check this line — "needs it" leans persuasive; consider a more neutral close |
| Error state (checkout fails) | `console.error` only — no visible error copy per BuyButton contract (voice rules: no toast/modal/banner) | Locked (BuyButton behavior byte-unchanged) |

**Draft copy for `#shop-positioning` paragraph:**

> Jesse A. Eisenbalm lip balm is a professional-grade formula. Beeswax base. No petrolatum. No synthetic emollients. No parabens. No petroleum derivatives.
>
> It was designed to address transepidermal water loss — the mechanism by which lips lose moisture — rather than to coat the surface and create dependency. One application. Lasting effect.

*This is the positioning paragraph. It should carry a `.drop-cap` on the first `<p>`. Mark TODO(Andrew) if final ingredient list differs.*

**Draft copy for `#shop-ingredient-story` paragraph:**

> The formula contains: beeswax, shea butter, vitamin E (tocopherol), and natural flavor. That is the complete ingredient list.
>
> What it does not contain: petrolatum (petroleum jelly), parabens, synthetic fragrance, mineral oil, or dimethicone. These omissions are deliberate. The goal was a formula a professional would use, not one a marketing department would describe.

*Mark TODO(Andrew): verify against actual manufacturer spec sheet before launch.*

---

## /shop/faq Route Decision

**Ruling: FAQ is INLINE on `/shop`. No separate `/shop/faq` route.**

Rationale: Six FAQ items fit comfortably inline using semantic `<details>`/`<summary>` (zero-JS progressive enhancement, matching Phase 2's deliberation-accordion pattern). The content does not exceed inline practicality. A separate route would create a navigation dead-end and fragment the product story. If FAQ content grows beyond ~10 items in a future phase, promote to `/shop/faq` at that time.

**FAQ Questions and Answers (in voice; 6 items):**

| # | Question | Answer |
|---|----------|--------|
| 1 | What is Jesse A. Eisenbalm? | A lip balm. Professional-grade, beeswax-based, petrolatum-free. Made for people who take their skin barrier seriously. |
| 2 | Where does the money go? | One hundred percent of proceeds from every sale go directly to the week's featured charity on The Eisenbalm Dispatch. No overhead. No administrative percentage. The full amount transfers. |
| 3 | What is Release 001? | The first edition of Jesse A. Eisenbalm lip balm. Each tube is hand-numbered at the manufacturing step. Release 001 is a designation — a marker of where this product began. TODO(Andrew): confirm hand-numbering process description. |
| 4 | What does petrolatum-free mean? | Petrolatum (petroleum jelly) creates an occlusive surface barrier — it seals in existing moisture but does not add moisture or support the skin's own function. The Jesse A. Eisenbalm formula uses beeswax and shea butter instead, which provide protection while allowing the skin barrier to function normally. |
| 5 | What is the shipping policy? | Ships flat-rate within the continental United States. TODO(Andrew): add shipping rates, carrier, and estimated delivery window before launch. |
| 6 | How do I contact you? | Email TODO(Andrew): insert contact email address. |

---

## Full Section-by-Section Page Outline

The page is a single `<main>` element containing these sections in order, top to bottom. All sections are within the existing warm-paper light theme. No per-section background changes except where noted.

---

### Section 1: `#shop-hero`

**Purpose:** First impression. The tagline is the hero. No image until Andrew uploads one.

**Layout:**
- Full-width container, `max-width: 1340px`, horizontal padding `px-4 md:px-10`.
- Vertical padding: `pt-24 pb-16` desktop / `pt-16 pb-12` mobile.
- Single column, centered.
- No background change from `--color-bg` (warm paper).

**Visual treatment:**
- Type-only hero. NO image placeholder in initial build — the tagline is the visual. A `TODO(Andrew): upload hero product image` comment in the JSX marks where an image slot will be inserted when photography is ready.
- When Andrew provides photography: a `next/image` slot goes here at `max-width: 760px`, aspect ratio `4:3`, centered above the tagline. Alt text template: `TODO(Andrew): descriptive alt text for hero product image`.

**Copy elements:**

| Element | Tag | Copy | Typography |
|---------|-----|------|------------|
| Eyebrow | `<p className="eyebrow">` | `Jesse A. Eisenbalm` | T1 — Inter 12px/600/uppercase |
| Primary tagline (h1) | `<h1>` | `Stop. Breathe. Balm.` | T4 — `font-display clamp(28px,4vw,72px)`, weight 400, lh 1.1, `--color-text` |
| Sub-tagline | `<p>` | `A human-only ritual for an AI-everywhere world.` | T4 — `font-display clamp(28px,4vw,72px)`, italic, weight 400, lh 1.3, `--color-text-dim`; sits inside `max-w-xl` wrapper so the vw multiplier resolves to a visually smaller reading size than the full-bleed h1 |
| First BuyButton | `<BuyButton />` | `Buy the lip balm` | BuyButton component — byte-unchanged. Wrap in `<div className="mt-8">` if top spacing is needed; do NOT modify the component source or pass spacing props. |
| Price context | `<p>` | `$8.99 — TODO(Andrew): confirm` | T2 — `font-body` 16px/400/1.5, `--color-text-mute`; immediately below BuyButton wrapper |

**Token usage:** `--color-text` (h1), `--color-text-dim` (sub-tagline), `--color-text-mute` (price line), `--color-primary` (`.eyebrow` tint via class).

**Image slot spec:**
- Location: `#shop-hero`
- Subject: Hero product shot (tube of lip balm, hero composition)
- Aspect ratio: `4:3` (landscape hero)
- Max-width class: `max-w-[760px]` centered
- Alt text template: `Jesse A. Eisenbalm lip balm — Release 001`
- Marker: `{/* TODO(Andrew): replace this comment with next/image when hero photography is ready */}`

---

### Section 2: `#shop-positioning`

**Purpose:** Short brand positioning paragraph. What the product IS, in the product's own voice.

**Layout:**
- Single column, centered, `prose-measure` width (68ch max).
- Vertical padding: `py-16`.
- Background: `--color-bg`.

**Copy elements:**

| Element | Tag | Copy | Typography |
|---------|-----|------|------------|
| Section eyebrow | `<p className="eyebrow">` | `The formula.` | T1 — `.eyebrow` |
| Positioning paragraphs | `<div className="drop-cap prose-measure">` containing 2 `<p>` | See Copywriting Contract above | T3 — `font-body` 18px/400/1.65; `.drop-cap` on outer div; `.prose-measure` width constraint |

**Drop cap:** YES — `.drop-cap` wraps the first `<p>` of this section. The drop cap letter is in Cormorant Garamond at 3.5em, `--color-primary` (gold-as-decoration, not text — the utility defines this already).

**Ornament divider:** `.ornament-divider` `<div>` appears BELOW this section (before `#shop-features`).

**Token usage:** `--color-text` (body), `--color-primary` (drop cap via utility).

---

### Section 3: `#shop-features`

**Purpose:** Three-column feature cluster — the three brand pillars at a glance.

**Layout:**
- Three columns on ≥ 768px. Single column (stacked) on < 768px.
- Max-width: `1040px`, centered with auto margins.
- Vertical padding: `py-16`.
- Each card: `background: --color-card` (`#EDE9E1`), `border: 1px solid --color-line`, `border-top: 2px solid --color-primary` (gold accent top stripe — decorative, NEVER confusable with urgency).
- Card padding: `p-6` (24px).

**Per-card structure:**

| Feature | Eyebrow label | Headline | Body copy |
|---------|--------------|----------|-----------|
| 1 | `BEESWAX FORMULA` | `The base.` | `Premium beeswax. Petrolatum-free. No synthetics, no parabens, no petroleum derivatives.` |
| 2 | `100% TO CHARITY` | `The cause.` | `Every dollar from every sale goes to this week's featured charity. No overhead. No administrative cut.` |
| 3 | `RELEASE 001` | `The edition.` | `Hand-numbered. The first edition. Each tube is marked at the manufacturing step. TODO(Andrew): confirm.` |

**Per-card typography:**
- Eyebrow label: T1 — `.eyebrow` Inter 12px/600/uppercase/`--color-text`/opacity 0.6
- Headline: T4 — `font-display clamp(28px,4vw,72px)` weight 400 lh 1.2 `--color-text` (inside `p-6` card, the clamp resolves small — this is correct; do not override with a fixed size)
- Body: T2 — `font-body` 16px/400/1.65/`--color-text-dim`

**Ornament divider:** `.ornament-divider` appears BELOW this section (before `#shop-ingredient-story`).

**Token usage:** `--color-card` (card bg), `--color-line` (card border), `--color-primary` (top-border accent strip — 2px, decorative), `--color-text`, `--color-text-dim`.

---

### Section 4: `#shop-ingredient-story`

**Purpose:** Expanded ingredient transparency — what's in it and what isn't. The most editorial section.

**Layout:**
- Single column, `prose-measure` (68ch), centered.
- Vertical padding: `py-16`.
- Background: `--color-surface` (`#F2EFE9`) — a subtle warm-paper surface shift to visually separate this section from the card section before and after it. Use `w-full bg-[color:var(--color-surface)]` outer wrapper with inner `.prose-measure` container.

  **Pattern note:** `bg-[color:var(--color-surface)]` is the intentional codebase pattern — a Tailwind arbitrary value referencing a CSS variable. Do NOT switch to `bg-surface` or any other form. Match this exact class string.

**Copy elements:**

| Element | Tag | Copy | Typography |
|---------|-----|------|------------|
| Section eyebrow | `<p className="eyebrow">` | `What's in it. What isn't.` | T1 — `.eyebrow` |
| Ingredient paragraphs | `<div className="prose-measure">` with 2 `<p>` | See Copywriting Contract above | T3 — `font-body` 18px/400/1.65; NO drop cap (drop cap is reserved for `#shop-positioning`) |

**Note:** Mark `{/* TODO(Andrew): verify ingredient list against manufacturer spec sheet before launch */}` as a JSX comment above the ingredient copy.

**Ornament divider:** `.ornament-divider` appears BELOW this section (before `#shop-charity`).

**Token usage:** `--color-surface` (section bg via `bg-[color:var(--color-surface)]`), `--color-text` (body), `--color-primary` (ornament divider via utility — opacity 0.5 already baked in class).

---

### Section 5: `#shop-charity`

**Purpose:** Dynamic charity callout — current published issue's featured charity, server-rendered from Sanity. Reuses the existing charity name query from Phase 8.

**Layout:**
- Full-width `--color-card` panel (`#EDE9E1`), `border-t border-b border-[--color-line-strong]`.
- Inner container: `max-width: 860px`, centered.
- Vertical padding: `py-16`, horizontal `px-4 md:px-6 lg:px-8`.

**Copy elements:**

| Element | Tag | Copy | Typography |
|---------|-----|------|------------|
| Section eyebrow | `<p className="eyebrow">` | `This week.` | T1 — `.eyebrow` |
| Dynamic charity line | `<p>` | `This week's proceeds benefit [charityName].` OR fallback: `Proceeds go to our featured charity each week.` | T4 — `font-display clamp(28px,4vw,72px)` weight 400 lh 1.3 `--color-text` |
| Supporting line | `<p>` | `One product. One weekly charity. One hundred percent of proceeds.` | T3 — `font-body` 18px/400/1.65/`--color-text-dim` |

**Stripe-machinery note:** This section does NOT contain a BuyButton. The charity callout is informational. BuyButton positions are `#shop-hero` and `#shop-buy` only (third optional at `#shop-footer-cta`).

**Implementation note:** The GROQ query for `charityName` is the existing inline `QUERY_LATEST_CHARITY_NAME` defined at the top of `apps/web/app/shop/page.tsx` (Phase 8). Preserve it byte-unchanged. The `try/catch` fallback path is also preserved. This section renders the same data with more visual weight than the Phase 8 one-line callout.

**Ornament divider:** `.ornament-divider` appears BELOW this section (before `#shop-buy`).

**Token usage:** `--color-card` (panel bg), `--color-line-strong` (panel borders), `--color-text` (charity name), `--color-text-dim` (supporting line).

---

### Section 6: `#shop-buy`

**Purpose:** The product moment. Product image slot + price + edition designation + second (and primary) BuyButton. This is the conversion section.

**Layout:**
- Two-column on ≥ 768px: image left, copy+button right. Stack (image above, copy below) on < 768px.
- Max-width: `1040px`, centered, gap `gap-12`.
- Vertical padding: `py-20`.
- Background: `--color-bg`.

**Left column — image slot:**
- `next/image` with `TODO(Andrew)` marker.
- Aspect ratio: `3:4` (portrait product still — tube of lip balm, styled).
- Max-width: `480px` on desktop. Full-width on mobile.
- Alt text template: `Jesse A. Eisenbalm lip balm — Release 001, close-up`
- Marker comment: `{/* TODO(Andrew): upload product still photography (3:4 portrait, close-up of tube). Replace this placeholder. */}`
- Placeholder until photography: a `--color-card` (#EDE9E1) block with `aspect-[3/4]` containing a centered `.eyebrow` label: `PRODUCT PHOTOGRAPHY COMING` — this looks intentional (editorial placeholder), not broken (empty image).

**Right column — copy and CTA:**

| Element | Tag | Copy | Typography |
|---------|-----|------|------------|
| Product eyebrow | `<p className="eyebrow">` | `THE BALM` | T1 — `.eyebrow` |
| Product name | `<h2>` | `Jesse A. Eisenbalm` | T4 — `font-display clamp(28px,4vw,72px)` weight 400 lh 1.1 `--color-text` |
| Price + edition | `<p>` | `$8.99 · Release 001 · hand-numbered — TODO(Andrew)` | T4 — `font-display clamp(28px,4vw,72px)` weight 400 lh 1.2 `--color-primary-text` (#7A5C0E — AA-safe gold text) |
| Edition sub-note | `<p>` | `Ships flat-rate, continental US — TODO(Andrew): confirm shipping details` | T2 — `font-body` 16px/400/1.5/`--color-text-mute` |
| Second BuyButton | `<BuyButton />` | `Buy the lip balm` | BuyButton component — byte-unchanged. Wrap in `<div className="mt-6">` for top spacing; do NOT modify the component source or pass spacing props. |

**WCAG note:** Price/edition uses `--color-primary-text` (#7A5C0E, 5.97:1) NOT raw `--color-primary` (#CDA434, 2.24:1 — fails AA as text).

**Image slot specs:**

| Property | Value |
|----------|-------|
| Location | `#shop-buy`, left column |
| Subject | Product still — tube of lip balm, close-up |
| Aspect ratio | `3:4` (portrait) |
| Max-width | `max-w-[480px]` desktop |
| Alt text | `Jesse A. Eisenbalm lip balm — Release 001` |
| TODO marker | `TODO(Andrew): upload product still photography (3:4 portrait)` |
| Placeholder | `--color-card` block, aspect-[3/4], centered `.eyebrow` text `PRODUCT PHOTOGRAPHY COMING` |

**Ornament divider:** `.ornament-divider` appears BELOW this section (before `#shop-faq`).

**Token usage:** `--color-text` (headlines), `--color-primary-text` (price/edition — AA-safe), `--color-text-mute` (sub-note), `--color-bg` (section bg).

---

### Section 7: `#shop-faq`

**Purpose:** Inline FAQ — six questions covering the most common reader questions. Semantic `<details>`/`<summary>` for zero-JS progressive enhancement (same pattern as Phase 2 deliberation accordion per Phase 2 state decision).

**Layout:**
- Single column, max-width `860px`, centered.
- Vertical padding: `py-16`.
- Background: `--color-bg`.
- Each FAQ item: `<details>` with `<summary>` + hidden `<div>`. Full-width. Border-bottom `--color-line` on each item.
- Open state: `<details open>` removes `border-bottom` gap but keeps the answer visible.

**Copy elements:**

| Element | Tag | Copy | Typography |
|---------|-----|------|------------|
| Section eyebrow | `<p className="eyebrow">` | `Questions.` | T1 — `.eyebrow` |
| Section headline | `<h2>` | (omit — the eyebrow IS the section header; no h2 needed for a 6-item FAQ) | — |
| FAQ question | `<summary>` | See FAQ table in Copywriting Contract | T2 — `font-body` 16px/600/1.4/`--color-text`; min-height 44px; cursor pointer |
| FAQ answer | `<div>` inside `<details>` | See FAQ table in Copywriting Contract | T2 — `font-body` 16px/400/1.65/`--color-text-dim`; `pb-4 pt-2` |
| Chevron icon | lucide-react `ChevronDown` 16px | aria-hidden | Rotates on `[open]` state; `--color-text-mute` |

**Interaction:** `<details>`/`<summary>` — zero JS, no client component needed. CSS `[open] summary > .chevron { transform: rotate(180deg); }` for the chevron rotation. Reduced-motion guard in globals.css already covers `transition-duration: 0.01ms`.

**Touch targets:** `<summary>` must have `min-height: 44px` (≥44px rule). Use `py-3` (12px top + bottom) + 16px text = 40px — bump to `py-[14px]` (28px + 16px = 44px).

**Route decision confirmed:** NO `/shop/faq` separate route. All six items inline.

**Token usage:** `--color-text` (questions), `--color-text-dim` (answers), `--color-line` (dividers between items), `--color-text-mute` (chevron icon).

---

### Section 8: `#shop-footer-cta`

**Purpose:** Optional meditative outro — a closing beat after the FAQ, above the site footer. Keeps the product voice front-of-mind at page-bottom. Light, not insistent.

**Layout:**
- Single column, centered.
- Max-width: `860px`.
- Vertical padding: `py-16`.
- Background: `--color-surface` (`#F2EFE9`) — a gentle surface shift signals "end of product content, before site footer." Use `w-full bg-[color:var(--color-surface)]` outer wrapper.
- `border-t border-[--color-line]`.

**Copy elements:**

| Element | Tag | Copy | Typography |
|---------|-----|------|------------|
| Outro line | `<p>` | `One product. This week's charity needs it.` {/* TODO(Andrew): voice-check — "needs it" leans persuasive; consider a more neutral close */} | T2 — `font-body` italic 16px/400/1.4/`--color-text-dim` |
| Optional third BuyButton | `<BuyButton />` | `Buy the lip balm` | BuyButton — byte-unchanged. Wrap in `<div className="mt-6">` for top spacing; do NOT modify the component source or pass spacing props. Include ONLY if user scroll analytics suggest high drop-off here (assumption: include it — three positions is justified for a long-scroll page). |

**Note on BuyButton count:** Three BuyButton instances on the page is acceptable and not "urgency mechanics." The three positions serve different reading contexts: hero (first impression), `#shop-buy` (considered purchase moment), footer-cta (after FAQ consideration). This is a single product with one checkout path — no cart, no variants.

**Token usage:** `--color-surface` (section bg via `bg-[color:var(--color-surface)]`), `--color-line` (top border), `--color-text-dim` (outro text).

---

## BuyButton Positions (Summary)

| Position # | Section ID | Context | Component |
|------------|------------|---------|-----------|
| 1 | `#shop-hero` | First impression, immediately below tagline | `<BuyButton />` — byte-unchanged; wrap in `<div className="mt-8">` for spacing |
| 2 | `#shop-buy` | Considered purchase moment, alongside product image | `<BuyButton />` — byte-unchanged; wrap in `<div className="mt-6">` for spacing |
| 3 | `#shop-footer-cta` | Post-FAQ closing beat | `<BuyButton />` — byte-unchanged; wrap in `<div className="mt-6">` for spacing |

**BuyButton wrapper rule (all three positions):** Spacing goes on a wrapper `<div>`, NOT on the component itself. `<BuyButton>` is byte-unchanged — do not modify the component source, do not pass `className`, `mt-*`, or any spacing props to the component. Example: `<div className="mt-8"><BuyButton /></div>`.

Minimum contractual requirement from ROADMAP success criteria: ≥2 positions. This spec adds a third (justified above). If space is tight, drop the third — but never drop below 2.

---

## Image Slot Specifications (all marked TODO Andrew)

| Slot # | Section | Subject | Aspect Ratio | Max-Width | Alt Text Template | TODO Marker |
|--------|---------|---------|--------------|-----------|-------------------|-------------|
| 1 | `#shop-hero` | Hero product shot (tube, lifestyle/editorial) | `4:3` | `max-w-[760px]` | `Jesse A. Eisenbalm lip balm — Release 001` | `TODO(Andrew): upload hero product photography (4:3 landscape). Replace JSX comment.` |
| 2 | `#shop-buy` | Product still (close-up, portrait) | `3:4` | `max-w-[480px]` | `Jesse A. Eisenbalm lip balm — Release 001, close-up` | `TODO(Andrew): upload product still photography (3:4 portrait). Replaces placeholder block.` |

Both slots use `next/image` with the existing `next.config` (no image config changes). Both show `--color-card` placeholder blocks with `.eyebrow` text until photography is uploaded.

---

## Stripe-Machinery Preservation Contract

The following are BYTE-UNCHANGED. The Phase 15 executor touches NONE of these files:

| Artifact | Path | Contract |
|----------|------|---------|
| BuyButton component | `apps/web/components/marketing/BuyButton.tsx` | Byte-unchanged. Used via `<BuyButton />` with no props added. |
| Checkout API route | `apps/web/app/api/checkout/create-session/route.ts` | Byte-unchanged. |
| Stripe webhook route | `apps/web/app/api/stripe/webhook/route.ts` | Byte-unchanged. |
| Thank-you page | `apps/web/app/shop/thank-you/page.tsx` | Byte-unchanged. |
| Legal pages | `apps/web/app/legal/privacy/page.tsx`, `apps/web/app/legal/terms/page.tsx` | Byte-unchanged. Reachable from the site footer (already wired). |
| Issue ShopCallout | `apps/web/components/issue/ShopCallout.tsx` | Byte-unchanged. Continues to render at the bottom of every issue page. |
| GROQ query | `QUERY_LATEST_CHARITY_NAME` inline in `apps/web/app/shop/page.tsx` | Preserved verbatim. The charity name query is the same query, same inline location. |

**The Phase 15 executor rewrites only `apps/web/app/shop/page.tsx` and adds no new routes.**

---

## Test Contract: CMR Preservation After Rebuild

After Phase 15, `apps/web/__tests__/shop-page.test.ts` must assert:

### Preserve from Phase 8 (these tests must stay green without modification):

1. `shop page file exists` — `apps/web/app/shop/page.tsx` exists.
2. `does NOT declare "use client" at the top of file` — the page remains a Server Component.
3. `imports sanityClient (server-side Sanity read)` — `from '@/lib/sanity/client'` still present.
4. `exports an async default function (server component)` — `export default async function` pattern.
5. `declares ISR with export const revalidate` — `export const revalidate = 60` preserved.

### New assertions to add in Phase 15 (update the test file):

6. `renders BuyButton in at least 2 positions` — `expect(source.match(/<BuyButton/g)?.length ?? 0).toBeGreaterThanOrEqual(2)`.
7. `renders #shop-hero section` — `expect(source).toMatch(/id=["']shop-hero["']/)`.
8. `renders #shop-features section` — `expect(source).toMatch(/id=["']shop-features["']/)`.
9. `renders #shop-buy section` — `expect(source).toMatch(/id=["']shop-buy["']/)`.
10. `renders #shop-faq section` — `expect(source).toMatch(/id=["']shop-faq["']/)`.
11. `hero tagline is present` — `expect(source).toMatch(/Stop\. Breathe\. Balm\./)`.
12. `no urgency vocabulary (CMR-09 extension)` — extend the existing no-urgency scan to the shop page source: no `limited`, no `only \d+ left`, no `countdown`, no `hurry`, no `act now` in code (stripping comments first, matching Phase 8's `codeOnly()` pattern).

### CMR-09 (issue-page-shop-callout.test.ts) — no change needed:
The `ShopCallout` component at `components/issue/ShopCallout.tsx` is byte-unchanged. The issue page continues to import and render it. CMR-09 stays green automatically.

---

## Spacing Scale

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Feature-cluster internal padding |
| md | 16px | Label-to-headline gap, paragraph spacing |
| lg | 24px | Feature card padding, `.ornament-divider` context |
| xl | 32px | Section-internal vertical spacing |
| 2xl | 48px | Standard section padding (py-12 = 48px) |
| 3xl | 64px | Section top/bottom padding (py-16 = 64px) |
| 4xl | 96px | Hero top padding (pt-24 = 96px) |

Exceptions: touch-target floor 44px (enforced on `<summary>`, `<BuyButton>`, any `<a>`).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | `Button` (existing from Phase 2) | not required — already installed |
| Third-party | none | not applicable |

No new shadcn components are added in this phase. The existing `Button` primitive (from `apps/web/components/ui/button.tsx`) is the only shadcn dependency — reused by `BuyButton`.

---

## Out-of-Scope Exclusions

The following are explicitly NOT part of Phase 15:

| Exclusion | Reason |
|-----------|--------|
| Cart system | Brief: one product, no cart |
| Multi-variant product | Brief: one product |
| Subscription / recurring purchase | Brief: one product |
| Reviews / ratings | Brief: no urgency mechanics; out of brand voice |
| Pricing tiers | Brief: one product, $8.99 |
| Address collection on `/shop` | Stripe Checkout handles it |
| New fonts | FONT_WHITELIST locked |
| New npm dependencies | Brief constraint |
| CDN scripts | Brief constraint |
| Image optimization config changes | Use existing `next/image` setup |
| Changes to Dispatch homepage (`/`) | Phase 15 scope is `/shop` only |
| Changes to `/about` or `/archive` | Out of scope |
| Changes to Phase 8 Stripe routes or components | Byte-unchanged contract |
| `/shop/faq` as a separate route | Inline FAQ is sufficient for 6 items |
| Shopify, Commerce.js, or third-party commerce | Brief: custom Stripe only |
| Urgency mechanics, scarcity language, countdown timers | Brief + CMR-09 tripwire |
| `allow-same-origin` on any iframe | Phase 7 game-sandbox tripwire enforces |
| Hardcoded hex values | Use `--color-*` tokens only |
| New Convex queries | Shop page has no real-time data needs |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
