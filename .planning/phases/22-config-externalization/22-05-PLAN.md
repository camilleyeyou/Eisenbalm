---
phase: 22-config-externalization
plan: 05
type: execute
wave: 3
depends_on: ["22-03"]
files_modified:
  - packages/pipeline/src/eisenbalm_pipeline/api/runs.py
  - packages/pipeline/src/eisenbalm_pipeline/agents/scout.py
  - packages/pipeline/src/eisenbalm_pipeline/agents/advocate.py
  - packages/pipeline/src/eisenbalm_pipeline/agents/calibrator.py
  - packages/pipeline/src/eisenbalm_pipeline/agents/researcher.py
  - packages/pipeline/src/eisenbalm_pipeline/agents/editor.py
  - packages/pipeline/src/eisenbalm_pipeline/agents/bonus.py
  - packages/pipeline/src/eisenbalm_pipeline/agents/game.py
  - packages/pipeline/src/eisenbalm_pipeline/agents/design/__init__.py
  - packages/pipeline/tests/api/test_runs_config_snapshot.py
autonomous: true
requirements: [CFG-01, CFG-04]
user_setup: []

must_haves:
  truths:
    - "load_run_config() then snapshot_config() are awaited in the run_weekly HTTP handler BEFORE asyncio.create_task() / graph.ainvoke()"
    - "runs:create is called with the required triggerSource; startedAt/status are set server-side by the Convex handler, NOT by the Python caller"
    - "The resolved RunConfig is threaded onto initial_state['config'] and reaches every agent via state['config']"
    - "All 11 load_prompt call sites read state['config'].agents[key].system_prompt instead of disk"
    - "The assembled system strings are byte-identical to the pre-swap strings when Convex == seeded defaults (str.replace preserved, not str.format)"
    - "The resume path does NOT re-snapshot — the original snapshot stays authoritative"
  artifacts:
    - path: "packages/pipeline/src/eisenbalm_pipeline/api/runs.py"
      provides: "run_weekly: runs:create (triggerSource only) + load_run_config + awaited snapshot_config before create_task; resume unchanged (no re-snap)"
      contains: "snapshot_config"
    - path: "packages/pipeline/src/eisenbalm_pipeline/agents/bonus.py"
      provides: "three bonus call-sites read state['config'].agents[bonus_*].system_prompt"
      contains: "config"
  key_links:
    - from: "api/runs.py run_weekly"
      to: "config_loader.snapshot_config"
      via: "awaited before asyncio.create_task"
      pattern: "snapshot_config"
    - from: "agents/*.py call sites"
      to: "state['config'].agents[key].system_prompt"
      via: "config-threaded prompt read"
      pattern: "config"
---

<objective>
Wire the run-start sequence and swap the 11 prompt call sites. In `api/runs.py` `run_weekly`, insert `runs:create` → `load_run_config()` → awaited `snapshot_config()` BEFORE `asyncio.create_task()` (the snapshot-race fix, CFG-04), and thread the resolved `RunConfig` onto `initial_state["config"]`. Then swap all 11 `load_prompt(...)` call sites across the 8 agent files to read `state["config"].agents[key].system_prompt`, preserving the exact `str.replace("{token}", …)` substitution. Leave the resume path WITHOUT a re-snapshot. Green the snapshot-ordering tests.

Purpose: This delivers CFG-01 (agents read Convex config via state) and CFG-04 (immutable pre-invoke snapshot). Byte-equivalence of the assembled system strings keeps the voice contract tests green.
Output: runs.py wiring + 11 call-site swaps + green test_runs_config_snapshot.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-config-externalization/22-CONTEXT.md
@.planning/phases/22-config-externalization/22-RESEARCH.md
@docs/API_CONTRACTS.md

<interfaces>
<!-- From lib/config_loader.py (Plan 03). -->
async def load_run_config(http) -> RunConfig
async def snapshot_config(http, run_id: str, config: RunConfig) -> None
RunConfig.agents: dict[str, AgentConfig]   # AgentConfig.system_prompt: str

<!-- From convex/runs.ts (Plan 02). runs:create REQUIRES triggerSource; status + startedAt are set
     server-side in the Convex handler (status="running", startedAt=Date.now()) — the Python caller
     does NOT pass them. -->
runs:create args: { workspace_id, runId, triggerSource (required), triggeredBy? }
runs:setConfigSnapshot args: { runId, configSnapshot }   // called inside snapshot_config (Plan 03)

<!-- Current call sites (file:line → key). All use str.replace, NOT str.format. -->
scout.py:192     load_prompt("scout").replace("{featured_keys}", ...)          → agents["scout"]
advocate.py:67   load_prompt("advocate")                                       → agents["advocate"]
calibrator.py:109 load_prompt("calibrator")                                    → agents["calibrator"]
researcher.py:85 load_prompt("researcher").replace("{VOICE_CONSTRAINTS}", ...) → agents["researcher"]
editor.py:194    load_prompt("editor")                                         → agents["editor_gate1"]
editor.py:440    load_prompt("editor-final").replace("{VOICE_CONSTRAINTS}",...) → agents["editor_final"]
bonus.py:130     load_prompt("bonus-big-budget").replace("{VOICE_CONSTRAINTS}",...) → agents["bonus_big_budget"]
bonus.py:144     load_prompt("bonus-jingle").replace("{VOICE_CONSTRAINTS}",...)     → agents["bonus_jingle"]
bonus.py:159     load_prompt("bonus-spec-ad")                                  → agents["bonus_spec_ad"]
game.py:61       load_prompt("game")                                           → agents["game"]
design/__init__.py:99 load_prompt("design")                                    → agents["design"]

<!-- Current run_weekly ordering (api/runs.py ~192-242): resolve issue# → new_run_id → begin_run → pipelineRuns:create → build initial_state → asyncio.create_task(_execute_run(app, run_id, initial_state, config)). NOTE: the existing `config` arg to _execute_run is the LangGraph configurable {"configurable":{"thread_id":...}} — do NOT confuse it with RunConfig. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire run_weekly — runs:create + load_run_config + awaited snapshot before create_task</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/api/runs.py</files>
  <read_first>
    - packages/pipeline/src/eisenbalm_pipeline/api/runs.py (run_weekly ~178-242 full body; note the pre-existing `config` LangGraph-configurable var name collision; resume_run ~279-321 — leave untouched per Pitfall 6)
    - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py (load_run_config, snapshot_config signatures)
    - convex/runs.ts (runs:create REQUIRES triggerSource; status + startedAt set server-side — Plan 02)
    - the existing trigger path in run_weekly — determine whether this run is "manual" or "cron" and pass that as triggerSource (match the value the existing code already uses for the trigger, if any; default "manual")
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Pattern 4 + Example 3 exact ordering; Pitfall 1 snapshot race; Pitfall 6 resume no re-snap)
  </read_first>
  <action>
    In `api/runs.py` `run_weekly`, after the existing `pipelineRuns:create` mutation and BEFORE building `initial_state`/`asyncio.create_task`:
    1. `await convex_mutation(request.app.state.convex_http, "runs:create", {"workspace_id": "eisenbalm", "runId": run_id, "triggerSource": "manual"})` — SAME runId as pipelineRuns:create (the join key). Pass `triggerSource` (required by the Convex schema) matching the existing trigger path: use `"manual"` for the manual/HTTP trigger, `"cron"` if this handler is the scheduled path. Do NOT pass `startedAt` or `status` — the Convex `runs:create` handler sets `status="running"` and `startedAt=Date.now()` server-side (Plan 02). Optionally pass `triggeredBy` if a user identity is available; omit otherwise.
    2. `from eisenbalm_pipeline.lib.config_loader import load_run_config, snapshot_config` (top-of-file import — this binds the names into the `api.runs` module namespace; the snapshot-ordering test in Task 3 patches `eisenbalm_pipeline.api.runs.snapshot_config` precisely because of this top-level import). config_loader does not import runs.py, so no cycle.
    3. `run_config = await load_run_config(request.app.state.convex_http)`.
    4. `await snapshot_config(request.app.state.convex_http, run_id, run_config)` — AWAITED, in the handler body, BEFORE create_task (CFG-04 / Pitfall 1).
    5. Add `initial_state["config"] = run_config` to the initial_state dict.
    Rename the RunConfig var `run_config` to avoid shadowing the LangGraph configurable local (which stays named `config` for `{"configurable": {...}}`). Do NOT add snapshot_config anywhere in `resume_run` or `_execute_run`.
    Confirm `_execute_run` still receives the LangGraph configurable (unchanged) and `initial_state` now carrying `config`.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run python -c "import ast; src=open('src/eisenbalm_pipeline/api/runs.py').read(); ast.parse(src); assert 'snapshot_config' in src and 'load_run_config' in src and 'runs:create' in src and 'triggerSource' in src; assert src.count('snapshot_config(') == 1, 'snapshot must be called exactly once (run_weekly only, never resume)'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "load_run_config" api/runs.py` AND `grep "snapshot_config" api/runs.py` AND `grep '"runs:create"' api/runs.py` present
    - `grep "triggerSource" api/runs.py` present in the runs:create call; `grep "startedAt\|\"status\"" api/runs.py` does NOT appear in the runs:create payload (server sets them)
    - the runs:create payload uses the SAME `run_id` variable passed to pipelineRuns:create (same join key)
    - `from eisenbalm_pipeline.lib.config_loader import load_run_config, snapshot_config` is a TOP-LEVEL import in api/runs.py (so the name resolves to `eisenbalm_pipeline.api.runs.snapshot_config` for patching)
    - `snapshot_config(` appears EXACTLY ONCE in api/runs.py (run_weekly only — proves resume does not re-snap)
    - `grep 'initial_state\["config"\]' api/runs.py` present
    - In run_weekly, the `await snapshot_config(...)` line precedes the `asyncio.create_task(` line (verified by line order; the snapshot-ordering pytest in Task 3 enforces this)
    - resume_run body unchanged except imports (no snapshot_config call)
  </acceptance_criteria>
  <done>run_weekly creates the runs row (triggerSource only; server sets status+startedAt), loads config, awaits the snapshot before launching the background task, and threads config onto state; resume untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Swap all 11 prompt call sites to read state['config'].agents[key].system_prompt</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/agents/scout.py, advocate.py, calibrator.py, researcher.py, editor.py, bonus.py, game.py, design/__init__.py</files>
  <read_first>
    - All 8 agent files at the exact lines in the interfaces block (scout/advocate/calibrator/researcher/editor[×2]/bonus[×3]/game/design)
    - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py (AgentConfig.system_prompt; the key names — note editor→editor_gate1)
    - packages/pipeline/tests/test_voice.py + tests/test_section_writer_voice_propagation.py (the byte-equivalence contract these swaps must not break)
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Pattern 6 swap; Pitfall 7 keep str.replace not str.format; Anti-Patterns: remove now-dead load_prompt imports)
  </read_first>
  <action>
    For each of the 11 call sites, replace `load_prompt("<file>")` with `state["config"].agents["<key>"].system_prompt`, PRESERVING the trailing `.replace("{token}", value)` chain verbatim. Mapping (file→key):
    - scout.py:192  → `state["config"].agents["scout"].system_prompt.replace("{featured_keys}", f"{featured_keys}")`
    - advocate.py:67 → `state["config"].agents["advocate"].system_prompt`
    - calibrator.py:109 → `state["config"].agents["calibrator"].system_prompt`
    - researcher.py:85 → `state["config"].agents["researcher"].system_prompt.replace("{VOICE_CONSTRAINTS}", VOICE_CONSTRAINTS)`
    - editor.py:194 → `state["config"].agents["editor_gate1"].system_prompt`
    - editor.py:440 → `state["config"].agents["editor_final"].system_prompt.replace("{VOICE_CONSTRAINTS}", VOICE_CONSTRAINTS)`
    - bonus.py:130 → `state["config"].agents["bonus_big_budget"].system_prompt.replace("{VOICE_CONSTRAINTS}", VOICE_CONSTRAINTS)`
    - bonus.py:144 → `state["config"].agents["bonus_jingle"].system_prompt.replace("{VOICE_CONSTRAINTS}", VOICE_CONSTRAINTS)`
    - bonus.py:159 → `state["config"].agents["bonus_spec_ad"].system_prompt`
    - game.py:61 → `state["config"].agents["game"].system_prompt`
    - design/__init__.py:99 → `state["config"].agents["design"].system_prompt`
    Each call site is inside an agent function that already receives `state` (confirm the param name; it is the DispatchState dict). Defensive guard: if a node could run without `config` in legacy/test paths, fall back to `load_prompt(...)` ONLY when `state.get("config") is None` — keep the `load_prompt` import where this guard is used; otherwise remove the now-dead `from eisenbalm_pipeline.lib.prompts import load_prompt` import. Do NOT convert any `.replace(...)` to `.format(...)`.
    Recommended pattern per site:
    ```python
    cfg = state.get("config")
    base = cfg.agents["scout"].system_prompt if cfg else load_prompt("scout")
    system = base.replace("{featured_keys}", f"{featured_keys}")
    ```
    (Keep the load_prompt import on files that use this guard.)
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/test_voice.py tests/test_section_writer_voice_propagation.py tests/test_chronicler.py tests/test_calibrator_narrator.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'agents\["scout"\].system_prompt\|agents\["editor_gate1"\].system_prompt\|agents\["bonus_big_budget"\].system_prompt' src/eisenbalm_pipeline/agents/` returns the swapped sites
    - `grep -rn '.format(' src/eisenbalm_pipeline/agents/scout.py src/eisenbalm_pipeline/agents/bonus.py src/eisenbalm_pipeline/agents/editor.py src/eisenbalm_pipeline/agents/researcher.py` returns NOTHING for the prompt assembly (str.replace preserved)
    - All 11 sites swapped (`grep -rc "state\[.config.\].agents\[" src/eisenbalm_pipeline/agents/` totals 11 across the files; bonus.py shows 3, editor.py shows 2)
    - tests/test_voice.py + tests/test_section_writer_voice_propagation.py PASS (byte-equivalence preserved)
  </acceptance_criteria>
  <done>All 11 call sites read from state['config'] with str.replace preserved; voice byte-equivalence tests green; dead load_prompt imports removed where unguarded.</done>
</task>

<task type="auto">
  <name>Task 3: Green the snapshot-ordering + resume-no-resnap tests; run full suite</name>
  <files>packages/pipeline/tests/api/test_runs_config_snapshot.py</files>
  <read_first>
    - packages/pipeline/tests/api/test_runs_config_snapshot.py (the Plan 01 xfail scaffolds to finalize)
    - packages/pipeline/src/eisenbalm_pipeline/api/runs.py (run_weekly + resume_run final bodies; note snapshot_config is imported at module top-level)
    - packages/pipeline/tests/api/test_runs.py (existing run_weekly test harness — mock graph, convex_http, trigger secret — reuse the fixture pattern)
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Pitfall 1 + Pitfall 6 — exact assertions)
  </read_first>
  <action>
    Finalize `tests/api/test_runs_config_snapshot.py`: remove xfail marks.
    - `test_snapshot_before_task`: build a call-order recorder. Patch `eisenbalm_pipeline.api.runs.snapshot_config` — patch it AT THE BINDING IN THE `api.runs` MODULE (where Task 1 imports it top-level), NOT `eisenbalm_pipeline.lib.config_loader.snapshot_config`. Rationale: `run_weekly` looks up `snapshot_config` as a module-global of `api.runs` (it was imported there), so patching the source module would not intercept the call. Also patch `asyncio.create_task` so both append to a shared list when invoked; mock `convex_mutation`/`load_run_config` and the graph. Invoke the `run_weekly` handler. Assert:
        (a) the snapshot mock WAS called (e.g. `mock_snapshot.assert_awaited()` / `assert mock_snapshot.await_count == 1`) — this prevents the test passing vacuously if snapshot_config is never triggered;
        (b) the recorder shows `snapshot_config` BEFORE `create_task` (index comparison).
    - `test_resume_no_resnap`: patch `eisenbalm_pipeline.api.runs.snapshot_config` (same binding); invoke `resume_run` (with a mocked paused graph state, `state.next` non-empty); assert `snapshot_config` was NEVER called on the resume path (`mock_snapshot.assert_not_called()` / `await_count == 0`).
    Reuse the existing test_runs.py fixtures (mock app.state.graph, app.state.convex_http, PIPELINE_TRIGGER_SECRET unset for local). Then run the FULL pipeline suite to confirm zero regressions.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/api/test_runs_config_snapshot.py -q && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "xfail" tests/api/test_runs_config_snapshot.py` returns NOTHING
    - `grep "eisenbalm_pipeline.api.runs.snapshot_config" tests/api/test_runs_config_snapshot.py` present (patches the api.runs binding, NOT lib.config_loader)
    - `grep "assert_awaited\|await_count == 1\|assert_called" tests/api/test_runs_config_snapshot.py` present in test_snapshot_before_task (the mock-WAS-called assertion, so the test cannot pass vacuously)
    - `uv run pytest tests/api/test_runs_config_snapshot.py -q` shows test_snapshot_before_task + test_resume_no_resnap PASSING
    - `uv run pytest -q` (full pipeline suite) exits 0 — voice, wiring, narrator, editor-resume, e2e all green
  </acceptance_criteria>
  <done>Snapshot-before-invoke and resume-no-resnap tests are green (patching the api.runs binding and asserting the snapshot mock was actually called); full pipeline suite passes.</done>
</task>

</tasks>

<verification>
- `cd packages/pipeline && uv run pytest -q` full suite green
- `uv run pytest tests/api/test_runs_config_snapshot.py tests/test_voice.py -q` green
- `snapshot_config(` appears exactly once in api/runs.py
- runs:create passes triggerSource and does NOT pass startedAt/status (server-side defaults)
- 11 call sites swapped; no `.format(` in prompt assembly; voice byte-equivalence intact
</verification>

<success_criteria>
- run_weekly creates the runs row with triggerSource, awaits load_run_config + snapshot_config BEFORE asyncio.create_task (CFG-04); config threaded onto state (CFG-01)
- All 11 prompt call sites read state['config']; str.replace preserved; voice tests green
- Snapshot test patches the api.runs binding and asserts the mock was called (no vacuous pass)
- Resume path does not re-snapshot
- Full pipeline pytest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/22-config-externalization/22-05-SUMMARY.md`
</output>
