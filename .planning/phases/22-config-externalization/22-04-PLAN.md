---
phase: 22-config-externalization
plan: 04
type: execute
wave: 3
depends_on: ["22-02", "22-03"]
files_modified:
  - packages/pipeline/scripts/seed_phase22.py
  - packages/pipeline/scripts/verify_prompt_seed.py
  - packages/pipeline/tests/lib/test_prompt_seed.py
autonomous: true
requirements: [CFG-02]
user_setup: []

must_haves:
  truths:
    - "An idempotent seed populates agents (15 keys), prompt_versions (11 v1 active rows), and pipeline_config from llm_config.py + load_prompt()"
    - "Each of the 11 seeded prompt_versions.content equals load_prompt(file) byte-for-byte (zero diff)"
    - "Seeded prompt rows are version==1, isActive==true"
    - "Running the seed twice produces no duplicate rows and leaves version==1"
    - "A standalone verify_prompt_seed.py checks live Convex; a pytest parametrize checks the seed logic mocked"
  artifacts:
    - path: "packages/pipeline/scripts/seed_phase22.py"
      provides: "idempotent seed of agents + prompt_versions + pipeline_config via Convex mutations, content sourced from load_prompt()"
      min_lines: 50
    - path: "packages/pipeline/scripts/verify_prompt_seed.py"
      provides: "standalone live-Convex byte-comparison of the 11 seeded rows vs load_prompt(); exit 1 on any diff"
    - path: "packages/pipeline/tests/lib/test_prompt_seed.py"
      provides: "green parametrized byte-parity over the 11 pairs + idempotency assertion (mocked Convex)"
  key_links:
    - from: "scripts/seed_phase22.py"
      to: "promptVersions:upsertActive with load_prompt(file) content"
      via: "byte-exact seed (NOT raw file read)"
      pattern: "load_prompt"
    - from: "scripts/verify_prompt_seed.py"
      to: "promptVersions:getActive"
      via: "live byte comparison"
      pattern: "getActive"
---

<objective>
Build the migration + verification for CFG-02: an idempotent seed script that writes the 15 `agents` rows (from `llm_config.py`), the 11 `prompt_versions` v1 active rows (content from `load_prompt()`, NEVER raw file reads), and the `pipeline_config` defaults; plus a standalone `verify_prompt_seed.py` (live Convex zero-diff check) and a green pytest parametrize over the 11 pairs (mocked Convex, runs in CI).

Purpose: CFG-02 demands the migrated prompts be byte-identical to what the pipeline currently sends. The seed and verification both route through `load_prompt()` (the oracle) so the seeded bytes match the fallback exactly — closing the byte-mismatch pitfall.
Output: seed script + verify script + green test_prompt_seed.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-config-externalization/22-CONTEXT.md
@.planning/phases/22-config-externalization/22-RESEARCH.md

<interfaces>
<!-- The seed reads from these (Plan 03). -->
from eisenbalm_pipeline.lib.config_loader import AGENT_KEY_TO_PROMPT_FILE, ALL_AGENT_KEYS, _llm_key_for, WORKSPACE_ID
from eisenbalm_pipeline.lib.prompts import load_prompt
from eisenbalm_pipeline.lib.llm_config import MODEL_BY_AGENT, SAMPLING_BY_AGENT, MAX_TOKENS_BY_AGENT
from eisenbalm_pipeline.lib.convex_client import convex_mutation, convex_query, get_client

<!-- Convex mutation/query paths (Plan 02). -->
"agents:upsert" {workspace_id, agentKey, enabled, model?, temperature?, top_p?, max_tokens?, description?}
"promptVersions:upsertActive" {workspace_id, agentKey, content, note?}
"pipelineConfig:upsert" {workspace_id, key, value (JSON string)}
"promptVersions:getActive" {workspace_id, agentKey}  -> row|null
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write idempotent seed_phase22.py</name>
  <files>packages/pipeline/scripts/seed_phase22.py</files>
  <read_first>
    - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py (AGENT_KEY_TO_PROMPT_FILE, ALL_AGENT_KEYS, _llm_key_for — reuse, do not redefine)
    - packages/pipeline/src/eisenbalm_pipeline/lib/llm_config.py (the seed source values)
    - packages/pipeline/src/eisenbalm_pipeline/lib/convex_client.py (convex_mutation + how the shared AsyncClient is constructed — the script needs its own AsyncClient with base_url=NEXT_PUBLIC_CONVEX_URL)
    - convex/agents.ts + convex/promptVersions.ts + convex/pipelineConfig.ts (the mutation arg shapes from Plan 02)
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Pattern 9 idempotent seed; Pitfall 2 byte mismatch — use load_prompt; Pitfall 3 editor_gate1 agentKey)
  </read_first>
  <action>
    Create `packages/pipeline/scripts/seed_phase22.py` — a runnable `async def main()` (guarded by `if __name__ == "__main__": asyncio.run(main())`). It constructs an `httpx.AsyncClient(base_url=os.environ["NEXT_PUBLIC_CONVEX_URL"])` (mirror how the FastAPI lifespan builds the convex client) and:
    1. For each `agent_key in ALL_AGENT_KEYS`: call `agents:upsert` with model=`MODEL_BY_AGENT[_llm_key_for(agent_key)]`, temperature=`SAMPLING_BY_AGENT[_llm_key_for(agent_key)].get("temperature", 0.3)`, top_p=`...get("top_p", 1.0)`, max_tokens=`MAX_TOKENS_BY_AGENT.get(_llm_key_for(agent_key))`, enabled=True, description=f"{agent_key} agent". (15 base keys + 3 bonus variants → use ALL_AGENT_KEYS so bonus_* get their own agents rows per D-04/research recommendation; all bonus_* point at the same model/temp.)
    2. For each `agent_key, prompt_file in AGENT_KEY_TO_PROMPT_FILE.items()` (the 11): call `promptVersions:upsertActive` with `content=load_prompt(prompt_file)` (NEVER `open(...).read()`), note="Phase 22 v1 seed".
    3. For pipeline_config: `pipelineConfig:upsert` for require_review=true, auto_publish=false, schedule_enabled=false (values JSON-encoded strings).
    Print a per-row OK line and a final summary count (agents seeded, prompts seeded). Idempotent by virtue of the Plan-02 upsert guards. Use WORKSPACE_ID from config_loader (no hardcoded literal in logic).
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run python -c "import ast,sys; src=open('scripts/seed_phase22.py').read(); ast.parse(src); assert 'load_prompt(' in src and 'open(' not in src, 'must use load_prompt, never raw open'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/pipeline/scripts/seed_phase22.py` exists and parses (ast.parse OK)
    - `grep "load_prompt(" scripts/seed_phase22.py` present
    - `grep "open(" scripts/seed_phase22.py` returns NOTHING (no raw file read — byte-mismatch guard)
    - `grep "promptVersions:upsertActive" scripts/seed_phase22.py` AND `grep "agents:upsert" scripts/seed_phase22.py` AND `grep "pipelineConfig:upsert" scripts/seed_phase22.py` present
    - `grep "AGENT_KEY_TO_PROMPT_FILE\|ALL_AGENT_KEYS" scripts/seed_phase22.py` present (reuses config_loader, no re-derivation)
  </acceptance_criteria>
  <done>seed_phase22.py seeds agents+prompts+config idempotently, prompt content sourced exclusively from load_prompt().</done>
</task>

<task type="auto">
  <name>Task 2: Write standalone verify_prompt_seed.py (live Convex byte check)</name>
  <files>packages/pipeline/scripts/verify_prompt_seed.py</files>
  <read_first>
    - packages/pipeline/scripts/seed_phase22.py (Task 1 — mirror the AsyncClient construction)
    - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py (AGENT_KEY_TO_PROMPT_FILE — the 11 pairs to verify)
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Pattern 10 verification script; Open Questions 3 placement)
    - .planning/phases/22-config-externalization/22-VALIDATION.md (Manual-Only Verifications — the live-seed instruction this script satisfies)
  </read_first>
  <action>
    Create `packages/pipeline/scripts/verify_prompt_seed.py` — `async def main()` that, for each `agent_key, prompt_file in AGENT_KEY_TO_PROMPT_FILE.items()`:
    1. `expected = load_prompt(prompt_file)`.
    2. `row = await convex_query(http, "promptVersions:getActive", {"workspace_id": WORKSPACE_ID, "agentKey": agent_key})`.
    3. If `row is None`: print FAIL (missing) and mark failure.
    4. Else assert `row["content"] == expected`; print OK or FAIL with the first differing line index on mismatch. Also assert `row.get("version") == 1` and `row.get("isActive") is True`.
    Track any failure; `sys.exit(1)` if any diff/missing, else `sys.exit(0)`. Print a final "11/11 byte-identical" summary on success. Construct its own AsyncClient like the seed script.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run python -c "import ast; src=open('scripts/verify_prompt_seed.py').read(); ast.parse(src); assert 'getActive' in src and 'load_prompt(' in src and 'sys.exit(1)' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists and parses
    - `grep "promptVersions:getActive" scripts/verify_prompt_seed.py` present
    - `grep "load_prompt(" scripts/verify_prompt_seed.py` present (oracle) AND no `open(` raw read
    - `grep "sys.exit(1)" scripts/verify_prompt_seed.py` present (non-zero on diff)
    - `grep 'version.*1\|isActive' scripts/verify_prompt_seed.py` present (v1 active assertion)
  </acceptance_criteria>
  <done>verify_prompt_seed.py performs a live zero-diff check across the 11 rows and exits non-zero on any mismatch.</done>
</task>

<task type="auto">
  <name>Task 3: Green the mocked byte-parity + idempotency pytest (test_prompt_seed.py)</name>
  <files>packages/pipeline/tests/lib/test_prompt_seed.py</files>
  <read_first>
    - packages/pipeline/tests/lib/test_prompt_seed.py (the Plan 01 xfail scaffold to finalize)
    - packages/pipeline/scripts/seed_phase22.py (the seed logic under test — content == load_prompt(file))
    - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py (AGENT_KEY_TO_PROMPT_FILE — parametrize source)
    - packages/pipeline/tests/conftest.py (mock fixtures)
  </read_first>
  <action>
    Finalize `tests/lib/test_prompt_seed.py`: remove the xfail marks. Import `AGENT_KEY_TO_PROMPT_FILE` from config_loader (now exists). Parametrize over its 11 items.
    - `test_seeded_content_byte_identical(agent_key, prompt_file)`: assert that the value the seed WOULD send for this pair equals `load_prompt(prompt_file)` — i.e., test the seed-content contract directly (the seed builds content via `load_prompt(prompt_file)`, so assert `load_prompt(prompt_file)` round-trips through a mocked `promptVersions:getActive` returning that same content; equality holds byte-for-byte). This proves the seed logic produces byte-identical content without needing live Convex.
    - `test_seed_idempotent`: mock `convex_mutation` to record calls; run the seed's prompt-upsert path twice (or assert the upsert is called with the same args both times and that `upsertActive` is the path used, which the Plan-02 mutation guarantees keeps version==1). Assert no duplicate-version logic (the call args never include an incremented version).
    Keep the test self-contained (no live Convex; mock `convex_query`/`convex_mutation`).
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/lib/test_prompt_seed.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "xfail" tests/lib/test_prompt_seed.py` returns NOTHING
    - `grep "from eisenbalm_pipeline.lib.config_loader import" tests/lib/test_prompt_seed.py` present (uses real AGENT_KEY_TO_PROMPT_FILE)
    - `uv run pytest tests/lib/test_prompt_seed.py -q` shows 11 (or 12 incl. idempotent) passed, 0 xfail, 0 failed
  </acceptance_criteria>
  <done>test_prompt_seed.py is green: 11 byte-parity cases + idempotency, all mocked.</done>
</task>

</tasks>

<verification>
- `cd packages/pipeline && uv run pytest tests/lib/test_prompt_seed.py -q` → all passed, 0 xfail
- `uv run pytest -q` full suite green
- seed_phase22.py + verify_prompt_seed.py both parse and use `load_prompt()` exclusively (no `open(`)
- Manual (post-deploy, per 22-VALIDATION.md): run `seed_phase22.py` then `verify_prompt_seed.py` against live Convex → expect "11/11 byte-identical", exit 0
</verification>

<success_criteria>
- Idempotent seed writes 15 agents rows + 11 v1 active prompt rows + 3 pipeline_config keys, content via load_prompt()
- Live verifier proves zero byte diff across the 11 and asserts version==1/isActive==true
- Mocked pytest proves the seed content contract is byte-identical and idempotent (CI-safe)
</success_criteria>

<output>
After completion, create `.planning/phases/22-config-externalization/22-04-SUMMARY.md`
</output>
