---
phase: 22-config-externalization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/API_CONTRACTS.md
  - packages/pipeline/tests/lib/test_config_loader.py
  - packages/pipeline/tests/lib/test_prompt_seed.py
  - packages/pipeline/tests/api/test_runs_config_snapshot.py
  - packages/pipeline/tests/test_package_data_prompts.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04]
user_setup: []

must_haves:
  truths:
    - "The DispatchState.config contract is documented in API_CONTRACTS.md §7 BEFORE any code adds the field"
    - "Failing test scaffolds exist for all four CFG requirements (load_run_config, byte-parity, snapshot-ordering)"
    - "A test proves the 11 prompt .md files resolve via importlib.resources from the installed package (wheel-safe fallback)"
  artifacts:
    - path: "docs/API_CONTRACTS.md"
      provides: "DispatchState.config §7 amendment + agents/prompt_versions/pipeline_config field shapes"
      contains: "config"
    - path: "packages/pipeline/tests/lib/test_config_loader.py"
      provides: "CFG-01/CFG-03/CFG-04 test scaffold (xfail/skip stubs naming the real functions)"
    - path: "packages/pipeline/tests/lib/test_prompt_seed.py"
      provides: "CFG-02 parametrized byte-parity scaffold over the 11 agentKey/prompt-file pairs"
    - path: "packages/pipeline/tests/api/test_runs_config_snapshot.py"
      provides: "CFG-04 snapshot-before-create_task ordering scaffold + resume-no-resnap scaffold"
    - path: "packages/pipeline/tests/test_package_data_prompts.py"
      provides: "wheel-safe prompt resolution test (load_prompt over all 11)"
  key_links:
    - from: "tests/lib/test_prompt_seed.py"
      to: "lib/prompts.load_prompt"
      via: "byte oracle import"
      pattern: "load_prompt"
    - from: "docs/API_CONTRACTS.md §7"
      to: "graph/state.py DispatchState"
      via: "contract-first documentation"
      pattern: "config"
---

<objective>
Lay the Wave 0 foundation for Phase 22: (1) amend `docs/API_CONTRACTS.md §7` to document the new `DispatchState.config` field and the fleshed-out `agents`/`prompt_versions`/`pipeline_config` shapes BEFORE any code touches them (CLAUDE.md hard rule: contracts first), and (2) create the failing/skipped test scaffolds that every downstream feature task verifies against, per `22-VALIDATION.md`.

Purpose: This phase's correctness hinges on two invariants the harness must lock in early — byte-parity of migrated prompts and snapshot-before-invoke ordering. Establishing the test seams and the contract amendment first prevents downstream tasks from inventing field names or skipping verification.
Output: API_CONTRACTS.md §7 amendment + 4 new pipeline test files (scaffolds that name the real symbols and are expected to fail/skip until implemented).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-config-externalization/22-CONTEXT.md
@.planning/phases/22-config-externalization/22-RESEARCH.md
@.planning/phases/22-config-externalization/22-VALIDATION.md

<interfaces>
<!-- The byte oracle the byte-parity test imports. Extracted from lib/prompts.py. -->
From packages/pipeline/src/eisenbalm_pipeline/lib/prompts.py:
```python
def load_prompt(name: str) -> str: ...   # extracts between <!-- PROMPT START/END -->, strips one leading + one trailing newline
```

<!-- The 11 agentKey → prompt-file pairs (RESEARCH Pattern 7). The byte-parity test parametrizes over these. -->
AGENT_KEY_TO_PROMPT_FILE (the canonical 11):
  scout→scout, advocate→advocate, editor_gate1→editor, editor_final→editor-final,
  calibrator→calibrator, researcher→researcher, design→design, game→game,
  bonus_big_budget→bonus-big-budget, bonus_jingle→bonus-jingle, bonus_spec_ad→bonus-spec-ad

<!-- The 15 llm_config agent keys (source for the agents seed; full roster). -->
From lib/llm_config.py MODEL_BY_AGENT keys: calibrator, chronicler, editor_gate1, editor_final, qa,
  researcher, origin_story, problem, founder_bio, case_study, bonus, game, scout, advocate, design
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend API_CONTRACTS.md §7 for DispatchState.config + control-plane table shapes</name>
  <files>docs/API_CONTRACTS.md</files>
  <read_first>
    - docs/API_CONTRACTS.md §7 (the DispatchState contract block — find the existing field listing)
    - packages/pipeline/src/eisenbalm_pipeline/graph/state.py (current DispatchState — config field NOT yet present; mirror the existing comment style)
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Example 4 — the exact §7 amendment text; Pattern 8 — agents column additions)
    - convex/schema.ts lines 251-285 (current agents/prompt_versions/pipeline_config stubs — agents is MISSING top_p, max_tokens, description)
  </read_first>
  <action>
    In `docs/API_CONTRACTS.md §7` (DispatchState contract), add a new field entry documenting:
    - `config: NotRequired[Optional[RunConfig]]` — loaded ONCE at run start by `load_run_config()`; immutable after run start (snapshot commits before `graph.ainvoke()`); consumed by all 11 `load_prompt` call sites. Note it carries per-agent `{model, temperature, top_p, max_tokens, enabled, system_prompt}` plus pipeline-level `{require_review, auto_publish, schedule_enabled}`.
    Document the `RunConfig`/`AgentConfig` dataclass shape (RESEARCH Pattern 1) as the in-memory config object.
    In the same doc, in the section that governs the control-plane Convex tables (or add a short "§4A Phase-22 control-plane tables" subsection), record the fleshed-out shapes:
    - `agents`: workspace_id, agentKey, enabled, model?, temperature?, top_p? (ADD), max_tokens? (ADD), description? (ADD)
    - `prompt_versions`: workspace_id, agentKey, version, content, isActive, createdAt, createdBy?, note?
    - `pipeline_config`: workspace_id, key, value (JSON), updatedAt, updatedBy?
    - `runs.configSnapshot`: JSON string of the full resolved RunConfig, written before invoke.
    Do NOT touch the frozen `pipelineRuns`/`deliberationEvents`/`agentVotes`/`qaCorrections`/`pitchLog` contract sections.
  </action>
  <verify>
    <automated>grep -n "config: NotRequired" docs/API_CONTRACTS.md && grep -n "top_p" docs/API_CONTRACTS.md && grep -n "configSnapshot" docs/API_CONTRACTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "config: NotRequired" docs/API_CONTRACTS.md` returns a line in the §7 region
    - `grep "RunConfig" docs/API_CONTRACTS.md` returns ≥1 line
    - `grep "top_p" docs/API_CONTRACTS.md` AND `grep "max_tokens" docs/API_CONTRACTS.md` AND `grep "description" docs/API_CONTRACTS.md` each return ≥1 line (agents table additions documented)
    - No diff to the frozen `pipelineRuns` contract block (the existing pipelineRuns field listing is unchanged)
  </acceptance_criteria>
  <done>DispatchState.config + the three control-plane table shapes + runs.configSnapshot are documented in API_CONTRACTS.md; frozen tables untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Create CFG-01/CFG-03/CFG-04 config_loader test scaffold</name>
  <files>packages/pipeline/tests/lib/test_config_loader.py</files>
  <read_first>
    - packages/pipeline/tests/conftest.py (existing fixtures + env-guard pattern; reuse, do not duplicate)
    - packages/pipeline/tests/lib/test_voice.py (existing tests/lib style; mirror import + asyncio conventions)
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Validation Architecture → Test Map: exact test names per CFG req; Patterns 2/3 for the function signatures under test)
    - packages/pipeline/src/eisenbalm_pipeline/lib/llm_config.py (MODEL_BY_AGENT 15 keys, SAMPLING_BY_AGENT, MAX_TOKENS_BY_AGENT — the values the loader must resolve)
  </read_first>
  <action>
    Create `packages/pipeline/tests/lib/test_config_loader.py` with these scaffolded tests, each importing the not-yet-existing `eisenbalm_pipeline.lib.config_loader` symbols and marked `pytest.mark.xfail(reason="Phase 22 Plan 03 implements config_loader", strict=False)` so the suite stays green at Wave 0 and the marks are removed by Plan 03:
    - `test_load_run_config_from_convex` — mocks `convex_query` to return agents + pipeline_config rows + active prompt versions; asserts `load_run_config()` returns a `RunConfig` whose `.agents` dict has all 15 `MODEL_BY_AGENT` keys, and each AgentConfig has model/temperature/top_p/max_tokens/enabled/system_prompt.
    - `test_hard_failure_fallback` (CFG-03) — mocks `convex_query` to raise; asserts `load_run_config()` returns a full disk/code `RunConfig` (does NOT raise) AND a single WARNING is logged (use `caplog`).
    - `test_partial_fallback_per_key` (CFG-03) — Convex reachable but one prompt row missing; asserts that one agent's `system_prompt == load_prompt(<file>)` and a per-agent WARNING is logged; other agents use Convex values.
    - `test_fallback_bytes_match_load_prompt` (CFG-03) — for every key in AGENT_KEY_TO_PROMPT_FILE, the fallback `RunConfig.agents[key].system_prompt == load_prompt(file)` byte-for-byte.
    - `test_snapshot_roundtrip` (CFG-04) — `json.loads(json.dumps(dataclasses.asdict(config)))` reconstructs the same per-agent values.
    Import `load_prompt` from `eisenbalm_pipeline.lib.prompts` (it exists) as the oracle. Use the existing conftest env-guard so the file imports cleanly even before config_loader exists (guard the config_loader import inside the test bodies OR at module top with the xfail import-error tolerance).
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/lib/test_config_loader.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/pipeline/tests/lib/test_config_loader.py` exists
    - `grep "def test_load_run_config_from_convex" tests/lib/test_config_loader.py` AND `def test_hard_failure_fallback` AND `def test_partial_fallback_per_key` AND `def test_fallback_bytes_match_load_prompt` AND `def test_snapshot_roundtrip` all present
    - `uv run pytest tests/lib/test_config_loader.py -q` exits 0 (xfails counted, no errors/collection failure)
    - `grep "from eisenbalm_pipeline.lib.prompts import load_prompt" tests/lib/test_config_loader.py` present
  </acceptance_criteria>
  <done>config_loader test scaffold exists, names the real functions, exits 0 as xfail at Wave 0.</done>
</task>

<task type="auto">
  <name>Task 3: Create CFG-02 byte-parity + CFG-04 snapshot-ordering + wheel-data test scaffolds</name>
  <files>packages/pipeline/tests/lib/test_prompt_seed.py, packages/pipeline/tests/api/test_runs_config_snapshot.py, packages/pipeline/tests/test_package_data_prompts.py</files>
  <read_first>
    - packages/pipeline/tests/api/test_runs.py (existing api-test style; mirror the FastAPI/asyncio + mock conventions for the snapshot-ordering test)
    - packages/pipeline/src/eisenbalm_pipeline/api/runs.py (run_weekly ~178-242: the create→create_task ordering the snapshot test asserts against; resume_run ~279-321: the no-resnap path)
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Pattern 10 byte-verification; Pitfall 1 snapshot race; Pitfall 6 resume no re-snap; Runtime State Inventory → package-data note)
    - packages/pipeline/src/eisenbalm_pipeline/lib/prompts.py (load_prompt — oracle for both byte-parity and wheel-data tests)
  </read_first>
  <action>
    Create three files.

    (a) `packages/pipeline/tests/lib/test_prompt_seed.py` (CFG-02) — define the canonical 11-pair list inline (or import AGENT_KEY_TO_PROMPT_FILE once Plan 03 lands; for Wave 0 inline it). `@pytest.mark.parametrize` over the 11 `(agentKey, prompt_file)` pairs. Two scaffolded tests, xfail until Plan 04:
      - `test_seeded_content_byte_identical` — mocks a Convex `promptVersions:getActive` response whose `content` should equal `load_prompt(prompt_file)`; asserts exact string equality (the SEED LOGIC contract, not live Convex).
      - `test_seed_idempotent` — placeholder asserting the seed upsert keeps version==1 / isActive==true on a second run (xfail; Plan 04 fills the real seed-call mock).

    (b) `packages/pipeline/tests/api/test_runs_config_snapshot.py` (CFG-04), xfail until Plan 05:
      - `test_snapshot_before_task` — patch `asyncio.create_task` and `eisenbalm_pipeline.lib.config_loader.snapshot_config` (and `convex_mutation`); call the `run_weekly` handler; assert `snapshot_config` was awaited BEFORE `asyncio.create_task` was called (use a shared call-order recorder / `mock_calls` index comparison).
      - `test_resume_no_resnap` — call `resume_run`; assert `snapshot_config` is NEVER called on the resume path.

    (c) `packages/pipeline/tests/test_package_data_prompts.py` (wheel-safe fallback, NOT xfail — must pass now) — parametrize over all 11 prompt file stems; assert `load_prompt(stem)` returns a non-empty string (proves `importlib.resources` resolves the bundled `.md` files; this is the production fallback path and the hatchling-packaging guard the research flagged).
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/lib/test_prompt_seed.py tests/api/test_runs_config_snapshot.py tests/test_package_data_prompts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist
    - `grep "parametrize" tests/lib/test_prompt_seed.py` present AND the pair list has exactly 11 entries (`grep -c "bonus-\|editor\|scout\|advocate\|calibrator\|researcher\|design\|game" ...` consistent with 11)
    - `grep "def test_snapshot_before_task" tests/api/test_runs_config_snapshot.py` AND `def test_resume_no_resnap` present
    - `tests/test_package_data_prompts.py` PASSES (not xfail) — `uv run pytest tests/test_package_data_prompts.py -q` shows 11 passed
    - Whole command exits 0
  </acceptance_criteria>
  <done>Byte-parity, snapshot-ordering, and wheel-data test scaffolds exist; package-data test passes; the rest xfail until their implementing plans.</done>
</task>

</tasks>

<verification>
- `cd packages/pipeline && uv run pytest tests/lib/test_config_loader.py tests/lib/test_prompt_seed.py tests/api/test_runs_config_snapshot.py tests/test_package_data_prompts.py -q` exits 0
- `uv run pytest -q` (full suite) stays green (no collection errors introduced)
- `grep "config: NotRequired" docs/API_CONTRACTS.md` returns a hit
- `grep -c "passed" ` on the package-data run shows 11 passing
</verification>

<success_criteria>
- API_CONTRACTS.md §7 documents `DispatchState.config` + RunConfig shape; agents/prompt_versions/pipeline_config/runs.configSnapshot shapes recorded; frozen tables untouched
- 4 new test files exist; all four CFG requirements have a named, discoverable test seam
- The wheel-safe prompt-resolution test passes now (fallback path proven)
- Full pipeline suite still green
</success_criteria>

<output>
After completion, create `.planning/phases/22-config-externalization/22-01-SUMMARY.md`
</output>
