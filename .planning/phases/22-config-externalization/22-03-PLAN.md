---
phase: 22-config-externalization
plan: 03
type: execute
wave: 2
depends_on: ["22-01", "22-02"]
files_modified:
  - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py
  - packages/pipeline/src/eisenbalm_pipeline/graph/state.py
  - packages/pipeline/tests/lib/test_config_loader.py
autonomous: true
requirements: [CFG-01, CFG-03, CFG-04]
user_setup: []

must_haves:
  truths:
    - "load_run_config() returns a RunConfig with all 15 agent keys resolved from Convex (model/temp/top_p/max_tokens/enabled/system_prompt)"
    - "On hard Convex failure the loader returns a full disk/code RunConfig and logs ONE WARNING, never raising"
    - "On a single missing prompt/agent row, only that agent falls back (per-agent WARNING); the rest use Convex values"
    - "Fallback system_prompt bytes are identical to load_prompt(name) for all 11 prompted agents"
    - "snapshot_config() serializes RunConfig to JSON and round-trips losslessly"
    - "DispatchState gains an optional config field (NotRequired) without breaking existing state construction"
  artifacts:
    - path: "packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py"
      provides: "RunConfig/AgentConfig dataclasses, AGENT_KEY_TO_PROMPT_FILE, load_run_config(), snapshot_config(), _build_fallback_config()"
      min_lines: 120
    - path: "packages/pipeline/src/eisenbalm_pipeline/graph/state.py"
      provides: "DispatchState.config: NotRequired[Optional[RunConfig]]"
      contains: "config"
  key_links:
    - from: "config_loader.load_run_config"
      to: "convex agents:listForWorkspace / promptVersions:getActive / pipelineConfig:getAll"
      via: "convex_query at run start"
      pattern: "convex_query"
    - from: "config_loader._build_fallback_config"
      to: "lib.prompts.load_prompt + lib.llm_config"
      via: "disk/code fallback oracle"
      pattern: "load_prompt"
    - from: "config_loader.snapshot_config"
      to: "convex runs:setConfigSnapshot"
      via: "awaited mutation"
      pattern: "setConfigSnapshot"
---

<objective>
Build `lib/config_loader.py`: the `RunConfig`/`AgentConfig` dataclasses, the canonical `AGENT_KEY_TO_PROMPT_FILE` mapping, `load_run_config()` (single-round-trip Convex read with two-tier fallback per D-06/D-07), `_build_fallback_config()` (all disk/code defaults), and `snapshot_config()` (awaited JSON write to `runs:setConfigSnapshot`). Add the `config` field to `DispatchState`. Remove the xfail marks from Plan 01's config_loader tests and make them green.

Purpose: This is the heart of CFG-01/CFG-03/CFG-04. It produces the in-memory config object agents read from (Plan 05) and the snapshot the run records. The fallback must be byte-identical to today's `load_prompt()` output so the call-site swap in Plan 05 keeps voice tests green.
Output: config_loader.py + state.py config field + green config_loader tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/22-config-externalization/22-CONTEXT.md
@.planning/phases/22-config-externalization/22-RESEARCH.md
@docs/API_CONTRACTS.md

<interfaces>
<!-- The Convex read/write helpers. From lib/convex_client.py. -->
async def convex_query(http: AsyncClient, path: str, args: dict) -> Any   # returns body["value"]; raises on body.status != "success"
async def convex_mutation(http: AsyncClient, path: str, args: dict) -> Any

<!-- The fallback oracles. From lib/prompts.py + lib/llm_config.py. -->
def load_prompt(name: str) -> str                       # byte-exact prompt body
MODEL_BY_AGENT: dict[str, str]                          # 15 keys (incl. editor_gate1, editor_final, chronicler, qa, bonus, game, section writers)
SAMPLING_BY_AGENT: dict[str, dict]                      # {"temperature": x, "top_p": y?}  — NOTE: name is SAMPLING_BY_AGENT, NOT AGENT_GEN_PARAMS
MAX_TOKENS_BY_AGENT: dict[str, int]                     # only scout=12000, researcher=20000

<!-- Convex function paths created in Plan 02. -->
"agents:listForWorkspace" {workspace_id}
"promptVersions:getActive" {workspace_id, agentKey}
"pipelineConfig:getAll" {workspace_id}
"runs:setConfigSnapshot" {runId, configSnapshot}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement RunConfig dataclasses, AGENT_KEY_TO_PROMPT_FILE, and snapshot_config()</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py</files>
  <read_first>
    - packages/pipeline/src/eisenbalm_pipeline/lib/llm_config.py (exact dict names + 15 keys + values to seed defaults from — confirm SAMPLING_BY_AGENT, NOT AGENT_GEN_PARAMS)
    - packages/pipeline/src/eisenbalm_pipeline/lib/prompts.py (load_prompt signature — the fallback oracle)
    - packages/pipeline/src/eisenbalm_pipeline/lib/convex_client.py (convex_query/convex_mutation signatures + error semantics)
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Patterns 1, 3, 7 — dataclass shape, snapshot_config, the exact AGENT_KEY_TO_PROMPT_FILE table; Pitfall 4 three bonus keys; Pitfall 5 dict name; Pitfall 6 resume note)
  </read_first>
  <action>
    Create `packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py`. Use ASCII section-header comments (project convention).
    Define:
    - `WORKSPACE_ID = "eisenbalm"` module constant.
    - `@dataclass AgentConfig`: `model: str`, `temperature: float`, `top_p: float`, `max_tokens: Optional[int]`, `enabled: bool`, `system_prompt: str`.
    - `@dataclass RunConfig`: `workspace_id: str`, `agents: dict[str, AgentConfig]`, `require_review: bool`, `auto_publish: bool`, `schedule_enabled: bool`.
    - `AGENT_KEY_TO_PROMPT_FILE: dict[str, str]` with EXACTLY these 11 entries (RESEARCH Pattern 7):
      scout→"scout", advocate→"advocate", editor_gate1→"editor", editor_final→"editor-final", calibrator→"calibrator", researcher→"researcher", design→"design", game→"game", bonus_big_budget→"bonus-big-budget", bonus_jingle→"bonus-jingle", bonus_spec_ad→"bonus-spec-ad". (chronicler, qa, origin_story, problem, founder_bio, case_study deliberately ABSENT — no .md this phase, D-02.)
    - `ALL_AGENT_KEYS`: the union of MODEL_BY_AGENT keys PLUS the three bonus-variant keys (bonus_big_budget, bonus_jingle, bonus_spec_ad). The three bonus variants share MODEL_BY_AGENT["bonus"]/SAMPLING_BY_AGENT["bonus"]/MAX_TOKENS_BY_AGENT for model/temp; only their prompts differ. Build a helper `_llm_key_for(agent_key)` that maps each bonus_* variant → "bonus" and otherwise returns the key unchanged, so model/temp resolution joins the single llm_config "bonus" row.
    - `async def snapshot_config(http, run_id: str, config: RunConfig) -> None`: `snapshot = json.dumps(dataclasses.asdict(config))`; `await convex_mutation(http, "runs:setConfigSnapshot", {"runId": run_id, "configSnapshot": snapshot})`. Awaited, not fire-and-forget.
    Import `SAMPLING_BY_AGENT` (NOT AGENT_GEN_PARAMS), `MODEL_BY_AGENT`, `MAX_TOKENS_BY_AGENT`, `load_prompt`, `convex_query`, `convex_mutation`. `log = logging.getLogger(__name__)`.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run python -c "from eisenbalm_pipeline.lib.config_loader import RunConfig, AgentConfig, AGENT_KEY_TO_PROMPT_FILE, snapshot_config; assert len(AGENT_KEY_TO_PROMPT_FILE)==11; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; `grep "def snapshot_config" config_loader.py` and `grep "class RunConfig" config_loader.py` and `grep "class AgentConfig" config_loader.py` present
    - `grep "SAMPLING_BY_AGENT" config_loader.py` present AND `grep "AGENT_GEN_PARAMS" config_loader.py` returns NOTHING
    - `len(AGENT_KEY_TO_PROMPT_FILE) == 11` (assert in the verify command passes)
    - `grep '"editor_gate1": "editor"' config_loader.py` present (the key-vs-filename mapping)
    - The three bonus variant keys present: `grep "bonus_big_budget\|bonus_jingle\|bonus_spec_ad" config_loader.py`
    - `grep "runs:setConfigSnapshot" config_loader.py` present
  </acceptance_criteria>
  <done>config_loader.py defines the dataclasses, the 11-entry mapping, the bonus-variant handling, and snapshot_config.</done>
</task>

<task type="auto">
  <name>Task 2: Implement load_run_config() two-tier fallback + _build_fallback_config()</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py</files>
  <read_first>
    - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py (Task 1 output — the dataclasses + mapping to use)
    - .planning/phases/22-config-externalization/22-RESEARCH.md (Pattern 2 load_run_config full body; D-06 all-or-nothing hard failure; D-07 per-key fallback; Pitfall 3 editor_gate1 lookup)
    - packages/pipeline/src/eisenbalm_pipeline/lib/llm_config.py (default temp/top_p/max_tokens values)
  </read_first>
  <action>
    Add to `config_loader.py`:
    - `def _build_fallback_config() -> RunConfig` (D-06): for each key in ALL_AGENT_KEYS, build AgentConfig from `MODEL_BY_AGENT[_llm_key_for(key)]`, `SAMPLING_BY_AGENT[_llm_key_for(key)]` (temperature default 0.3, top_p default 1.0 when absent), `MAX_TOKENS_BY_AGENT.get(_llm_key_for(key))`, `enabled=True`, and `system_prompt = load_prompt(AGENT_KEY_TO_PROMPT_FILE[key]) if key in AGENT_KEY_TO_PROMPT_FILE else ""`. Pipeline-level defaults: require_review=True, auto_publish=False, schedule_enabled=False.
    - `async def load_run_config(http) -> RunConfig`:
      1. Try one Convex round-trip: `agents:listForWorkspace`, `pipelineConfig:getAll`. On ANY exception → `log.warning("load_run_config: Convex unreachable — using full disk/llm_config fallback for this run")` and `return _build_fallback_config()` (D-06, single WARNING, no raise).
      2. Build `agents_by_key = {r["agentKey"]: r for r in agents_rows}` and `pc = {r["key"]: json.loads(r["value"]) for r in pc_rows}`.
      3. For each key in ALL_AGENT_KEYS: resolve model = row.model or MODEL_BY_AGENT[_llm_key_for(key)]; temperature/top_p = row value or SAMPLING_BY_AGENT default; max_tokens = row.max_tokens or MAX_TOKENS_BY_AGENT.get(...); enabled = row.get("enabled", True).
      4. For prompted keys (key in AGENT_KEY_TO_PROMPT_FILE): try `promptVersions:getActive`; on exception OR None result → `log.warning("load_run_config: no active prompt_version for %s — using file fallback", key)` and `system_prompt = load_prompt(AGENT_KEY_TO_PROMPT_FILE[key])` (D-07 per-key fallback). Non-prompted keys → system_prompt="".
      5. Build pipeline-level flags from `pc` with the same defaults as fallback.
      6. Return RunConfig.
    Never raise on a single missing row (D-07); never raise on hard failure (D-06).
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/lib/test_config_loader.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "def load_run_config" config_loader.py` AND `grep "def _build_fallback_config" config_loader.py` present
    - `grep "Convex unreachable" config_loader.py` present (D-06 single WARNING string)
    - `grep "using file fallback" config_loader.py` present (D-07 per-agent WARNING)
    - `uv run pytest tests/lib/test_config_loader.py -q` shows test_hard_failure_fallback, test_partial_fallback_per_key, test_fallback_bytes_match_load_prompt, test_load_run_config_from_convex, test_snapshot_roundtrip all PASSING (xfail marks removed in Task 3)
  </acceptance_criteria>
  <done>load_run_config resolves all keys with two-tier fallback; fallback bytes equal load_prompt output.</done>
</task>

<task type="auto">
  <name>Task 3: Add DispatchState.config field + un-xfail and green the config_loader tests</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/graph/state.py, packages/pipeline/tests/lib/test_config_loader.py</files>
  <read_first>
    - packages/pipeline/src/eisenbalm_pipeline/graph/state.py (DispatchState — add field after model_versions/narrator block; mirror existing comment style; NotRequired already imported)
    - docs/API_CONTRACTS.md §7 (the config amendment from Plan 01 — keep state.py consistent)
    - packages/pipeline/tests/lib/test_config_loader.py (the Plan 01 xfail scaffolds to finalize)
    - packages/pipeline/tests/test_builder_wiring.py + tests/test_voice.py (must stay green after the field addition)
  </read_first>
  <action>
    In `graph/state.py` DispatchState, add (after the Phase-16 narrator fields, before the error/test-toggle block):
    ```
    # ── Phase 22: Config Externalization (CFG-01) ─────────────────────────────
    # Loaded ONCE at run start by lib/config_loader.load_run_config(); snapshotted
    # to runs.configSnapshot BEFORE graph.ainvoke(); consumed by the 11 prompt
    # call sites. Never mutated mid-run. See docs/API_CONTRACTS.md §7.
    config: NotRequired[Optional["RunConfig"]]
    ```
    Add a `TYPE_CHECKING` import of `RunConfig` from `eisenbalm_pipeline.lib.config_loader` (forward-ref string in the annotation avoids a runtime circular import — state.py must not import config_loader at module load). Confirm `NotRequired` and `Optional` are already imported (they are).
    Then finalize `tests/lib/test_config_loader.py`: remove the `pytest.mark.xfail` marks added in Plan 01 and make the test bodies concrete (mock `convex_query`/`convex_mutation` via the conftest pattern or `respx`/`unittest.mock`). All five tests must pass.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/lib/test_config_loader.py tests/test_builder_wiring.py tests/test_voice.py tests/test_section_writer_voice_propagation.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "config: NotRequired" graph/state.py` present
    - `grep "TYPE_CHECKING" graph/state.py` present AND config_loader is NOT imported at module top-level (no unconditional `from eisenbalm_pipeline.lib.config_loader import` outside a TYPE_CHECKING block)
    - `grep "xfail" tests/lib/test_config_loader.py` returns NOTHING (marks removed)
    - `uv run pytest tests/lib/test_config_loader.py -q` shows 5 passed, 0 xfail
    - test_builder_wiring.py, test_voice.py, test_section_writer_voice_propagation.py all still PASS (no regression from the state field)
  </acceptance_criteria>
  <done>DispatchState.config added without circular import; all five config_loader tests green; voice/wiring tests unregressed.</done>
</task>

</tasks>

<verification>
- `cd packages/pipeline && uv run pytest tests/lib/test_config_loader.py -q` → 5 passed, 0 xfail
- `uv run pytest -q` (full suite) green (voice + wiring + narrator suites unregressed)
- `grep -c "xfail" tests/lib/test_config_loader.py` == 0
- config_loader imports SAMPLING_BY_AGENT (not AGENT_GEN_PARAMS)
</verification>

<success_criteria>
- config_loader.py: dataclasses + 11-entry mapping + bonus-variant handling + two-tier fallback + snapshot_config
- Fallback system_prompt bytes == load_prompt(name) for all 11 (byte-parity foundation for Plan 05)
- DispatchState.config field added (NotRequired, forward-ref) with no circular import
- All CFG-01/CFG-03/CFG-04 loader tests green; voice/wiring tests unregressed
</success_criteria>

<output>
After completion, create `.planning/phases/22-config-externalization/22-03-SUMMARY.md`
</output>
