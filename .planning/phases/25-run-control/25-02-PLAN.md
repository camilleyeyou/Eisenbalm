---
phase: 25-run-control
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - packages/pipeline/src/eisenbalm_pipeline/api/control.py
  - packages/pipeline/src/eisenbalm_pipeline/api/runs.py
  - packages/pipeline/src/eisenbalm_pipeline/api/main.py
  - packages/pipeline/src/eisenbalm_pipeline/lib/scheduler.py
  - packages/pipeline/src/eisenbalm_pipeline/cli.py
  - convex/runs.ts
  - convex/auditLog.ts
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03]
must_haves:
  truths:
    - "An authenticated operator can POST /pipeline/run; the run lands with triggerSource='manual' and triggeredBy=the Clerk sub"
    - "POST /pipeline/tick checks schedule_enabled FIRST and no-ops when off; otherwise fires only when due and advances the next-run cursor"
    - "A second trigger while a run is 'running' is rejected 409 (manual) / skipped (tick)"
    - "Operator trigger and kill-switch-relevant cron fires write an audit_log row"
  artifacts:
    - path: "packages/pipeline/src/eisenbalm_pipeline/api/control.py"
      provides: "/pipeline/run + /pipeline/tick endpoints + shared _start_run helper"
      contains: "def pipeline_tick"
    - path: "packages/pipeline/src/eisenbalm_pipeline/lib/scheduler.py"
      provides: "_is_due + next-run cursor computation"
      contains: "def _is_due"
    - path: "convex/runs.ts"
      provides: "runs:latest already exists; triggeredBy already supported by create"
      contains: "triggeredBy"
  key_links:
    - from: "api/control.py pipeline_tick"
      to: "pipeline_config schedule_enabled"
      via: "convex_query pipelineConfig:getAll, checked FIRST"
      pattern: "schedule_enabled"
    - from: "api/control.py pipeline_run/_start_run"
      to: "runs:create"
      via: "triggerSource + triggeredBy from Clerk claims"
      pattern: "triggeredBy"
---

<objective>
Build the on-demand trigger, the kill-switch-gated scheduler tick, and the cadence engine (RUN-01, RUN-02, RUN-03). Create `api/control.py` with `/pipeline/run` (Clerk-authed, records operator identity) and `/pipeline/tick` (trigger-secret-authed, checks `schedule_enabled` first, fires only when due), factor the existing `run_weekly` body into a reusable `_start_run` helper, and add `lib/scheduler.py` for cadence/next-run math. This is the trigger keystone that Plans 03 (cancel/re-roll) and 04 (budget gate) extend.

Purpose: Give the operator manual control and a data-driven schedule with no Railway redeploy (D-10).
Output: control router mounted in main.py, scheduler lib, audit emission, cli.py repointed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-run-control/25-CONTEXT.md
@.planning/phases/25-run-control/25-RESEARCH.md
@docs/API_CONTRACTS.md

<interfaces>
<!-- Ground-truth from the codebase the executor must reuse verbatim. -->

api/runs.py::run_weekly already does, in order (PRESERVE this exact ordering — CFG-04):
```python
_require_trigger_secret(request); graph = _require_graph(request)
issue_number = await _resolve_issue_number(body.issueNumber)   # local helper, reuse
run_id = new_run_id(); started_at_ms = int(time.time()*1000); begin_run(run_id)
await convex_mutation(http, "pipelineRuns:create", {"runId","issueNumber","startedAt"})
await convex_mutation(http, "runs:create", {"workspace_id":"eisenbalm","runId","triggerSource"})  # add triggeredBy
await convex_mutation(http, "agentRuns:queueForRun", {"workspace_id","runId","agentKeys":[...]})
run_config = await load_run_config(http); await snapshot_config(http, run_id, run_config)  # MUST be before create_task
initial_state = {...}; config = {"configurable":{"thread_id": run_id}}
task = asyncio.create_task(_execute_run(app, run_id, initial_state, config))  # strong-ref'd
```
The `agent_keys` list is: ["calibrator","scout","advocate","editor_gate_1","chronicler","researcher","verify_research", *SECTION_WRITERS, "validate_sections","qa","editor_final","publisher"].

convex/runs.ts::create already accepts `triggeredBy: v.optional(v.string())` and sets status/startedAt server-side. runs:latest({workspace_id}) and runs:byRunId({runId}) already exist.

convex/auditLog.ts::write is an internalMutation (NOT callable from the Python http client directly — it is `internalMutation`). To emit audit rows from the pipeline you must add a PUBLIC mutation wrapper in auditLog.ts (e.g. `record`) that calls the same insert, OR insert directly; reuse the audit_log table shape {workspace_id, actorId, action, resourceType?, resourceId?, before?, after?, timestamp}.

Auth: api/auth.py::require_clerk_jwt returns claims dict (claims["sub"] = operator id; dev sentinel {"sub":"local-dev-operator"}). api/runs.py::_require_trigger_secret(request) enforces X-Pipeline-Trigger-Secret (skips with warning if env unset).

cli.py::trigger_weekly currently POSTs /run/weekly unconditionally (Pitfall 4.1 — must NOT be the cron target; repoint to /pipeline/tick).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor run_weekly into _start_run + add lib/scheduler.py cadence engine</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/api/runs.py, packages/pipeline/src/eisenbalm_pipeline/lib/scheduler.py</files>
  <read_first>
    - packages/pipeline/src/eisenbalm_pipeline/api/runs.py (run_weekly L186-292, _execute_run, _resolve_issue_number, _require_graph — the body to factor out without behavior change)
    - packages/pipeline/src/eisenbalm_pipeline/graph/builder.py (SECTION_WRITERS def L79-88 — re-import, do not re-list)
    - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py (load_run_config + snapshot_config signatures)
    - packages/pipeline/tests/test_scheduler.py (the RED tests this task makes green)
  </read_first>
  <behavior>
    - _is_due(pc, now): returns True iff now >= schedule_next_run_at (Unix ms); False when next_run is in the future or 0/unset is treated as "due now" only if schedule has never run — choose: 0 means "compute first occurrence and fire". Document the rule.
    - compute_next_run_at(cadence, after): returns the next Unix-ms timestamp strictly after `after` matching {dayOfWeek, hourUtc, minuteUtc} in UTC (Pitfall 6 — strictly future so an hourly tick can't double-fire).
  </behavior>
  <action>
    1. In `packages/pipeline/src/eisenbalm_pipeline/api/runs.py`, extract the run-launch body of `run_weekly` (everything from `issue_number = await _resolve_issue_number(...)` through `asyncio.create_task(...)`) into a new module-level coroutine:
       `async def _start_run(app, *, issue_number: Optional[int], trigger_source: str, triggered_by: Optional[str] = None, force_no_winner: bool = False, force_fail_agent: Optional[str] = None, narrator_slug: Optional[str] = None) -> str:` that returns `run_id`.
       Inside it: resolve issue_number (call `_resolve_issue_number` only when `issue_number is None`), generate run_id, begin_run, write `pipelineRuns:create`, write `runs:create` WITH the new `triggeredBy` arg (`triggered_by`) and `triggerSource=trigger_source`, queue agent_runs, load+snapshot config (BEFORE create_task — preserve CFG-04 ordering), build initial_state, create the strong-ref'd background task. Use `app.state.graph`/`app.state.convex_http`/`app.state.background_tasks` (it receives `app`, not `request`).
       Rewrite the existing `run_weekly` handler to call `_start_run(request.app, issue_number=body.issueNumber, trigger_source="manual", narrator_slug=body.narratorSlug, force_no_winner=body.forceNoWinner, force_fail_agent=body.forceFailAgent)` after `_require_trigger_secret`/`_require_graph`. Behavior of `/run/weekly` must stay byte-equivalent (existing tests stay green).

    2. Create `packages/pipeline/src/eisenbalm_pipeline/lib/scheduler.py` with:
       - `def compute_next_run_at(cadence: dict, after_ms: int) -> int:` — using `datetime`/`timedelta` in `timezone.utc`, returns the Unix-ms of the next `{dayOfWeek(0=Mon..6=Sun), hourUtc, minuteUtc}` occurrence strictly greater than `after_ms`. (Match Python `datetime.weekday()` semantics; document the dayOfWeek convention in a docstring and mirror it in API_CONTRACTS if it differs — cadence default `{dayOfWeek:4,...}` must mean Thursday per `0 14 * * 4`; note cron Sunday=0 vs Python Monday=0 and pick ONE — use cron convention 0=Sun..6=Sat to match `cli.py`'s `0 14 * * 4` and convert internally).
       - `def _is_due(pc: dict, now_ms: int) -> bool:` — returns `now_ms >= pc.get("schedule_next_run_at", 0)` where a value of `0` (never scheduled) is treated as due. Document.
       Make `tests/test_scheduler.py` green.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/test_scheduler.py -x -q && uv run pytest tests/test_runs.py -q 2>/dev/null || cd packages/pipeline && uv run pytest tests/test_scheduler.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "async def _start_run" packages/pipeline/src/eisenbalm_pipeline/api/runs.py` succeeds
    - `run_weekly` no longer inlines the trigger body — `grep -c "asyncio.create_task" packages/pipeline/src/eisenbalm_pipeline/api/runs.py` shows the create_task lives in `_start_run` (the resume path's create_task may remain — confirm `_start_run` contains one)
    - `lib/scheduler.py` defines both functions: `grep -E "def compute_next_run_at|def _is_due" packages/pipeline/src/eisenbalm_pipeline/lib/scheduler.py` returns 2 lines
    - `cd packages/pipeline && uv run pytest tests/test_scheduler.py -x -q` exits 0 (all scheduler tests green)
    - Existing pipeline run tests still green: `cd packages/pipeline && uv run pytest -k "run_weekly or runs" -q` exits 0 (no regression in /run/weekly behavior)
    - `snapshot_config` is still awaited BEFORE `asyncio.create_task` inside `_start_run` (read the function body to confirm ordering — CFG-04)
  </acceptance_criteria>
  <done>_start_run is the single reusable trigger body (now passing triggeredBy); scheduler.py computes due/next-run; scheduler tests green; /run/weekly unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add api/control.py with /pipeline/run + /pipeline/tick, mount it, add audit emission, repoint cli</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/api/control.py, packages/pipeline/src/eisenbalm_pipeline/api/main.py, packages/pipeline/src/eisenbalm_pipeline/cli.py, convex/auditLog.ts</files>
  <read_first>
    - packages/pipeline/src/eisenbalm_pipeline/api/runs.py (the new _start_run from Task 1, _require_trigger_secret, _require_graph, RunWeeklyBody)
    - packages/pipeline/src/eisenbalm_pipeline/api/main.py (L143-146 include_router calls + app.state usage)
    - packages/pipeline/src/eisenbalm_pipeline/api/auth.py (require_clerk_jwt signature + claims["sub"])
    - convex/auditLog.ts (write internalMutation — you must add a public wrapper to call from Python)
    - packages/pipeline/tests/test_control.py (the RED tests this task makes green)
  </read_first>
  <behavior>
    - test_manual_trigger_records_operator: POST /pipeline/run with a Clerk sentinel → runs:create called with triggerSource="manual" AND triggeredBy="local-dev-operator"; an audit_log row with action="run.triggered" is written.
    - test_tick_kill_switch_noop: with schedule_enabled=false in pipeline_config, POST /pipeline/tick → returns {"status":"skipped","reason":"schedule_disabled"} and ZERO runs:create / pipelineRuns:create calls.
  </behavior>
  <action>
    Create `packages/pipeline/src/eisenbalm_pipeline/api/control.py` with `router = APIRouter()` and:

    1. `POST /pipeline/run` (`async def pipeline_run(request, body: RunWeeklyBody, claims: dict = Depends(require_clerk_jwt))`):
       - `graph = _require_graph(request)` (imported from api.runs).
       - One-at-a-time gate (D-12): `latest = await convex_query(http, "runs:latest", {"workspace_id":"eisenbalm"})`; if `latest and latest.get("status")=="running"` → `raise HTTPException(409, "A run is already in progress. Wait for it to complete or cancel it first.")`.
       - Call `run_id = await _start_run(request.app, issue_number=body.issueNumber, trigger_source="manual", triggered_by=claims.get("sub"), narrator_slug=body.narratorSlug)`.
       - Emit an audit row: `action="run.triggered"`, `actorId=claims.get("sub")`, `resourceType="run"`, `resourceId=run_id` (via a new public Convex mutation — see step 3).
       - Return `{"runId": run_id}`.
       - NOTE: the budget start-gate (D-06) is a deliberate seam — leave a clearly-commented `# Budget start-gate inserted by Plan 04 (RUN-06) here` placeholder BEFORE _start_run so Plan 04 drops it in without restructuring.

    2. `POST /pipeline/tick` (`async def pipeline_tick(request)`):
       - `_require_trigger_secret(request)` (imported from api.runs).
       - Read pipeline_config: `pc_rows = await convex_query(http,"pipelineConfig:getAll",{"workspace_id":"eisenbalm"})`; `pc = {r["key"]: json.loads(r["value"]) for r in pc_rows}`.
       - STEP 1 (FIRST, Pitfall 4.2): `if not pc.get("schedule_enabled", False): return {"status":"skipped","reason":"schedule_disabled"}`.
       - STEP 2 (due): `from eisenbalm_pipeline.lib.scheduler import _is_due, compute_next_run_at`; `if not _is_due(pc, int(time.time()*1000)): return {"status":"skipped","reason":"not_due"}`.
       - STEP 3 (one-at-a-time): `latest = await convex_query(...,"runs:latest",...)`; `if latest and latest.get("status")=="running": return {"status":"skipped","reason":"run_in_progress"}`.
       - STEP 4 budget seam: `# Budget start-gate inserted by Plan 04 (RUN-06) here` placeholder returning `{"status":"skipped","reason":"budget_projection_exceeds_cap"}` when over.
       - STEP 5 (fire): `run_id = await _start_run(request.app, issue_number=None, trigger_source="cron")`; advance cursor — write new `schedule_next_run_at` via `pipelineConfig:upsert` using `compute_next_run_at(pc["schedule_cadence"], now_ms)`; emit audit `action="run.triggered"`, `actorId="cron"`; return `{"status":"triggered","runId":run_id}`.

    3. In `convex/auditLog.ts`, add a PUBLIC `mutation` named `record` (args: `{workspace_id, actorId, action, resourceType?, resourceId?, before?, after?}`) that inserts into `audit_log` with server-side `timestamp: Date.now()` — so the Python control router can write audit rows (the existing `write` is `internalMutation`, not callable over the http client). Keep `write` and `listForWorkspace` unchanged.

    4. In `packages/pipeline/src/eisenbalm_pipeline/api/main.py`, add `from eisenbalm_pipeline.api import ... control` to the import on L30 and add `app.include_router(control.router)` after the existing include_router calls (L143-146).

    5. In `packages/pipeline/src/eisenbalm_pipeline/cli.py`, repoint `trigger_weekly` so the Railway cron target is `/pipeline/tick` not `/run/weekly` (Pitfall 4.1): change the POST URL path to `/pipeline/tick`, keep the `X-Pipeline-Trigger-Secret` header, and update the docstring to state the cron MUST hit the kill-switch-gated tick. Preserve exit codes.

    Make `tests/test_control.py::test_manual_trigger_records_operator` and `::test_tick_kill_switch_noop` green.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/test_control.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "def pipeline_run|def pipeline_tick" packages/pipeline/src/eisenbalm_pipeline/api/control.py` returns 2 lines
    - control router is mounted: `grep -q "control.router" packages/pipeline/src/eisenbalm_pipeline/api/main.py` and `grep -q "import.*control" packages/pipeline/src/eisenbalm_pipeline/api/main.py` succeed
    - kill-switch is checked FIRST: in `pipeline_tick`, the `schedule_enabled` read+return appears BEFORE any `_start_run`/`runs:create` reference (verify by reading; the `schedule_disabled` return is the first early-return)
    - both budget seams present: `grep -c "Budget start-gate inserted by Plan 04" packages/pipeline/src/eisenbalm_pipeline/api/control.py` returns 2
    - `convex/auditLog.ts` exports a public `record` mutation: `grep -q "export const record = mutation" convex/auditLog.ts`
    - cli repointed: `grep -q "/pipeline/tick" packages/pipeline/src/eisenbalm_pipeline/cli.py` and `/run/weekly` no longer the cron POST target in trigger_weekly
    - `cd packages/pipeline && uv run pytest tests/test_control.py -x -q` exits 0
    - regression: `cd packages/pipeline && uv run pytest tests/test_cost_double_count.py -q` exits 0
  </acceptance_criteria>
  <done>/pipeline/run (operator-attributed) + /pipeline/tick (kill-switch-first, due-gated, cursor-advancing) exist, mounted, audited; cli repointed; control tests green.</done>
</task>

</tasks>

<verification>
- `cd packages/pipeline && uv run pytest tests/test_control.py tests/test_scheduler.py -x -q` exits 0.
- /run/weekly behavior unchanged (existing tests green).
- Kill switch checked first; budget seams placed for Plan 04.
</verification>

<success_criteria>
- RUN-01 manual trigger records operator identity + audit row.
- RUN-02 tick no-ops when schedule_enabled=false.
- RUN-03 tick fires only when due and advances the cursor.
- All acceptance grep + pytest checks pass; no regression.
</success_criteria>

<output>
After completion, create `.planning/phases/25-run-control/25-02-SUMMARY.md`.
</output>
