---
phase: 25-run-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/API_CONTRACTS.md
  - convex/schema.ts
  - convex/pipelineConfig.ts
  - packages/pipeline/src/eisenbalm_pipeline/lib/errors.py
  - packages/pipeline/tests/test_control.py
  - packages/pipeline/tests/test_cancel.py
  - packages/pipeline/tests/test_reroll.py
  - packages/pipeline/tests/test_budget_gate.py
  - packages/pipeline/tests/test_scheduler.py
  - packages/pipeline/tests/conftest.py
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03, RUN-04, RUN-05, RUN-06]
must_haves:
  truths:
    - "docs/API_CONTRACTS.md documents /pipeline/run, /pipeline/tick, /runs/{id}/cancel, the re-roll route, the cancel-flag, and the new pipeline_config keys BEFORE any endpoint code exists"
    - "convex/runs schema carries a cancelRequested field; pipeline_config seeds the new run-control keys with idempotent upserts"
    - "RunCancelled exception exists in lib/errors.py alongside CostCapExceeded"
    - "Five Wave 0 pytest files exist (RED) and a runs/pipeline_config Convex stub fixture is available in conftest"
  artifacts:
    - path: "docs/API_CONTRACTS.md"
      provides: "Contract-first amendment for all Phase 25 endpoints + flag + config keys"
      contains: "/pipeline/tick"
    - path: "convex/schema.ts"
      provides: "additive runs.cancelRequested field"
      contains: "cancelRequested"
    - path: "packages/pipeline/src/eisenbalm_pipeline/lib/errors.py"
      provides: "RunCancelled exception"
      contains: "class RunCancelled"
    - path: "packages/pipeline/tests/test_scheduler.py"
      provides: "Wave 0 RED scaffold for _is_due / next-run cursor"
      contains: "def test_"
  key_links:
    - from: "convex/schema.ts runs table"
      to: "convex/runs.ts requestCancel/isCancelRequested (Plan 03)"
      via: "cancelRequested optional boolean field"
      pattern: "cancelRequested"
---

<objective>
Lay the contract-first + schema + test foundation for Phase 25 Run Control. This plan is the prerequisite gate for all backend work: it amends `docs/API_CONTRACTS.md` (a CLAUDE.md hard rule — endpoints must be documented before code), adds the additive Convex schema field + idempotent `pipeline_config` seeds, defines the `RunCancelled` exception, and authors the five Wave 0 pytest scaffolds (RED) plus the shared Convex stub fixture every later plan consumes.

Purpose: Make every downstream plan (02–05) implement against a written contract and a failing test, never against assumptions.
Output: Amended API_CONTRACTS, additive schema field, seeded config keys, RunCancelled error, 5 RED test files, conftest fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-run-control/25-CONTEXT.md
@.planning/phases/25-run-control/25-RESEARCH.md

<interfaces>
<!-- Ground-truth shapes the executor must respect — extracted from the codebase. -->

Existing pipeline_config row shape (convex/schema.ts):
```
pipeline_config: defineTable({ workspace_id, key, value (JSON string), updatedBy?, updatedAt })
  .index('by_workspace', ['workspace_id'])
  .index('by_workspace_key', ['workspace_id','key'])
```

Existing runs table (convex/schema.ts ~L220) — ADD cancelRequested only, do NOT touch other fields:
```
runs: defineTable({
  workspace_id, runId, triggerSource, triggeredBy?, configSnapshot?,
  status (v.string — FREE STRING, "cancelled" is a new allowed value, no enum change),
  startedAt, completedAt?, cost?, durationMs?,
}).index('by_workspace',['workspace_id']).index('by_runId',['runId'])
```

Existing convex/pipelineConfig.ts exports: `upsert({workspace_id,key,value,updatedBy?})` (idempotent via by_workspace_key) and `getAll({workspace_id})`.

Existing lib/config_loader.py reads pipeline_config via `pc.get("schedule_enabled", False)` etc. — RunConfig already has require_review/auto_publish/schedule_enabled.

Existing lib/errors.py contains `CostCapExceeded(total_usd, cap_usd, agent_id)` and `AgentToolCallLimitExceeded`.

FROZEN — do NOT modify: `pipelineRuns.status` union (running|awaiting-review|complete|failed) and `deliberationEvents.eventType` union (Pitfall 1, CLAUDE.md). `cancelled` lives ONLY on `runs.status`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend docs/API_CONTRACTS.md contract-first for all Phase 25 surfaces</name>
  <files>docs/API_CONTRACTS.md</files>
  <read_first>
    - docs/API_CONTRACTS.md (read §3A Dashboard→Pipeline, §4A pipeline_config, §5.x webhook handler, §7 LangGraph State Contract — match their heading style + table format exactly)
    - .planning/phases/25-run-control/25-RESEARCH.md (Patterns 1–4 + Code Examples — the endpoint shapes to document verbatim)
    - .planning/phases/25-run-control/25-CONTEXT.md (D-01..D-12 — the locked decisions each contract must reflect)
  </read_first>
  <action>
    Add a new top-level section to docs/API_CONTRACTS.md titled "## 3B. Dashboard → Pipeline (run control)" placed immediately after the existing "## 3A. Dashboard → Pipeline (single-agent test-run)" section. Document these exact endpoints with request/response shapes and the locked-decision reference for each:

    1. `POST /pipeline/run` — Auth: `require_clerk_jwt` (Clerk JWT → `claims["sub"]` = `triggeredBy`). Body: `{ issueNumber?: int, narratorSlug?: string }`. Behavior: one-at-a-time gate (409 "A run is already in progress" when `runs:latest` status == "running", D-12); budget start-gate (409 when projected over monthly cap, D-06); on pass, runs the SAME work as `/run/weekly` with `triggerSource="manual"` + `triggeredBy`. Returns `{ runId }`.
    2. `POST /pipeline/tick` — Auth: `X-Pipeline-Trigger-Secret` header (NOT Clerk — Railway cron calls it). Behavior order (D-10, Pitfall 4): (a) read `schedule_enabled` FIRST → `{"status":"skipped","reason":"schedule_disabled"}` when off; (b) `_is_due` check vs `schedule_cadence`/`schedule_next_run_at` → `{"status":"skipped","reason":"not_due"}`; (c) one-at-a-time → `{"status":"skipped","reason":"run_in_progress"}`; (d) budget start-gate → `{"status":"skipped","reason":"budget_projection_exceeds_cap"}`; (e) fire with `triggerSource="cron"`, advance `schedule_next_run_at`, return `{"status":"triggered","runId":...}`.
    3. `POST /runs/{run_id}/cancel` — Auth: `require_clerk_jwt`. Behavior (D-01/D-02): 404 if run unknown; idempotent no-op `{runId,status,alreadyTerminal:true}` if not "running"; else set the cooperative cancel flag via `runs:requestCancel` and return `{runId, cancelRequested:true}`. The wrapper polls the flag; `_execute_run` writes terminal `runs.status="cancelled"`.
    4. `POST /runs/{run_id}/agents/{agent_key}/rerun` — Auth: `require_clerk_jwt`. Behavior (D-03/D-04/D-05): 422 if `agent_key` not in the re-rollable set (the 7 section writers `origin_story, problem, founder_bio, case_study, game, bonus, design`); 409 if `runs:byRunId` status == "running" (D-04); else fork the checkpoint (`aget_state` → run bare node fn → `aupdate_state(as_node=agent_key)` → re-call `write_issue_draft(merged_state)`), returns `{runId, agentKey, rerolled:true}`. Note explicitly: does NOT `ainvoke(None)` after (Pitfall 2 — siblings untouched).

    Add a "Cancel-flag contract" subsection documenting `runs.cancelRequested: optional boolean`, the new `runs:requestCancel` / `runs:isCancelRequested` / `runs:updateStatus` mutations (Plan 03 implements), and the Pitfall-1 split: `runs.status = "cancelled"` (dashboard record) while `pipelineRuns.status = "failed"` + `errorMessage="cancelled by operator"` (frozen public union preserved).

    Extend the existing "### 4A.3 — `pipeline_config`" subsection with the new keys (JSON values): `per_run_cap_usd` (number, default 10.0), `monthly_cap_usd` (number), `alert_threshold_pct` (number 50–100), `schedule_cadence` (`{dayOfWeek:int(0-6), hourUtc:int, minuteUtc:int}`), `schedule_next_run_at` (Unix ms UTC). Note `schedule_enabled` already exists.

    Do NOT modify the frozen §4.1–4.5 (pipelineRuns/deliberationEvents) shapes.
  </action>
  <verify>
    <automated>grep -q "## 3B. Dashboard → Pipeline (run control)" docs/API_CONTRACTS.md && grep -q "/pipeline/tick" docs/API_CONTRACTS.md && grep -q "schedule_next_run_at" docs/API_CONTRACTS.md && grep -q "cancelRequested" docs/API_CONTRACTS.md && grep -q "/runs/{run_id}/agents/{agent_key}/rerun" docs/API_CONTRACTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## 3B" docs/API_CONTRACTS.md` returns 1
    - All four endpoint paths appear: `grep -E "/pipeline/run|/pipeline/tick|/runs/\{run_id\}/cancel|/rerun" docs/API_CONTRACTS.md` returns ≥4 lines
    - `grep -E "per_run_cap_usd|monthly_cap_usd|alert_threshold_pct|schedule_cadence|schedule_next_run_at" docs/API_CONTRACTS.md` returns ≥5 lines
    - `grep -q "cancelled by operator" docs/API_CONTRACTS.md` succeeds (Pitfall-1 split documented)
    - The pre-existing string "running|awaiting-review|complete|failed" (pipelineRuns frozen union) still appears unchanged
  </acceptance_criteria>
  <done>API_CONTRACTS.md documents every new endpoint, the cancel-flag contract, the Pitfall-1 status split, and the five new pipeline_config keys; frozen unions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add additive runs.cancelRequested schema field, RunCancelled error, and idempotent pipeline_config seeds</name>
  <files>convex/schema.ts, convex/pipelineConfig.ts, packages/pipeline/src/eisenbalm_pipeline/lib/errors.py, packages/pipeline/scripts/seed_phase25_config.py</files>
  <read_first>
    - convex/schema.ts (the `runs:` defineTable block ~L220 — add ONE field, change nothing else)
    - convex/pipelineConfig.ts (the `upsert` mutation signature — reuse it for seeds; do NOT add new mutations here)
    - packages/pipeline/src/eisenbalm_pipeline/lib/errors.py (match the existing CostCapExceeded class shape/docstring style)
    - .planning/phases/24-prompt-editor-versioning/24-CONTEXT.md (the Phase 22/24 idempotent-upsert seed convention)
  </read_first>
  <action>
    1. In `convex/schema.ts`, inside the `runs: defineTable({...})` block, add exactly one field after `durationMs: v.optional(v.number()),`:
       `cancelRequested: v.optional(v.boolean()), // Phase 25 RUN-04 cooperative cancel flag the wrapper polls`
       Do NOT touch the `status: v.string()` field (it already accepts the new "cancelled" value as a free string), do NOT touch indexes, do NOT touch `pipelineRuns`.

    2. In `packages/pipeline/src/eisenbalm_pipeline/lib/errors.py`, add a new exception class `RunCancelled(Exception)` modeled on `CostCapExceeded`:
       ```python
       class RunCancelled(Exception):
           """Raised by wrap_agent_node when the cooperative cancel flag is set (RUN-04, D-02).

           The wrapper no-ops the node cleanly (no started/completed emit) and raises
           this; api/runs.py::_execute_run catches it and writes runs.status='cancelled'
           (Pitfall 1: pipelineRuns.status stays 'failed' + errorMessage)."""
           def __init__(self, run_id: str) -> None:
               self.run_id = run_id
               super().__init__(f"run-cancelled: {run_id}")
       ```

    3. Create a new idempotent seed script `packages/pipeline/scripts/seed_phase25_config.py` (mirror the Phase 22/24 seed style — calls `convex/pipelineConfig.ts::upsert` per key with `workspace_id="eisenbalm"`, JSON-encoded values, idempotent because upsert patches by `by_workspace_key`). Seed these keys with default JSON values: `per_run_cap_usd` → `10.0`, `monthly_cap_usd` → `200.0`, `alert_threshold_pct` → `80`, `schedule_cadence` → `{"dayOfWeek":4,"hourUtc":14,"minuteUtc":0}`, `schedule_next_run_at` → `0`. Do NOT seed `schedule_enabled` (it already exists and must default OFF). Include a module docstring noting the script is idempotent and safe to re-run, and that `schedule_enabled` is intentionally NOT seeded so automation stays off by default.
  </action>
  <verify>
    <automated>grep -q "cancelRequested: v.optional(v.boolean())" convex/schema.ts && grep -q "class RunCancelled" packages/pipeline/src/eisenbalm_pipeline/lib/errors.py && test -f packages/pipeline/scripts/seed_phase25_config.py && grep -q "monthly_cap_usd" packages/pipeline/scripts/seed_phase25_config.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cancelRequested" convex/schema.ts` returns ≥1 and the line is inside the `runs:` block (within 15 lines after `runId: v.string()` of runs)
    - `grep -q "v.union" convex/schema.ts` for the `pipelineRuns` status block still shows `running|awaiting-review|complete|failed` unchanged
    - `python -c "import ast; ast.parse(open('packages/pipeline/src/eisenbalm_pipeline/lib/errors.py').read())"` exits 0
    - `grep -E "per_run_cap_usd|monthly_cap_usd|alert_threshold_pct|schedule_cadence|schedule_next_run_at" packages/pipeline/scripts/seed_phase25_config.py` returns ≥5 lines
    - `seed_phase25_config.py` does NOT contain the string `schedule_enabled` (grep returns nothing)
  </acceptance_criteria>
  <done>runs.cancelRequested added additively; RunCancelled defined; idempotent seed script seeds the 5 new config keys (not schedule_enabled).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author the 5 Wave 0 RED pytest scaffolds + the runs/pipeline_config conftest stub fixture</name>
  <files>packages/pipeline/tests/test_control.py, packages/pipeline/tests/test_cancel.py, packages/pipeline/tests/test_reroll.py, packages/pipeline/tests/test_budget_gate.py, packages/pipeline/tests/test_scheduler.py, packages/pipeline/tests/conftest.py</files>
  <read_first>
    - packages/pipeline/tests/conftest.py (the `convex_mutation_safe` patch fixture ~L197 + `convex_query_fn` fixture ~L77 — mirror these to add a runs/pipeline_config stub)
    - packages/pipeline/tests/test_test_run.py (existing convex-stub integration test pattern to copy for stub-mode + monkeypatch)
    - .planning/phases/25-run-control/25-RESEARCH.md (## Validation Architecture → "Phase Requirements → Test Map" table — the exact test names + assertions to scaffold)
  </read_first>
  <behavior>
    - test_control.py: `test_manual_trigger_records_operator` (RUN-01 — runs row has triggerSource="manual" + triggeredBy), `test_tick_kill_switch_noop` (RUN-02 — schedule_enabled=false → zero runs:create, reason "schedule_disabled").
    - test_cancel.py: `test_cancel_lands_cancelled` (RUN-04 — after requestCancel, next node never emits agentRuns:started; runs.status becomes "cancelled"), `test_cooperative_not_violent` (RUN-04 — in-flight node finishes, no task.cancel()).
    - test_reroll.py: `test_reroll_leaves_siblings_unchanged` (RUN-05 — every doc key except the re-rolled section byte-identical), `test_reroll_blocked_while_running` (RUN-05/D-04 — 409), `test_reroll_rejects_non_section` (RUN-05/D-03 — 422).
    - test_budget_gate.py: `test_start_gate_refuses_over_budget` (RUN-06 — 409, no run created), `test_per_run_cap_from_db` (RUN-06/D-08), `test_monthly_alert_no_cancel` (RUN-06/D-07 — cost-warning scope=monthly, no cancel).
    - test_scheduler.py: `test_is_due_fires_when_due`, `test_is_due_skips_not_due`, `test_next_run_cursor_advances` (RUN-03/Pitfall 6).
  </behavior>
  <action>
    Create the five files under `packages/pipeline/tests/`: `test_control.py`, `test_cancel.py`, `test_reroll.py`, `test_budget_gate.py`, `test_scheduler.py`. Each test from the behavior block above is written as a real test function that imports the not-yet-existing target (e.g. `from eisenbalm_pipeline.api.control import ...`, `from eisenbalm_pipeline.lib.scheduler import _is_due`) so it FAILS RED until Plans 02–04 land. Where the import target does not exist yet, wrap the import in the test body (not module top-level) OR use `pytest.importorskip(...)` is NOT allowed — instead, use a top-level `import` so collection fails clearly RED, matching the Wave 0 convention. Each test must contain a real `assert` reflecting its behavior (not `assert True`); use the conftest stub fixture for Convex calls.

    In `packages/pipeline/tests/conftest.py`, add a new fixture `convex_runs_store` (and a paired `convex_config_store`) that provides an in-memory dict-backed stub for `runs:create`/`runs:byRunId`/`runs:latest`/`runs:requestCancel`/`runs:isCancelRequested`/`runs:updateStatus` and `pipelineConfig:getAll`/`pipelineConfig:upsert`, by monkeypatching `lib.convex_client.convex_mutation`/`convex_query`/`convex_mutation_safe` the same way the existing `convex_mutation_safe` fixture does. The fixture must let a test seed config rows (e.g. `schedule_enabled`, `monthly_cap_usd`) and read back recorded mutation calls (so `test_tick_kill_switch_noop` can assert zero `runs:create` calls).
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/test_control.py tests/test_cancel.py tests/test_reroll.py tests/test_budget_gate.py tests/test_scheduler.py --collect-only -q 2>&1 | tail -5; echo "EXPECT: collection errors / RED until Plans 02-04"</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist: `ls packages/pipeline/tests/test_{control,cancel,reroll,budget_gate,scheduler}.py` lists 5 files
    - Each file defines its named tests: `grep -l "def test_manual_trigger_records_operator" packages/pipeline/tests/test_control.py`, `def test_cancel_lands_cancelled` in test_cancel.py, `def test_reroll_leaves_siblings_unchanged` in test_reroll.py, `def test_start_gate_refuses_over_budget` in test_budget_gate.py, `def test_is_due_fires_when_due` in test_scheduler.py all match
    - No test body contains the bare line `assert True` (grep returns nothing across the 5 files)
    - `grep -q "def convex_runs_store" packages/pipeline/tests/conftest.py` and `grep -q "pipelineConfig:getAll" packages/pipeline/tests/conftest.py` succeed
    - The 5 new tests fail RED (collection error or assertion failure) — they are NOT green yet (Plans 02–04 make them green)
    - The pre-existing suite still collects: `cd packages/pipeline && uv run pytest tests/test_cost_double_count.py -q` stays green
  </acceptance_criteria>
  <done>Five RED Wave 0 test files encode RUN-01..RUN-06 acceptance bars; conftest provides a runs/pipeline_config Convex stub fixture consumed by all five.</done>
</task>

</tasks>

<verification>
- API_CONTRACTS.md amended before any endpoint code (CLAUDE.md hard rule satisfied).
- Schema change is additive only (runs.cancelRequested); frozen unions untouched.
- RunCancelled importable; seed script idempotent.
- 5 RED test files + conftest fixture present; existing tripwires green.
</verification>

<success_criteria>
- `grep` checks in all three tasks pass.
- `cd packages/pipeline && uv run pytest tests/test_cost_double_count.py -q` exits 0 (no regression).
- The five new test files are RED (expected — they gate Plans 02–04).
</success_criteria>

<output>
After completion, create `.planning/phases/25-run-control/25-01-SUMMARY.md`.
</output>
