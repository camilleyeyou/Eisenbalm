---
phase: 25-run-control
plan: 05
type: execute
wave: 5
depends_on: ["25-02", "25-03", "25-04"]
files_modified:
  - apps/dispatch-control/lib/pipelineControlClient.ts
  - apps/dispatch-control/app/(dashboard)/runs/_components/RunControlBar.tsx
  - apps/dispatch-control/app/(dashboard)/runs/_components/BudgetAlertBanner.tsx
  - apps/dispatch-control/app/(dashboard)/runs/_components/RunsTable.tsx
  - apps/dispatch-control/app/(dashboard)/runs/page.tsx
  - apps/dispatch-control/app/(dashboard)/runs/_components/RunDetail.tsx
  - apps/dispatch-control/app/(dashboard)/runs/_components/CancelRunButton.tsx
  - apps/dispatch-control/app/(dashboard)/runs/_components/RerollButton.tsx
  - apps/dispatch-control/app/(dashboard)/config/page.tsx
  - apps/dispatch-control/app/(dashboard)/config/_components/AutomationPanel.tsx
  - apps/dispatch-control/app/(dashboard)/config/_components/BudgetCapsPanel.tsx
  - apps/dispatch-control/app/(dashboard)/config/_components/NextRunDisplay.tsx
  - convex/pipelineConfig.ts
  - apps/dispatch-control/__tests__/runControl.test.tsx
  - apps/dispatch-control/__tests__/nextRunDisplay.test.tsx
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03, RUN-04, RUN-05, RUN-06]
must_haves:
  truths:
    - "The Runs page has a Trigger Run button (two-step inline confirm) that is disabled with a tooltip when a run is already running"
    - "The Run-detail page shows a Cancel Run button (running only) and a Re-roll link per section-writer row (disabled while running)"
    - "The Config page renders three panels: Automation (kill switch + schedule), Budget Caps, Danger Zone — with the kill-switch ON/OFF colored toggle as the focal point"
    - "Next-run shows the operator's local timezone with UTC alongside; cancelled status renders a neutral badge"
  artifacts:
    - path: "apps/dispatch-control/lib/pipelineControlClient.ts"
      provides: "Clerk-authed fetch helpers for /pipeline/run, /runs/{id}/cancel, /runs/{id}/agents/{key}/rerun"
      contains: "triggerRun"
    - path: "apps/dispatch-control/app/(dashboard)/config/_components/AutomationPanel.tsx"
      provides: "kill-switch toggle + schedule editor (RUN-02/RUN-03)"
      contains: "role=\"switch\""
    - path: "apps/dispatch-control/app/(dashboard)/runs/_components/RerollButton.tsx"
      provides: "per-section re-roll control (RUN-05)"
      contains: "rerun"
  key_links:
    - from: "RunControlBar Trigger Run"
      to: "pipelineControlClient.triggerRun → POST /pipeline/run"
      via: "useAuth getToken bearer"
      pattern: "triggerRun"
    - from: "AutomationPanel kill switch"
      to: "convex pipelineConfig:upsert schedule_enabled"
      via: "useMutation, role=switch aria-checked"
      pattern: "schedule_enabled"
    - from: "RerollButton"
      to: "pipelineControlClient.rerollAgent → POST /runs/{id}/agents/{key}/rerun"
      via: "section-writer-only, disabled while running"
      pattern: "rerollAgent"
---

<objective>
Build all the run-control dashboard surfaces in `apps/dispatch-control` per `25-UI-SPEC.md` (RUN-01..RUN-06): the Runs-page control bar (Trigger Run + budget alert banner + cancelled status badge), the Run-detail cancel + per-section re-roll controls, and the three-panel Config page (kill-switch + schedule editor + budget caps + danger zone). All built from existing Tailwind/CSS-vars/lucide-react — zero new deps. A typed Clerk-authed pipeline-control client mirrors the existing `testRunClient.ts`.

Purpose: Give the operator the visible control plane over triggering, cancelling, re-rolling, scheduling, and budgeting runs.
Output: pipeline-control client, 5 Runs/Run-detail components, 3 Config panels, cancelled badge, 2 vitest files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-run-control/25-UI-SPEC.md
@.planning/phases/25-run-control/25-CONTEXT.md
@docs/API_CONTRACTS.md

<interfaces>
<!-- Ground-truth dashboard patterns the executor must reuse verbatim. -->

testRunClient.ts pattern (mirror exactly): `pipelineBaseUrl()` reads NEXT_PUBLIC_PIPELINE_URL (throws if unset, strips trailing slash); fetch with `Authorization: Bearer ${token}` from `useAuth().getToken()`; on !res.ok throw `Error(`... (${res.status}): ${detail}`)`.

Convex client hooks: `import { useQuery, useMutation } from 'convex/react'` + `import { api } from '@convex/...'` (see RunDetail.tsx). Existing: api.runs.byRunId, api.runs.latest, api.runs.listForWorkspace, api.agentRuns.byRunId, api.pipelineConfig.getAll, api.pipelineConfig.upsert, api.runs.monthToDateCost (Plan 04).

RunsTable.tsx STATUS_CLASSES (lines 39-44) — running/done/failed/awaiting-review. ADD `cancelled: 'bg-neutral-100 text-neutral-600'` (UI-SPEC color table). Used by both RunsTable and RunDetail.

RunDetail.tsx per-agent table: `agentRuns.map(ar => ...)` with columns Agent/Status/Cost/Duration/TokensIn/TokensOut/Error. Add a Re-roll cell. Section-writer set: origin_story, problem, founder_bio, case_study, game, bonus, design (UI-SPEC; matches RE_ROLLABLE).

UI-SPEC locked specifics (apply verbatim):
- Every <button> `min-h-[44px]`; focus `focus-visible:ring-2 focus-visible:ring-neutral-900 focus-visible:ring-offset-1`.
- Trigger Run: primary `min-h-[44px] px-4` neutral-900 bg / white text; disabled+tooltip "A run is already in progress. Wait for it to complete or cancel it first." when latest run status==="running". Two-step INLINE confirm (not modal): click → "Confirm Run?" + "Cancel" text link → POST.
- Budget alert banner: `role="alert"` `bg-amber-50 border border-amber-200 rounded-md px-4 py-3 text-sm text-amber-800` with AlertTriangle 16px; copy "Month-to-date spend has reached {N}% of your monthly cap (${X} of ${Y}). Add capacity or pause automation." dismissible X (min-h/w-[44px]).
- Cancel Run: `bg-white border border-red-200 text-red-600 hover:bg-red-50 min-h-[44px] px-4 rounded-md text-sm`; running-only; two-step inline ("Confirm Cancel?" + "Keep Running"); not rendered when status is cancelled/done/failed.
- Re-roll: text-link `text-xs text-neutral-500 hover:text-neutral-900 underline`; section-writers only; disabled `text-neutral-300 cursor-not-allowed` + tooltip "Re-roll is available only after the run completes (D-04)." when running; one-step `window.confirm("Re-roll [section]? This will regenerate just this section and rewrite the Sanity draft. All other sections are unchanged.")`; success "Re-rolled ✓" green for 3s.
- Kill switch: `<button role="switch" aria-checked={scheduleEnabled}>`; ON `bg-neutral-900 text-white`; OFF `bg-red-500 text-white`; always-visible badge "Automation ON" (neutral-100/600) or "Automation OFF" (red-50/700, border-red-200). Focal point of /config.
- Schedule editor visible only when scheduleEnabled===true: day-of-week select Mon–Sun, hour+minute UTC selects, "Next run:" line in local tz + UTC, "Save Schedule" secondary min-h-[44px].
- Budget Caps panel: per-run cap + monthly cap number inputs (min 0 step 0.01), alert threshold % (min 50 max 100), "Save Caps"; read-only projection "Projected next run cost: $X.XXXX" (4-decimal) + "Month-to-date: $X.XX of $Y.YY cap" progress bar (track bg-neutral-200 h-1.5 rounded-full, fill bg-amber-400 when ≥ threshold else bg-neutral-400); "No completed runs yet — projection unavailable" when no history.
- Danger Zone panel: `border-red-200`, h2 text-red-700, copy-only "To force-cancel a running run, go to Runs and click Cancel on the active run."
- Copy: no exclamation marks, no urgency vocabulary; all strings verbatim from the UI-SPEC Copywriting Contract.

force-dynamic is required on dashboard pages that use Convex useQuery (existing pattern on runs/page.tsx) — keep it on config/page.tsx too.

Vitest: `pnpm --filter dispatch-control test:unit`; tests live in apps/dispatch-control/__tests__/; component tests use the existing testing-library setup (see PromptEditor.test.tsx / AgentNode.test.tsx).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pipeline-control client + Runs-page control bar, budget banner, cancelled badge</name>
  <files>apps/dispatch-control/lib/pipelineControlClient.ts, apps/dispatch-control/app/(dashboard)/runs/_components/RunControlBar.tsx, apps/dispatch-control/app/(dashboard)/runs/_components/BudgetAlertBanner.tsx, apps/dispatch-control/app/(dashboard)/runs/_components/RunsTable.tsx, apps/dispatch-control/app/(dashboard)/runs/page.tsx, apps/dispatch-control/__tests__/runControl.test.tsx</files>
  <read_first>
    - apps/dispatch-control/lib/testRunClient.ts (mirror pipelineBaseUrl + fetch + error pattern exactly)
    - apps/dispatch-control/app/(dashboard)/runs/page.tsx (server component — where to mount the control bar)
    - apps/dispatch-control/app/(dashboard)/runs/_components/RunsTable.tsx (STATUS_CLASSES L39-44 — add cancelled; latest-run lookup pattern via useQuery)
    - apps/dispatch-control/app/(dashboard)/prompts/_components/TestRunPanel.tsx (useAuth getToken + loading/error state pattern)
    - .planning/phases/25-run-control/25-UI-SPEC.md (Run control header + Trigger confirmation flow + Budget alert surface + Status badge palette)
  </read_first>
  <action>
    1. Create `apps/dispatch-control/lib/pipelineControlClient.ts` mirroring testRunClient.ts (same `pipelineBaseUrl()` + bearer fetch + error handling). Export:
       - `triggerRun(body: {issueNumber?: number; narratorSlug?: string}, token: string|null): Promise<{runId: string}>` → POST `${base}/pipeline/run`.
       - `cancelRun(runId: string, token: string|null): Promise<{runId: string; cancelRequested?: boolean; alreadyTerminal?: boolean; status?: string}>` → POST `${base}/runs/${encodeURIComponent(runId)}/cancel`.
       - `rerollAgent(runId: string, agentKey: string, token: string|null): Promise<{runId: string; agentKey: string; rerolled: boolean}>` → POST `${base}/runs/${encodeURIComponent(runId)}/agents/${encodeURIComponent(agentKey)}/rerun`.
       On !res.ok throw `Error` including res.status + body text (the 409/422 detail strings surface to the operator).

    2. In `RunsTable.tsx`, add `cancelled: 'bg-neutral-100 text-neutral-600'` to STATUS_CLASSES (RUN-04 badge, UI-SPEC color table). Export STATUS_CLASSES so RunDetail/other components reuse it (or keep duplicated consistently — prefer a shared export).

    3. Create `apps/dispatch-control/app/(dashboard)/runs/_components/RunControlBar.tsx` (Client Component, `'use client'`). It: reads `api.runs.latest({workspace_id})`; renders a flex `justify-between items-center` bar with `<h1>Runs</h1>` left and the Trigger Run button right. Trigger button: primary neutral-900/white `min-h-[44px] px-4`; when `latest?.status==="running"` it is disabled with `title="A run is already in progress. Wait for it to complete or cancel it first."`. Two-step inline confirm: click → swap to secondary "Confirm Run?" + a "Cancel" text link → on Confirm call `triggerRun({}, await getToken())`, show the loading spinner (Loader2 animate-spin 16px + sr-only "Loading…"), revert/disable while running; on error show the error text. No modal.

    4. Create `apps/dispatch-control/app/(dashboard)/runs/_components/BudgetAlertBanner.tsx` (Client Component). Reads `api.pipelineConfig.getAll` + `api.runs.monthToDateCost` (workspace_id); computes pct = mtdUsd / monthly_cap_usd; renders the `role="alert"` amber banner with the verbatim copy when `pct*100 >= alert_threshold_pct` and the cap > 0; dismissible X (`aria-label="Dismiss budget alert"`, min-h/w-[44px]); renders nothing otherwise.

    5. In `runs/page.tsx`, replace the inline `<h1>Runs</h1>` with `<BudgetAlertBanner workspace_id=.../>` then `<RunControlBar workspace_id=.../>` above the existing `<CostRollup/>` + `<RunsTable/>` (the h1 now lives inside RunControlBar). Keep `export const dynamic = 'force-dynamic'`.

    Add the trigger + banner + cancelled-badge assertions to a new `apps/dispatch-control/__tests__/runControl.test.tsx` (render RunControlBar with a mocked running latest → button disabled + tooltip; render with idle latest → enabled; assert STATUS_CLASSES has a cancelled entry; assert no exclamation marks / no urgency words in the rendered copy).
  </action>
  <verify>
    <automated>cd apps/dispatch-control && pnpm test:unit -- runControl 2>&1 | tail -20 || pnpm --filter dispatch-control test:unit -- runControl</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "export async function triggerRun|export async function cancelRun|export async function rerollAgent" apps/dispatch-control/lib/pipelineControlClient.ts` returns 3 lines
    - `grep -q "NEXT_PUBLIC_PIPELINE_URL" apps/dispatch-control/lib/pipelineControlClient.ts` succeeds
    - `grep -q "cancelled: 'bg-neutral-100 text-neutral-600'" apps/dispatch-control/app/\(dashboard\)/runs/_components/RunsTable.tsx` succeeds
    - RunControlBar disables on running: `grep -q "A run is already in progress" apps/dispatch-control/app/\(dashboard\)/runs/_components/RunControlBar.tsx`
    - BudgetAlertBanner uses role=alert + verbatim copy: `grep -q 'role="alert"' apps/dispatch-control/app/\(dashboard\)/runs/_components/BudgetAlertBanner.tsx` and `grep -q "Add capacity or pause automation" apps/dispatch-control/app/\(dashboard\)/runs/_components/BudgetAlertBanner.tsx`
    - runs/page.tsx mounts both: `grep -q "RunControlBar" apps/dispatch-control/app/\(dashboard\)/runs/page.tsx` and `grep -q "BudgetAlertBanner" apps/dispatch-control/app/\(dashboard\)/runs/page.tsx`
    - no exclamation mark in any new file copy: `grep -rn "!" apps/dispatch-control/app/\(dashboard\)/runs/_components/RunControlBar.tsx apps/dispatch-control/app/\(dashboard\)/runs/_components/BudgetAlertBanner.tsx | grep -v "!==" | grep -v "!=" | grep -v "className" ` returns no user-facing copy lines with "!"
    - `cd apps/dispatch-control && pnpm test:unit -- runControl` exits 0
  </acceptance_criteria>
  <done>Pipeline-control client + Runs control bar (Trigger Run two-step, disabled-while-running), budget alert banner, and cancelled badge exist; runControl test green.</done>
</task>

<task type="auto">
  <name>Task 2: Run-detail Cancel Run + per-section Re-roll controls</name>
  <files>apps/dispatch-control/app/(dashboard)/runs/_components/RunDetail.tsx, apps/dispatch-control/app/(dashboard)/runs/_components/CancelRunButton.tsx, apps/dispatch-control/app/(dashboard)/runs/_components/RerollButton.tsx</files>
  <read_first>
    - apps/dispatch-control/app/(dashboard)/runs/_components/RunDetail.tsx (run header card + per-agent table map — add CancelRunButton below header, Re-roll cell in the table)
    - apps/dispatch-control/lib/pipelineControlClient.ts (cancelRun + rerollAgent from Task 1)
    - apps/dispatch-control/app/(dashboard)/prompts/_components/TestRunPanel.tsx (useAuth getToken + running/error state for the POST flow)
    - .planning/phases/25-run-control/25-UI-SPEC.md (Cancel run + Re-roll agent section specs + Copywriting Contract)
  </read_first>
  <action>
    1. Create `apps/dispatch-control/app/(dashboard)/runs/_components/CancelRunButton.tsx` (Client). Props `{runId, status}`. Renders nothing unless `status==="running"`. Button styling per UI-SPEC (`bg-white border border-red-200 text-red-600 hover:bg-red-50 min-h-[44px] px-4 rounded-md text-sm`). Two-step inline: click → "Confirm Cancel?" (focused via autoFocus) + "Keep Running" text link → on Confirm `await cancelRun(runId, await getToken())` with Loader2 loading state; status badge updates via the existing Convex subscription (no manual refetch needed). Copy verbatim from the Contract.

    2. Create `apps/dispatch-control/app/(dashboard)/runs/_components/RerollButton.tsx` (Client). Props `{runId, agentKey, runStatus}`. Renders only for the 7 section writers (module-level `const SECTION_WRITERS = ['origin_story','problem','founder_bio','case_study','game','bonus','design']`); for any other agentKey render an empty cell (return null). When `runStatus==="running"`: render "Re-roll" disabled `text-neutral-300 cursor-not-allowed` with `title="Re-roll is available only after the run completes (D-04)."`. Otherwise render the text-link "Re-roll" (`text-xs text-neutral-500 hover:text-neutral-900 underline`); on click `window.confirm("Re-roll " + sectionLabel + "? This will regenerate just this section and rewrite the Sanity draft. All other sections are unchanged.")`; if confirmed `await rerollAgent(runId, agentKey, await getToken())`; on success show inline `text-green-600 text-xs` "Re-rolled ✓" for 3 seconds then clear; on error show the error text.

    3. In `RunDetail.tsx`: import + render `<CancelRunButton runId={runId} status={run.status} />` below the run header card. Add a "Re-roll" `<th>` to the per-agent table header and a `<td>` rendering `<RerollButton runId={runId} agentKey={ar.agentKey} runStatus={run.status} />` per row. Reuse the shared STATUS_CLASSES (so the cancelled badge renders here too).

    Extend `apps/dispatch-control/__tests__/runControl.test.tsx` (or add to it): CancelRunButton renders for running, not for cancelled; RerollButton renders the link for a section-writer key on a completed run, returns null for a non-section key (e.g. "scout"/"qa"), and is disabled with the D-04 tooltip when running.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && pnpm test:unit -- runControl 2>&1 | tail -20 || pnpm --filter dispatch-control test:unit -- runControl</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "status === 'running'" apps/dispatch-control/app/\(dashboard\)/runs/_components/CancelRunButton.tsx` (running-only render) and `grep -q "Confirm Cancel?" apps/dispatch-control/app/\(dashboard\)/runs/_components/CancelRunButton.tsx`
    - RerollButton restricts to section writers: `grep -q "SECTION_WRITERS" apps/dispatch-control/app/\(dashboard\)/runs/_components/RerollButton.tsx` and the array contains all 7 keys (`grep -E "origin_story.*problem.*founder_bio.*case_study.*game.*bonus.*design" apps/dispatch-control/app/\(dashboard\)/runs/_components/RerollButton.tsx` matches across the array)
    - D-04 disabled tooltip present: `grep -q "Re-roll is available only after the run completes (D-04)." apps/dispatch-control/app/\(dashboard\)/runs/_components/RerollButton.tsx`
    - re-roll confirm copy verbatim: `grep -q "All other sections are unchanged." apps/dispatch-control/app/\(dashboard\)/runs/_components/RerollButton.tsx`
    - RunDetail mounts both: `grep -q "CancelRunButton" apps/dispatch-control/app/\(dashboard\)/runs/_components/RunDetail.tsx` and `grep -q "RerollButton" apps/dispatch-control/app/\(dashboard\)/runs/_components/RunDetail.tsx`
    - `cd apps/dispatch-control && pnpm test:unit -- runControl` exits 0
  </acceptance_criteria>
  <done>Run-detail page has a running-only Cancel Run (two-step) and per-row Re-roll (section-only, D-04-disabled, isolated-confirm) controls; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Config page — Automation, Budget Caps, Danger Zone panels + next-run tz display</name>
  <files>apps/dispatch-control/app/(dashboard)/config/page.tsx, apps/dispatch-control/app/(dashboard)/config/_components/AutomationPanel.tsx, apps/dispatch-control/app/(dashboard)/config/_components/BudgetCapsPanel.tsx, apps/dispatch-control/app/(dashboard)/config/_components/NextRunDisplay.tsx, apps/dispatch-control/__tests__/nextRunDisplay.test.tsx</files>
  <read_first>
    - apps/dispatch-control/app/(dashboard)/config/page.tsx (the placeholder being replaced entirely)
    - apps/dispatch-control/app/(dashboard)/runs/page.tsx (force-dynamic + getCurrentWorkspace server-component pattern)
    - convex/pipelineConfig.ts (getAll/upsert — the read + write the panels use)
    - .planning/phases/25-run-control/25-UI-SPEC.md (New surface: Config page — three panels, focal point, exact copy + styling)
  </read_first>
  <action>
    1. Create `apps/dispatch-control/app/(dashboard)/config/_components/NextRunDisplay.tsx` (Client). Props `{cadence, nextRunAt}` (nextRunAt = Unix ms UTC). Renders "Next run: {Day} {Month} {D}, {H}:{MM} {TZ} ({HH}:{MM} UTC)" using `Intl.DateTimeFormat` for the operator-local part (timeZoneName:'short') and a UTC-formatted part alongside (D-11). When nextRunAt is 0/unset, render a "Not scheduled yet" line.

    2. Create `apps/dispatch-control/app/(dashboard)/config/_components/AutomationPanel.tsx` (Client). Reads `api.pipelineConfig.getAll`; parses `schedule_enabled` + `schedule_cadence` + `schedule_next_run_at`. Card `rounded-lg border border-neutral-200 bg-white p-5`, h2 "Automation". Kill-switch row: label "Scheduled runs"; `<button role="switch" aria-checked={scheduleEnabled} min-h-[44px]>` ON `bg-neutral-900 text-white` / OFF `bg-red-500 text-white`; always-visible badge "Automation ON" (neutral-100/600) or "Automation OFF" (red-50/700 border-red-200); on toggle call `useMutation(api.pipelineConfig.upsert)` writing `schedule_enabled` JSON. Schedule editor (rendered only when scheduleEnabled===true): day-of-week select (Mon–Sun → cron dayOfWeek value), hour + minute UTC selects, `<NextRunDisplay .../>`, "Save Schedule" secondary `min-h-[44px]` writing `schedule_cadence`.

    3. Create `apps/dispatch-control/app/(dashboard)/config/_components/BudgetCapsPanel.tsx` (Client). Reads `api.pipelineConfig.getAll` + `api.runs.monthToDateCost`. Card same styling, h2 "Budget Caps". Number inputs: "Per-run cap (USD)" (per_run_cap_usd, min 0 step 0.01, placeholder "e.g. 10.00"), "Monthly cap (USD)" (monthly_cap_usd), "Alert threshold (%)" (alert_threshold_pct, min 50 max 100, helper "Alert fires when month-to-date spend reaches this % of monthly cap"); "Save Caps" `min-h-[44px]` writing each via upsert. Read-only projection: "Projected next run cost: $X.XXXX" (4-decimal, from monthToDateCost trailingCosts average; "No completed runs yet — projection unavailable" when empty) and "Month-to-date: $X.XX of $Y.YY cap" with a progress bar (track `bg-neutral-200 rounded-full h-1.5`; fill `bg-amber-400` when mtd/cap ≥ threshold else `bg-neutral-400`).

    4. Rewrite `apps/dispatch-control/app/(dashboard)/config/page.tsx` (Server Component, keep `export const dynamic = 'force-dynamic'`): resolve workspace_id via `getCurrentWorkspace()`, render `<h1>Config</h1>` then the three stacked panels with `space-y-12` (2xl section break): `<AutomationPanel workspace_id/>`, `<BudgetCapsPanel workspace_id/>`, and an inline Danger Zone card (`border-red-200`, h2 text-red-700 "Danger Zone", copy "To force-cancel a running run, go to Runs and click Cancel on the active run."). Pass workspace_id into the client panels.

    Create `apps/dispatch-control/__tests__/nextRunDisplay.test.tsx`: render NextRunDisplay with a fixed UTC timestamp and assert the output contains both a UTC time segment and a timezone abbreviation (local). Add an AutomationPanel kill-switch assertion (role=switch + aria-checked + the two badge labels). Assert no exclamation marks / no urgency words across the three new config files.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && pnpm test:unit -- nextRunDisplay 2>&1 | tail -20 || pnpm --filter dispatch-control test:unit -- nextRunDisplay</automated>
  </verify>
  <acceptance_criteria>
    - three panels exist: `ls apps/dispatch-control/app/\(dashboard\)/config/_components/AutomationPanel.tsx apps/dispatch-control/app/\(dashboard\)/config/_components/BudgetCapsPanel.tsx apps/dispatch-control/app/\(dashboard\)/config/_components/NextRunDisplay.tsx` lists 3 files
    - config/page.tsx renders all three + Danger Zone + keeps force-dynamic: `grep -q "AutomationPanel" .../config/page.tsx`, `grep -q "BudgetCapsPanel" .../config/page.tsx`, `grep -q "Danger Zone" .../config/page.tsx`, `grep -q "force-dynamic" .../config/page.tsx`
    - kill switch is a proper a11y toggle with both states: `grep -q 'role="switch"' .../config/_components/AutomationPanel.tsx`, `grep -q "aria-checked" .../config/_components/AutomationPanel.tsx`, `grep -q "Automation ON" .../config/_components/AutomationPanel.tsx`, `grep -q "Automation OFF" .../config/_components/AutomationPanel.tsx`, `grep -q "bg-red-500" .../config/_components/AutomationPanel.tsx`
    - schedule editor gated on enabled: `grep -q "schedule_cadence" .../config/_components/AutomationPanel.tsx`
    - budget panel writes the 3 caps + shows 4-decimal projection: `grep -E "per_run_cap_usd|monthly_cap_usd|alert_threshold_pct" .../config/_components/BudgetCapsPanel.tsx` returns ≥3 lines; `grep -q "Projected next run cost" .../config/_components/BudgetCapsPanel.tsx`
    - next-run shows UTC + local: `grep -q "UTC" .../config/_components/NextRunDisplay.tsx` and `grep -q "Intl.DateTimeFormat" .../config/_components/NextRunDisplay.tsx`
    - Danger Zone copy verbatim: `grep -q "To force-cancel a running run, go to Runs and click Cancel on the active run." .../config/page.tsx`
    - `cd apps/dispatch-control && pnpm test:unit -- nextRunDisplay` exits 0
    - full dashboard suite green: `cd apps/dispatch-control && pnpm test:unit` exits 0
  </acceptance_criteria>
  <done>Config page rebuilt to Automation (kill-switch focal point + schedule editor) + Budget Caps (3 caps + projection + MTD bar) + Danger Zone; next-run shows local+UTC; tests green.</done>
</task>

</tasks>

<verification>
- `cd apps/dispatch-control && pnpm test:unit` exits 0 (all dashboard tests, including the 2 new files).
- `pnpm --filter dispatch-control build` exits 0 (no type errors; force-dynamic on config preserved).
- Every new <button> has min-h-[44px]; no exclamation marks / urgency vocabulary in any new copy.
</verification>

<success_criteria>
- RUN-01 Trigger Run (operator-attributed, two-step, disabled-while-running) on the Runs page.
- RUN-02/RUN-03 kill-switch toggle + schedule editor + next-run (local+UTC) on Config.
- RUN-04 Cancel Run + RUN-05 per-section Re-roll on Run detail.
- RUN-06 budget caps panel + start-gate-refusal surfaced via the budget alert banner.
- All acceptance + verification checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/25-run-control/25-05-SUMMARY.md`.
</output>
