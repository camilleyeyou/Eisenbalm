---
phase: 25-run-control
plan: 04
type: execute
wave: 4
depends_on: ["25-03"]
files_modified:
  - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py
  - packages/pipeline/src/eisenbalm_pipeline/lib/cost.py
  - packages/pipeline/src/eisenbalm_pipeline/lib/budget.py
  - packages/pipeline/src/eisenbalm_pipeline/api/control.py
  - convex/runs.ts
autonomous: true
requirements: [RUN-06]
must_haves:
  truths:
    - "The per-run cost cap is read from pipeline_config (per_run_cap_usd), overriding the env default, snapshotted once at run start (no Convex read inside the hot acomplete path)"
    - "The trigger and tick refuse to start a run when month-to-date spend + trailing-average projection would exceed the monthly cap"
    - "When MTD crosses the alert threshold, a cost-warning event (scope=monthly) is emitted and the run is NOT auto-cancelled"
    - "Budget math reads actual recorded runs.cost only — no second record_cost writer, no model_pricing derivation"
  artifacts:
    - path: "packages/pipeline/src/eisenbalm_pipeline/lib/budget.py"
      provides: "trailing-average projection + would_exceed_monthly_cap helpers"
      contains: "def would_exceed_monthly_cap"
    - path: "convex/runs.ts"
      provides: "monthToDateCost query summing actual runs.cost"
      contains: "monthToDateCost"
    - path: "packages/pipeline/src/eisenbalm_pipeline/lib/cost.py"
      provides: "DB-sourced per-run cap + monthly alert (extends cost-warning)"
      contains: "scope"
  key_links:
    - from: "api/control.py pipeline_run/pipeline_tick budget seam"
      to: "lib/budget.would_exceed_monthly_cap"
      via: "start-gate before _start_run; 409 (manual) / skipped (tick)"
      pattern: "would_exceed_monthly_cap"
    - from: "lib/cost.py check_cap"
      to: "RunConfig.per_run_cap_usd snapshot"
      via: "in-memory per-run cap set at begin_run, not a Convex read in acomplete"
      pattern: "per_run_cap"
---

<objective>
Enforce budget caps (RUN-06). Read the per-run cap from `pipeline_config` (snapshotted once at run start, never read inside the hot `acomplete` path), add a trailing-average start-gate that refuses to begin a run projected to exceed the monthly cap, and emit a monthly-scope `cost-warning` alert (reusing the existing eventType — the union is frozen) when MTD crosses the threshold without auto-cancelling. All budget math reads actual recorded `runs.cost`; the single-cost-writer rule is preserved.

Purpose: Make the "100% of proceeds donated" promise auditable by capping spend on accurate, non-double-counted cost data.
Output: budget lib, DB-sourced per-run cap, monthly alert, start-gate wired into the two seams Plan 02 left.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-run-control/25-CONTEXT.md
@.planning/phases/25-run-control/25-RESEARCH.md
@docs/API_CONTRACTS.md

<interfaces>
<!-- Ground-truth shapes the executor must use verbatim. -->

lib/config_loader.py RunConfig (dataclass) — ADD two fields after schedule_enabled:
```python
require_review: bool
auto_publish: bool
schedule_enabled: bool
# ADD:
per_run_cap_usd: float = 10.0
monthly_cap_usd: float = 0.0   # 0 = unset/disabled
alert_threshold_pct: float = 80.0
```
Both load_run_config() (Convex path, reads `pc.get(...)`) and _build_fallback_config() must populate them. pc keys: per_run_cap_usd, monthly_cap_usd, alert_threshold_pct.

lib/cost.py::CostRecorder.check_cap() currently reads `cap = float(os.environ.get("PIPELINE_COST_CAP_USD","10.0"))` on EVERY acomplete call (hot path — RESEARCH Pattern 4 forbids a Convex read here). The existing 70% soft-warn emits a `deliberationEvents:insert` cost-warning with payload {totalUsd, percentOfCap, perAgent, capUsd}. begin_run(run_id) seeds the per-run store.

Single-cost-writer rule (Phase 23, Pitfall 3): record_cost is called ONLY by openrouter_client.acomplete. Budget reads are READ-ONLY. NEVER add a record_cost call.

cost-warning eventType is a VALID existing deliberationEvents eventType (do NOT add a new one — Anti-Patterns). Distinguish monthly alerts with a payload field `scope: "monthly"` vs the per-run 70% warn (scope absent / "per_run").

convex/runs.ts::listForWorkspace({workspace_id}) returns all runs newest-first; each row has `cost` (optional JSON string, sourced from pipelineRuns.cost; parse `.total`). The per-run cap from RESEARCH: thread into RunConfig at run start, set an in-memory module-level cap at begin_run so check_cap reads from memory, not Convex.

api/control.py (Plan 02/03) left TWO budget seams: `# Budget start-gate inserted by Plan 04 (RUN-06) here` (one in pipeline_run before _start_run, one in pipeline_tick step 4).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB-sourced per-run cap (snapshot at run start) + monthly alert in cost.py</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py, packages/pipeline/src/eisenbalm_pipeline/lib/cost.py, packages/pipeline/src/eisenbalm_pipeline/api/runs.py</files>
  <read_first>
    - packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py (RunConfig dataclass + both builders — add 3 fields, populate in both paths)
    - packages/pipeline/src/eisenbalm_pipeline/lib/cost.py (check_cap L214-262, begin_run L73, the soft-warn emit — extend, never add a record_cost)
    - packages/pipeline/src/eisenbalm_pipeline/api/runs.py (_start_run — where begin_run + load_run_config + snapshot_config run; thread the cap in here)
    - packages/pipeline/tests/test_budget_gate.py (the per-run-cap-from-db + monthly-alert tests this task makes green)
  </read_first>
  <behavior>
    - test_per_run_cap_from_db: with pipeline_config per_run_cap_usd=5.0 (DB) and env PIPELINE_COST_CAP_USD=10.0, check_cap raises CostCapExceeded at $5, not $10 — the DB value wins.
    - test_monthly_alert_no_cancel: when MTD crosses alert_threshold_pct of monthly_cap_usd, a deliberationEvents cost-warning with payload scope="monthly" is emitted, and NO CostCapExceeded is raised (monthly cap alerts only, D-07).
  </behavior>
  <action>
    1. In `lib/config_loader.py`: add `per_run_cap_usd: float = 10.0`, `monthly_cap_usd: float = 0.0`, `alert_threshold_pct: float = 80.0` to the `RunConfig` dataclass (after `schedule_enabled`). In `load_run_config()` populate them from `pc.get("per_run_cap_usd", 10.0)`, `pc.get("monthly_cap_usd", 0.0)`, `pc.get("alert_threshold_pct", 80.0)`. In `_build_fallback_config()` populate them with the same defaults (no Convex). Both constructors must pass all three.

    2. In `lib/cost.py`: add a module-level per-run cap registry so check_cap reads from memory (RESEARCH Pattern 4 — no Convex in the hot path). Add `def set_run_cap(run_id: str, cap_usd: float) -> None:` storing the cap keyed by run_id, and have `check_cap` resolve the cap as: in-memory `set_run_cap` value for this run_id if present, else `float(os.environ.get("PIPELINE_COST_CAP_USD","10.0"))`. Keep the existing 100% hard-raise (`CostCapExceeded`) and 70% soft-warn behavior using the resolved cap. Document that the resolved cap is the DB `per_run_cap_usd` when set_run_cap was called at run start.

    3. In `lib/cost.py`, add an async helper `async def emit_monthly_alert(run_id: str, mtd_usd: float, monthly_cap: float, threshold_pct: float) -> None:` that fire-and-forgets a `deliberationEvents:insert` with eventType `"cost-warning"` (REUSE — do not add a new eventType) and a JSON payload `{"scope":"monthly","mtdUsd":mtd_usd,"monthlyCapUsd":monthly_cap,"percentOfCap": mtd_usd/monthly_cap if monthly_cap>0 else 0.0}`. It must NOT raise and must NOT call record_cost. (Caller invokes it from the start-gate / tick — Task 2.)

    4. In `api/runs.py::_start_run`, after `run_config = await load_run_config(...)` and before `asyncio.create_task`, call `from eisenbalm_pipeline.lib.cost import set_run_cap; set_run_cap(run_id, run_config.per_run_cap_usd)` so the DB cap governs this run's acomplete calls.

    Make `tests/test_budget_gate.py::test_per_run_cap_from_db` and `::test_monthly_alert_no_cancel` green.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/test_budget_gate.py::test_per_run_cap_from_db tests/test_budget_gate.py::test_monthly_alert_no_cancel -x -q</automated>
  </verify>
  <acceptance_criteria>
    - RunConfig carries the three fields: `grep -E "per_run_cap_usd|monthly_cap_usd|alert_threshold_pct" packages/pipeline/src/eisenbalm_pipeline/lib/config_loader.py` returns ≥6 lines (dataclass + 2 builder populations each)
    - `grep -q "def set_run_cap" packages/pipeline/src/eisenbalm_pipeline/lib/cost.py` and `grep -q "def emit_monthly_alert" packages/pipeline/src/eisenbalm_pipeline/lib/cost.py` succeed
    - monthly alert reuses cost-warning + scope=monthly: `grep -q '"scope": "monthly"' packages/pipeline/src/eisenbalm_pipeline/lib/cost.py` and `grep -q '"cost-warning"' packages/pipeline/src/eisenbalm_pipeline/lib/cost.py` succeed
    - NO new record_cost writer added: `grep -c "def record_cost\|record_cost(" packages/pipeline/src/eisenbalm_pipeline/lib/cost.py` is unchanged except existing call sites (no new invocation inside set_run_cap/emit_monthly_alert)
    - set_run_cap wired in _start_run: `grep -q "set_run_cap(run_id" packages/pipeline/src/eisenbalm_pipeline/api/runs.py`
    - `cd packages/pipeline && uv run pytest tests/test_budget_gate.py::test_per_run_cap_from_db tests/test_budget_gate.py::test_monthly_alert_no_cancel -x -q` exits 0
    - regression: `cd packages/pipeline && uv run pytest tests/test_cost_double_count.py -q` exits 0
  </acceptance_criteria>
  <done>Per-run cap is DB-sourced + snapshotted (no hot-path Convex read); monthly alert reuses cost-warning with scope=monthly and never cancels; no second cost writer; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Trailing-average start-gate (budget.py + monthToDateCost) wired into both control seams</name>
  <files>convex/runs.ts, packages/pipeline/src/eisenbalm_pipeline/lib/budget.py, packages/pipeline/src/eisenbalm_pipeline/api/control.py</files>
  <read_first>
    - convex/runs.ts (listForWorkspace — model monthToDateCost on it; each row.cost is a JSON string with .total)
    - packages/pipeline/src/eisenbalm_pipeline/api/control.py (the two `# Budget start-gate inserted by Plan 04` seams from Plan 02)
    - packages/pipeline/src/eisenbalm_pipeline/lib/cost.py (emit_monthly_alert from Task 1)
    - packages/pipeline/tests/test_budget_gate.py (test_start_gate_refuses_over_budget — the RED test this makes green)
  </read_first>
  <behavior>
    - test_start_gate_refuses_over_budget: seed runs.cost history + a low monthly_cap_usd in pipeline_config → POST /pipeline/run returns 409 and NO pipelineRuns:create / runs:create is called.
    - Zero-history edge: with 0 completed runs, would_exceed_monthly_cap returns False (allow the run — no projection data; D-06 first-run edge).
  </behavior>
  <action>
    1. In `convex/runs.ts`, add `export const monthToDateCost = query({ args: { workspace_id: v.string() }, handler })` that collects runs for the workspace, filters to those whose `startedAt` falls in the current calendar month (UTC), parses each `cost` JSON string and sums `.total` (skip rows with no cost / unparseable), and returns `{ mtdUsd: number, completedCount: number, trailingCosts: number[] }` where `trailingCosts` is the `.total` of the last up-to-4 completed runs (status in {"complete","awaiting-review"}) newest-first. This is the single round-trip the start-gate needs (RESEARCH Pattern 4 — read actual cost, never model_pricing).

    2. Create `packages/pipeline/src/eisenbalm_pipeline/lib/budget.py` with:
       - `def trailing_average(trailing_costs: list[float]) -> float | None:` — average of the provided costs (the query already limits to last 4); returns None when the list is empty (zero history).
       - `async def would_exceed_monthly_cap(http, *, monthly_cap_usd: float) -> tuple[bool, dict]:` — when `monthly_cap_usd <= 0` return `(False, {...})` (cap disabled); else `rollup = await convex_query(http, "runs:monthToDateCost", {"workspace_id":"eisenbalm"})`; `proj = trailing_average(rollup["trailingCosts"])`; if `proj is None` return `(False, {"reason":"no_history","mtd":rollup["mtdUsd"]})` (D-06 first-run edge → allow); else `over = (rollup["mtdUsd"] + proj) > monthly_cap_usd`; return `(over, {"mtd":rollup["mtdUsd"],"projected":proj,"cap":monthly_cap_usd})`. READ-ONLY (no record_cost).

    3. In `api/control.py`, replace BOTH `# Budget start-gate inserted by Plan 04 (RUN-06) here` seams:
       - In `pipeline_run` (before `_start_run`): load the monthly cap from pipeline_config (`pc = {...}` via `pipelineConfig:getAll`), `over, info = await would_exceed_monthly_cap(http, monthly_cap_usd=pc.get("monthly_cap_usd",0.0))`; if `over` → `raise HTTPException(409, f"Projected cost would exceed the monthly cap (${info['mtd']:.2f} MTD + ${info['projected']:.2f} projected > ${info['cap']:.2f} cap). Raise the cap or wait until next month.")`. (Manual trigger reuses pc if Plan 02 already read it — avoid a duplicate getAll; one read is enough.)
       - In `pipeline_tick` step 4: `over, info = await would_exceed_monthly_cap(http, monthly_cap_usd=pc.get("monthly_cap_usd",0.0))`; if `over` → emit the monthly alert (`emit_monthly_alert`) and `return {"status":"skipped","reason":"budget_projection_exceeds_cap"}`.

    Make `tests/test_budget_gate.py::test_start_gate_refuses_over_budget` green and confirm the full file is green.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/test_budget_gate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "export const monthToDateCost = query" convex/runs.ts` succeeds; the handler parses `cost` and sums `.total` (`grep -q "total" convex/runs.ts` in that handler region)
    - `grep -E "def trailing_average|async def would_exceed_monthly_cap" packages/pipeline/src/eisenbalm_pipeline/lib/budget.py` returns 2 lines
    - budget.py contains NO record_cost call: `grep -c "record_cost" packages/pipeline/src/eisenbalm_pipeline/lib/budget.py` returns 0
    - both seams replaced: `grep -c "Budget start-gate inserted by Plan 04" packages/pipeline/src/eisenbalm_pipeline/api/control.py` returns 0 (placeholders gone) and `grep -c "would_exceed_monthly_cap" packages/pipeline/src/eisenbalm_pipeline/api/control.py` returns ≥2
    - tick over-budget emits the alert: `grep -q "emit_monthly_alert" packages/pipeline/src/eisenbalm_pipeline/api/control.py`
    - `cd packages/pipeline && uv run pytest tests/test_budget_gate.py -x -q` exits 0 (full file green)
    - regression: `cd packages/pipeline && uv run pytest tests/test_cost_double_count.py -q` exits 0
  </acceptance_criteria>
  <done>monthToDateCost sums actual cost; budget.py projects via trailing average (zero-history allows); start-gate refuses over-budget in both seams; full budget-gate test file green; no double-count.</done>
</task>

</tasks>

<verification>
- `cd packages/pipeline && uv run pytest tests/test_budget_gate.py -x -q` exits 0.
- All budget math read-only against actual runs.cost; single-cost-writer preserved.
- cost-warning eventType reused (no frozen-union change).
</verification>

<success_criteria>
- RUN-06: per-run cap DB-sourced; start-gate refuses over-budget runs; monthly threshold alerts without cancelling.
- All acceptance checks pass; cost-double-count regression green.
</success_criteria>

<output>
After completion, create `.planning/phases/25-run-control/25-04-SUMMARY.md`.
</output>
