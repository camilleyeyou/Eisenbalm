---
phase: 12
slug: machine-editorial-design-adoption-and-designagent-suppression
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-22
revised: 2026-05-22
revision_reason: checker-fix — typography collapsed to 4 sizes / 2 weights; 4 non-multiple-of-4 spacing values corrected
---

# Phase 12 — UI Design Contract

> Visual and interaction contract for the two visual rebuilds (MED-04 SectionNavigator
> Vertical Timeline + MED-05 DeliberationSlot Carousel & Flow) and the fixed-palette
> adoption as it manifests on screen (MED-01). Backend flag plumbing (MED-02) and
> DesignAgent prompt changes (MED-03) are excluded — no visual contract needed there.
>
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (CSS custom properties + Tailwind v4 @theme) |
| Preset | not applicable |
| Component library | none (no shadcn; no new npm deps) |
| Icon library | none (Unicode glyphs + CSS only) |
| Font — display | Cormorant Garamond (next/font/google, already loaded) |
| Font — body | Lora (next/font/google, already loaded) |
| Font — ui / labels | Inter (next/font/google, already loaded) |

**Source of truth:** `apps/web/app/globals.css :root` — every color and font token
reference below maps to a variable already declared there. This phase adds NO new
palette values, NO new font loads, and NO new npm dependencies.

**shadcn gate:** Not applicable. React/Next.js project, but `components.json` is absent
and all components are bespoke. This phase rebuilds two existing bespoke components —
no shadcn initialization required.

---

## Palette Adoption — MED-01 Contract

The live site previously allowed per-issue `theme` CSS overrides to win over the
`:root` house palette. Phase 12 locks the house palette as the sole source. The spec
describes what the fixed palette LOOKS LIKE visually, not the mechanism (see MED-02).

All downstream components must use only the tokens below. No hardcoded hex literals
outside of `globals.css :root`.

| CSS Variable | Value | Role |
|---|---|---|
| `--color-bg` | `#0C0B0A` | Canvas — near-black warm ground |
| `--color-surface` | `#14110D` | Elevated surface layer |
| `--color-card` | `#1A1611` | Card background |
| `--color-card-hover` | `#221D16` | Card hover/active state |
| `--color-text` | `#F0EAD9` | Primary text — warm cream |
| `--color-text-dim` | `#A89F8A` | Secondary text (7.5:1 on bg — AA large) |
| `--color-text-mute` | `#938A77` | Muted text (5.8:1 on bg — AA) |
| `--color-primary` | `#CDA434` | Gold — accent for progress, selected, numbers |
| `--color-accent` | `#C2502A` | Ember — error states, low-confidence flag |
| `--color-scout` | `#8A9B7A` | Scout agent identity (6.6:1 — AA) |
| `--color-advocate` | `#6E92B8` | Advocate agent identity (6.1:1 — AA) |
| `--color-line` | `color-mix(in srgb, var(--color-text) 8%, transparent)` | Hairlines |
| `--color-line-strong` | `color-mix(in srgb, var(--color-text) 16%, transparent)` | Strong hairlines |
| `--color-primary-glow` | `color-mix(in srgb, var(--color-primary) 40%, transparent)` | Radial glow wash |
| `--color-primary-bright` | `color-mix(in srgb, var(--color-primary) 78%, white 22%)` | Progress bar highlight |

**WCAG AA contract (locked, tested by `theme-aa-tones.test.ts`):**
- `--color-text` on `--color-bg`: verified AA.
- `--color-text-dim` on `--color-bg`: 7.5:1 — AA.
- `--color-text-mute` on `--color-bg`: 5.8:1 — AA.
- `--color-scout`, `--color-advocate` on `--color-bg`: both ≥6:1 — AA.
- Never place `--color-primary` or `--color-accent` as body-text on `--color-bg`
  (they are accent, not text colors; use only for borders, glows, badges, numbers).

---

## Spacing Scale

Standard 8-point scale, multiples of 4. Both rebuilt components must use these values
for all layout decisions.

| Token | Value | Usage |
|---|---|---|
| xs | 4px | Icon gaps, tag internal padding (vertical) |
| sm | 8px | Inline element spacing, badge padding, content gap |
| md | 16px | Card internal gap, row gap |
| lg | 24px | Column gap, section internal padding |
| xl | 32px | Between major sub-blocks |
| 2xl | 48px | Section-level padding (mobile) |
| 3xl | 80px | Section-level padding (desktop) — justified as a comfortable desktop reading margin; multiple of 4 |

**Exceptions:**
- Touch target minimum: 44px (≥44px on any interactive hit area — enforced on
  all node-dot links, pitch-card listitem, summary toggle, agent-chip links).
- Spine dot diameter: 12px visual, but the surrounding `<a>` has min 44px
  hit area via padding.
- Section row height (timeline): minimum 88px per row (2× touch-target ensures
  comfortable tap on mobile).

---

## Typography

Three fonts only (FONT_WHITELIST; no IBM Plex Mono, no Spectral).

**Exactly 4 font sizes, exactly 2 font weights.**

| Role | Font | Size (token) | Weight | Line Height | Letter Spacing |
|---|---|---|---|---|---|
| Display | Cormorant Garamond | `clamp(32px, 3.5vw, 48px)` | 400 | 1.1 | — |
| Title | Cormorant Garamond | `27px` (responsive: `22px` on ≤480px) | 600 | 1.05 | — |
| Body | Lora / Inter | `15px` | 400 | 1.65 | — |
| Micro-label | Inter | `11px` | 400 or 600 (see roles below) | 1.5 | see below |

**Size token assignments:**

- **Display** (`clamp(32px, 3.5vw, 48px)`): the "In this Issue" display heading AND
  the confidence percentage value. Both share this single size token; the confidence
  value is differentiated from the heading by context, weight (600), and color.
- **Title** (`27px` / `22px` mobile): section titles in timeline rows. The `22px` mobile
  override is a responsive variant of the same Title token — it is NOT a 5th size.
  Title role uses weight 600 on the main title word; italic subordinate word uses weight
  400 italic (the `<em>` inside `.snw-section-title`).
- **Body** (`15px`): body prose, subtitles, scout card summaries, flow-line action text,
  AND pitch-card charity name. Distinguish elements within the Body size using italic
  (`font-style: italic`) or weight 600, never a separate size. Specifically:
  - Subtitle / scout summary: Body 15px, weight 400, italic, `--color-text-dim`.
  - Charity name: Body 15px, weight 600, `--color-text`.
  - Flow-line action text: Body 15px, weight 400, italic, `--color-text-dim`.
- **Micro-label** (`11px`): ALL machine-readout labels, category tags, section numbers,
  confidence labels, agent chip names and roles, read-status label and value, flow-line
  agent name. Differentiate values vs. labels using weight (600 = value/name, 400 =
  label/role), letter-spacing, and color — never a smaller size. The `9px` size from
  prior drafts is eliminated.

**Font weights — exactly 2:**

- **400** (regular + italic): body prose, subtitles, scout summary, italic display word
  ("In this *Issue*"), italic title word (`<em>` inside `.snw-section-title`), micro-label
  labels, tag pill text, agent chip role, flow-line action text, read-status label,
  section number, `.snw-module-label`.
- **600** (emphasis): section title main word, confidence value (Display size), charity
  name, micro-label value text (`snw-read-value`), agent chip name.
- **Weight 500 is eliminated.** All former `500` usages remapped: section titles →
  `600`; confidence value → `600`; charity name → `600`.

**Machine-readout label treatment (IBM Plex Mono approximation — locked):**
- Font: Inter, `11px` (Micro-label size token).
- Label text: weight 400, `text-transform: uppercase`, `letter-spacing: 0.18em`,
  color `--color-text-mute`.
- Value / number text: weight 600, `text-transform: uppercase`, `letter-spacing: 0.10em`,
  color `--color-primary`.
- Example rendering: `READ  STATUS:  0%  ——` — uppercase Inter with generous tracking
  simulating a terminal readout. The `——` trailing dash (two em-dashes) styled in
  `--color-text-mute` at opacity 0.5.

---

## Color Contract (60/30/10)

| Role | Token | Usage |
|---|---|---|
| Dominant 60% | `--color-bg` `#0C0B0A` | Full page canvas; the fixed lock |
| Secondary 30% | `--color-card` / `--color-surface` | Timeline row backgrounds, pitch cards, agent chips, QA panels |
| Accent 10% | `--color-primary` `#CDA434` | Section numbers (§ 01), category tag borders on selected row, progress bar fill, selected pitch-card border, confidence meter fill, winner card glow; winner card luminous box-shadow |
| Accent-2 | `--color-accent` `#C2502A` | Low-confidence flag border (< 0.70), error QA severity border/text — no other usage |

**Accent reserved for:**
- `§ NN` section numbers on timeline rows.
- Border and glow of the actively-scrolled / active section row (the reading-progress
  color fill is gold).
- Progress readout percentage value text.
- Pitch card left-border when `card.selected === true`.
- Winner card luminous `box-shadow` glow.
- Confidence meter bar fill and percentage display.
- Flow-line node circles for Scout → Advocate → Editor.

**Never use accent for:** body copy, section headings, tag labels on non-selected items,
background fills on cards, decorative geometry.

---

## Spacing — Component-Specific

### MED-04 SectionNavigator Vertical Timeline

| Element | Spec |
|---|---|
| Outer wrapper max-width | 1340px, centered, `padding: 80px 40px` desktop / `48px 20px` mobile |
| Header area (`In this Issue`) | `margin-bottom: 48px` |
| Header label (NAVIGATION MODULE) | `margin-bottom: 12px` above display heading |
| Row gap (between section rows) | 0px — rows are separated by 1px `--color-line` border only |
| Row internal padding | `24px 24px 24px 0` — number column is 56px wide; content starts after |
| Number column width | 56px fixed |
| Tag pill padding | `4px 8px` |
| Read status column width | 120px fixed, right-aligned |
| Spine (left edge of number col) | 2px wide, full section height, color `--color-line-strong` |
| Node dot | 12px diameter circle on spine, centered vertically on each row |
| Node dot active (reading-progress) | Gold fill `--color-primary`, 12px |
| Node dot inactive | `--color-card-hover` fill, 1px `--color-line-strong` border |
| Progress fill on spine | From top to active node: `--color-primary` 2px, animated by scroll |

### MED-05 DeliberationSlot Carousel & Flow

| Element | Spec |
|---|---|
| Outer section max-width | 1340px, centered, `padding-inline: 1rem–2rem` |
| Header toggle (`How this issue was made`) | Existing `<details>/<summary>` pattern preserved, min-height 44px |
| Pitch card width (mobile snap) | 85vw, max-width 400px, `flex: 0 0 85vw` |
| Pitch card internal padding | `24px` |
| Gap between pitch cards | 16px |
| Winner card glow spread | `box-shadow: 0 0 32px color-mix(in srgb, var(--color-primary) 28%, transparent)` |
| Flow line column width | 48px (the Scout → Advocate → Editor vertical diagram) |
| Flow line node circle | 10px diameter; Scout = `--color-scout`, Advocate = `--color-advocate`, Editor = `--color-primary` |
| Flow line connector | 2px `--color-line-strong` vertical line between nodes |
| Tape-reel meter bar height | 8px, `border-radius: 4px` |
| Confidence label | Inter 11px (Micro-label) weight 400 uppercase 0.18em tracking |
| Confidence value | Cormorant Garamond Display size (`clamp(32px, 3.5vw, 48px)`) weight 600 |
| QA panel padding | `16px` |

---

## SectionNavigator — Vertical Timeline (MED-04)

### Layout Structure

Replace the 4-column card grid with a full-width vertical list. Each section is one
horizontal row. The component renders a `<nav aria-label="Sections">` containing:

1. **Header block** — machine-readout module label above the display heading.
2. **8 section rows** — one per CARDS entry, stacked vertically, no grid columns.
3. **Radial cursor glow** — JS-driven, one per row, reusing existing `--mx`/`--my`
   pattern from the current Phase 11 mousemove handler.

### Header Block

```
NAVIGATION MODULE 01-B          ← Inter 11px (Micro-label) weight 400 uppercase 0.18em tracking, --color-primary
In this Issue                   ← Cormorant Garamond Display size, italic weight 400, --color-text
                                  ("Issue" word in gold --color-primary, remainder --color-text)
```

The board shows "In this *Issue*" where "Issue" renders in the gold accent. This is
the ONE exception to the "accent not for headings" rule — it is a deliberate design
decision on the module label itself, not body copy.

### Section Row Anatomy (per row)

Each row is an `<a>` element with `href={card.href}` (canonical anchor id preserved).

```
┌──────────────────────────────────────────────────────────────────────────────┐
│  ● ──  § 01   [EDITORIAL]   The Architecture of Absence               READ STATUS: 0%  —— │
│         (spine) (number)   (tag pill)  (title in Cormorant)         (progress, right)  │
│                             A typography prioritizes the unheard.          │
│                             (subtitle line — Body 15px italic, --color-text-dim)│
└──────────────────────────────────────────────────────────────────────────────┘
```

**Left column (56px):**
- Spine: 2px vertical line, `--color-line-strong`, full row height.
- Node dot: 12px circle centered vertically on the spine. Inactive = card bg +
  line-strong border. Active (currently-reading) = `--color-primary` fill.
- Section number: `§ NN` — Inter 11px (Micro-label), weight 400, `--color-primary`,
  letter-spacing 0.14em, below the dot.

**Center content block:**
- Tag pill: Inter 11px (Micro-label) weight 400 uppercase 0.10em tracking,
  `--color-line-strong` border 1px, `border-radius: 2px`, `padding: 4px 8px`,
  color `--color-text-mute`. On the selected/active row: border-color `--color-primary`,
  color `--color-primary`. (Differentiated from the 0.18em machine-readout labels by
  its tighter 0.10em tracking — same Micro-label size, distinct tracking.)
- Section title: Cormorant Garamond 27px (Title size) weight 600, color `--color-text`.
  The board shows one italic word within the title (the noun). Markup: the italic word
  is a `<em>` inside the title `<span>` — the `<em>` renders at weight 400 italic.
  No hardcoded which word — use the existing `card.title` string; the italic word is
  defined per card in the CARDS data (add an `italicWord?: string` field to `SectionCard`;
  if absent render plain).
- Subtitle (optional): Lora 15px (Body size) weight 400 italic, `--color-text-dim`,
  `line-height: 1.55`. The board shows a subtitle below each title. This maps to a new
  `subtitle?: string` on `SectionCard` data — short editorial tagline per section
  (see Copywriting Contract).

**Right column (120px, right-aligned):**
- Machine-readout: `READ  STATUS:` — Inter 11px (Micro-label) weight 400 uppercase
  0.18em tracking, `--color-text-mute`, opacity 0.7.
- Percentage: `0%` (or live reading-progress value) — Inter 11px (Micro-label) weight
  600 uppercase 0.10em tracking, `--color-primary`.
- Trailing dashes: `——` — `--color-text-mute`, opacity 0.5.
- This is a single `<span aria-label="Read status: {N}%">` with `aria-live="polite"`.

### Radial Cursor Glow

Reuse the existing `--mx`/`--my` mousemove pattern from Phase 11 `SectionNavigator.tsx`.
Apply to each row `<a>` (not the whole nav). The `::before` pseudo-element carries
the radial gradient:
```css
background: radial-gradient(
  circle at var(--mx, 50%) var(--my, 50%),
  var(--color-primary-glow),
  transparent 60%
);
```
Under `prefers-reduced-motion`: JS listener early-returns (existing pattern). Glow is
centred at 50%/50% via CSS default — a faint static presence.

### Reading Progress

Scroll-driven. A `useEffect` + `IntersectionObserver` (one per section anchor) tracks
which section is currently in the viewport and sets that row's node dot to gold fill.
A `scrollTop / scrollHeight` fraction drives the spine fill (a `::after` pseudo or
an inline `height` CSS variable on the spine element).

Under `prefers-reduced-motion`: reading progress shows the FINAL computed state
immediately (no animation of the fill). Node dots reflect which section is in view
statically — no animated transitions on the fill color or spine height.

### Row States

| State | Border | Background | Node dot | Tag pill |
|---|---|---|---|---|
| Default | `--color-line` bottom only | `--color-card` | Inactive (line-strong border) | `--color-text-mute` |
| Hover | `--color-primary` bottom | `--color-card-hover` | Inactive | `--color-primary` border |
| Active (in view) | `--color-primary` left 2px | `--color-card` | Gold fill | `--color-primary` |

### Reduced-Motion Checklist (MED-04)

- [ ] No JS cursor tracking runs under `prefers-reduced-motion: reduce`.
- [ ] Glow stays at `50% 50%` default — CSS only, no opacity transition.
- [ ] Spine fill/node state shows the current value instantly, no CSS transition.
- [ ] `transition-duration: 0.01ms` on all row `transition` properties (enforced by
  the global `@media (prefers-reduced-motion: reduce)` block in `globals.css`).
- [ ] Row hover `transform: translateY(-2px)` is neutralized by the global guard.

### Accessibility (MED-04)

- `<nav aria-label="Sections">` — preserved from existing.
- Each row `<a>` gets `aria-label="§ {NN} {title} — {tag}"`.
- Read-status `<span>` gets `aria-live="polite"` and `aria-label="Read status: {N}%"`.
- Node dots are `aria-hidden="true"` (decorative).
- Touch target: each row `<a>` has `min-height: 88px` (2× 44px for comfortable
  vertical scrolling context).
- No `<main>`, no `role="main"` — the nav lives inside `<main id="main">` from root layout.

---

## DeliberationSlot — Carousel & Flow (MED-05)

### Layout Structure

Replace the two-column `lg:grid-cols-[1fr_1fr]` layout with a vertical stack of three
named zones inside the existing `<details>/<summary>` accordion:

```
Zone 1: Pitch Log Carousel (horizontal scroll-snap, full width)
Zone 2: Flow Line + Editor Decision + Confidence Meter (vertical, centered)
Zone 3: QA Findings (full width, collapsible sub-panel)
```

**Data bindings are unchanged.** All five Convex subscriptions, `AGENT_LABELS`,
`DEL-04` guard, and confidence-meter `IntersectionObserver + rAF count-up` are
preserved byte-compatible with the Phase 11 implementation.

### Zone 1 — Horizontal Pitch Log Carousel

Extends the existing `.pitch-card-list` scroll-snap CSS from Phase 11 (`globals.css`
lines 611–643). No CSS changes to `.pitch-card-list` are needed at the base layout
level — only the card interior is restyled.

**Card interior (new visual treatment vs. existing rounded-p-6 card):**

- Background: `--color-card`, `border-radius: 4px`.
- Left border accent: `3px solid --color-primary` when `card.selected`, else `3px solid transparent`.
- Winner card luminous glow: `box-shadow: 0 0 32px color-mix(in srgb, var(--color-primary) 28%, transparent), 0 0 0 1px color-mix(in srgb, var(--color-primary) 18%, transparent)` — only when `card.selected`.
- Internal padding: `24px`.
- Charity name: Cormorant Garamond 15px (Body size) weight 600, `--color-text`. Uses
  Body size but distinguishes itself via weight 600 and the display font family.
- Location: Inter 11px (Micro-label) weight 400, `--color-text-dim`.
- Scout summary: Lora 15px (Body size) weight 400 italic, `--color-text-dim`,
  `line-height: 1.55`.
- `★ Selected this week` badge: existing treatment preserved (primary color + bg tint).
- Advocate score bar: existing treatment preserved (gold fill, `--color-line-strong` track).

**Scroll-snap behavior:**
- Mobile (< 960px): horizontal scroll-snap, `scroll-snap-type: x mandatory`, cards
  are `85vw` wide, max 400px. Existing `.pitch-card-list` CSS handles this.
- Desktop (≥ 960px): `.pitch-card-list` switches to `flex-direction: column` via
  the existing media query override. Cards stack vertically.

**Winner card luminous glow — motion:**
- The glow is always on (no animation) — it is a static `box-shadow`.
- Under `prefers-reduced-motion`: identical appearance (no change needed; static shadow).

### Zone 2 — Scout → Advocate → Editor Flow Line

A new vertical visual element between the pitch log and the QA section. Renders only
when at least one of `pitchLog`, `editorWinner`, or `editorConfidence` is present.

**Structure:**
```
┌────────────────────────────────────────────────┐
│  ● THE SCOUT  ──────  CANDIDATES FOUND         │
│  │                                              │
│  ● THE ADVOCATE ───  ARGUMENTS SCORED          │
│  │                                              │
│  ● THE EDITOR ─────  {editorWinner} selected   │
│  │                                              │
│  ▼ [Confidence meter block]                     │
└────────────────────────────────────────────────┘
```

- Container: `display: flex; flex-direction: column; align-items: flex-start; gap: 0;`
  max-width 640px, centered, `margin: 40px auto`.
- Each flow node: a flex row with a circle (10px), a label cluster (agent name + action
  text), right-side separator line.
- Circle colors: Scout = `--color-scout`, Advocate = `--color-advocate`,
  Editor = `--color-primary`.
- Connector line between nodes: 2px `--color-line-strong` vertical, 24px height.
- Agent name: Inter 11px (Micro-label) weight 400 uppercase 0.18em tracking,
  agent-specific color (same as circle).
- Action text: Lora 15px (Body size) weight 400 italic, `--color-text-dim`.
- The flow line is `aria-hidden="true"` (decorative diagram); screen-reader users
  get the same information from the existing event timeline text.

**Confidence meter (tape-reel style):**
Preserved from Phase 11: `IntersectionObserver + rAF count-up`. Visual changes:
- Move the confidence meter BELOW the flow line (not inside the editor decision card).
- Keep the `ref={confidenceSectionRef}` on the outer wrapper div.
- Bar height: 8px (up from 2px `h-1.5`) with `border-radius: 4px`.
- Label: `EDITOR CONFIDENCE` — Inter 11px (Micro-label) weight 400 uppercase 0.18em
  tracking, `--color-text-mute`.
- Value: `{displayValue}%` — Cormorant Garamond Display size (`clamp(32px, 3.5vw, 48px)`)
  weight 600, `--color-primary`, `aria-live="polite"`. (Shares Display size token with
  the "In this Issue" heading; differentiated by color and surrounding context.)
- Bar track: `--color-line-strong`, `border-radius: 4px`.
- Bar fill: `--color-primary`, `width: {displayValue}%`, transition removed under
  reduced-motion (global guard handles).
- Below-threshold flag (`< 0.70`): `--color-accent` border-left, same copy as Phase 11.
- Renders ONLY when `editorConfidence !== null` (finite 0..1 number present) — unchanged.

**Reduced-motion — confidence meter:**
- Under `prefers-reduced-motion`: `setDisplayValue(target)` immediately (existing
  Phase 11 code path — preserved).
- No JS count-up animation runs; final value shown on mount.

### Zone 3 — QA Findings

Unchanged from Phase 11 layout. The section heading `QA Findings` uses the machine-
readout label treatment: Inter 11px (Micro-label) weight 400 uppercase 0.18em tracking,
`--color-text-dim`.

### Accordion Toggle — Preserved

The `<details>/<summary>` pattern (DEL-03) is preserved exactly. The summary line reads
"How this issue was made" — no copy change. `min-height: 44px` preserved.

### Reduced-Motion Checklist (MED-05)

- [ ] Winner card glow: static `box-shadow` — no animation needed; passes as-is.
- [ ] Pitch card scroll-snap: NO auto-advance; `scroll-snap-type: x mandatory` scrolls
  on user interaction only (already Phase 11 behavior). Under reduced-motion the
  global guard sets `scroll-behavior: auto` (existing).
- [ ] Confidence meter: shows final value instantly via `setDisplayValue(target)` —
  existing Phase 11 code path preserved.
- [ ] Flow-line connector: static decorative element, no animation.
- [ ] Score bar widths: `transition: none` under reduced-motion (global guard).

### Accessibility (MED-05)

- `<section id="deliberation">` — preserved.
- `<details>/<summary>` accordion: preserved; `min-height: 44px` on summary.
- Pitch card list items: `role="listitem"`, `tabIndex={0}` — preserved.
- Flow-line diagram: wrapped in `aria-hidden="true"` container; diagram is decorative.
- Confidence meter value: `aria-live="polite"` — preserved.
- Agent chip links: `href="/agents/{agentId}"` — preserved; `min-height: 44px`
  via existing flex + padding.
- DEL-04: Model names never rendered. `AGENT_LABELS` persona names only — preserved.
- 5 Convex subscriptions: byte-compatible. `useQuery(api.*, runId ? {...} : 'skip')` —
  preserved.

---

## Copywriting Contract

### SectionNavigator Subtitles (new `subtitle` field on CARDS)

These are short one-line editorial taglines per section. Jesse voice: dry, precise.
No exclamation marks. No winking.

| Section | `tag` pill | `subtitle` (new) | `italicWord` (new) |
|---|---|---|---|
| Origin Story | EDITORIAL | How a thing with no funding found a reason to exist. | *Absence* |
| The Problem | ANALYSIS | The precise mechanism by which the world fails this population. | *Problem* |
| Founder Bio | PROFILE | The person who decided this was worth doing. | *Bio* |
| Case Study | IMPACT | One documented instance. One outcome. | *Study* |
| The Game | INTERACTIVE | A game about the stakes. | *Game* |
| The Bonus | EXTRA | Something we could not leave out. | *Bonus* |
| The Deliberation | PROCESS | A full audit of the candidate selection process. | *Deliberation* |
| The Podcast | AUDIO | Listen to the decision being made. | *Podcast* |

Note: `italicWord` is the word inside the section title that renders as `<em>`.
This is consistent with the board's typographic treatment (e.g. "The Architecture of
*Absence*", "The Agent's *Veto*").

### Header Module Label

```
NAVIGATION MODULE 01-B
```
Fixed string — machine-readout register, Inter 11px (Micro-label) weight 400 uppercase
0.18em tracking, gold. This is a decorative editorial device, not navigational UI copy.

### DeliberationSlot — existing copy preserved

| Element | Copy |
|---|---|
| Accordion toggle | How this issue was made |
| Loading state | Loading the deliberation. |
| Empty state | This issue predates the open deliberation record. |
| Pitch log heading | The Scout's Candidates — Pitch Log |
| Deliberation heading | The Deliberation |
| Live indicator | ● live |
| Editor decision heading | Editor's Decision |
| Confidence label | EDITOR CONFIDENCE |
| Below-threshold flag | Below 0.70 threshold — human review flagged. |
| QA findings heading | QA Findings |
| No pitch log | No pitch log entries for this run. |
| Score did not complete | Scores did not complete this cycle. |
| Flow line — Scout action | CANDIDATES FOUND |
| Flow line — Advocate action | ARGUMENTS SCORED |
| Flow line — Editor action | {editorWinner} selected |

**Flow-line copy treatment:** Inter 11px (Micro-label) weight 400 uppercase 0.18em
tracking, `--color-text-dim`. `{editorWinner}` is the dynamic charity name from
`editorWinner` string — Lora 15px (Body size) weight 400 italic, `--color-text`,
displayed inline with the "selected" suffix.

### Primary CTA

Not applicable for these two components — both are navigation / informational. No
form submission, no destructive action.

### Destructive actions

None in this phase's visual scope.

---

## Motion Contract

All motion is behind `prefers-reduced-motion: reduce` guards. The global `@media
(prefers-reduced-motion: reduce)` block in `globals.css` (setting `animation-duration:
0.01ms`, `transition-duration: 0.01ms`) is the system-wide backstop. Each component
also has explicit early-return logic in JS.

| Motion | Component | Normal behavior | Reduced-motion behavior |
|---|---|---|---|
| Cursor-following glow (--mx/--my) | Navigator | JS mousemove → `--mx`/`--my` per row | JS listener skipped; glow stays at `50% 50%` (CSS default) |
| Spine progress fill | Navigator | CSS `height` transitions as scroll position changes | Transition: 0.01ms (global guard); fill shows final state instantly |
| Node dot color transition | Navigator | `transition: background-color 0.3s` | 0.01ms (global guard) |
| Row hover translateY(-2px) | Navigator | CSS transform on hover | 0.01ms (global guard); no movement |
| Confidence count-up (rAF) | Deliberation | 0→N over 1200ms via rAF | `setDisplayValue(target)` immediately on mount |
| Score bar width transition | Deliberation | `transition: width 0.6s ease` | `transition: none` (conditional in inline style, already Phase 11 code) |
| Winner card glow | Deliberation | Static box-shadow (no animation) | Same — passes as-is |
| Flow line connector | Deliberation | Static decorative (no animation) | Same — passes as-is |

---

## Globals.css Extension Contract

New CSS classes to add to `globals.css` under a `Phase 12` banner, following the
existing pattern (section separator, no color literals outside variables):

### Navigator Vertical Timeline additions

```css
/* ── SECTION NAVIGATOR — VERTICAL TIMELINE (Phase 12 MED-04) ─────────── */

/* Replace .section-cards grid with .snw-timeline vertical list */
.snw-timeline {
  display: flex;
  flex-direction: column;
  /* border-top handled by first row */
}

/* Module label above "In this Issue" heading */
.snw-module-label {
  font-family: var(--font-ui);
  font-size: 11px;
  font-weight: 400;
  text-transform: uppercase;
  letter-spacing: 0.18em;
  color: var(--color-primary);
  margin-bottom: 12px;
}

/* Updated heading: "In this" plain + "Issue" gold */
.snw-title-plain {
  font-family: var(--font-display);
  font-size: clamp(32px, 3.5vw, 48px);
  font-style: italic;
  font-weight: 400;
  color: var(--color-text);
}

.snw-title-accent {
  color: var(--color-primary);
}

/* Spine container */
.snw-spine {
  position: relative;
  width: 56px;
  flex-shrink: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.snw-spine-line {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  width: 2px;
  top: 0;
  bottom: 0;
  background: var(--color-line-strong);
}

.snw-spine-progress {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  width: 2px;
  top: 0;
  background: var(--color-primary);
  transition: height 0.3s ease;
}

/* Spine node dot */
.snw-node {
  position: relative;
  z-index: 1;
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: var(--color-card-hover);
  border: 1px solid var(--color-line-strong);
  transition: background 0.3s, border-color 0.3s;
  flex-shrink: 0;
  margin-top: 40px; /* centers dot vertically in row — multiple of 4 */
}

.snw-node.active {
  background: var(--color-primary);
  border-color: var(--color-primary);
  box-shadow: 0 0 8px var(--color-primary-glow);
}

/* Section number */
.snw-section-num {
  font-family: var(--font-ui);
  font-size: 11px;
  font-weight: 400;
  color: var(--color-primary);
  letter-spacing: 0.14em;
  margin-top: 8px; /* multiple of 4 */
}

/* Timeline row — full-width anchor */
.snw-row {
  position: relative;
  display: flex;
  align-items: flex-start;
  min-height: 88px;
  padding: 24px 24px 24px 0;
  border-bottom: 1px solid var(--color-line);
  text-decoration: none;
  color: var(--color-text);
  transition: background 0.3s, border-left-color 0.3s;
  overflow: hidden;
}

.snw-row:first-child {
  border-top: 1px solid var(--color-line);
}

.snw-row::before {
  content: '';
  position: absolute;
  inset: 0;
  background: radial-gradient(
    circle at var(--mx, 50%) var(--my, 50%),
    var(--color-primary-glow),
    transparent 60%
  );
  opacity: 0;
  transition: opacity 0.35s;
  pointer-events: none;
}

.snw-row:hover::before {
  opacity: 1;
}

.snw-row:hover {
  background: var(--color-card-hover);
}

.snw-row.active {
  border-left: 2px solid var(--color-primary);
}

/* Row content (tag + title + subtitle) */
.snw-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 8px; /* multiple of 4 */
}

/* Category tag pill */
.snw-tag-pill {
  display: inline-block;
  font-family: var(--font-ui);
  font-size: 11px;
  font-weight: 400;
  text-transform: uppercase;
  letter-spacing: 0.10em;
  color: var(--color-text-mute);
  border: 1px solid var(--color-line-strong);
  border-radius: 2px;
  padding: 4px 8px; /* multiple of 4 */
  align-self: flex-start;
}

.snw-row:hover .snw-tag-pill,
.snw-row.active .snw-tag-pill {
  border-color: var(--color-primary);
  color: var(--color-primary);
}

/* Section title */
.snw-section-title {
  font-family: var(--font-display);
  font-size: 27px;
  font-weight: 600;
  line-height: 1.05;
  color: var(--color-text);
}

.snw-section-title em {
  font-style: italic;
  font-weight: 400;
}

/* Section subtitle */
.snw-subtitle {
  font-family: var(--font-body);
  font-size: 15px;
  font-style: italic;
  line-height: 1.55;
  color: var(--color-text-dim);
}

/* Read status column */
.snw-read-status {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  justify-content: center;
  width: 120px;
  flex-shrink: 0;
  padding-top: 24px;
}

.snw-read-label {
  font-family: var(--font-ui);
  font-size: 11px;
  font-weight: 400;
  text-transform: uppercase;
  letter-spacing: 0.18em;
  color: var(--color-text-mute);
  opacity: 0.7;
  white-space: nowrap;
}

.snw-read-value {
  font-family: var(--font-ui);
  font-size: 11px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.10em;
  color: var(--color-primary);
}

.snw-read-dash {
  font-family: var(--font-ui);
  font-size: 11px;
  font-weight: 400;
  color: var(--color-text-mute);
  opacity: 0.5;
  letter-spacing: 0.05em;
}

@media (max-width: 480px) {
  .snw-read-status {
    display: none; /* read-status column hidden on narrowest mobile */
  }
  .snw-section-title {
    font-size: 22px; /* responsive variant of Title token — not a 5th size */
  }
}
```

### DeliberationSlot Flow Line additions

```css
/* ── DELIBERATION FLOW LINE (Phase 12 MED-05) ──────────────────────────── */

.del-flow {
  display: flex;
  flex-direction: column;
  max-width: 640px;
  margin: 40px auto;
}

.del-flow-node {
  display: flex;
  align-items: center;
  gap: 16px;
  min-height: 44px;
}

.del-flow-circle {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  flex-shrink: 0;
}

.del-flow-connector {
  width: 2px;
  height: 24px;
  background: var(--color-line-strong);
  margin-left: 4px; /* aligns connector center with circle center */
}

.del-flow-label {
  font-family: var(--font-ui);
  font-size: 11px;
  font-weight: 400;
  text-transform: uppercase;
  letter-spacing: 0.18em;
}

.del-flow-action {
  font-family: var(--font-body);
  font-size: 15px;
  font-style: italic;
  color: var(--color-text-dim);
}

/* Confidence meter — updated from Phase 11 */
.del-confidence-bar-track {
  width: 100%;
  height: 8px;
  border-radius: 4px;
  background: var(--color-line-strong);
  overflow: hidden;
}

.del-confidence-bar-fill {
  height: 100%;
  border-radius: 4px;
  background: var(--color-primary);
  transition: width 0.6s ease;
}

/* Winner card luminous glow (applied via inline style in TSX) */
/* box-shadow: 0 0 32px color-mix(in srgb, var(--color-primary) 28%, transparent),
              0 0 0 1px color-mix(in srgb, var(--color-primary) 18%, transparent) */
```

---

## Print Contract

No change from Phase 11. The print stylesheet in `globals.css` already hides
`.section-navigator` and `.confidence-meter` via `[data-deliberation-slot]`.
The rebuilt components must preserve the existing `print:hidden` class on the
`DeliberationSlot` outer `<section>` and the `data-print-hide` attribute convention
for the navigator.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|---|---|---|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No registry usage. All UI is bespoke CSS + React. No new npm packages.

---

## Component Inventory

### To Rebuild (full visual replacement, data binding preserved)

| File | Change Type | Notes |
|---|---|---|
| `apps/web/components/issue/SectionNavigator.tsx` | Full visual rebuild | Vertical Timeline layout. Keep `CARDS` array + anchor hrefs. Add `subtitle` + `italicWord` fields. Preserve `prefers-reduced-motion` early-return + mousemove pattern. |
| `apps/web/components/issue/DeliberationSlot.tsx` | Visual rebuild (layout + card styling) | Carousel & Flow layout. Keep all 5 Convex subscriptions byte-compatible. Keep `AGENT_LABELS`, DEL-04, confidence-meter rAF count-up. |

### To Extend (additive CSS only)

| File | Change Type | Notes |
|---|---|---|
| `apps/web/app/globals.css` | Additive only | Add Phase 12 CSS classes under the `Phase 12` section banner. Do NOT modify existing `.section-card`, `.pitch-card-list`, or `:root` variable declarations. |

### To Suppress (MED-01/MED-02 — backend contract, no visual spec needed)

| File | Change Type | Notes |
|---|---|---|
| `apps/web/app/issue/[slug]/layout.tsx` | Gate `serializeThemeCss` behind flag | No visual output change — when flag ON, `:root` house palette wins unchanged |
| `apps/web/components/issue/ThemeApplier.tsx` | Gate `applyTheme` behind flag | Same — no visual difference when flag is ON |

---

## Tests That Must Remain Green

These tripwires constrain what the executor may change:

| Test file | What it guards |
|---|---|
| `apps/web/__tests__/game-sandbox.test.ts` | `sandbox="allow-scripts"` in GameSlot.tsx; deny-list |
| `apps/web/__tests__/theme-aa-tones.test.ts` | `--color-text-dim` 7.5:1, `--color-text-mute` 5.8:1 — do not change `:root` values |
| `apps/web/__tests__/issue-page-typography.test.ts` | 42 source-scan assertions; `.eyebrow`, `.drop-cap`, canonical anchor ids |
| `apps/web/__tests__/site-header-nav.test.ts` | Single `<main>` in root layout |
| `apps/web/lib/theme.test.ts` | `theme.ts` validation logic (hex, FONT_WHITELIST, setProperty-only) — theme.ts is READ-ONLY |

**New source-scan tripwires to ADD (executor responsibility):**

1. Canonical anchor ids still present in rebuilt `SectionNavigator.tsx`:
   `#origin-story`, `#problem`, `#founder-bio`, `#case-study`, `#game`,
   `#bonus`, `#deliberation`, `#podcast`.
2. DEL-04 phrase `AGENT_LABELS` still present in rebuilt `DeliberationSlot.tsx`.
3. No `model`, no `claude`, no `gpt`, no `anthropic` string in `DeliberationSlot.tsx`
   (model-name absence check).
4. `prefers-reduced-motion` early-return still present in rebuilt `SectionNavigator.tsx`.

---

## Typography Conformance Check

Final scan confirming ≤4 sizes, ≤2 weights, all spacing multiples of 4:

**Sizes (4 total — verified):**
1. Display = `clamp(32px, 3.5vw, 48px)` — heading + confidence value
2. Title = `27px` (responsive `22px` = same token, mobile variant)
3. Body = `15px` — prose, subtitles, scout summary, flow action, charity name
4. Micro-label = `11px` — all labels, tags, section numbers, agent chips, read-status

**Weights (2 total — verified):**
1. `400` — body, italic, labels, tags, module label, subtitle, flow label, read-label, dash
2. `600` — section title, confidence value, charity name, read-value

**No `500` weight anywhere in this spec.**
**No `9px`, `14px`, `17px`, `18px`, or `32px` as standalone tokens — all folded into the 4 above.**

**Spacing multiples-of-4 check (all CSS px values):**
- `.snw-tag-pill` padding: `4px 8px` ✓
- `.snw-node` margin-top: `40px` ✓
- `.snw-content` gap: `8px` ✓
- `.snw-section-num` margin-top: `8px` ✓
- `.snw-module-label` margin-bottom: `12px` ✓ (3 × 4)
- Row padding: `24px` ✓
- `3xl` token: `80px` ✓ (20 × 4 — retained with justification as desktop reading margin)
- All other spacing values: 4, 8, 12, 16, 24, 32, 40, 44, 48, 80, 88 — all multiples of 4 ✓

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS (revised — 4 sizes, 2 weights)
- [ ] Dimension 5 Spacing: PASS (revised — all px values multiples of 4)
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Pre-Population Audit

| Decision | Source |
|---|---|
| Fixed palette tokens (`--color-*`) | `globals.css :root` — read directly |
| Font stack (Cormorant / Lora / Inter only) | 12-CONTEXT.md D-07/D-08 + 11-CONTEXT.md decisions |
| IBM Plex Mono → Inter uppercase tracking approximation | 12-CONTEXT.md D-08 (locked) |
| Vertical Timeline chosen (board node #1) | 12-CONTEXT.md D-07 |
| Carousel & Flow chosen (board node #4) | 12-CONTEXT.md D-10 |
| prefers-reduced-motion early-return pattern | 12-CONTEXT.md D-08/D-09, existing SectionNavigator.tsx |
| DEL-04 / AGENT_LABELS / 5 Convex subs preserved | 12-CONTEXT.md D-11, existing DeliberationSlot.tsx |
| Confidence meter rAF count-up + IntersectionObserver | 12-CONTEXT.md D-11, existing DeliberationSlot.tsx |
| Single `<main id="main">` constraint | 11-CONTEXT.md hard constraints |
| ≥44px touch targets | 11-CONTEXT.md hard constraints |
| WCAG AA contract (specific contrast ratios) | theme-aa-tones.test.ts + globals.css comments |
| No new npm deps | 11-CONTEXT.md + 12-CONTEXT.md (both phases lock this) |
| No CDN scripts | 11-CONTEXT.md |
| game-sandbox.test.ts stays green | 12-CONTEXT.md canonical refs |
| Row subtitle + italicWord copy | Claude's discretion (board shows pattern, copy is new) |
| Flow-line node colors (scout/advocate/editor identity) | globals.css agent identity colors |
| Winner card box-shadow spec | Claude's discretion within board direction |
| Spine thickness, dot diameter, row min-height | Claude's discretion within board direction |
| `NAVIGATION MODULE 01-B` exact label | Board screenshot (node #1 title area) |
| Board Vertical Timeline layout description | Board API node #2 description + screenshot read |
| Board Carousel & Flow description | Board API node #4 description |
| MED-02/MED-03 excluded from UI-SPEC scope | Phase prompt instruction (backend-only concerns) |
| Typography collapsed to 4 sizes / 2 weights | Checker revision — Issue 1 fix |
| Spacing corrected to multiples of 4 | Checker revision — Issue 2 fix |
