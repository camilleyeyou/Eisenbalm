
## Plan 32-05 (annotation-primitives)

- `SectionChipList.test.tsx` "Phase 32 GLY-05 upgrade" describe block (2 tests: numeric open-finding
  count on chip, `data-unresolved` marker) remains RED. This is pre-existing scaffold from Plan 32-01
  scoped explicitly to Plan 32-07 (chip-counts-and-page-wiring) — out of scope for 32-05's
  annotation-primitives work (AnnotationMark.tsx, UnresolvedFindingCard.tsx, galley CSS). Not touched.
