---
phase: 03-convex-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - convex/.env.local
  - convex/convex.json
  - apps/web/.env.local
autonomous: false
requirements:
  - CVX-01
  - CVX-04
must_haves:
  truths:
    - "Andrew has run `pnpm --filter @eisenbalm/convex exec convex dev --once --configure` and accepted/created a Convex production deployment"
    - "convex/convex.json exists with `\"functions\": \"./\"` (so the CLI knows our queries/mutations are siblings of package.json, NOT under a nested convex/ subdir)"
    - "convex/.env.local (gitignored) exists with a CONVEX_DEPLOYMENT line"
    - "apps/web/.env.local exists with both NEXT_PUBLIC_CONVEX_URL and CONVEX_DEPLOY_KEY populated"
    - "Andrew has access to the Convex dashboard for that deployment"
  artifacts:
    - path: "convex/convex.json"
      provides: "Convex CLI project configuration (functions dir = ./)"
      contains: "\"functions\": \"./\""
    - path: "convex/.env.local"
      provides: "Local CLI deployment hint (gitignored)"
      contains: "CONVEX_DEPLOYMENT="
    - path: "apps/web/.env.local"
      provides: "Web app's Convex env (gitignored)"
      contains: "NEXT_PUBLIC_CONVEX_URL=https://"
  key_links:
    - from: "convex/convex.json"
      to: "Convex Cloud deployment"
      via: "deployment URL matches CONVEX_DEPLOYMENT in convex/.env.local"
      pattern: "\"functions\": \"\\./\""
    - from: "apps/web/.env.local NEXT_PUBLIC_CONVEX_URL"
      to: "Convex deployment"
      via: "URL matches Convex dashboard Settings → Deployment URL"
      pattern: "convex.cloud"
---

<objective>
Capture Andrew's mandatory manual step: run `pnpm --filter @eisenbalm/convex exec convex dev --once --configure` once to provision the Convex production deployment, then commit `convex/convex.json` (with the critical `"functions": "./"` override per 03-RESEARCH Pitfall 6) and write the resulting `NEXT_PUBLIC_CONVEX_URL` + `CONVEX_DEPLOY_KEY` into `apps/web/.env.local` so Plans 03+ can wire them into ConvexProvider and the smoke test.

This plan is `autonomous: false`. It mirrors Phase 1's 01-02 sanity-init checkpoint. The Convex CLI is interactive (OAuth login + project naming prompts).

Purpose: Honors D-02 (production-only deployment), D-03 (manual checkpoint command), D-04 (commit `convex.json`, gitignore secrets), D-22 (Vercel + Railway env provisioning deferred but documented).
Output: A live Convex deployment, `convex/convex.json` committed, `convex/.env.local` populated (gitignored), `apps/web/.env.local` populated (gitignored), Andrew has access to the dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-convex-deployment/03-CONTEXT.md
@.planning/phases/03-convex-deployment/03-RESEARCH.md
@convex/package.json
@convex/.gitignore
@apps/web/.env.example
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Andrew runs `pnpm --filter @eisenbalm/convex exec convex dev --once --configure` and creates the production deployment</name>
  <files>
    - convex/.env.local (autogenerated by Convex CLI — never committed)
    - convex/convex.json (committed)
    - apps/web/.env.local (Andrew populates from Convex dashboard — never committed)
  </files>
  <read_first>
    - .planning/phases/03-convex-deployment/03-CONTEXT.md (D-03 — manual checkpoint command; D-04 — commit convex.json after init)
    - .planning/phases/03-convex-deployment/03-RESEARCH.md §Code Examples §6 (Andrew's one-time init command sequence)
    - .planning/phases/03-convex-deployment/03-RESEARCH.md §Pitfall 6 (convex.json `functions: "./"` requirement — sibling layout not nested)
    - convex/package.json (just landed in Plan 03-01 — confirms `pnpm --filter @eisenbalm/convex exec convex` resolves)
  </read_first>
  <what-built>
    Plan 03-01 created the `@eisenbalm/convex` workspace with `convex@^1.38.0` as a devDependency, plus `convex/tsconfig.json`, `convex/.gitignore`, and root scripts. `pnpm-workspace.yaml` now includes `convex`. Two `.env.example` files document the two new env vars Andrew will populate after this task.

    What is NOT yet done:
    - `pnpm install` has not run with the new workspace (Andrew runs it as Step A below)
    - No Convex Cloud project exists yet — Andrew creates it via the interactive CLI
    - `convex.json` does not exist yet — the CLI writes it during `--configure`
    - `convex/.env.local` does not exist yet — the CLI writes it during `--configure`
    - `apps/web/.env.local` does not yet have Convex env vars
  </what-built>
  <how-to-verify>
    Andrew, please complete the following steps in order. The executor will resume only after you confirm.

    **Step A — Install workspace dependencies.** From the repo root:

    ```bash
    pnpm install
    ```

    Expected: `pnpm install` exits 0 and resolves `@eisenbalm/convex` as a workspace (you'll see `+ @eisenbalm/convex` or similar in the install summary). If `pnpm` reports `Cannot find dependency convex/...`, re-check Plan 03-01's `convex/package.json` and `pnpm-workspace.yaml`.

    **Step B — Ensure you are logged in to a Convex account.** Open https://dashboard.convex.dev in a browser. Sign in with GitHub (recommended) or Google. The free tier is sufficient for Phase 3 (the Phase 4 Pipeline will keep usage well within free limits during stub runs).

    **Step C — Run the interactive Convex init.** From the repo root (NOT from inside `convex/`):

    ```bash
    pnpm --filter @eisenbalm/convex exec convex dev --once --configure
    ```

    The Convex CLI will:
    1. Open your browser to authenticate (OAuth). Accept the login flow.
    2. Prompt: **"What would you like to configure?"** — choose **a new project** (D-02: production-only — no dev/staging cloud deployments).
    3. Prompt: **"Project name"** — suggest `eisenbalm-dispatch` (anything readable; this is the dashboard label).
    4. Prompt: **"Team"** — pick your Convex team (usually your username).
    5. Prompt: **"How would you like to use Convex?"** — if asked about a dev deployment, choose **Cloud** (NOT local). The brief is single-engineer so we run all Convex usage against the one production deployment.
    6. The CLI then:
       - Writes `convex/convex.json` (CLI config)
       - Writes `convex/.env.local` (gitignored — contains `CONVEX_DEPLOYMENT=...`)
       - Tries to push `convex/schema.ts` to the new deployment
       - Generates `convex/_generated/` (codegen output)

       **Note:** The push may emit warnings about missing query/mutation files — Plan 03-03 creates them. That is OK. The schema deploy itself should succeed.

    **Step D — Verify and (if needed) correct `convex/convex.json`.** Per 03-RESEARCH §Pitfall 6, our layout has `convex/package.json` and `convex/schema.ts` as siblings (NOT under a nested `convex/convex/`). The CLI may have written `convex.json` with the default `"functions": "convex/"` (wrong for our layout). Hand-correct if needed:

    ```bash
    cat convex/convex.json
    ```

    If `convex.json` shows `"functions": "convex/"` OR is missing the `functions` field entirely, replace its contents with:

    ```json
    {
      "$schema": "https://convex.dev/schemas/convex.json",
      "functions": "./"
    }
    ```

    Verify:
    ```bash
    grep -q '"functions"' convex/convex.json && grep -q '"./"' convex/convex.json && echo OK
    ```
    Expected output: `OK`.

    **Step E — Capture the deployment URL.** From the Convex dashboard (https://dashboard.convex.dev), open your new project → **Settings** (or top-right header). Find the **Deployment URL** — looks like `https://<adjective>-<animal>-<number>.convex.cloud`. Copy it.

    **Step F — Generate a Production Deploy Key.** In the same Settings page → **Deploy Keys** section → click **Generate Production Deploy Key** (or "Generate Key" / similar wording). Copy the key — it starts with `prod:`. **You will not see it again** — copy it now into a safe place.

    **Step G — Populate `apps/web/.env.local`.** If `apps/web/.env.local` does not exist, copy from the template first:

    ```bash
    cp apps/web/.env.example apps/web/.env.local
    ```

    Then edit (or append to) `apps/web/.env.local` so it contains:

    ```
    NEXT_PUBLIC_SANITY_PROJECT_ID=6h1vd9mf
    NEXT_PUBLIC_SANITY_DATASET=production
    NEXT_PUBLIC_SITE_URL=http://localhost:3000
    NEXT_PUBLIC_CONVEX_URL=https://<your-deployment>.convex.cloud
    CONVEX_DEPLOY_KEY=prod:<your-key>
    ```

    Replace `<your-deployment>` and `<your-key>` with the actual values from Steps E and F.

    Verify:
    ```bash
    grep -E '^NEXT_PUBLIC_CONVEX_URL=https://[a-z0-9-]+\.convex\.cloud$' apps/web/.env.local && \
      grep -E '^CONVEX_DEPLOY_KEY=prod:' apps/web/.env.local && echo OK
    ```
    Expected output: `OK`.

    **Step H — Confirm gitignore is working.** From the repo root:

    ```bash
    git check-ignore apps/web/.env.local convex/.env.local
    ```
    Expected: both paths are echoed (meaning both are gitignored).

    ```bash
    git check-ignore convex/convex.json
    ```
    Expected: non-zero exit code, no output (meaning `convex.json` is NOT gitignored — it should be committable).

    **Step I — Commit `convex/convex.json` to git.** This is the only file Andrew commits in this checkpoint. (`apps/web/.env.local` and `convex/.env.local` stay gitignored; `convex/_generated/` will be committed in Plan 03-04 once the function files exist.)

    ```bash
    git add convex/convex.json
    git status
    ```
    Expected: `convex/convex.json` listed as staged. (Do not commit yet — the executor will run a combined commit at the end of the phase, or follow the project's existing GSD commit cadence.)
  </how-to-verify>
  <acceptance_criteria>
    - `pnpm install` from repo root exits 0 and lists `@eisenbalm/convex` as a workspace
    - `convex/convex.json` exists on disk and contains `"functions"` key with value `"./"` (verify: `grep -q '"functions"[[:space:]]*:[[:space:]]*"\./"' convex/convex.json`)
    - `convex/.env.local` exists and contains a `CONVEX_DEPLOYMENT=` line with a non-empty value
    - `convex/.env.local` is gitignored — `git check-ignore convex/.env.local` exits 0
    - `apps/web/.env.local` exists and contains a line matching `NEXT_PUBLIC_CONVEX_URL=https://.+\.convex\.cloud`
    - `apps/web/.env.local` contains a line matching `CONVEX_DEPLOY_KEY=prod:.+`
    - `apps/web/.env.local` is gitignored — `git check-ignore apps/web/.env.local` exits 0
    - `convex/convex.json` is NOT gitignored — `git check-ignore convex/convex.json` exits non-zero
    - A live deployment exists on the Convex dashboard at the URL Andrew populated into `apps/web/.env.local`
  </acceptance_criteria>
  <resume-signal>
    Type "approved" once `convex/convex.json` shows `"functions": "./"`, both `.env.local` files exist with the four expected entries, and the deployment is visible in dashboard.convex.dev. Or describe any issue (e.g. "the CLI failed at OAuth" or "no Production Deploy Key option in dashboard").

    Common failure modes:
    - `pnpm install` reports a workspace conflict → re-check Plan 03-01's `pnpm-workspace.yaml` and `convex/package.json` for typos
    - CLI exits with `not in a Convex project` → re-run from repo root (NOT from inside `convex/`); the `--filter @eisenbalm/convex` selects the right workspace
    - CLI writes `convex.json` with `"functions": "convex/"` → hand-correct per Step D; this is expected per 03-RESEARCH §Pitfall 6
    - Schema push fails with "missing function module" → expected if Plan 03-03 has not run yet; the schema itself should still upload
  </resume-signal>
</task>

</tasks>

<verification>
After Andrew approves:
- `test -f convex/convex.json` exits 0
- `grep -q '"functions"[[:space:]]*:[[:space:]]*"\./"' convex/convex.json` exits 0
- `test -f convex/.env.local && grep -q "^CONVEX_DEPLOYMENT=" convex/.env.local` exits 0
- `test -f apps/web/.env.local && grep -qE "^NEXT_PUBLIC_CONVEX_URL=https://.+\.convex\.cloud" apps/web/.env.local` exits 0
- `grep -qE "^CONVEX_DEPLOY_KEY=prod:" apps/web/.env.local` exits 0
- `git check-ignore convex/.env.local apps/web/.env.local` echoes both paths (both gitignored)
- `git check-ignore convex/convex.json` exits non-zero (committable)
</verification>

<success_criteria>
- A real Convex production deployment exists in Andrew's Convex Cloud organization
- `convex/convex.json` carries `"functions": "./"` so the CLI finds schema.ts and the (yet-to-be-written) function files
- `convex/.env.local` (gitignored) holds the CLI's deployment hint
- `apps/web/.env.local` (gitignored) holds both `NEXT_PUBLIC_CONVEX_URL` and `CONVEX_DEPLOY_KEY` so Plans 03-05+ can wire them
- Andrew has confirmed access to https://dashboard.convex.dev for the project
- The repo is in a state where Plan 03-03 (writing the five function files) can begin
</success_criteria>

<output>
After completion, create `.planning/phases/03-convex-deployment/03-02-SUMMARY.md` recording:
  (a) the Convex project/team name Andrew chose,
  (b) the deployment URL (or a placeholder note if Andrew prefers not to record it — the value lives only in `apps/web/.env.local`),
  (c) whether the CLI wrote `"functions": "./"` automatically or it required hand-editing (resolves Open Question 1 from 03-RESEARCH),
  (d) any extraneous files the CLI scaffolded that Plan 03-03 / 03-04 will need to handle (rare — but log them),
  (e) confirmation that `convex/_generated/` was created (the directory should exist even if its contents are incomplete pending Plan 03-03).
</output>
</content>
</invoke>