---
phase: 21-auth-app-shell-convex-schema
plan: 04
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/pipeline/src/eisenbalm_pipeline/api/auth.py
  - packages/pipeline/src/eisenbalm_pipeline/api/runs.py
  - packages/pipeline/tests/api/test_clerk_auth.py
  - packages/pipeline/.env.example
autonomous: true
requirements: [AUTH-03]
user_setup:
  - service: clerk
    why: "FastAPI verifies Clerk JWTs against Clerk's JWKS endpoint"
    env_vars:
      - name: CLERK_JWT_ISSUER_DOMAIN
        source: "Clerk Dashboard → API Keys → Frontend API URL; set on Railway (same value as the Convex deployment env)"
must_haves:
  truths:
    - "A dashboard-control FastAPI endpoint returns 401 when called without a valid Clerk JWT"
    - "A dashboard-control endpoint returns 401 on an expired/invalid JWT"
    - "The Railway cron path (X-Pipeline-Trigger-Secret) is unchanged and still accepted"
    - "A valid JWT yields claims whose sub is available as the operator id"
  artifacts:
    - path: "packages/pipeline/src/eisenbalm_pipeline/api/auth.py"
      provides: "require_clerk_jwt FastAPI dependency (PyJWT + JWKS)"
      exports: ["require_clerk_jwt"]
    - path: "packages/pipeline/tests/api/test_clerk_auth.py"
      provides: "AUTH-03 unit tests (JWKS-mocked 401s + cron-secret-unaffected)"
      contains: "require_clerk_jwt"
  key_links:
    - from: "packages/pipeline/src/eisenbalm_pipeline/api/runs.py"
      to: "require_clerk_jwt"
      via: "Depends() on a dashboard-control endpoint"
      pattern: "Depends(require_clerk_jwt)"
    - from: "packages/pipeline/src/eisenbalm_pipeline/api/auth.py"
      to: "Clerk JWKS"
      via: "{CLERK_JWT_ISSUER_DOMAIN}/.well-known/jwks.json"
      pattern: "jwks.json"
---

<objective>
Add a `require_clerk_jwt` FastAPI dependency (PyJWT + Clerk JWKS, RS256) and apply it to a dashboard-control endpoint, while leaving the Railway cron path's `X-Pipeline-Trigger-Secret` guard completely untouched. Fill the Wave 0 `test_clerk_auth.py` stub with real assertions.

Purpose: Satisfies AUTH-03 — FastAPI dashboard endpoints reject requests without a valid Clerk JWT; the cron secret path is unaffected. The decoded `sub` claim is the operator id available for attribution when later phases wire the run-trigger write flow.
Output: A reusable JWT guard, one dashboard-control endpoint protected by it, and a green pytest suite proving 401 behavior + cron-path preservation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-auth-app-shell-convex-schema/21-CONTEXT.md
@.planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md
@packages/pipeline/src/eisenbalm_pipeline/api/main.py
@packages/pipeline/src/eisenbalm_pipeline/api/runs.py
@packages/pipeline/tests/api/test_runs.py

<interfaces>
<!-- Exact JWT dependency from 21-RESEARCH Pattern 4. Use verbatim, including the degraded-dev no-domain branch. -->
- security = HTTPBearer()
- _get_clerk_jwks_url(): from CLERK_JWT_ISSUER_DOMAIN env -> f"{domain.rstrip('/')}/.well-known/jwks.json"  (lru_cache)
- _fetch_public_key(kid): GET jwks, RSAAlgorithm.from_jwk(matching key); 401 on unknown kid
- async def require_clerk_jwt(credentials: HTTPAuthorizationCredentials = Depends(security)) -> dict:
    if not CLERK_JWT_ISSUER_DOMAIN: log warning, return {"sub": "local-dev-operator"}  # mirrors _require_trigger_secret degraded path
    header = jwt.get_unverified_header(token); key = _fetch_public_key(header["kid"])
    claims = jwt.decode(token, key, algorithms=["RS256"], options={"verify_aud": False})
    return claims  # claims["sub"] = Clerk userId
    except ExpiredSignatureError -> 401 "Token expired"; except Exception -> 401 "Invalid token"

<!-- Existing cron guard to leave UNTOUCHED (api/runs.py). -->
- _require_trigger_secret(request): checks X-Pipeline-Trigger-Secret against PIPELINE_TRIGGER_SECRET; skip-with-warning if unset. DO NOT MODIFY.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: require_clerk_jwt dependency + apply to a dashboard-control endpoint</name>
  <read_first>
    - packages/pipeline/src/eisenbalm_pipeline/api/runs.py (the file being modified — study _require_trigger_secret, the router, and how endpoints are structured; DO NOT touch the cron guard)
    - packages/pipeline/src/eisenbalm_pipeline/api/main.py (router registration; degraded-boot logging convention to mirror)
    - .planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md (Pattern 4 full code + Pitfall 4 JWKS caching + Code Examples "FastAPI Route Using Both Auth Guards")
  </read_first>
  <action>
    1. `packages/pipeline/src/eisenbalm_pipeline/api/auth.py` (NEW): implement exactly 21-RESEARCH Pattern 4. Use `import jwt` (PyJWT — already vendored), `import requests`, `from fastapi import Depends, HTTPException, status`, `from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer`, `from functools import lru_cache`. Define `security = HTTPBearer()`, `_get_clerk_jwks_url()` (lru_cache on the URL getter — Pitfall 4), `_fetch_public_key(kid)` (GET JWKS, `RSAAlgorithm.from_jwk` for the matching kid, 401 on unknown kid), and `async def require_clerk_jwt(credentials=Depends(security)) -> dict`. Include the degraded-dev branch: if `CLERK_JWT_ISSUER_DOMAIN` is unset, log a warning and `return {"sub": "local-dev-operator"}` — mirroring `_require_trigger_secret`'s skip-with-warning so local dev and the test suite don't require live Clerk. Verify with RS256, `options={"verify_aud": False}`. Raise 401 on `jwt.ExpiredSignatureError` ("Token expired") and on any other exception ("Invalid token"). Docstring: claims["sub"] is the Clerk userId = actorId/triggeredBy for later phases.

    2. `packages/pipeline/src/eisenbalm_pipeline/api/runs.py` (MODIFY): add a NEW dashboard-control endpoint that uses the Clerk guard — `from eisenbalm_pipeline.api.auth import require_clerk_jwt`. Add a route `@router.post("/dashboard/whoami")` (or `/pipeline/whoami`) `async def dashboard_whoami(claims: dict = Depends(require_clerk_jwt)) -> dict: return {"operatorId": claims.get("sub")}`. This is a minimal real dashboard-control endpoint that proves the guard is wired; it does NOT trigger a run (D-04: no synthetic run-trigger this phase). Do NOT add `require_clerk_jwt` to `/run/weekly`, `/run/{id}/resume`, `/run/{id}/publish`, or `/run/{id}/status` — those keep their existing guards (cron path stays on X-Pipeline-Trigger-Secret, AUTH-03). Do NOT modify `_require_trigger_secret`.

    3. `packages/pipeline/.env.example` (MODIFY): add `CLERK_JWT_ISSUER_DOMAIN=` with a comment that it's the Clerk Frontend API URL, used by require_clerk_jwt for JWKS, and that leaving it unset skips Clerk verification in local dev (same pattern as PIPELINE_TRIGGER_SECRET).
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run python -c "from eisenbalm_pipeline.api.auth import require_clerk_jwt; from eisenbalm_pipeline.api.main import app; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/pipeline/src/eisenbalm_pipeline/api/auth.py` exists and exports `require_clerk_jwt`
    - `auth.py` contains `HTTPBearer`, `RSAAlgorithm.from_jwk`, `algorithms=["RS256"]`, and the `.well-known/jwks.json` URL construction
    - `auth.py` has the degraded branch returning `{"sub": "local-dev-operator"}` when CLERK_JWT_ISSUER_DOMAIN is unset
    - `runs.py` contains `Depends(require_clerk_jwt)` on the new dashboard endpoint
    - `runs.py` `_require_trigger_secret` function body is unchanged (git diff shows no edits inside it) and `/run/weekly` still calls `_require_trigger_secret(request)`
    - The import smoke command exits 0
  </acceptance_criteria>
  <done>require_clerk_jwt exists and guards a dashboard-control endpoint; cron secret path untouched; app imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Fill test_clerk_auth.py — 401 cases (JWKS mocked) + cron path unaffected</name>
  <read_first>
    - packages/pipeline/tests/api/test_clerk_auth.py (the Plan 01 stub to fill — remove the skip markers)
    - packages/pipeline/tests/conftest.py (the `client` ASGITransport fixture + how PIPELINE_TRIGGER_SECRET / env is handled)
    - packages/pipeline/tests/api/test_runs.py (style for the cron-secret-path test to confirm it still passes)
    - packages/pipeline/src/eisenbalm_pipeline/api/auth.py (the dependency under test — created in Task 1)
  </read_first>
  <action>
    Replace the 4 skipped stubs in `packages/pipeline/tests/api/test_clerk_auth.py` with real tests. Mock the JWKS network call (monkeypatch `eisenbalm_pipeline.api.auth._fetch_public_key` and/or `requests.get`) so no real Clerk endpoint is hit. Force the non-degraded path by setting `CLERK_JWT_ISSUER_DOMAIN` via `monkeypatch.setenv` in the tests that assert 401 behavior (otherwise the dev-skip branch returns the sentinel and there's no 401 to assert).

    Tests:
    - `test_require_clerk_jwt_missing_returns_401`: with CLERK_JWT_ISSUER_DOMAIN set, POST the dashboard endpoint with NO Authorization header → expect 401 or 403 (HTTPBearer auto-raises 403 on missing credentials — assert status in (401, 403); document which).
    - `test_require_clerk_jwt_expired_returns_401`: monkeypatch `_fetch_public_key` to return a dummy key and monkeypatch `jwt.decode` (in the auth module namespace) to raise `jwt.ExpiredSignatureError` → call the endpoint with a bogus Bearer token → expect 401 with detail "Token expired".
    - `test_require_clerk_jwt_valid_returns_claims`: monkeypatch `jwt.get_unverified_header` to return `{"kid": "k1"}`, `_fetch_public_key` to return a dummy key, and `jwt.decode` to return `{"sub": "user_abc"}` → call the endpoint with a Bearer token → expect 200 and body `{"operatorId": "user_abc"}`.
    - `test_cron_trigger_secret_path_unaffected`: assert the existing cron path still behaves — call `/run/weekly` (or assert via the existing `_require_trigger_secret` contract) and confirm Clerk is NOT required on it. If PIPELINE_TRIGGER_SECRET is unset in the test env, assert the dev-skip path returns the run normally (mirror test_runs.py's skip handling); the key assertion is that NO Clerk Authorization header is needed for the cron route.

    Use the existing `client` fixture (ASGITransport). No watch mode.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/api/test_clerk_auth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `packages/pipeline/tests/api/test_clerk_auth.py` has ZERO `@pytest.mark.skip` markers remaining
    - The expired-token test asserts a 401 with detail "Token expired"
    - The valid-token test asserts the endpoint returns the `sub` claim as the operator id
    - The cron-path test confirms the dashboard Clerk guard does NOT apply to /run/weekly (no Authorization header required)
    - JWKS is mocked — the tests make no real network call to Clerk
    - `cd packages/pipeline && uv run pytest tests/api/test_clerk_auth.py -q` exits 0
    - `cd packages/pipeline && uv run pytest tests/api/test_runs.py -q` still exits 0 (cron path regression check)
  </acceptance_criteria>
  <done>AUTH-03 is automated: missing/expired tokens 401, valid token yields claims, cron secret path unaffected.</done>
</task>

</tasks>

<verification>
- `cd packages/pipeline && uv run pytest tests/api/test_clerk_auth.py tests/api/test_runs.py -q` exits 0
- `grep -c "Depends(require_clerk_jwt)" packages/pipeline/src/eisenbalm_pipeline/api/runs.py` ≥ 1
- `_require_trigger_secret` unchanged (git diff)
- App imports cleanly with the new auth module
</verification>

<success_criteria>
- Dashboard-control endpoint rejects missing/invalid/expired Clerk JWTs (AUTH-03)
- Railway cron path keeps X-Pipeline-Trigger-Secret, unaffected (AUTH-03)
- Decoded sub claim is available as the operator id for later attribution flows
</success_criteria>

<output>
After completion, create `.planning/phases/21-auth-app-shell-convex-schema/21-04-SUMMARY.md`
</output>
