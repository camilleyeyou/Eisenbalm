---
phase: 21-auth-app-shell-convex-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dispatch-control/package.json
  - apps/dispatch-control/tsconfig.json
  - apps/dispatch-control/next.config.ts
  - apps/dispatch-control/postcss.config.mjs
  - apps/dispatch-control/components.json
  - apps/dispatch-control/app/globals.css
  - apps/dispatch-control/app/layout.tsx
  - apps/dispatch-control/app/page.tsx
  - apps/dispatch-control/lib/utils.ts
  - apps/dispatch-control/lib/workspace.ts
  - apps/dispatch-control/vitest.config.ts
  - apps/dispatch-control/.env.example
  - apps/dispatch-control/__tests__/harness.test.ts
  - apps/dispatch-control/__tests__/middleware-matcher.test.ts
  - apps/dispatch-control/__tests__/apps-web-no-clerk.test.ts
  - apps/dispatch-control/__tests__/workspace-upsert.test.ts
  - packages/pipeline/tests/api/test_clerk_auth.py
  - package.json
autonomous: true
requirements: [AUTH-01, AUTH-02, CFG-05]
must_haves:
  truths:
    - "dispatch-control is a pnpm workspace package that installs and typechecks"
    - "Vitest runs in dispatch-control and discovers __tests__/*.test.ts"
    - "pytest collects the new Clerk-auth test file in the pipeline suite"
    - "Root package.json exposes dev/build/typecheck/lint scripts for dispatch-control"
  artifacts:
    - path: "apps/dispatch-control/package.json"
      provides: "Next 15 / React 19 / Tailwind v4 / vitest toolchain matching apps/web"
      contains: "\"name\": \"dispatch-control\""
    - path: "apps/dispatch-control/vitest.config.ts"
      provides: "Test harness mirroring apps/web vitest config"
      contains: "__tests__"
    - path: "apps/dispatch-control/lib/workspace.ts"
      provides: "DEFAULT_WORKSPACE_ID constant (D-16)"
      contains: "DEFAULT_WORKSPACE_ID"
    - path: "packages/pipeline/tests/api/test_clerk_auth.py"
      provides: "Wave 0 stub tests for AUTH-03 (filled in Plan 04)"
      contains: "require_clerk_jwt"
  key_links:
    - from: "package.json"
      to: "apps/dispatch-control"
      via: "pnpm --filter dispatch-control scripts"
      pattern: "dispatch-control"
---

<objective>
Scaffold the new `apps/dispatch-control` Next.js 15 app (cloning the apps/web toolchain) and stand up the Phase 21 test harness BEFORE any feature code lands. This is Wave 0 per 21-VALIDATION.md: establish vitest in the new app, a pytest stub file for the FastAPI Clerk guard, and the shared `DEFAULT_WORKSPACE_ID` constant.

Purpose: Every subsequent Phase 21 plan (auth wiring, schema, FastAPI guard, app shell) needs a buildable, testable dispatch-control app and a place to put failing tests first. No Clerk, no Convex provider, no shell yet — just the skeleton + harness.
Output: A new workspace app that installs, typechecks, and runs an empty-but-green vitest suite; a pytest stub file; root scripts registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-auth-app-shell-convex-schema/21-CONTEXT.md
@.planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md
@.planning/phases/21-auth-app-shell-convex-schema/21-VALIDATION.md
@apps/web/package.json
@apps/web/components.json
@apps/web/vitest.config.ts
@package.json

<interfaces>
<!-- Clone these from apps/web. Versions are LOCKED — match exactly. -->
From apps/web/package.json (versions to clone):
  next ^15.3.9 · react ^19.2.6 · react-dom ^19.2.6 · tailwindcss ^4.3.0 ·
  @tailwindcss/postcss ^4.3.0 · lucide-react ^1.14.0 · clsx ^2.1.1 ·
  tailwind-merge ^3.6.0 · class-variance-authority ^0.7.1 · tailwindcss-animate ^1.0.7 ·
  convex ^1.38.0 · typescript ^5.6.0 · @types/node ^22.0.0 · @types/react ^19.0.0 ·
  @types/react-dom ^19.0.0 · vitest ^3.2.0 · @vitest/ui ^3.2.0 · vite-tsconfig-paths ^5.1.0

From apps/web/components.json (shadcn config to replicate):
  style: "default" · rsc: true · tsx: true · baseColor: "neutral" · cssVariables: true
  aliases: components @/components, ui @/components/ui, lib @/lib, utils @/lib/utils, hooks @/hooks
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold dispatch-control app skeleton + workspace constant</name>
  <read_first>
    - apps/web/package.json (clone exact dependency versions)
    - apps/web/components.json (replicate shadcn config)
    - apps/web/tsconfig.json (clone compilerOptions + paths)
    - apps/web/next.config.ts and apps/web/postcss.config.mjs (clone build config)
    - .planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md (Recommended Project Structure + Workspace Constant example)
    - package.json (root — where new scripts are added)
  </read_first>
  <action>
    Create `apps/dispatch-control/` as a Next 15 App Router app cloning apps/web's toolchain. NO Clerk, NO Convex provider, NO shell — skeleton only.

    1. `apps/dispatch-control/package.json`: name "dispatch-control", private, version "0.0.0". Scripts: `"dev": "next dev --port 3001"`, `"build": "next build"`, `"start": "next start --port 3001"`, `"lint": "next lint"`, `"typecheck": "tsc --noEmit"`, `"test:unit": "vitest run"`, `"test": "vitest run"`. Dependencies (clone EXACT versions from interfaces block): convex ^1.38.0, class-variance-authority ^0.7.1, clsx ^2.1.1, lucide-react ^1.14.0, next ^15.3.9, react ^19.2.6, react-dom ^19.2.6, tailwind-merge ^3.6.0, tailwindcss-animate ^1.0.7, @eisenbalm/shared workspace:*, @eisenbalm/convex workspace:*. devDependencies: @tailwindcss/postcss ^4.3.0, @types/node ^22.0.0, @types/react ^19.0.0, @types/react-dom ^19.0.0, @vitest/ui ^3.2.0, tailwindcss ^4.3.0, typescript ^5.6.0, vite-tsconfig-paths ^5.1.0, vitest ^3.2.0. (Clerk is added in Plan 03, NOT here.)
    2. `apps/dispatch-control/tsconfig.json`: clone apps/web/tsconfig.json verbatim (moduleResolution "Bundler", paths `"@/*": ["./*"]`, etc.).
    3. `apps/dispatch-control/next.config.ts`: clone apps/web/next.config.ts (minimal/default).
    4. `apps/dispatch-control/postcss.config.mjs`: clone apps/web/postcss.config.mjs (`{ plugins: { '@tailwindcss/postcss': {} } }`).
    5. `apps/dispatch-control/components.json`: replicate apps/web/components.json exactly (style default, rsc true, tsx true, baseColor neutral, cssVariables true, aliases per interfaces block, tailwind.css "app/globals.css").
    6. `apps/dispatch-control/app/globals.css`: `@import "tailwindcss";` plus a minimal neutral `:root` token block (clone the shadcn variable shim from apps/web/app/globals.css if present, otherwise just the @import line + `@plugin "tailwindcss-animate";`).
    7. `apps/dispatch-control/app/layout.tsx`: plain Server Component root layout — `<html lang="en"><body>{children}</body></html>`, import globals.css. NO ClerkProvider yet (Plan 03 adds it). Export `metadata = { title: 'Eisenbalm Dispatch Control' }`.
    8. `apps/dispatch-control/app/page.tsx`: minimal placeholder `export default function Home() { return <main>Dispatch Control</main> }`.
    9. `apps/dispatch-control/lib/utils.ts`: clone apps/web/lib/utils.ts (the `cn()` clsx+tailwind-merge helper).
    10. `apps/dispatch-control/lib/workspace.ts`: per 21-RESEARCH.md Workspace Constant — `export const DEFAULT_WORKSPACE_ID = 'eisenbalm'` plus `export async function getCurrentWorkspace(): Promise<string> { return DEFAULT_WORKSPACE_ID }` with the Phase 28 comment. This is the ONLY place "eisenbalm" appears in control-plane code (D-16).
    11. `apps/dispatch-control/.env.example`: document (commented) NEXT_PUBLIC_CONVEX_URL, NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, CLERK_SECRET_KEY, NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in, NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/ — with a note that CLERK_SECRET_KEY must NEVER appear in apps/web (Pitfall 6).
    12. Root `package.json`: append scripts `"dev:dispatch-control": "pnpm --filter dispatch-control dev"`, `"build:dispatch-control": "pnpm --filter dispatch-control build"`, `"lint:dispatch-control": "pnpm --filter dispatch-control lint"`, `"typecheck:dispatch-control": "pnpm --filter dispatch-control typecheck"`, `"test:dispatch-control": "pnpm --filter dispatch-control test"`.

    Run `pnpm install` from repo root so the new workspace package is linked. Do NOT change pnpm-workspace.yaml (apps/* already covers it — research Open Question 2).
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/dispatch-control/package.json` exists and contains `"name": "dispatch-control"`
    - `apps/dispatch-control/package.json` contains `"dev": "next dev --port 3001"`
    - `apps/dispatch-control/package.json` has ZERO occurrences of `@clerk` (added in Plan 03)
    - File `apps/dispatch-control/lib/workspace.ts` contains `DEFAULT_WORKSPACE_ID = 'eisenbalm'`
    - Root `package.json` contains `"typecheck:dispatch-control"` and `"dev:dispatch-control"`
    - `pnpm --filter dispatch-control typecheck` exits 0
    - `pnpm-workspace.yaml` is unchanged (git diff shows no modification to that file)
  </acceptance_criteria>
  <done>dispatch-control installs as a workspace package and typechecks; root scripts registered; workspace constant in place; no Clerk dependency yet.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest harness + AUTH/CFG test stubs (frontend) + FastAPI pytest stub</name>
  <read_first>
    - apps/web/vitest.config.ts (clone test config)
    - apps/dispatch-control/package.json (created in Task 1 — confirm test scripts)
    - .planning/phases/21-auth-app-shell-convex-schema/21-VALIDATION.md (Wave 0 Requirements + Phase Requirements → Test Map)
    - .planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md (Pattern 1 middleware, Pattern 7 JIT upsert — to author meaningful stub assertions)
    - packages/pipeline/tests/api/test_runs.py (pytest style + conftest `client` fixture usage)
  </read_first>
  <action>
    Stand up the test harness so later plans can write failing tests first. Stubs that CAN pass now (harness sanity, root-config-driven grep) should pass; stubs for behavior built in later plans use `test.todo`/`pytest.mark.skip` with a clear reason so the suite stays green but the seam is reserved.

    1. `apps/dispatch-control/vitest.config.ts`: clone apps/web/vitest.config.ts (defineConfig + tsconfigPaths plugin, environment 'node', `include: ['__tests__/**/*.test.ts', '__tests__/**/*.test.tsx']`, globals false, reporters default).

    2. `apps/dispatch-control/__tests__/harness.test.ts`: a trivial sanity test — `import { describe, it, expect } from 'vitest'` then assert `DEFAULT_WORKSPACE_ID` imported from `@/lib/workspace` equals `'eisenbalm'`. This proves vitest + path aliases work. MUST pass now.

    3. `apps/dispatch-control/__tests__/middleware-matcher.test.ts` (AUTH-01 seam, filled in Plan 03): use `it.todo('clerkMiddleware protects all non-public routes')` and `it.todo('/sign-in is a public route')`. Add a comment referencing 21-RESEARCH Pattern 1.

    4. `apps/dispatch-control/__tests__/apps-web-no-clerk.test.ts` (AUTH-02 grep seam): this CAN run now and MUST pass. Read `apps/web/package.json` via `fs.readFileSync` (relative path from repo root: `path.resolve(__dirname, '../../web/package.json')`) and assert the JSON string contains zero occurrences of `@clerk`. This is the standing no-leak guard (Pitfall 2) — it stays green this phase and trips if any later phase leaks Clerk into apps/web.

    5. `apps/dispatch-control/__tests__/workspace-upsert.test.ts` (AUTH-04 / CFG-05 seam, filled in Plan 02): `it.todo('upsertCurrentUser writes Clerk sub as clerkUserId')` and `it.todo('seedEisenbalm is idempotent on second call')`. Comment referencing 21-RESEARCH Pattern 6 + 7.

    6. `packages/pipeline/tests/api/test_clerk_auth.py` (AUTH-03 seam, filled in Plan 04): create the file with imports (`import pytest`) and skipped stub tests: `test_require_clerk_jwt_missing_returns_401`, `test_require_clerk_jwt_expired_returns_401`, `test_require_clerk_jwt_valid_returns_claims`, `test_cron_trigger_secret_path_unaffected` — each `@pytest.mark.skip(reason="Filled in Plan 21-04: require_clerk_jwt dependency")`. The file MUST be collectible by pytest (valid Python, valid test signatures). Add a module docstring referencing 21-RESEARCH Pattern 4 (`require_clerk_jwt` via PyJWT + JWKS).

    Do NOT use watch mode anywhere. All commands are single-run.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test:unit && cd packages/pipeline && uv run pytest tests/api/test_clerk_auth.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter dispatch-control test:unit` exits 0 (harness + no-clerk grep pass; todos do not fail the run)
    - `apps/dispatch-control/__tests__/apps-web-no-clerk.test.ts` asserts `@clerk` absent from apps/web/package.json
    - `apps/dispatch-control/__tests__/harness.test.ts` imports from `@/lib/workspace` and passes
    - `cd packages/pipeline && uv run pytest tests/api/test_clerk_auth.py --collect-only -q` lists 4 tests and exits 0
    - `packages/pipeline/tests/api/test_clerk_auth.py` contains the string `require_clerk_jwt`
    - No `--watch` flag appears in any added script or command
  </acceptance_criteria>
  <done>Frontend vitest harness green with AUTH-01/AUTH-04 todo seams + a live AUTH-02 no-leak grep; pytest collects the 4-test AUTH-03 stub file. Later plans fill the seams.</done>
</task>

</tasks>

<verification>
- `pnpm --filter dispatch-control typecheck` exits 0
- `pnpm --filter dispatch-control test:unit` exits 0
- `cd packages/pipeline && uv run pytest tests/api/test_clerk_auth.py --collect-only -q` exits 0
- `grep -c "@clerk" apps/dispatch-control/package.json` returns 0
- Root `package.json` has 5 new dispatch-control scripts
</verification>

<success_criteria>
- New workspace app installs, typechecks, and runs a green vitest suite
- Test seams reserved for AUTH-01 (middleware), AUTH-03 (FastAPI), AUTH-04/CFG-05 (Convex mutations)
- AUTH-02 no-Clerk-in-apps/web grep guard is live and green
- DEFAULT_WORKSPACE_ID constant established (D-16)
</success_criteria>

<output>
After completion, create `.planning/phases/21-auth-app-shell-convex-schema/21-01-SUMMARY.md`
</output>
