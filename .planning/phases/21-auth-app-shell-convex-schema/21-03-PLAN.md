---
phase: 21-auth-app-shell-convex-schema
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - apps/dispatch-control/package.json
  - apps/dispatch-control/middleware.ts
  - apps/dispatch-control/app/layout.tsx
  - apps/dispatch-control/components/ConvexClientProvider.tsx
  - apps/dispatch-control/app/sign-in/[[...sign-in]]/page.tsx
  - apps/dispatch-control/.env.example
  - apps/dispatch-control/__tests__/middleware-matcher.test.ts
autonomous: false
requirements: [AUTH-01, AUTH-02]
user_setup:
  - service: clerk
    why: "Sign-in UI and route protection require Clerk application keys"
    env_vars:
      - name: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard → API Keys → Publishable key"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard → API Keys → Secret key (NEVER set on apps/web)"
      - name: NEXT_PUBLIC_CLERK_SIGN_IN_URL
        source: "Set to /sign-in in dispatch-control env"
    dashboard_config:
      - task: "Create a separate Vercel project for dispatch-control so CLERK_SECRET_KEY never bleeds into apps/web (Pitfall 6)"
        location: "Vercel → New Project → root apps/dispatch-control"
must_haves:
  truths:
    - "Unauthenticated requests to any dispatch-control route except /sign-in redirect to Clerk sign-in"
    - "ClerkProvider wraps a ConvexProviderWithClerk client wrapper in the root layout"
    - "/sign-in renders the Clerk <SignIn /> component via a catch-all route"
    - "apps/web still has ZERO Clerk dependency (no-leak grep stays green)"
  artifacts:
    - path: "apps/dispatch-control/middleware.ts"
      provides: "clerkMiddleware + createRouteMatcher protecting all non-public routes"
      contains: "clerkMiddleware"
    - path: "apps/dispatch-control/components/ConvexClientProvider.tsx"
      provides: "Client-side ConvexProviderWithClerk bound to Clerk useAuth"
      contains: "ConvexProviderWithClerk"
    - path: "apps/dispatch-control/app/sign-in/[[...sign-in]]/page.tsx"
      provides: "Clerk-hosted sign-in surface"
      contains: "SignIn"
  key_links:
    - from: "apps/dispatch-control/middleware.ts"
      to: "auth.protect()"
      via: "clerkMiddleware on non-public routes"
      pattern: "auth.protect"
    - from: "apps/dispatch-control/components/ConvexClientProvider.tsx"
      to: "@clerk/nextjs useAuth"
      via: "ConvexProviderWithClerk useAuth prop"
      pattern: "useAuth"
---

<objective>
Wire Clerk authentication into `apps/dispatch-control`: install `@clerk/nextjs`, add `clerkMiddleware` + `createRouteMatcher` protecting every route except `/sign-in`, set up the `ClerkProvider` → `ConvexProviderWithClerk` provider hierarchy, and add the Clerk `<SignIn />` catch-all route. Auth is implemented FIRST before any shell functionality (D-10), and `apps/web` must remain 100% unauthenticated (Pitfall 2).

Purpose: Satisfies AUTH-01 (operator sign-in; every dashboard route protected) and AUTH-02 (unauthenticated → redirect; apps/web zero Clerk dependency). This is the gate everything else builds on.
Output: A dispatch-control app where unauthenticated access redirects to Clerk sign-in, authenticated sessions reach Convex with Clerk identity, and apps/web is provably untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-auth-app-shell-convex-schema/21-CONTEXT.md
@.planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md
@apps/dispatch-control/package.json
@apps/dispatch-control/app/layout.tsx
@convex/auth.config.ts

<interfaces>
<!-- Exact Clerk/Convex wiring from 21-RESEARCH (Patterns 1, 2, 8). Use verbatim. -->
- middleware: clerkMiddleware(async (auth, req) => { if (!isPublicRoute(req)) await auth.protect() })
  isPublicRoute = createRouteMatcher(['/sign-in(.*)'])
  config.matcher: ['/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)', '/(api|trpc)(.*)', '/__clerk/(.*)']
- layout (Server Component, NO 'use client'): <ClerkProvider><ConvexClientProvider>{children}</ConvexClientProvider></ClerkProvider>
- ConvexClientProvider ('use client'): ConvexReactClient(process.env.NEXT_PUBLIC_CONVEX_URL!) + ConvexProviderWithClerk client={convex} useAuth={useAuth}  // useAuth FROM @clerk/nextjs
- sign-in page: apps/dispatch-control/app/sign-in/[[...sign-in]]/page.tsx exporting <SignIn />
- env: NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in, NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Clerk + middleware + provider hierarchy + sign-in route</name>
  <read_first>
    - apps/dispatch-control/app/layout.tsx (the file being modified — currently the plain Plan 01 skeleton)
    - apps/dispatch-control/package.json (add @clerk/nextjs)
    - .planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md (Patterns 1, 2, 3, 8 + Pitfalls 1, 3, 7 + State of the Art — middleware.ts NOT proxy.ts, useAuth from @clerk/nextjs NOT @clerk/clerk-react)
    - convex/auth.config.ts (confirm applicationID "convex" exists — created in Plan 02)
  </read_first>
  <action>
    1. `pnpm --filter dispatch-control add @clerk/nextjs@^7.5.7`. (convex is already a dep from Plan 01.)

    2. `apps/dispatch-control/middleware.ts` (NEW): exactly 21-RESEARCH Pattern 1. Import `clerkMiddleware, createRouteMatcher` from `@clerk/nextjs/server`. `const isPublicRoute = createRouteMatcher(['/sign-in(.*)'])`. Default export `clerkMiddleware(async (auth, req) => { if (!isPublicRoute(req)) { await auth.protect() } })`. Export the `config.matcher` array verbatim from the interfaces block. This is `middleware.ts` (Next 15), NOT `proxy.ts` (Next 16) — State of the Art note.

    3. `apps/dispatch-control/components/ConvexClientProvider.tsx` (NEW): `'use client'` directive at top. Import `ConvexReactClient` from `convex/react`, `ConvexProviderWithClerk` from `convex/react-clerk`, `useAuth` from `@clerk/nextjs` (NOT @clerk/clerk-react — Pitfall 3 / anti-pattern). Construct `const convex = new ConvexReactClient(process.env.NEXT_PUBLIC_CONVEX_URL!)` at module scope (one client per session). Default export wraps children in `<ConvexProviderWithClerk client={convex} useAuth={useAuth}>`.

    4. `apps/dispatch-control/app/layout.tsx` (MODIFY): keep it a Server Component (do NOT add 'use client' — Pitfall 3). Import `ClerkProvider` from `@clerk/nextjs` and `ConvexClientProvider` from `@/components/ConvexClientProvider`. Wrap: `<html lang="en"><body><ClerkProvider><ConvexClientProvider>{children}</ConvexClientProvider></ClerkProvider></body></html>`. Keep the globals.css import and metadata export.

    5. `apps/dispatch-control/app/sign-in/[[...sign-in]]/page.tsx` (NEW): per 21-RESEARCH Pattern 8 — import `SignIn` from `@clerk/nextjs`, default export a centered `<SignIn />` (flex min-h-screen items-center justify-center).

    6. `apps/dispatch-control/.env.example` (MODIFY): uncomment/confirm NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, CLERK_SECRET_KEY, NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in, NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/, NEXT_PUBLIC_CONVEX_URL — with the Pitfall 6 warning that CLERK_SECRET_KEY belongs ONLY on the dispatch-control Vercel project.

    Verify `apps/web` is untouched — do NOT add any Clerk import, middleware, or env there.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dispatch-control/package.json` now contains `@clerk/nextjs`
    - `apps/dispatch-control/middleware.ts` contains `clerkMiddleware`, `createRouteMatcher`, `auth.protect`, and `'/sign-in(.*)'`
    - File `apps/dispatch-control/proxy.ts` does NOT exist (Next 15 uses middleware.ts)
    - `apps/dispatch-control/components/ConvexClientProvider.tsx` starts with `'use client'` and contains `ConvexProviderWithClerk` and imports `useAuth` from `@clerk/nextjs` (grep confirms `from '@clerk/nextjs'`, NOT `@clerk/clerk-react`)
    - `apps/dispatch-control/app/layout.tsx` contains `<ClerkProvider>` and does NOT contain `'use client'`
    - File `apps/dispatch-control/app/sign-in/[[...sign-in]]/page.tsx` exists and contains `SignIn`
    - `apps/web/package.json` has ZERO occurrences of `@clerk` (Pitfall 2)
    - `pnpm --filter dispatch-control typecheck` exits 0
  </acceptance_criteria>
  <done>Clerk middleware protects all non-public routes; provider hierarchy is correct (Server ClerkProvider → client ConvexProviderWithClerk); sign-in route renders; apps/web untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fill AUTH-01 middleware matcher tests + AUTH-02 no-leak guard</name>
  <read_first>
    - apps/dispatch-control/middleware.ts (created in Task 1 — the matcher + isPublicRoute to assert against)
    - apps/dispatch-control/__tests__/middleware-matcher.test.ts (the Plan 01 todo seam to fill)
    - apps/dispatch-control/__tests__/apps-web-no-clerk.test.ts (the Plan 01 live no-leak grep — confirm still green)
    - .planning/phases/21-auth-app-shell-convex-schema/21-VALIDATION.md (Success-Criterion → Validation Seam rows 1 + 2)
  </read_first>
  <action>
    Fill `apps/dispatch-control/__tests__/middleware-matcher.test.ts` (replace the Plan 01 `it.todo`s) with unit assertions on the route-matching logic — avoid spinning a Next server. Import the public-route matcher behavior by re-implementing the assertion against the same `createRouteMatcher(['/sign-in(.*)'])` pattern, OR import `isPublicRoute` if you export it from middleware.ts (prefer exporting `isPublicRoute` as a named export from middleware.ts so the test imports the real matcher — add that named export in this task without changing default behavior).

    Tests:
    - `isPublicRoute` returns true for a Request to `/sign-in` and `/sign-in/factor-one`.
    - `isPublicRoute` returns false for `/graph`, `/runs`, `/config`, `/` (root).
    Build minimal `new Request('http://localhost:3001/graph')` objects to feed the matcher (createRouteMatcher accepts a request-like with `nextUrl`/`url`; if it requires `NextRequest`, construct via `new NextRequest(new URL(...))` from `next/server`).

    Extend `apps/dispatch-control/__tests__/apps-web-no-clerk.test.ts` to ALSO assert no `@clerk` import appears in any apps/web source: glob `apps/web/app/**/*.{ts,tsx}` and `apps/web/components/**/*.{ts,tsx}` via fs and assert none contains the substring `@clerk` (Pitfall 2 — covers both package.json AND source). Keep the existing package.json assertion.

    This makes AUTH-01 (matcher protects dashboard routes, /sign-in public) and AUTH-02 (no Clerk leak into apps/web) automated.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dispatch-control/__tests__/middleware-matcher.test.ts` has NO `it.todo` / `test.todo` remaining
    - The matcher test asserts `/sign-in` is public and `/graph` + `/runs` + `/` are NOT public
    - `apps/dispatch-control/__tests__/apps-web-no-clerk.test.ts` asserts `@clerk` absent from apps/web source files (not just package.json)
    - `middleware.ts` exports a named `isPublicRoute` (used by the test) while preserving the default clerkMiddleware export
    - `pnpm --filter dispatch-control test:unit` exits 0 with the matcher + no-leak tests passing
  </acceptance_criteria>
  <done>AUTH-01 route-protection and AUTH-02 no-leak are both automated and green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify sign-in flow + apps/web unaffected</name>
  <action>Human verifies the auth flow end-to-end (no code action — confirmation gate). See how-to-verify.</action>
  <what-built>
    Clerk auth on dispatch-control: middleware route protection, ClerkProvider → ConvexProviderWithClerk hierarchy, and the /sign-in page. Convex auth.config.ts (Plan 02) trusts Clerk JWTs.
  </what-built>
  <how-to-verify>
    Prereqs (manual, see user_setup): in Clerk Dashboard create a JWT template named exactly `convex`; set NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY + CLERK_SECRET_KEY + NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in + NEXT_PUBLIC_CONVEX_URL in apps/dispatch-control/.env.local; set CLERK_JWT_ISSUER_DOMAIN on the Convex deployment (`npx convex env set ...`) and run `pnpm --filter @eisenbalm/convex dev` once to push auth.config.
    1. Run `pnpm --filter dispatch-control dev` and `pnpm --filter @eisenbalm/convex dev` (separate terminals).
    2. Visit http://localhost:3001/graph in a fresh/incognito browser → expect redirect to the Clerk sign-in.
    3. Sign in → expect to land back in the app (root or /graph) with no Convex 401 in the console (confirms the JWT template + auth.config wiring).
    4. Confirm http://localhost:3000/ (apps/web public site) still loads with NO sign-in prompt.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (redirect, Convex 401, or apps/web broke).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter dispatch-control typecheck` exits 0
- `pnpm --filter dispatch-control test:unit` exits 0 (matcher + no-leak green)
- `grep -rc "@clerk" apps/web/` returns 0 across package.json + source
- Manual checkpoint: unauthenticated /graph redirects; sign-in works without Convex 401; apps/web unaffected
</verification>

<success_criteria>
- Every non-public dispatch-control route is protected; /sign-in is public (AUTH-01)
- Unauthenticated requests redirect to Clerk; apps/web has zero Clerk dependency (AUTH-02)
- Provider hierarchy correct; Convex receives Clerk identity
</success_criteria>

<output>
After completion, create `.planning/phases/21-auth-app-shell-convex-schema/21-03-SUMMARY.md`
</output>
