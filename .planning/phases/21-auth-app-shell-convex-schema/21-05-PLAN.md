---
phase: 21-auth-app-shell-convex-schema
plan: 05
type: execute
wave: 3
depends_on: ["01", "03"]
files_modified:
  - apps/dispatch-control/components/AppSidebar.tsx
  - apps/dispatch-control/app/(dashboard)/layout.tsx
  - apps/dispatch-control/app/(dashboard)/page.tsx
  - apps/dispatch-control/app/(dashboard)/graph/page.tsx
  - apps/dispatch-control/app/(dashboard)/runs/page.tsx
  - apps/dispatch-control/app/(dashboard)/config/page.tsx
  - apps/dispatch-control/app/(dashboard)/prompts/page.tsx
  - apps/dispatch-control/app/(dashboard)/registry/page.tsx
  - apps/dispatch-control/app/(dashboard)/finance/page.tsx
  - apps/dispatch-control/app/(dashboard)/settings/page.tsx
  - apps/dispatch-control/lib/nav.ts
  - apps/dispatch-control/__tests__/nav.test.ts
autonomous: false
requirements: [AUTH-01]
must_haves:
  truths:
    - "The shell renders a persistent left sidebar with 7 nav items: Graph, Runs, Config, Prompts, Registry, Finance, Settings"
    - "Each nav item routes to a real placeholder page that renders (not a dead link)"
    - "Graph is the default home view (dashboard index redirects to /graph)"
    - "The Clerk user button / sign-out affordance is present in the chrome"
    - "The active route is highlighted in the nav"
  artifacts:
    - path: "apps/dispatch-control/lib/nav.ts"
      provides: "Single source of truth for the 7 nav items (label, href, icon)"
      contains: "graph"
    - path: "apps/dispatch-control/components/AppSidebar.tsx"
      provides: "Sidebar chrome with nav + Clerk UserButton + active highlighting"
      contains: "UserButton"
    - path: "apps/dispatch-control/app/(dashboard)/layout.tsx"
      provides: "Sidebar shell layout wrapping all dashboard routes"
      contains: "AppSidebar"
  key_links:
    - from: "apps/dispatch-control/app/(dashboard)/page.tsx"
      to: "/graph"
      via: "redirect"
      pattern: "redirect"
    - from: "apps/dispatch-control/components/AppSidebar.tsx"
      to: "lib/nav.ts"
      via: "maps NAV_ITEMS to links"
      pattern: "NAV_ITEMS"
---

<objective>
Build the dispatch-control app shell: a persistent left sidebar (shadcn-style, neutral base) with the 7 nav items (Graph, Runs, Config, Prompts, Registry, Finance, Settings) each routing to a real placeholder page, Graph as the default home, the Clerk `UserButton` in the chrome, and active-route highlighting. Auth (Plan 03) is already in place, so every route here is automatically protected.

Purpose: Completes AUTH-01's "after sign-in, the app shell renders with top-level navigation". The 7 nav routes are phase-labeled placeholders (D-07) that later phases (22–27) flesh out.
Output: A navigable, authenticated shell — the canvas every subsequent Mission Control phase fills.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-auth-app-shell-convex-schema/21-CONTEXT.md
@.planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md
@apps/dispatch-control/app/layout.tsx
@apps/dispatch-control/lib/utils.ts

<interfaces>
<!-- Shell structure from 21-RESEARCH Recommended Project Structure + D-07/D-08/D-09/D-10. -->
- Route group app/(dashboard)/ with layout.tsx (sidebar) wrapping 7 child route pages
- app/(dashboard)/page.tsx -> redirect('/graph')  (Graph is home, D-09)
- AppSidebar: maps NAV_ITEMS; usePathname() for active highlighting; Clerk <UserButton /> in chrome (D-10)
- Clerk UserButton import: import { UserButton } from '@clerk/nextjs'
- cn() helper available at @/lib/utils for active-class merging
NAV_ITEMS order (D-07): Graph, Runs, Config, Prompts, Registry, Finance, Settings (Graph first/default)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav source-of-truth + sidebar chrome + dashboard layout</name>
  <read_first>
    - apps/dispatch-control/app/layout.tsx (root layout from Plan 03 — confirm ClerkProvider wraps everything so UserButton works)
    - apps/dispatch-control/lib/utils.ts (cn helper for active classes)
    - .planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md (Recommended Project Structure, Don't Hand-Roll → sidebar)
    - apps/dispatch-control/components.json (shadcn aliases — if using `npx shadcn add sidebar`, else hand-roll a flex sidebar)
  </read_first>
  <action>
    1. `apps/dispatch-control/lib/nav.ts` (NEW): export `NAV_ITEMS` — an array of `{ label, href, icon }` in this exact order (D-07): Graph → /graph, Runs → /runs, Config → /config, Prompts → /prompts, Registry → /registry, Finance → /finance, Settings → /settings. Pick sensible lucide-react icons (Claude's discretion, e.g. Workflow/Network for Graph, Play/Activity for Runs, Settings2/Sliders for Config, FileText for Prompts, BookMarked/Library for Registry, DollarSign for Finance, Settings for Settings). Type the array so AppSidebar and the nav test consume it.

    2. `apps/dispatch-control/components/AppSidebar.tsx` (NEW): `'use client'`. Import `NAV_ITEMS` from `@/lib/nav`, `usePathname` from `next/navigation`, `Link` from `next/link`, `UserButton` from `@clerk/nextjs`, `cn` from `@/lib/utils`. Render a persistent left sidebar (D-08): a header/brand line ("Dispatch Control"), the 7 nav links mapped from NAV_ITEMS with the icon + label, active-route highlighting via `cn(base, pathname === item.href && activeClasses)` (also treat the root/graph default as active when pathname is `/graph`), and the Clerk `<UserButton />` pinned in the chrome footer (D-10). Use neutral Tailwind classes consistent with the shadcn neutral base. Either install the shadcn `Sidebar` primitive (`pnpm --filter dispatch-control dlx shadcn@latest add sidebar` or hand-place the component) OR hand-roll a simple accessible `<nav>` flex sidebar — Claude's discretion (research Don't Hand-Roll allows either). Ensure ≥44px tap targets and a focus-visible ring on links.

    3. `apps/dispatch-control/app/(dashboard)/layout.tsx` (NEW): Server Component shell — a flex row: `<AppSidebar />` + a `<main className="flex-1 ...">{children}</main>`. Single `<main>`. This wraps all dashboard routes.

    4. `apps/dispatch-control/app/(dashboard)/page.tsx` (NEW): the dashboard index — `import { redirect } from 'next/navigation'; export default function Page() { redirect('/graph') }` (D-09 — Graph is home).
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/dispatch-control/lib/nav.ts` exports `NAV_ITEMS` containing all 7 hrefs: `/graph`, `/runs`, `/config`, `/prompts`, `/registry`, `/finance`, `/settings`
    - `apps/dispatch-control/components/AppSidebar.tsx` imports `UserButton` from `@clerk/nextjs` and `usePathname` from `next/navigation` and references `NAV_ITEMS`
    - `AppSidebar.tsx` applies an active-state class conditioned on the current pathname (grep `pathname`)
    - `apps/dispatch-control/app/(dashboard)/layout.tsx` renders `<AppSidebar` and a single `<main`
    - `apps/dispatch-control/app/(dashboard)/page.tsx` contains `redirect('/graph')`
    - `pnpm --filter dispatch-control typecheck` exits 0
  </acceptance_criteria>
  <done>Sidebar chrome renders the 7 nav items with active highlighting + Clerk user button; dashboard index redirects to Graph.</done>
</task>

<task type="auto">
  <name>Task 2: 7 placeholder route pages + nav-coverage test</name>
  <read_first>
    - apps/dispatch-control/lib/nav.ts (created in Task 1 — the 7 hrefs each need a matching page)
    - apps/dispatch-control/__tests__/nav.test.ts (new test asserting nav coverage)
    - .planning/phases/21-auth-app-shell-convex-schema/21-CONTEXT.md (D-07 placeholder pages, NOT dead links; Deferred Ideas — note which phase each route belongs to for the placeholder copy)
  </read_first>
  <action>
    1. Create a real page for each of the 7 routes under `app/(dashboard)/`: `graph/page.tsx`, `runs/page.tsx`, `config/page.tsx`, `prompts/page.tsx`, `registry/page.tsx`, `finance/page.tsx`, `settings/page.tsx`. Each is a Server Component rendering a phase-labeled placeholder (D-07) — a heading (the section name) + a short "coming soon" line naming the owning phase from the CONTEXT Deferred Ideas (Config → Phase 22, Prompts → Phase 24, Registry/Review → Phase 26, Finance → Phase 27, Runs/Graph → Phase 23/25). Graph's placeholder should hint at "the live pipeline graph". Keep styling minimal and consistent; no dead links.

    2. `apps/dispatch-control/__tests__/nav.test.ts` (NEW): import `NAV_ITEMS` from `@/lib/nav`; assert it has exactly 7 items in the expected order (Graph first); then assert that a corresponding page file exists on disk for every nav href — for each `item.href`, resolve `apps/dispatch-control/app/(dashboard)${href}/page.tsx` via fs and assert it exists. This proves "every nav item routes to a real page" (no dead links) automatically.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control typecheck && pnpm --filter dispatch-control test:unit</automated>
  </verify>
  <acceptance_criteria>
    - All 7 files exist: `app/(dashboard)/{graph,runs,config,prompts,registry,finance,settings}/page.tsx`
    - Each page renders its section name (grep each file for its label)
    - `apps/dispatch-control/__tests__/nav.test.ts` asserts NAV_ITEMS has 7 items with Graph first AND that each href has a real page file on disk
    - `pnpm --filter dispatch-control test:unit` exits 0 with the nav test passing
    - `pnpm --filter dispatch-control typecheck` exits 0
  </acceptance_criteria>
  <done>All 7 nav routes render real placeholder pages; nav-coverage is automated (no dead links).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify app shell + 7 nav routes</name>
  <action>Human verifies the shell renders and all 7 routes navigate (no code action — confirmation gate). See how-to-verify.</action>
  <what-built>
    The full authenticated app shell: persistent left sidebar with Graph/Runs/Config/Prompts/Registry/Finance/Settings, active-route highlighting, Clerk user button, Graph as the default home, and 7 placeholder pages.
  </what-built>
  <how-to-verify>
    1. Run `pnpm --filter dispatch-control dev` (and `pnpm --filter @eisenbalm/convex dev`), sign in.
    2. Confirm you land on /graph (redirect from the dashboard index).
    3. Confirm the left sidebar shows all 7 items in order: Graph, Runs, Config, Prompts, Registry, Finance, Settings.
    4. Click each nav item → confirm each navigates to a rendering placeholder page (no 404, no blank).
    5. Confirm the active item is visually highlighted as you navigate.
    6. Confirm the Clerk user button appears in the chrome and opens the account/sign-out menu.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what's off (missing nav item, dead route, no highlight, missing user button).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter dispatch-control typecheck` exits 0
- `pnpm --filter dispatch-control test:unit` exits 0 (nav coverage green)
- 7 page files present under app/(dashboard)/
- Manual checkpoint: sign-in → /graph → all 7 routes render → active highlight → user button
</verification>

<success_criteria>
- Shell renders 7-item sidebar nav after sign-in, Graph default (AUTH-01)
- Every nav item routes to a real placeholder page (D-07)
- Active-route highlighting + Clerk user button present (D-08, D-10)
</success_criteria>

<output>
After completion, create `.planning/phases/21-auth-app-shell-convex-schema/21-05-SUMMARY.md`
</output>
