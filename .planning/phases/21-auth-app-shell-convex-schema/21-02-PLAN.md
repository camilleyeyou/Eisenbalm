---
phase: 21-auth-app-shell-convex-schema
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/schema.ts
  - convex/auth.config.ts
  - convex/workspace.ts
  - convex/users.ts
autonomous: true
requirements: [CFG-05, AUTH-04]
user_setup:
  - service: clerk
    why: "Convex must trust Clerk-issued JWTs; deployment needs the issuer domain"
    env_vars:
      - name: CLERK_JWT_ISSUER_DOMAIN
        source: "Clerk Dashboard → API Keys → Frontend API URL (e.g. https://verb-noun-00.clerk.accounts.dev); set on the Convex deployment via `npx convex env set CLERK_JWT_ISSUER_DOMAIN <url>`"
    dashboard_config:
      - task: "Create a JWT template named exactly 'convex'"
        location: "Clerk Dashboard → JWT Templates → New template → Convex preset"
must_haves:
  truths:
    - "All 11 new tables exist in convex/schema.ts each with workspace_id and a by_workspace index"
    - "workspaces, users, runs, audit_log carry full field sets including attribution (actorId / triggeredBy)"
    - "seedEisenbalm mutation is idempotent — second call returns seeded:false"
    - "upsertCurrentUser writes the Clerk sub claim as clerkUserId keyed by Clerk identity"
    - "Convex schema typechecks and codegens"
  artifacts:
    - path: "convex/schema.ts"
      provides: "11 new Convex tables (4 full, 7 stub) appended inline with ASCII headers"
      contains: "workspaces: defineTable"
    - path: "convex/auth.config.ts"
      provides: "Convex deployment-level trust of Clerk JWTs"
      contains: "applicationID"
    - path: "convex/workspace.ts"
      provides: "Idempotent eisenbalm seed mutation"
      exports: ["seedEisenbalm"]
    - path: "convex/users.ts"
      provides: "JIT user upsert keyed by Clerk sub (AUTH-04)"
      exports: ["upsertCurrentUser"]
  key_links:
    - from: "convex/users.ts"
      to: "ctx.auth.getUserIdentity().subject"
      via: "Clerk identity in mutation"
      pattern: "getUserIdentity"
    - from: "convex/auth.config.ts"
      to: "CLERK_JWT_ISSUER_DOMAIN"
      via: "env-driven provider domain"
      pattern: "CLERK_JWT_ISSUER_DOMAIN"
---

<objective>
Append all 11 Mission Control tables to `convex/schema.ts` (4 full-shape, 7 stub) — each carrying `workspace_id: v.string()` + a `by_workspace` index — add the Convex `auth.config.ts` so the deployment trusts Clerk JWTs, and add the idempotent `seedEisenbalm` workspace seed + the JIT `upsertCurrentUser` mutation that attributes the signed-in operator from day one.

Purpose: Satisfies CFG-05 (workspace_id on every new table, seeded eisenbalm workspace, no hardcoded brand logic) and AUTH-04 (operator attribution carried by audit_log/runs schema + written by the JIT user upsert). The existing 9 tables and all API_CONTRACTS §4 contracts are FROZEN — append only, do not touch pipelineRuns or deliberationEvents.
Output: Extended schema + 3 new convex files; seed runs idempotently; user upsert keys on Clerk sub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-auth-app-shell-convex-schema/21-CONTEXT.md
@.planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md
@convex/schema.ts
@convex/pipelineRuns.ts
@docs/API_CONTRACTS.md
@convex/CLAUDE.md

<interfaces>
<!-- Existing schema conventions to mirror EXACTLY (from convex/schema.ts). -->
- defineTable({ field: v.string()/v.number()/v.optional(...)/v.union(v.literal(...)) })
- .index('by_x', ['x']) chained after the table body
- ASCII section header comment style: // ── name (description) ──────
- camelCase identifiers; kebab-case status string literals; timestamps as v.number() Unix ms
- pipelineRuns.runId is the join key the new `runs` table shares (do NOT modify pipelineRuns)

<!-- Full field sets for the 4 full-shape tables — use VERBATIM (21-RESEARCH Pattern 5). -->
workspaces: { workspace_id, name, createdAt:number } idx by_workspace
users: { workspace_id, clerkUserId, email, displayName?, role?, createdAt:number, lastSeenAt:number } idx by_workspace + by_clerkUserId
runs: { workspace_id, runId, triggerSource, triggeredBy?, configSnapshot?, status, startedAt:number, completedAt?:number, cost?, durationMs?:number } idx by_workspace + by_runId
audit_log: { workspace_id, actorId, action, resourceType?, resourceId?, before?, after?, timestamp:number } idx by_workspace + by_workspace_timestamp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 11 Mission Control tables to convex/schema.ts</name>
  <read_first>
    - convex/schema.ts (the file being modified — study the existing 9-table conventions, ASCII headers, .index() patterns)
    - .planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md (Pattern 5 — the full 11-table block, use verbatim)
    - docs/API_CONTRACTS.md §4 (confirm pipelineRuns is FROZEN; the new `runs` table is additive on the same runId)
    - convex/CLAUDE.md (read convex/_generated/ai/guidelines.md first if present; run `npx convex ai-files install` if uncertain)
  </read_first>
  <action>
    Append a single new block to `convex/schema.ts` BELOW the existing `emailSends` table (inside the `defineSchema({ ... })` object, after the last existing table's closing `,`). Do NOT modify any of the 9 existing tables. Use the exact 11-table definitions from 21-RESEARCH.md Pattern 5, preserving every field, optionality, and index. Lead with the ASCII header `// ── Mission Control v2.0 — Phase 21 ─────────────────────────────────────────`.

    FULL-shape tables (all fields):
    - `workspaces`: workspace_id v.string(), name v.string(), createdAt v.number(). Index: by_workspace.
    - `users`: workspace_id, clerkUserId v.string(), email v.string(), displayName v.optional(v.string()), role v.optional(v.string()), createdAt v.number(), lastSeenAt v.number(). Indexes: by_workspace, by_clerkUserId ['clerkUserId'].
    - `runs`: workspace_id, runId v.string(), triggerSource v.string(), triggeredBy v.optional(v.string()), configSnapshot v.optional(v.string()), status v.string(), startedAt v.number(), completedAt v.optional(v.number()), cost v.optional(v.string()), durationMs v.optional(v.number()). Indexes: by_workspace, by_runId ['runId'].
    - `audit_log`: workspace_id, actorId v.string(), action v.string(), resourceType v.optional(v.string()), resourceId v.optional(v.string()), before v.optional(v.string()), after v.optional(v.string()), timestamp v.number(). Indexes: by_workspace, by_workspace_timestamp ['workspace_id','timestamp'].

    STUB tables (workspace_id + minimal keys; owning phase fleshes out):
    - `agents`: workspace_id, agentKey v.string(), enabled v.boolean(), model v.optional(v.string()), temperature v.optional(v.number()). Indexes: by_workspace, by_workspace_agentKey ['workspace_id','agentKey'].
    - `prompt_versions`: workspace_id, agentKey v.string(), version v.number(), content v.string(), isActive v.boolean(), createdAt v.number(), createdBy v.optional(v.string()), note v.optional(v.string()). Indexes: by_workspace, by_workspace_agentKey.
    - `pipeline_config`: workspace_id, key v.string(), value v.string(), updatedAt v.number(), updatedBy v.optional(v.string()). Indexes: by_workspace, by_workspace_key ['workspace_id','key'].
    - `agent_runs`: workspace_id, runId v.string(), agentKey v.string(), status v.string(), startedAt v.optional(v.number()), completedAt v.optional(v.number()), costUsd v.optional(v.number()), durationMs v.optional(v.number()). Indexes: by_workspace, by_runId.
    - `charities`: workspace_id, name v.string(), status v.string(), timesFeatured v.optional(v.number()), lastFeaturedAt v.optional(v.number()). Indexes: by_workspace. (NOTE: name the count field `timesFeatured` — the research draft has a typo `timesFeatureD`; use correct camelCase `timesFeatured`.)
    - `model_pricing`: workspace_id, model v.string(), inputPricePer1M v.number(), outputPricePer1M v.number(), updatedAt v.number(). Indexes: by_workspace, by_workspace_model ['workspace_id','model'].
    - `review_actions`: workspace_id, runId v.string(), actorId v.string(), action v.string(), note v.optional(v.string()), timestamp v.number(). Indexes: by_workspace, by_runId.

    Every one of the 11 tables MUST have `workspace_id: v.string()` (NOT v.id — D-14) and `.index('by_workspace', ['workspace_id'])` (D-05). Use the compound `by_workspace_<key>` index where the table has a natural secondary key, as listed above.

    After editing, run `pnpm --filter @eisenbalm/convex codegen` (or `convex dev --once` against the dev deployment per Phase 3 D-04 convention) so `_generated/` reflects the new tables.
  </action>
  <verify>
    <automated>pnpm --filter @eisenbalm/convex typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `convex/schema.ts` contains all 11 strings: `workspaces: defineTable`, `users: defineTable`, `runs: defineTable`, `audit_log: defineTable`, `agents: defineTable`, `prompt_versions: defineTable`, `pipeline_config: defineTable`, `agent_runs: defineTable`, `charities: defineTable`, `model_pricing: defineTable`, `review_actions: defineTable`
    - Every new table block contains `workspace_id: v.string()` (grep for `workspace_id: v.string()` returns ≥ 11 matches)
    - Every new table has `.index('by_workspace', ['workspace_id'])` (grep `by_workspace'` returns ≥ 11 matches)
    - `runs` table contains `runId: v.string()` and `triggeredBy: v.optional(v.string())`
    - `audit_log` table contains `actorId: v.string()`
    - The 9 existing tables are unchanged (git diff shows only additions below emailSends; `pipelineRuns:` and `deliberationEvents:` blocks untouched)
    - `convex/schema.ts` contains `timesFeatured` (NOT `timesFeatureD`)
    - `pnpm --filter @eisenbalm/convex typecheck` exits 0
  </acceptance_criteria>
  <done>Schema carries 11 workspace-scoped tables; full attribution fields on runs/audit_log; existing frozen tables untouched; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: auth.config.ts + idempotent seed + JIT user upsert (AUTH-04)</name>
  <read_first>
    - convex/schema.ts (post-Task-1 — confirm workspaces/users/audit_log shapes)
    - convex/pipelineRuns.ts (per-table mutation file pattern: import mutation from _generated/server, withIndex queries, deterministic upsert)
    - .planning/phases/21-auth-app-shell-convex-schema/21-RESEARCH.md (Pattern 3 auth.config.ts, Pattern 6 seed, Pattern 7 JIT upsert — use verbatim)
    - apps/dispatch-control/__tests__/workspace-upsert.test.ts (the todo seam from Plan 01 to fill, if Plan 01 already merged)
  </read_first>
  <behavior>
    - seedEisenbalm: first call inserts one workspaces row (workspace_id "eisenbalm") + one audit_log row (action "workspace.seed", actorId "system:seed"); returns { seeded: true }.
    - seedEisenbalm: second call finds the existing row via by_workspace index and returns { seeded: false } WITHOUT inserting a duplicate (idempotent).
    - upsertCurrentUser: throws "Not authenticated" when ctx.auth.getUserIdentity() is null.
    - upsertCurrentUser: on first authenticated call inserts a users row with clerkUserId = identity.subject, workspace_id = DEFAULT_WORKSPACE_ID.
    - upsertCurrentUser: on a repeat call for the same Clerk subject patches lastSeenAt and does NOT insert a second row (keyed by by_clerkUserId).
  </behavior>
  <action>
    1. `convex/auth.config.ts` (NEW): export default `{ providers: [{ domain: process.env.CLERK_JWT_ISSUER_DOMAIN!, applicationID: "convex" }] }`. The `applicationID` MUST be the literal string `"convex"` to match the Clerk JWT template name (Pitfall 1). Add a comment that CLERK_JWT_ISSUER_DOMAIN is set on the deployment via `npx convex env set` and that the "convex" JWT template is a manual Clerk Dashboard step.

    2. `convex/workspace.ts` (NEW): export `seedEisenbalm = mutation({ handler })` per 21-RESEARCH Pattern 6 — query `workspaces` by `by_workspace` for workspace_id 'eisenbalm'; if found return `{ seeded: false, message: 'eisenbalm workspace already exists' }`; else insert the workspaces row (name 'The Eisenbalm Dispatch', createdAt Date.now()) AND an audit_log row (workspace_id 'eisenbalm', actorId 'system:seed', action 'workspace.seed', resourceType 'workspace', resourceId 'eisenbalm', timestamp Date.now()) and return `{ seeded: true }`. The literal 'eisenbalm' is allowed here because this is SEED DATA, not control-plane logic (D-16); add a comment noting that.

    3. `convex/users.ts` (NEW): per 21-RESEARCH Pattern 7 — define a module-local `const DEFAULT_WORKSPACE_ID = 'eisenbalm'` (the only allowed control-plane occurrence — comment it as the workspace default constant). Export `upsertCurrentUser = mutation({ args: { email: v.string(), displayName: v.optional(v.string()) }, handler })`: call `const identity = await ctx.auth.getUserIdentity()`; if null `throw new Error('Not authenticated')`; `const clerkUserId = identity.subject`; query `users` by `by_clerkUserId`; if existing `ctx.db.patch(existing._id, { lastSeenAt: Date.now() })` and return existing._id; else insert a users row (workspace_id DEFAULT_WORKSPACE_ID, clerkUserId, email, displayName, createdAt + lastSeenAt = Date.now()).

    4. Fill the Plan 01 vitest seam `apps/dispatch-control/__tests__/workspace-upsert.test.ts` IF the convex-test harness is feasible: prefer `convex-test` (if installable as a devDependency in @eisenbalm/convex) to drive seedEisenbalm twice + assert idempotency, and upsertCurrentUser identity behavior with a mocked identity. If convex-test is NOT available/practical, instead author the assertions in a new `convex/__tests__/workspace.test.ts` run under the convex package, OR — as a documented fallback — convert the todos to a manual-verification note in the SUMMARY and keep the schema typecheck + the live `npx convex run workspace:seedEisenbalm` x2 idempotency check as the CFG-05 proof. Choose the lightest path that gives an automated assertion; document the choice in the SUMMARY.

    Run codegen so the new mutations appear in `_generated/api`.
  </action>
  <verify>
    <automated>pnpm --filter @eisenbalm/convex typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `convex/auth.config.ts` exists and contains `applicationID` set to `"convex"` and `CLERK_JWT_ISSUER_DOMAIN`
    - `convex/workspace.ts` exports `seedEisenbalm` and contains both a `workspaces` insert and an `audit_log` insert with `action: 'workspace.seed'`
    - `convex/workspace.ts` returns `seeded: false` on the existing-row branch (idempotent)
    - `convex/users.ts` exports `upsertCurrentUser`, calls `ctx.auth.getUserIdentity()`, throws on null, and inserts `clerkUserId: identity.subject` (or `clerkUserId,` after destructuring `const clerkUserId = identity.subject`)
    - `convex/users.ts` patches `lastSeenAt` on the repeat-call branch (no duplicate insert)
    - `pnpm --filter @eisenbalm/convex typecheck` exits 0
    - An automated idempotency + upsert assertion exists (convex-test OR convex package test OR the SUMMARY documents the chosen `npx convex run` x2 manual proof)
  </acceptance_criteria>
  <done>Convex trusts Clerk JWTs; seed is idempotent; user upsert attributes the Clerk subject as clerkUserId — AUTH-04 shape proven and written for seed/auth flows.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @eisenbalm/convex typecheck` exits 0
- `grep -c "defineTable" convex/schema.ts` increased by exactly 11 vs the pre-phase count
- `grep -c "by_workspace'" convex/schema.ts` ≥ 11
- `convex/auth.config.ts`, `convex/workspace.ts`, `convex/users.ts` all exist
- (Manual, documented in CONTEXT user_setup) Clerk "convex" JWT template created + CLERK_JWT_ISSUER_DOMAIN set on the deployment before live Convex auth works
</verification>

<success_criteria>
- All 11 tables carry workspace_id + by_workspace index (CFG-05)
- eisenbalm workspace seeds idempotently (CFG-05)
- users/audit_log/runs carry operator attribution; JIT upsert writes Clerk sub (AUTH-04)
- No hardcoded "eisenbalm" outside seed data + the DEFAULT_WORKSPACE_ID constant (D-16)
</success_criteria>

<output>
After completion, create `.planning/phases/21-auth-app-shell-convex-schema/21-02-SUMMARY.md`
</output>
