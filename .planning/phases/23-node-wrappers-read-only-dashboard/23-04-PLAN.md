---
phase: 23-node-wrappers-read-only-dashboard
plan: 04
type: execute
wave: 1
depends_on: ["23-01"]
files_modified:
  - convex/runs.ts
  - apps/dispatch-control/app/(dashboard)/runs/page.tsx
  - apps/dispatch-control/app/(dashboard)/runs/[runId]/page.tsx
  - apps/dispatch-control/app/(dashboard)/runs/_components/RunsTable.tsx
  - apps/dispatch-control/app/(dashboard)/runs/_components/RunDetail.tsx
  - apps/dispatch-control/app/(dashboard)/runs/_components/CostRollup.tsx
  - apps/dispatch-control/app/(dashboard)/settings/_components/AuditLogViewer.tsx
  - apps/dispatch-control/app/(dashboard)/settings/page.tsx
  - apps/dispatch-control/__tests__/runs.test.ts
  - apps/dispatch-control/__tests__/auditViewer.test.ts
autonomous: true
requirements: [OBS-02, OBS-04, AUD-01]

must_haves:
  truths:
    - "The Runs page lists all runs for the workspace (status, trigger source, who triggered, duration, cost) newest-first (OBS-02)"
    - "Each run row links to a run-detail page that shows per-agent status + cost + error for that run"
    - "A cost roll-up shows per-agent + per-run totals plus a week/month aggregate sum, reading runs.cost (OBS-04)"
    - "The run detail surfaces the per-agent sum next to runs.cost.total so reconciliation is visible (no second recorder)"
    - "A read-only audit-log viewer lists audit_log rows (timestamp, actor, action, before/after) via auditLog:listForWorkspace (AUD-01)"
  artifacts:
    - path: "convex/runs.ts"
      provides: "listForWorkspace query returning all runs newest-first"
      exports: ["listForWorkspace"]
    - path: "apps/dispatch-control/app/(dashboard)/runs/_components/RunsTable.tsx"
      provides: "Run history table (OBS-02)"
      contains: "triggerSource"
    - path: "apps/dispatch-control/app/(dashboard)/runs/_components/CostRollup.tsx"
      provides: "Per-agent + per-run + week/month cost aggregation UI (OBS-04)"
      contains: "sumRunsCost"
    - path: "apps/dispatch-control/app/(dashboard)/settings/_components/AuditLogViewer.tsx"
      provides: "Read-only audit-log table (AUD-01)"
      contains: "listForWorkspace"
  key_links:
    - from: "apps/dispatch-control/app/(dashboard)/runs/_components/RunsTable.tsx"
      to: "convex runs:listForWorkspace"
      via: "useQuery"
      pattern: "runs.listForWorkspace"
    - from: "apps/dispatch-control/app/(dashboard)/runs/_components/CostRollup.tsx"
      to: "lib/costRollup parseCostJson + sumRunsCost"
      via: "import"
      pattern: "sumRunsCost"
    - from: "apps/dispatch-control/app/(dashboard)/settings/_components/AuditLogViewer.tsx"
      to: "convex auditLog:listForWorkspace"
      via: "useQuery"
      pattern: "auditLog.listForWorkspace"
---

<objective>
Build the read-only Runs history + run-detail views, the cost roll-up (per agent + per run + per week/month,
reading already-captured cost), and the read-only audit-log viewer. These are the non-graph operator surfaces:
full run history (OBS-02), spend observability with reconciliation (OBS-04), and audit infrastructure
visibility (AUD-01).

Purpose: Make every past run inspectable, surface cost against the single recorder, and prove the audit
infrastructure end-to-end via a viewer.
Output: runs:listForWorkspace query, Runs table + run detail + cost roll-up, audit viewer, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-node-wrappers-read-only-dashboard/23-CONTEXT.md
@.planning/phases/23-node-wrappers-read-only-dashboard/23-RESEARCH.md

<interfaces>
<!-- Exact shapes the executor consumes. No exploration needed. -->

From convex/runs.ts (existing): byRunId, create, setConfigSnapshot. ADD listForWorkspace here.
  runs row shape: workspace_id, runId, triggerSource, triggeredBy?, configSnapshot?, status,
  startedAt, completedAt?, cost? (JSON string {total, agents}), durationMs?.

From convex/agentRuns.ts (Plan 23-01): query agentRuns.byRunId { runId } returns an array of per-agent rows
  (agentKey, status, costUsd?, durationMs?, tokensIn?, tokensOut?, error?).

From convex/auditLog.ts (Plan 23-01): query auditLog.listForWorkspace { workspace_id, limit? }
  returns an array of { timestamp, actorId, action, resourceType?, resourceId?, before?, after? } newest-first.

From apps/dispatch-control/lib/costRollup.ts (Plan 23-01):
  parseCostJson(raw) returns { total, agents } ; sumRunsCost(runs) returns a number.

From apps/dispatch-control/lib/workspace.ts: DEFAULT_WORKSPACE_ID is 'eisenbalm'; getCurrentWorkspace().

From apps/dispatch-control/app/(dashboard)/graph/_components/AgentIOPanel.tsx (Plan 23-03): reusable I/O panel
  (RunDetail MAY import it for node-level I/O on the detail screen — the D-04 secondary access path).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: runs:listForWorkspace query + runs convex-test spec</name>
  <files>convex/runs.ts, apps/dispatch-control/__tests__/runs.test.ts</files>
  <read_first>
    - convex/runs.ts (existing exports; by_workspace index; do not alter create/byRunId/setConfigSnapshot)
    - apps/dispatch-control/__tests__/agentRuns.test.ts (Plan 23-01 convex-test pattern: convexTest(schema), api import)
    - apps/dispatch-control/__tests__/setup.ts (Plan 23-01 schema re-export)
    - .planning/phases/23-node-wrappers-read-only-dashboard/23-RESEARCH.md (OBS-02 row shape)
  </read_first>
  <action>
    1. Append to convex/runs.ts (do NOT modify existing exports): a query named listForWorkspace taking
       args workspace_id (v.string()). Handler: query the 'runs' table withIndex('by_workspace', q => q.eq('workspace_id', workspace_id)),
       .collect(), then return sorted by startedAt descending (b.startedAt - a.startedAt).
       Note: Plan 23-03 may also append a runs:latest query to this file. latest and listForWorkspace are
       distinct exports — append yours; if latest already exists from 23-03, leave it intact.
    2. Create apps/dispatch-control/__tests__/runs.test.ts using convexTest(schema) (edge-runtime glob from
       Plan 23-01). Insert two runs via t.mutation(api.runs.create, ...) with different startedAt for workspace
       'eisenbalm', then assert api.runs.listForWorkspace returns both, newest-first.
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Eisenbalm && grep -q "export const listForWorkspace" convex/runs.ts && pnpm --filter dispatch-control test:unit -- runs</automated>
  </verify>
  <acceptance_criteria>
    - convex/runs.ts exports listForWorkspace AND still exports create, byRunId, setConfigSnapshot
    - runs.test.ts uses convexTest(schema) and asserts newest-first ordering
    - `pnpm --filter dispatch-control test:unit -- runs` exits 0
  </acceptance_criteria>
  <done>runs:listForWorkspace returns all workspace runs newest-first; convex-test spec passes.</done>
</task>

<task type="auto">
  <name>Task 2: Runs history table + run detail page + cost roll-up</name>
  <files>apps/dispatch-control/app/(dashboard)/runs/page.tsx, apps/dispatch-control/app/(dashboard)/runs/[runId]/page.tsx, apps/dispatch-control/app/(dashboard)/runs/_components/RunsTable.tsx, apps/dispatch-control/app/(dashboard)/runs/_components/RunDetail.tsx, apps/dispatch-control/app/(dashboard)/runs/_components/CostRollup.tsx</files>
  <read_first>
    - apps/dispatch-control/app/(dashboard)/runs/page.tsx (placeholder being replaced)
    - apps/dispatch-control/lib/costRollup.ts (Plan 23-01 parseCostJson + sumRunsCost — import, do not reimplement)
    - apps/dispatch-control/lib/workspace.ts (getCurrentWorkspace)
    - apps/dispatch-control/components/ConvexClientProvider.tsx (provider already in the dashboard tree)
    - .planning/phases/23-node-wrappers-read-only-dashboard/23-RESEARCH.md (Pattern 5 cost roll-up + Open Question 3: per-run on detail, week/month at top of Runs list)
  </read_first>
  <action>
    1. Create apps/dispatch-control/app/(dashboard)/runs/_components/RunsTable.tsx ('use client'):
       call useQuery(api.runs.listForWorkspace, { workspace_id }). Render a table with columns: status,
       triggerSource, triggeredBy (who triggered), startedAt (formatted), durationMs, and cost total via
       parseCostJson(run.cost).total. Each row wraps a Next Link to /runs/{run.runId}. Read-only. Handle the
       empty (no runs) state with a friendly message.
    2. Create apps/dispatch-control/app/(dashboard)/runs/_components/CostRollup.tsx ('use client'):
       a collapsible section rendered at the TOP of the Runs list showing week + month aggregate spend. Import
       sumRunsCost + parseCostJson from lib/costRollup. Read the runs list (useQuery(api.runs.listForWorkspace)),
       filter by startedAt within the last 7 / 30 days, and sumRunsCost(filtered) per window. Render a small table
       (not a chart).
    3. Replace apps/dispatch-control/app/(dashboard)/runs/page.tsx: a Server Component resolving
       workspace_id = await getCurrentWorkspace(), rendering CostRollup then RunsTable (both passed workspace_id).
    4. Create apps/dispatch-control/app/(dashboard)/runs/_components/RunDetail.tsx ('use client') taking { runId }:
       useQuery(api.runs.byRunId, { runId }) for the run header (status/trigger/cost/duration), and
       useQuery(api.agentRuns.byRunId, { runId }) for the per-agent list. Render a per-agent table: agentKey,
       status, costUsd, durationMs, tokensIn, tokensOut, error. Below it, compute the per-agent sum from
       parseCostJson(run.cost).agents and display BOTH that summed value AND parseCostJson(run.cost).total side
       by side — the visible OBS-04 reconciliation surface. Read-only.
    5. Create apps/dispatch-control/app/(dashboard)/runs/[runId]/page.tsx: a Server Component reading the runId
       route param and rendering RunDetail with runId.
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Eisenbalm && pnpm --filter dispatch-control build</automated>
  </verify>
  <acceptance_criteria>
    - RunsTable.tsx contains `runs.listForWorkspace`, `triggerSource`, `triggeredBy`, and a Link to `/runs/`
    - CostRollup.tsx imports `sumRunsCost` and references a 7-day and 30-day window
    - RunDetail.tsx contains `agentRuns.byRunId` and renders both a per-agent sum and `parseCostJson(run.cost).total`
    - runs/[runId]/page.tsx renders `RunDetail` and reads the `runId` param
    - `pnpm --filter dispatch-control build` exits 0
  </acceptance_criteria>
  <done>Runs history lists every run linking to a detail page; cost roll-up shows week/month aggregates; run detail surfaces per-agent cost reconciled against runs.cost.total.</done>
</task>

<task type="auto">
  <name>Task 3: Read-only audit-log viewer + auditViewer convex-test spec</name>
  <files>apps/dispatch-control/app/(dashboard)/settings/_components/AuditLogViewer.tsx, apps/dispatch-control/app/(dashboard)/settings/page.tsx, apps/dispatch-control/__tests__/auditViewer.test.ts</files>
  <read_first>
    - apps/dispatch-control/app/(dashboard)/settings/page.tsx (where the viewer mounts)
    - convex/auditLog.ts (Plan 23-01 write + listForWorkspace signatures)
    - apps/dispatch-control/__tests__/auditLog.test.ts (Plan 23-01 convex-test pattern for auditLog)
    - .planning/phases/23-node-wrappers-read-only-dashboard/23-RESEARCH.md (Pattern 6 audit infra — viewer only this phase; emissions land in Phases 24-26)
  </read_first>
  <action>
    1. Create apps/dispatch-control/app/(dashboard)/settings/_components/AuditLogViewer.tsx ('use client'):
       call useQuery(api.auditLog.listForWorkspace, { workspace_id, limit: 50 }). Render a read-only table:
       timestamp (formatted), actorId, action, resourceType, resourceId, and before/after rendered as collapsed
       JSON (a <details><summary> with a <pre> body). Handle the empty state ("No audit events yet — actions are
       recorded starting in Phase 24"). NO write controls — this phase builds infrastructure + viewer only
       (AUD-01); do NOT synthesize fake audit rows.
    2. Edit apps/dispatch-control/app/(dashboard)/settings/page.tsx to render the AuditLogViewer below the
       existing settings content, resolving workspace_id via getCurrentWorkspace() (keep the page a Server
       Component passing workspace_id as a prop; hooks stay in the client viewer). Do not remove existing settings content.
    3. Create apps/dispatch-control/__tests__/auditViewer.test.ts using convexTest(schema) (edge-runtime glob):
       call t.mutation(internal.auditLog.write, { workspace_id:'eisenbalm', actorId:'user_x', action:'dashboard.viewed',
       before: undefined, after: undefined }), then assert api.auditLog.listForWorkspace returns a row with
       action 'dashboard.viewed' and a numeric timestamp. (This validates the AUD-01 row shape: actor + timestamp + before/after.)
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Eisenbalm && grep -q "auditLog.listForWorkspace" apps/dispatch-control/app/\(dashboard\)/settings/_components/AuditLogViewer.tsx && pnpm --filter dispatch-control test:unit -- auditViewer</automated>
  </verify>
  <acceptance_criteria>
    - AuditLogViewer.tsx starts with `'use client'`, calls `api.auditLog.listForWorkspace`, and renders timestamp/actorId/action/before/after
    - settings/page.tsx renders `AuditLogViewer`
    - auditViewer.test.ts asserts the written row's action and a numeric timestamp
    - `pnpm --filter dispatch-control test:unit -- auditViewer` exits 0
    - No mutation/write button rendered in AuditLogViewer (read-only)
  </acceptance_criteria>
  <done>The read-only audit viewer lists audit_log rows via the AUD-01 infrastructure; the row shape (actor/timestamp/before/after) is test-verified.</done>
</task>

</tasks>

<verification>
- `pnpm --filter dispatch-control test:unit -- runs` and `-- auditViewer` green.
- `pnpm --filter dispatch-control build` exits 0.
- Manual: open Runs, click a run, confirm per-agent status + cost + reconciliation; open Settings and confirm the audit viewer renders (empty state acceptable this phase).
</verification>

<success_criteria>
- Operator can view full run history and open any run (OBS-02).
- Cost rolls up per agent + per run + per week/month from already-captured cost, reconciled against runs.cost.total (OBS-04).
- A read-only audit-log viewer surfaces the AUD-01 infrastructure (AUD-01).
</success_criteria>

<output>
After completion, create `.planning/phases/23-node-wrappers-read-only-dashboard/23-04-SUMMARY.md`
</output>
