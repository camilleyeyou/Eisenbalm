---
phase: 23-node-wrappers-read-only-dashboard
plan: 03
type: execute
wave: 1
depends_on: ["23-01"]
files_modified:
  - apps/dispatch-control/package.json
  - convex/runs.ts
  - apps/dispatch-control/app/(dashboard)/graph/page.tsx
  - apps/dispatch-control/app/(dashboard)/graph/_components/PipelineGraph.tsx
  - apps/dispatch-control/app/(dashboard)/graph/_components/AgentNode.tsx
  - apps/dispatch-control/app/(dashboard)/graph/_components/AgentIOPanel.tsx
  - apps/dispatch-control/app/(dashboard)/graph/_components/pipelineTopology.ts
  - apps/dispatch-control/app/(dashboard)/graph/_components/useGraphLayout.ts
  - apps/dispatch-control/__tests__/AgentNode.test.tsx
  - apps/dispatch-control/__tests__/pipelineTopology.test.ts
autonomous: true
requirements: [OBS-01, OBS-03, OBS-05]

must_haves:
  truths:
    - "The Graph view renders the 14+ pipeline nodes connected by edges with auto-layout (sequential spine + 7-writer fan-out)"
    - "At rest each node shows agent name + model + enabled flag + description from the agents table (OBS-01)"
    - "A disabled/suppressed agent (enabled=false) renders visually dimmed"
    - "During a run each node colors by status (queued/running/done/failed), shows a spinner when running, and renders inline cost + duration (OBS-03)"
    - "Status updates live via Convex useQuery subscription with no page refresh"
    - "Clicking a node opens an I/O + error + cost panel loaded from agent_run_payloads (OBS-05)"
  artifacts:
    - path: "apps/dispatch-control/app/(dashboard)/graph/_components/pipelineTopology.ts"
      provides: "Static node + edge definition for the fixed pipeline DAG"
      contains: "PIPELINE_NODES"
    - path: "apps/dispatch-control/app/(dashboard)/graph/_components/AgentNode.tsx"
      provides: "Custom React Flow node — config-at-rest + live-status render"
      contains: "AgentNode"
    - path: "apps/dispatch-control/app/(dashboard)/graph/_components/PipelineGraph.tsx"
      provides: "ReactFlow canvas + live subscription wiring"
      contains: "ReactFlowProvider"
    - path: "convex/runs.ts"
      provides: "latest query returning the most recent run for the workspace"
      exports: ["latest"]
  key_links:
    - from: "apps/dispatch-control/app/(dashboard)/graph/_components/PipelineGraph.tsx"
      to: "convex agentRuns:byRunId live subscription"
      via: "useQuery(api.agentRuns.byRunId, ...)"
      pattern: "agentRuns.byRunId"
    - from: "apps/dispatch-control/app/(dashboard)/graph/_components/AgentIOPanel.tsx"
      to: "convex agentRuns:payloadByRunIdAgentKey"
      via: "useQuery"
      pattern: "payloadByRunIdAgentKey"
    - from: "apps/dispatch-control/app/(dashboard)/graph/_components/AgentNode.tsx"
      to: "agents table config (model, enabled, description)"
      via: "node data props"
      pattern: "enabled"
---

<objective>
Build the dashboard home Graph view: a real React Flow DAG of the pipeline that shows each agent's config at
rest (OBS-01), repaints nodes live with status + cost + duration during a run via Convex subscription (OBS-03),
and opens a per-agent input/output + error + cost panel on node click (OBS-05). The graph is the primary entry
point to run inspection (D-04).

Purpose: The centerpiece operator surface — config-at-rest, live-status-during-run, and click-through to I/O.
Output: React Flow graph + custom node + I/O panel + dagre layout + a `runs:latest` query + component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/23-node-wrappers-read-only-dashboard/23-CONTEXT.md
@.planning/phases/23-node-wrappers-read-only-dashboard/23-RESEARCH.md

<interfaces>
<!-- Exact shapes the executor consumes. No exploration needed. -->

From convex/agentRuns.ts (Plan 23-01):
- query agentRuns.byRunId { runId } -> Array<{ workspace_id, runId, agentKey, status, startedAt?, completedAt?, costUsd?, durationMs?, tokensIn?, tokensOut?, error? }>
- query agentRuns.payloadByRunIdAgentKey { runId, agentKey } -> { inputSnapshot?, outputSnapshot? } | null

From convex/agents.ts (existing):
- query agents.listForWorkspace { workspace_id } -> Array<{ agentKey, enabled, model?, temperature?, top_p?, max_tokens?, description? }>

From convex/runs.ts (existing — byRunId, create, setConfigSnapshot). ADD a `latest` query in this plan.

From apps/dispatch-control/lib/workspace.ts:
  DEFAULT_WORKSPACE_ID = 'eisenbalm'; getCurrentWorkspace() -> Promise<string>

React Flow v12 (`@xyflow/react`): import { ReactFlow, ReactFlowProvider, Handle, Position, Background, Controls } from '@xyflow/react'
  + 'import "@xyflow/react/dist/style.css"'. Custom node registered via nodeTypes={{ agent: AgentNode }}.
  ReactFlow MUST be in a Client Component ('use client') wrapped in ReactFlowProvider (Pitfall 3).

Dagre (`@dagrejs/dagre`): new dagre.graphlib.Graph(); g.setGraph({ rankdir:'TB', nodesep:60, ranksep:80 });
  g.setNode(id,{width,height}); g.setEdge(a,b); dagre.layout(g); g.node(id).x/.y → node positions.

The 18-node topology (must match builder.py exactly): calibrator → scout → advocate → editor_gate_1 →
chronicler → researcher → verify_research → {origin_story, problem, founder_bio, case_study, game, bonus,
design} → validate_sections → qa → editor_final → publisher. The 7 writers fan out from verify_research and
fan in to validate_sections. `design` is always present in the topology but dimmed when its agents-row
enabled=false (Pitfall 4 — dim, do not remove).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Flow + dagre; add runs:latest; define static topology + dagre layout</name>
  <files>apps/dispatch-control/package.json, convex/runs.ts, apps/dispatch-control/app/(dashboard)/graph/_components/pipelineTopology.ts, apps/dispatch-control/app/(dashboard)/graph/_components/useGraphLayout.ts</files>
  <read_first>
    - apps/dispatch-control/package.json (current deps; confirm @xyflow/react + @dagrejs/dagre NOT present)
    - convex/runs.ts (byRunId query pattern + by_workspace index)
    - packages/pipeline/src/eisenbalm_pipeline/graph/builder.py (canonical node + edge set — replicate exactly)
    - .planning/phases/23-node-wrappers-read-only-dashboard/23-RESEARCH.md (Standard Stack install command + Pattern 3 dagre code + Pitfall 4)
  </read_first>
  <action>
    1. Install: `pnpm --filter dispatch-control add @xyflow/react@^12.11.0 @dagrejs/dagre@^3.0.0`.
       (Skip @types/dagre unless the build reports missing types — dagre 3.0.0 ships its own.)
    2. Add a `latest` query to convex/runs.ts (do NOT alter existing exports):
       `export const latest = query({ args: { workspace_id: v.string() }, handler: async (ctx, { workspace_id }) =>
        (await ctx.db.query('runs').withIndex('by_workspace', q => q.eq('workspace_id', workspace_id)).collect())
          .sort((a,b) => b.startedAt - a.startedAt)[0] ?? null })`.
       (Frontend aggregation acceptable at 52 runs/year per RESEARCH Pattern 5.)
    3. Create apps/dispatch-control/app/(dashboard)/graph/_components/pipelineTopology.ts exporting:
       - `PIPELINE_NODES: string[]` = the 18 agentKeys in builder order (design included).
       - `PIPELINE_EDGES: [string,string][]` encoding the spine + 7-way fan-out from verify_research +
         fan-in to validate_sections + post-fan-in spine, matching builder.py add_edge calls exactly.
       - `SECTION_WRITER_KEYS: string[]` = ['origin_story','problem','founder_bio','case_study','game','bonus','design'].
    4. Create apps/dispatch-control/app/(dashboard)/graph/_components/useGraphLayout.ts: a pure
       `computeLayout(nodes, edges)` using @dagrejs/dagre (rankdir 'TB', nodesep 60, ranksep 80, node 176x72)
       returning nodes with `{ position: { x, y } }` set from `g.node(id)`. No React hooks required —
       a plain function the canvas calls once (topology is static).
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Eisenbalm && grep -q "@xyflow/react" apps/dispatch-control/package.json && grep -q "@dagrejs/dagre" apps/dispatch-control/package.json && grep -q "export const latest" convex/runs.ts && grep -q "PIPELINE_NODES" apps/dispatch-control/app/\(dashboard\)/graph/_components/pipelineTopology.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `@xyflow/react` and `@dagrejs/dagre`
    - convex/runs.ts exports `latest` (and still exports byRunId, create, setConfigSnapshot)
    - pipelineTopology.ts exports PIPELINE_NODES (length 18 incl. design), PIPELINE_EDGES, SECTION_WRITER_KEYS
    - `grep -c "verify_research" apps/dispatch-control/app/\(dashboard\)/graph/_components/pipelineTopology.ts` ≥ 8 (7 fan-out edges + node)
    - useGraphLayout.ts imports from '@dagrejs/dagre'
  </acceptance_criteria>
  <done>Dependencies installed; runs:latest exists; the static DAG topology + dagre layout function are defined and match builder.py.</done>
</task>

<task type="auto">
  <name>Task 2: AgentNode custom node (config-at-rest + live status) + AgentNode test</name>
  <files>apps/dispatch-control/app/(dashboard)/graph/_components/AgentNode.tsx, apps/dispatch-control/vitest.config.ts, apps/dispatch-control/package.json, apps/dispatch-control/__tests__/AgentNode.test.tsx</files>
  <read_first>
    - .planning/phases/23-node-wrappers-read-only-dashboard/23-RESEARCH.md (Code Examples — AgentNode skeleton, D-02/D-03 rules)
    - apps/dispatch-control/lib/utils.ts (cn helper)
    - apps/dispatch-control/__tests__/harness.test.ts (vitest import-alias baseline)
    - apps/dispatch-control/vitest.config.ts (note: jsdom NOT configured — add for .tsx component tests)
  </read_first>
  <action>
    1. Create apps/dispatch-control/app/(dashboard)/graph/_components/AgentNode.tsx ('use client'):
       a custom React Flow node taking `data: { agentKey, displayName, model?, enabled, description?,
       status?: 'queued'|'running'|'done'|'failed', costUsd?, durationMs? }`.
       - Status colors: queued=neutral, running=blue + <Loader2 className="animate-spin"/> (lucide-react),
         done=green, failed=red. When `status` is undefined (at rest): white border, but if `enabled===false`
         apply `opacity-40` + a "suppressed" badge (D-03 dim).
       - At rest (no status): render displayName + model + (enabled ? '' : badge) + truncated description.
       - During run (status set, not queued): render inline `$${costUsd.toFixed(4)}` and `${(durationMs/1000).toFixed(1)}s`.
       - Render <Handle type="target" position={Position.Top}/> and <Handle type="source" position={Position.Bottom}/>.
       The node must render meaningfully under jsdom for the test.
    2. Configure jsdom for component tests: `pnpm --filter dispatch-control add -D jsdom @testing-library/react @testing-library/jest-dom`,
       and in vitest.config.ts add an `environmentMatchGlobs` entry mapping `__tests__/*.test.tsx` to 'jsdom'
       (preserve the Plan 23-01 edge-runtime glob for agentRuns/auditLog and node default).
    3. Create apps/dispatch-control/__tests__/AgentNode.test.tsx (render via @testing-library/react):
       - Renders model text when status undefined and enabled true (config-at-rest, OBS-01).
       - Applies opacity-40 / shows 'suppressed' when enabled=false.
       - Renders the cost string '$0.0420' when status='done' and costUsd=0.042.
       - Shows a spinner element when status='running'.
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Eisenbalm && pnpm --filter dispatch-control test:unit -- AgentNode 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - AgentNode.tsx starts with `'use client'` and exports `AgentNode`
    - file references `enabled`, `status`, `costUsd`, `durationMs`, and `opacity-40`
    - `pnpm --filter dispatch-control test:unit -- AgentNode` exits 0 (4 assertions pass)
    - vitest.config.ts maps `*.test.tsx` to jsdom AND retains the edge-runtime glob from Plan 23-01
  </acceptance_criteria>
  <done>AgentNode renders config at rest, dims suppressed agents, and shows live status/cost/duration; covered by passing jsdom tests.</done>
</task>

<task type="auto">
  <name>Task 3: PipelineGraph canvas (live subscription) + AgentIOPanel + graph page wiring</name>
  <files>apps/dispatch-control/app/(dashboard)/graph/_components/PipelineGraph.tsx, apps/dispatch-control/app/(dashboard)/graph/_components/AgentIOPanel.tsx, apps/dispatch-control/app/(dashboard)/graph/page.tsx, apps/dispatch-control/__tests__/pipelineTopology.test.ts</files>
  <read_first>
    - apps/dispatch-control/app/(dashboard)/graph/page.tsx (placeholder being replaced)
    - apps/dispatch-control/components/ConvexClientProvider.tsx (provider already in the dashboard tree)
    - apps/dispatch-control/lib/workspace.ts (DEFAULT_WORKSPACE_ID / getCurrentWorkspace)
    - apps/dispatch-control/app/(dashboard)/graph/_components/pipelineTopology.ts (from Task 1)
    - apps/dispatch-control/app/(dashboard)/graph/_components/AgentNode.tsx (from Task 2)
    - .planning/phases/23-node-wrappers-read-only-dashboard/23-RESEARCH.md (Pattern 3 live-subscription wiring + Pitfall 3 ReactFlowProvider)
  </read_first>
  <action>
    1. Create apps/dispatch-control/app/(dashboard)/graph/_components/PipelineGraph.tsx ('use client'):
       - Wrap content in <ReactFlowProvider> (Pitfall 3). Import '@xyflow/react/dist/style.css'.
       - `const agents = useQuery(api.agents.listForWorkspace, { workspace_id })` for config-at-rest.
       - `const latest = useQuery(api.runs.latest, { workspace_id })`; `const runId = latest?.runId`.
       - `const agentRuns = useQuery(api.agentRuns.byRunId, runId ? { runId } : 'skip')` — live subscription.
       - Build nodes from PIPELINE_NODES: merge agents-config (by agentKey) with the matching agent_runs row
         (by agentKey) for live status/cost/duration; pass as `data` to AgentNode. type:'agent'.
       - Build edges from PIPELINE_EDGES. Run `computeLayout(nodes, edges)` (Task 1) for positions.
       - nodeTypes={{ agent: AgentNode }}. Add <Background/> and <Controls/>. On node click, set selected
         agentKey state and open <AgentIOPanel runId={runId} agentKey={selected} agentRun={...}/>.
       - When `latest` is null (no runs) render the at-rest config graph (status undefined on all nodes).
    2. Create apps/dispatch-control/app/(dashboard)/graph/_components/AgentIOPanel.tsx ('use client'):
       a slide-over panel taking { runId, agentKey, agentRun }. Calls
       `useQuery(api.agentRuns.payloadByRunIdAgentKey, runId && agentKey ? { runId, agentKey } : 'skip')`.
       Renders inputSnapshot + outputSnapshot (pretty-printed JSON in <pre>), the agentRun.error if status==='failed',
       and costUsd/durationMs/tokensIn/tokensOut. Read-only (no buttons that mutate).
    3. Replace apps/dispatch-control/app/(dashboard)/graph/page.tsx: a Server Component that resolves
       `const workspace_id = await getCurrentWorkspace()` and renders `<PipelineGraph workspace_id={workspace_id} />`.
       Keep the page minimal — all hooks live in the Client Component (Pitfall 3).
    4. Create apps/dispatch-control/__tests__/pipelineTopology.test.ts: assert PIPELINE_NODES has 18 entries,
       includes 'design', every edge endpoint is a known node, and exactly 7 edges originate from 'verify_research'
       and exactly 7 edges terminate at 'validate_sections'.
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Eisenbalm && pnpm --filter dispatch-control test:unit 2>&1 | tail -15 && pnpm --filter dispatch-control build 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - PipelineGraph.tsx contains `ReactFlowProvider`, `useQuery(api.agentRuns.byRunId`, `useQuery(api.runs.latest`, and `useQuery(api.agents.listForWorkspace`
    - AgentIOPanel.tsx contains `payloadByRunIdAgentKey` and renders `inputSnapshot` + `outputSnapshot` + `error`
    - graph/page.tsx renders `<PipelineGraph` and calls `getCurrentWorkspace`
    - `pnpm --filter dispatch-control test:unit` exits 0 (pipelineTopology + AgentNode + Plan 23-01 specs)
    - `pnpm --filter dispatch-control build` exits 0 (no ReactFlowProvider/server-component error)
  </acceptance_criteria>
  <done>The Graph view renders the live DAG with config-at-rest, live status repaint, and node-click I/O panel; the app builds clean.</done>
</task>

</tasks>

<verification>
- `pnpm --filter dispatch-control test:unit` green (AgentNode + pipelineTopology + carried-over Plan 23-01 specs).
- `pnpm --filter dispatch-control build` exits 0.
- Manual (per 23-VALIDATION.md): trigger a run, watch /graph repaint queued→running→done with inline cost; load /graph at rest with DESIGNAGENT_SUPPRESSED and confirm the design node is dimmed; click a node and confirm the I/O panel opens.
</verification>

<success_criteria>
- Operator sees the real agent graph with per-node config (model/enabled/description) sourced from Convex (OBS-01).
- Nodes repaint live (queued→running→done/failed) with inline cost + duration via Convex subscription (OBS-03).
- Node click opens per-agent input/output + error panel from agent_run_payloads (OBS-05).
</success_criteria>

<output>
After completion, create `.planning/phases/23-node-wrappers-read-only-dashboard/23-03-SUMMARY.md`
</output>
