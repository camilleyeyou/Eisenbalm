---
phase: quick-260724-x4b
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [LD-1-MASTHEAD-DOOR, LD-2-DRAFT-READY-BANNER, LD-3-CARD-EDIT-SHORTCUT, LD-4-RUN-MONITOR-BACKLINK]
files_modified:
  - apps/dispatch-control/components/Masthead.tsx
  - apps/dispatch-control/__tests__/Masthead.test.tsx
  - apps/dispatch-control/app/(dashboard)/issues/_components/WorkspaceStateProvider.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/DraftReadyBanner.tsx
  - apps/dispatch-control/__tests__/DraftReadyBanner.test.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryDeskGrid.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx
  - apps/dispatch-control/__tests__/StoryDeskGrid.test.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunControlBar.tsx
  - apps/dispatch-control/__tests__/runControl.test.tsx
  - apps/dispatch-control/__tests__/runDetailActionNames.test.ts

must_haves:
  truths:
    - "When the latest run is awaiting-review, the Masthead 'Paused for you' system-activity readout becomes a link to /issues/{n}/draft (the run's issue's Draft desk); for every other run status it stays a plain non-link readout."
    - "When the workspace's run status is awaiting-review AND the operator is NOT on the Draft stage, a slim dismissible banner appears under the stage tabs offering a 'Go to the Draft desk →' CTA to /issues/{n}/draft; it never appears mid-run, on the Draft stage, or after dismissal."
    - "Each Draft-desk (StoryDeskGrid) card exposes a secondary 'Edit →' affordance that opens that section straight into edit mode, while a normal card click still opens the section read-only on its Draft tab."
    - "RunDetail shows an 'Open issue →' link near 'Back to all runs' whenever the run has an issueNumber (Draft desk when awaiting-review/complete, issue overview otherwise); RunControlBar's Trigger Run, on success, shows an inline 'Open Issue {n} →' link to the just-started issue."
    - "The full dispatch-control vitest suite passes AND `pnpm --filter dispatch-control build` succeeds."
  artifacts:
    - path: "apps/dispatch-control/components/Masthead.tsx"
      provides: "SystemActivityReadout renders as a next/link to issueDraftHref when awaiting-review + issueNumber resolved"
      contains: "issueDraftHref"
    - path: "apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/DraftReadyBanner.tsx"
      provides: "Session-dismissible draft-ready banner gated on runStatus === 'awaiting-review' && stage !== draft"
      contains: "awaiting-review"
    - path: "apps/dispatch-control/app/(dashboard)/issues/_components/WorkspaceStateProvider.tsx"
      provides: "runStatus exposed on WorkspaceStateValue for the banner"
      contains: "runStatus"
    - path: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryDeskGrid.tsx"
      provides: "per-card secondary Edit affordance (onOpenEdit prop) that stopsPropagation from the card's open-story click"
      contains: "onOpenEdit"
    - path: "apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx"
      provides: "Open issue link resolved via pipelineRuns.byRunId issueNumber"
      contains: "issueDraftHref"
    - path: "apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunControlBar.tsx"
      provides: "post-launch inline success line linking to the started issue"
      contains: "issueHref"
  key_links:
    - from: "components/Masthead.tsx"
      to: "issueDraftHref(issueNumber)"
      via: "SystemActivityReadout href prop, issueNumber from the already-subscribed pipelineRuns.byRunId"
      pattern: "issueDraftHref"
    - from: "issues/[issueNumber]/layout.tsx (FrameChrome)"
      to: "DraftReadyBanner"
      via: "mounted under </nav>, reads runStatus from useWorkspaceState()"
      pattern: "DraftReadyBanner"
    - from: "StoryDeskGrid card Edit button"
      to: "ReviewDeskRunView.openStoryEditing"
      via: "onOpenEdit(section.id) -> setEditing(true) + router.push(?story=&tab=draft)"
      pattern: "onOpenEdit"
    - from: "RunControlBar.handleConfirm"
      to: "pipelineRuns.byRunId(startedRunId).issueNumber"
      via: "triggerRun result runId stored in state, resolved to issueNumber for the success link"
      pattern: "issueHref"
---

<objective>
Client feedback on dispatch-control: "It's still too hard to get to editing the run once you launch it." Shorten the launch→editing path with four connected affordances, each matching the Uniform System Sheet (hard edges, 1c tokens, square mono-caps labels, cobalt links):

1. The Masthead's always-mounted "Paused for you" readout becomes a DOOR to the Draft desk when a run is awaiting review.
2. A dismissible draft-ready banner in the issue workspace frame carries the operator forward once the draft lands (any stage except Draft).
3. Every Draft-desk card gets a secondary "Edit →" shortcut that skips the "open card → Edit story" two-click tax.
4. The Run Monitor connects back to the work — RunDetail links to the issue, and Trigger Run gives post-launch navigation.

Purpose: Every one of these reuses signals/machinery that ALREADY exist (Masthead already knows the awaiting-review transition; ReviewDeskRunView already supports zero-click edit) — no surface emits the shortcut today.
Output: Four wired affordances + extended/added component tests, gated by the full vitest suite and a strict production build.

STRICT SCOPE (locked): no pipeline/API changes, no new Convex functions, no AwaitingYouInbox routing change, no deriveTasks CTA change, no banner-dismissal persistence, Approval stage untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Verified this session. Executor: use these directly, no re-exploration. -->

lib/issueRouteResolver.ts (pure, no imports):
  export function issueHref(issueNumber: number): string        // /issues/{n}
  export function issueDraftHref(issueNumber: number): string   // /issues/{n}/draft

CRITICAL DATA FACT: the `runs` table row (api.runs.byRunId / api.runs.latest) does
NOT carry issueNumber. Only api.pipelineRuns.byRunId({runId}) and
api.pipelineRuns.byIssueNumber({issueNumber}) return issueNumber. Use
pipelineRuns.byRunId to resolve runId -> issueNumber (this is EXACTLY what
Masthead already does; it is an existing query — NOT a new Convex function).

Masthead.tsx (already subscribed, no new query needed for Fix 1):
  const latest = useQuery(api.runs.latest, {workspace_id})          // ~L209
  const pipelineRun = useQuery(api.pipelineRuns.byRunId, latest ? {runId: latest.runId} : 'skip')  // ~L210
  const issueNumber = pipelineRun?.issueNumber                      // ~L220 (already exists)
  const systemActivity = systemActivityFromRunStatus(latest?.status) // ~L254; 'awaiting-review' -> 'paused-for-you'
  function SystemActivityReadout({activity}) -> renders a <span>    // ~L167-179

WorkspaceStateProvider.tsx:
  const run = useQuery(api.pipelineRuns.byIssueNumber, ...)   // ~L181 (has .status + issueNumber)
  const runRow = useQuery(api.runs.byRunId, ...)              // ~L194 (has .status)
  derivationInputs.runStatus = runRow?.status                // ~L296 (computed, NOT exposed on the value)
  interface WorkspaceStateValue { issueNumber, runId, status(IssueStatus), ... } // status is DERIVED IssueStatus, not the raw run status

layout.tsx (FrameChrome): has `n` (issueNumber) and `currentStageSegment`
  (one of 'story'|'draft'|'fact-check'|'voice'|'approval'|undefined) in scope; the
  stage-tab <nav> closes ~L365, the content grid <div className="grid ..."> starts ~L367.

StoryDeskGrid.tsx: each card is currently ONE <button data-testid={`story-card-${section.id}`}
  onClick={() => onOpen(section.id)}> wrapping all content (~L174-226). onOpen comes
  from ReviewDeskRunView `onOpen={id => openStory(id, 'draft')}`.

ReviewDeskRunView.tsx:
  function openStory(id, tab='draft')     // ~L260 -> guardDirty + router.push(?story=&tab=)
  function handleEdit()                   // ~L291 -> setEditing(true)
  buildHref({story, tab})                 // drops all other query params
  <StoryDeskGrid ... onOpen={id => openStory(id, 'draft')} /> // ~L481-487
  NOTE: StoryFocusView renders SectionEditorPanel for ALL sections (incl game/theme/
  podcast) when editing===true — so "editing + navigate" == what "Edit story" does today.

RunDetail.tsx: `run = useQuery(api.runs.byRunId, {runId})` (no issueNumber). Link
  already imported. "← Back to all runs" ~L281-286.

RunControlBar.tsx: `latest = useQuery(api.runs.latest, {workspace_id})`; handleConfirm
  ~L45-57 awaits `triggerRun({}, token)`.

lib/pipelineControlClient.ts: triggerRun(body, token) -> Promise<{ runId: string }>.
  The response exposes ONLY runId (NO issueNumber) — so Fix 4b MUST take the fallback:
  resolve issueNumber via pipelineRuns.byRunId on the returned runId, then show an
  inline success line with a link (do NOT change the API).

SectionChipList.tsx: EDITABLE_SECTIONS[0] === { id: 'originStory', label: 'Origin Story' }.
</interfaces>

<system_sheet>
Uniform System Sheet vocabulary every new affordance must match:
- Hard edges (radius 0–2px; NO rounded-lg / rounded-full — dots are the only circles).
- 1c tokens only: var(--color-ink | ink-soft | faint | cobalt | vermilion | marigold |
  marigold-text | green | card | card-alt | faint). Never neutral/gray/slate.
- Fonts: Newsreader=headlines, Lora italic=deks/excerpts, Space Grotesk (var(--font-ui))=UI,
  IBM Plex Mono caps (var(--font-mono))=kickers/labels/numerals.
- Inline link = cobalt, font-ui, weight 600, "text →".
- Status never color alone. Every page head: mono kicker → display h1 → italic dek.
Accessibility: links/buttons min-h-[44px] where the pattern does elsewhere;
focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[color:var(--color-cobalt)];
aria-labels on icon-only / ambiguous affordances.
</system_sheet>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Awaiting-review becomes a door — Masthead link + workspace draft-ready banner</name>
  <files>apps/dispatch-control/components/Masthead.tsx, apps/dispatch-control/app/(dashboard)/issues/_components/WorkspaceStateProvider.tsx, apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/DraftReadyBanner.tsx, apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx, apps/dispatch-control/__tests__/Masthead.test.tsx, apps/dispatch-control/__tests__/DraftReadyBanner.test.tsx</files>
  <behavior>
    Masthead (Fix 1):
    - When latest.status === 'awaiting-review' AND issueNumber != null → the system-activity
      readout ("Paused for you") renders as a next/link to issueDraftHref(issueNumber),
      role="link", accessible name mentioning the Draft desk.
    - For any other status (running/complete/failed/idle) OR when issueNumber is null →
      it stays a plain <span> (no link with the Draft-desk name).
    DraftReadyBanner (Fix 2):
    - runStatus === 'awaiting-review' AND currentStageSegment !== 'draft' AND not dismissed
      → renders the copy "The draft is ready — nine stories are waiting." + a
      "Go to the Draft desk →" link to issueDraftHref(n).
    - currentStageSegment === 'draft' → renders null.
    - runStatus !== 'awaiting-review' (e.g. 'running') → renders null.
    - clicking the dismiss button hides it for the session.
  </behavior>
  <action>
FIX 1 — Masthead.tsx (the signal becomes a door):
- Add imports: `import Link from 'next/link'` and `import { issueDraftHref } from '@/lib/issueRouteResolver'`.
- Change `SystemActivityReadout` to accept an optional `href?: string`. When `href` is
  provided, render a `<Link href={href} aria-label="The draft is ready — go to the Draft desk">`
  wrapping the SAME icon + label content, keeping the identical visual classes (mono caps,
  `style={{ color: meta.color }}`) PLUS `min-h-[44px]` + `hover:opacity-90` +
  `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[color:var(--color-cobalt)]`.
  When `href` is undefined, render the EXISTING `<span>` unchanged.
- In `Masthead()`, after `systemActivity`/`issueNumber` are computed, derive:
  `const draftDeskHref = systemActivity === 'paused-for-you' && issueNumber != null ? issueDraftHref(issueNumber) : undefined`
  and pass `href={draftDeskHref}` into `<SystemActivityReadout activity={systemActivity} href={draftDeskHref} />` (~L287).
  Preserve masthead visual language (masthead-muted/masthead-text unchanged elsewhere).

FIX 2a — WorkspaceStateProvider.tsx (expose the raw run status):
- Add `runStatus?: string` to the `WorkspaceStateValue` interface (near `status`, with a
  one-line doc comment: raw run status, the SAME value threaded into derivationInputs.runStatus;
  primitive → referentially stable → no render-loop risk).
- In the `value` object (~L383) set `runStatus: runRow?.status ?? run?.status`.
  Do NOT touch any memoized derivation block — this is a scalar addition only.

FIX 2b — new DraftReadyBanner.tsx at
  apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/DraftReadyBanner.tsx:
- `'use client'`. Props: `{ issueNumber: number; currentStageSegment: string | undefined }`.
- `import { useState } from 'react'`, `import Link from 'next/link'`,
  `import { issueDraftHref } from '@/lib/issueRouteResolver'`,
  `import { useWorkspaceState } from '../../_components/WorkspaceStateProvider'` (verify the
  relative depth resolves to app/(dashboard)/issues/_components/WorkspaceStateProvider).
- `const { runStatus } = useWorkspaceState()`; `const [dismissed, setDismissed] = useState(false)`.
- Guard: `if (runStatus !== 'awaiting-review' || currentStageSegment === 'draft' || dismissed) return null`.
- Render a slim banner matching the System Sheet: container
  `border border-[color:var(--color-faint)] border-l-[3px] border-l-[color:var(--color-marigold)] bg-[color:var(--color-card)] px-4 py-2.5 flex flex-wrap items-center gap-3`.
  Inside: a `<p>` (font-ui, text-[13px], text-[color:var(--color-ink)]) reading
  "The draft is ready — nine stories are waiting."; a `<Link href={issueDraftHref(issueNumber)}>`
  styled as the cobalt inline link (font-ui, font-semibold, text-[color:var(--color-cobalt)],
  min-h-[44px] inline-flex items-center, focus-visible ring) reading "Go to the Draft desk →";
  and a right-aligned dismiss `<button type="button" aria-label="Dismiss draft-ready notice"
  onClick={() => setDismissed(true)}>` (min-h-[44px] hit target, faint ✕ glyph, focus-visible ring, ml-auto).

FIX 2c — layout.tsx (mount the banner):
- `import DraftReadyBanner from './_components/DraftReadyBanner'`.
- Mount `<DraftReadyBanner issueNumber={n} currentStageSegment={currentStageSegment} />`
  immediately AFTER the stage-tab `</nav>` (~L365) and BEFORE the content grid
  `<div className="grid ...">` (~L367). Both `n` and `currentStageSegment` are already in scope.

TESTS:
- Extend Masthead.test.tsx: add a test — with `latest: { status: 'awaiting-review', runId: 'run-1', startedAt: 1 }`
  and `pipelineRun: { issueNumber: 42 }`, `screen.getByRole('link', { name: /draft desk/i })`
  has href `/issues/42/draft`. Add a negative test — with `latest.status: 'complete'`,
  `screen.queryByRole('link', { name: /draft desk/i })` is null. (pipelineRuns.byRunId is
  already in that test's api mock; next/link renders an anchor in jsdom — AwaitingYouInbox
  proves this — so no new mock is needed.)
- Add DraftReadyBanner.test.tsx: `vi.mock` the WorkspaceStateProvider module exposing
  `useWorkspaceState: vi.fn()` (untyped return, mirroring WorkspaceOutline.test.tsx). Cases:
  (a) runStatus 'awaiting-review' + currentStageSegment 'story' → CTA link to /issues/12/draft
  present; (b) currentStageSegment 'draft' → container empty (renders null); (c) runStatus
  'running' → renders null; (d) dismiss button click → banner text gone.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && pnpm exec vitest run __tests__/Masthead.test.tsx __tests__/DraftReadyBanner.test.tsx</automated>
  </verify>
  <done>Masthead "Paused for you" is a link to the run's Draft desk only when awaiting-review; the workspace banner appears on non-draft stages during awaiting-review, is dismissible, and never shows otherwise; both test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Per-card Edit shortcut on the Story Desk</name>
  <files>apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryDeskGrid.tsx, apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx, apps/dispatch-control/__tests__/StoryDeskGrid.test.tsx</files>
  <action>
StoryDeskGrid.tsx — add a secondary Edit affordance without nesting a button inside a button
(the current card is a single <button>, which is invalid to nest interactive content in):
- Add `onOpenEdit: (sectionId: string) => void` to `StoryDeskGridProps`; destructure it in the
  component signature (`export default function StoryDeskGrid({ draft, chipCounts, reviewedIds, onOpen, onOpenEdit })`).
- Convert each card's outer `<button>` (~L174) into a `<div>` that keeps the card visual classes
  (the `relative flex min-h-[190px] flex-col rounded-[2px] border ... hover:bg-... ${TOP_RULE[status]}`
  classes) and stays `relative`. Inside it, render two REAL sibling buttons:
  1. A stretched primary button (the open-story action) that keeps the testid:
     `<button type="button" data-testid={`story-card-${section.id}`} onClick={() => onOpen(section.id)}
       aria-label={`Open ${headlineFor(draft, section.id, section.label)}`}
       className="absolute inset-0 z-0 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[color:var(--color-cobalt)]" />`
  2. The existing visible content moved into a wrapper `<div className="pointer-events-none relative z-[1] flex flex-1 flex-col">`
     so ordinary clicks fall through to the stretched button. (The "Reviewed" badge, kicker,
     headline, excerpt, and the chip/dots/word-count meta line all live here, unchanged in markup
     except the wrapper.)
  3. In the footer, ADD a right-aligned Edit affordance on its own line BELOW the existing meta
     span: `<div className="mt-2.5 flex justify-end pointer-events-none"><button type="button"
       data-testid={`story-card-edit-${section.id}`}
       onClick={e => { e.stopPropagation(); onOpenEdit(section.id) }}
       aria-label={`Edit ${headlineFor(draft, section.id, section.label)}`}
       className="pointer-events-auto relative z-[2] min-h-[36px] inline-flex items-center
       font-[family-name:var(--font-ui)] text-[12px] font-semibold text-[color:var(--color-cobalt)]
       hover:text-[color:var(--color-cobalt-dark)] focus-visible:outline-none focus-visible:ring-2
       focus-visible:ring-[color:var(--color-cobalt)]">Edit &rarr;</button></div>`
  Card click behavior (open story → Draft tab, read-only) is unchanged.

ReviewDeskRunView.tsx — wire the shortcut, reusing existing machinery:
- Add a handler near openStory/handleEdit:
  `function openStoryEditing(id: string) { void guardDirty(() => { setEditFinding(null); setEditing(true); router.push(buildHref({ story: id, tab: 'draft' })) }) }`
  (Sets editing synchronously and navigates to the story's Draft tab — StoryFocusView then
  mounts SectionEditorPanel, identical to clicking a card then "Edit story", including for
  structured game/theme/podcast sections.)
- Pass `onOpenEdit={openStoryEditing}` to `<StoryDeskGrid ... />` (~L481-487), keeping the
  existing `onOpen={id => openStory(id, 'draft')}`.

TEST — add StoryDeskGrid.test.tsx:
- `import { EDITABLE_SECTIONS }` from the SectionChipList module and build a minimal
  DraftResponse (empty `sections` map, empty `game`/`bonus`/`theme` objects, empty `conversation`
  array — the excerpt/word-count helpers tolerate absence). Use `EDITABLE_SECTIONS[0].id`
  (`'originStory'`).
- Render `<StoryDeskGrid draft={draft} chipCounts={{}} reviewedIds={new Set()} onOpen={onOpen} onOpenEdit={onOpenEdit} />`.
- Assert clicking `getByTestId('story-card-originStory')` calls `onOpen` with `'originStory'`
  and does NOT call `onOpenEdit`.
- Assert clicking `getByTestId('story-card-edit-originStory')` calls `onOpenEdit` with
  `'originStory'` and does NOT call `onOpen` (stopPropagation).
  </action>
  <verify>
    <automated>cd apps/dispatch-control && pnpm exec vitest run __tests__/StoryDeskGrid.test.tsx</automated>
  </verify>
  <done>Each Story Desk card has a discrete "Edit →" that jumps straight into edit mode for that section; a normal card click still opens the section read-only; the new test proves both paths route to their own callback.</done>
</task>

<task type="auto">
  <name>Task 3: Run Monitor connects back to the work + full-suite/strict-build gate</name>
  <files>apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunControlBar.tsx, apps/dispatch-control/__tests__/runControl.test.tsx, apps/dispatch-control/__tests__/runDetailActionNames.test.ts</files>
  <action>
FIX 4a — RunDetail.tsx ("Open issue →"):
- `import { issueHref, issueDraftHref } from '@/lib/issueRouteResolver'` (Link already imported).
- Add `const pipelineRun = useQuery(api.pipelineRuns.byRunId, { runId })` and
  `const issueNumber = pipelineRun?.issueNumber` (existing query — resolves runId→issueNumber
  because the `runs` row does NOT carry issueNumber).
- Near the top "← Back to all runs" link (~L281-286), wrap it and a new link in a flex row and,
  ONLY when `issueNumber != null`, render an "Open issue →" `<Link>` styled like the back link
  (cobalt, font-ui text-[13px], min-h-[44px] inline-flex items-center, focus-visible ring) with
  href `run.status === 'awaiting-review' || run.status === 'complete' ? issueDraftHref(issueNumber) : issueHref(issueNumber)`.

FIX 4b — RunControlBar.tsx (post-launch navigation via inline success link):
- `import Link from 'next/link'` and `import { issueHref } from '@/lib/issueRouteResolver'`.
- Add `const [startedRunId, setStartedRunId] = useState<string | null>(null)`.
- In `handleConfirm`, capture the result and store its runId:
  `const result = await triggerRun({}, token); setStartedRunId(result.runId)` (triggerRun exposes
  only runId — no issueNumber — so we resolve it below rather than router.push from the response).
- Add `const startedRun = useQuery(api.pipelineRuns.byRunId, startedRunId ? { runId: startedRunId } : 'skip')`;
  `const startedIssueNumber = startedRun?.issueNumber`.
- When `startedRunId` is set, render an inline success line in the right cluster (near the error
  span): if `startedIssueNumber != null`, a `<Link href={issueHref(startedIssueNumber)}>` reading
  "Run started · Open Issue {startedIssueNumber} →" (cobalt, font-ui, font-semibold, min-h-[44px]
  inline-flex, focus-visible ring); else a plain span "Run started — resolving issue…" (NEVER a
  broken link). No exclamation marks. Do not change any endpoint or the trigger button behavior.

TESTS:
- runControl.test.tsx: add `pipelineRuns: { byRunId: 'pipelineRuns:byRunId' }` to the
  `@convex/_generated/api` mock (REQUIRED — RunControlBar now references it; without it the
  render crashes). Add a new RunControlBar test that dispatches useQuery by queryRef
  (`mockImplementation`, mirroring Masthead.test.tsx): `runs:latest` → idle (e.g. null so Trigger
  is enabled), `pipelineRuns:byRunId` → `{ issueNumber: 12 }`. Click "Trigger Run" → click
  "Confirm Run?" → `await waitFor` that `getByRole('link', { name: /open issue 12/i })` has href
  `/issues/12`. Keep existing tests passing (they use a single mockReturnValue; the new
  pipelineRuns ref returns the same value but the success line stays hidden while startedRunId is null).
- runDetailActionNames.test.ts: add a source-scan assertion (matching that file's existing
  read-file style) that RunDetail.tsx references `issueDraftHref` / `issueHref` and renders an
  "Open issue" affordance.

FINAL HARD GATE (run both, both must pass clean):
- Full suite: `pnpm --filter dispatch-control test`
- Strict production build: `pnpm --filter dispatch-control build`
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Eisenbalm && pnpm --filter dispatch-control test && pnpm --filter dispatch-control build</automated>
  </verify>
  <done>RunDetail shows "Open issue →" (Draft desk when awaiting-review/complete, else overview) whenever the run has an issueNumber; Trigger Run shows an inline "Open Issue {n} →" success link after launch; the full dispatch-control vitest suite passes AND the strict Next production build succeeds.</done>
</task>

</tasks>

<verification>
- Masthead "Paused for you" is a link ONLY when awaiting-review + issueNumber resolved; plain readout otherwise.
- Draft-ready banner: shows on non-draft stages during awaiting-review, hidden on Draft stage / mid-run / after dismiss.
- Story Desk cards: primary click → read-only open; "Edit →" → straight into edit mode; both keyboard-accessible with focus-visible rings; card open testid unchanged.
- Run Monitor: RunDetail "Open issue →" gated on issueNumber; RunControlBar inline success link after launch.
- No pipeline/API/Convex-function changes; AwaitingYouInbox routing, deriveTasks CTAs, Approval stage untouched.
- Full vitest suite + `pnpm --filter dispatch-control build` both pass.
</verification>

<success_criteria>
- All four locked-decision affordances implemented exactly as specified and matching the Uniform System Sheet.
- issueNumber for links is resolved only via existing queries (pipelineRuns.byRunId / already-subscribed data) — no new Convex functions.
- New/updated tests: Masthead door + negative, DraftReadyBanner (4 cases), StoryDeskGrid open-vs-edit, RunControlBar post-launch link, RunDetail source-scan.
- `pnpm --filter dispatch-control test` and `pnpm --filter dispatch-control build` both succeed.
</success_criteria>

<output>
After completion, create `.planning/quick/260724-x4b-shorten-the-launch-to-editing-path-click/260724-x4b-SUMMARY.md`
</output>
