---
phase: quick-260520-0kt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/globals.css
  - apps/web/components/AnchorCopyButton.tsx
  - apps/web/components/SiteHeader.tsx
  - apps/web/components/archive/ArchiveList.tsx
  - apps/web/components/issue/IssueHero.tsx
  - apps/web/app/layout.tsx
  - apps/web/app/charities/page.tsx
  - apps/web/app/archive/page.tsx
autonomous: true
requirements: [WCAG-A11Y-PASS]

must_haves:
  truths:
    - "Users who set prefers-reduced-motion see no animations or transitions site-wide"
    - "Every interactive control (copy button, nav links, sort buttons, PDF link) has a >=44px tap target"
    - "Keyboard users can tab to a 'Skip to content' link as the first focusable element and jump to the main content"
    - "Each page has exactly one <main> landmark (no nested or duplicate main)"
  artifacts:
    - path: "apps/web/app/globals.css"
      provides: "Global prefers-reduced-motion guard + skip-link focus styling"
      contains: "prefers-reduced-motion"
    - path: "apps/web/app/layout.tsx"
      provides: "Skip-to-content anchor as first focusable element in <body>; sole <main id=\"main\"> landmark"
      contains: "href=\"#main\""
    - path: "apps/web/components/AnchorCopyButton.tsx"
      provides: "44px hit area on copy button, icon glyph unchanged"
    - path: "apps/web/components/SiteHeader.tsx"
      provides: "44px tap targets on nav links"
    - path: "apps/web/components/archive/ArchiveList.tsx"
      provides: "44px tap targets on sort buttons"
    - path: "apps/web/components/issue/IssueHero.tsx"
      provides: "44px tap target on PDF download link"
    - path: "apps/web/app/charities/page.tsx"
      provides: "No second <main> — reconciled to single-landmark pattern"
    - path: "apps/web/app/archive/page.tsx"
      provides: "Nested <main> removed — single-landmark pattern"
  key_links:
    - from: "apps/web/app/layout.tsx skip-link"
      to: "apps/web/app/layout.tsx <main id=\"main\">"
      via: "anchor href=#main targeting the layout's main landmark id"
      pattern: "href=\"#main\""
---

<objective>
Apply four small, related, low-risk WCAG AA conformance fixes to the Next.js frontend (apps/web), surfaced by a UI/UX audit of the live site. All changes are markup/CSS-only: NO behavior changes, NO data changes, NO color changes. Each fix lands as its own atomic commit.

Purpose: Bring apps/web to WCAG AA conformance for motion sensitivity (2.3.3), target size (2.5.5), bypass blocks (2.4.1), and landmark structure (1.3.1) without touching the theme engine, the game sandbox, or any color value.

Output: Updated globals.css, layout.tsx, charities/page.tsx, archive/page.tsx, and four interactive components — all colors continue to reference existing `--color-*` CSS variables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Files under change (all read during planning)
@apps/web/app/globals.css
@apps/web/components/AnchorCopyButton.tsx
@apps/web/components/SiteHeader.tsx
@apps/web/components/archive/ArchiveList.tsx
@apps/web/components/issue/IssueHero.tsx
@apps/web/app/layout.tsx
@apps/web/app/charities/page.tsx
@apps/web/app/archive/page.tsx

<constraints>
HARD CONSTRAINTS — these MUST be honored and MUST NOT be violated:
- DO NOT modify apps/web/lib/theme.ts (theme engine).
- DO NOT modify apps/web/components/issue/GameSlot.tsx (game sandbox).
- DO NOT introduce ANY hardcoded color values. Every color MUST keep referencing existing CSS variables (--color-*). The reduced-motion block touches only animation/transition/scroll properties — never color.
- This is a markup/CSS-only pass: no behavior changes, no data changes, no color changes.
</constraints>

<key_facts>
Discovered during planning — use these directly, no re-exploration needed:

1. globals.css currently ends at line 261 (the `.metadata-block dd` rule). Append new blocks after it.
2. globals.css already has a global focus ring at lines 87-90:
   `:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 2px; }`
   The new skip-link is a focusable element and will inherit this ring automatically — do NOT duplicate or override the outline; only handle its visibility (sr-only → visible on focus) and positioning.
3. Tailwind v4 is in use (`@import "tailwindcss"` at globals.css L1; tailwindcss ^4.3.0 in package.json). Tailwind core ships `sr-only` AND `not-sr-only` utilities by default — confirm they resolve (grep the built output or just rely on core); if for any reason `sr-only` is unavailable, add a minimal `.sr-only` / `.focus\:not-sr-only:focus` utility to globals.css. Prefer the Tailwind core utilities; only add custom CSS as a fallback.
4. layout.tsx L121 already renders `<main className="flex-1" id="main">{children}</main>` — this is the SINGLE canonical main landmark for every route.
5. LANDMARK INCONSISTENCY (important for FIX 4): the layout provides `<main id="main">`, but `apps/web/app/archive/page.tsx` L35 ALSO renders a `<main>` (nested main-in-main = duplicate-landmark violation), while `apps/web/app/about/page.tsx` correctly uses `<article>` (no second main), and `apps/web/app/charities/page.tsx` L46 uses a `<div>`. The CORRECT single-landmark pattern is: layout owns the one `<main id="main">`; page-level wrappers are NOT `<main>`. So the correct reconciliation is to make archive's `<main>` a `<div>` (fixing the existing violation) and leave charities as a `<div>` (it already does NOT introduce a second main — matching about's no-second-main approach). Do NOT swap charities' `<div>`→`<main>` (that would CREATE a duplicate-landmark bug). about/page.tsx already follows the correct pattern and needs no change.
6. Touch target sizes today: AnchorCopyButton L57 `h-6 w-6` (24px); SiteHeader nav `<Link>` L33-37 has no padding; ArchiveList sort buttons L40-61 are text-only; IssueHero PDF link L95-103 is `inline-block` with no min-height.
7. Test runner: `pnpm --filter web test:unit` runs `vitest run` (package name token is `web`, not `apps/web`). 11 test files in apps/web/__tests__/. Phase 8 Wave 0 sentinel tests are EXPECTED to fail (29 fail / 79 pass baseline per STATE.md Plan 10-03) — these are pre-existing and OUT OF SCOPE; do not "fix" them. Confirm the count is unchanged before+after this plan.
</key_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: prefers-reduced-motion guard (FIX 1) — own commit</name>
  <files>apps/web/app/globals.css</files>
  <action>
Append a new `@media (prefers-reduced-motion: reduce)` block to the END of globals.css (after the current last rule on L261), behind a clear comment banner consistent with the file's existing `═══` banner style (e.g. a "WCAG AA — Accessibility conformance pass" header).

The block must neutralize animation and transition across the site:
```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```
Rationale to capture in the comment: this neutralizes the DeliberationSlot chevron rotate (transition-transform group-open:rotate-180), prose link opacity fades, and the shadcn tooltip zoom (animate-in fade-in-0 zoom-in-95) for users who request reduced motion.

CRITICAL: this block touches ONLY animation/transition/scroll properties. It MUST NOT contain any color, background, layout, sizing, or font property. Under a normal (no-preference) motion setting it has zero effect — purely additive. Do not modify any existing rule in the file.

Commit this fix alone: `fix(a11y): add prefers-reduced-motion guard to globals.css`
  </action>
  <verify>
    <automated>grep -q "prefers-reduced-motion" apps/web/app/globals.css && ! grep -A8 "prefers-reduced-motion: reduce" apps/web/app/globals.css | grep -iE "color|background|font-|width|height|margin|padding|#[0-9a-fA-F]{3,6}" && echo PASS</automated>
  </verify>
  <done>globals.css contains a `@media (prefers-reduced-motion: reduce)` block targeting `*, *::before, *::after` that sets only animation/transition/scroll properties; no existing rule changed; no color/layout/sizing/font property inside the block.</done>
</task>

<task type="auto">
  <name>Task 2: 44px touch targets across four components (FIX 2) — own commit</name>
  <files>apps/web/components/AnchorCopyButton.tsx, apps/web/components/SiteHeader.tsx, apps/web/components/archive/ArchiveList.tsx, apps/web/components/issue/IssueHero.tsx</files>
  <action>
Expand the hit area of four interactive controls to >=44px (WCAG 2.5.5 Target Size), WITHOUT changing any visible glyph size, color, or behavior. Use Tailwind sizing utilities only — no inline styles, no new colors.

1) apps/web/components/AnchorCopyButton.tsx (~L57): the `<button>` is `inline-flex h-6 w-6 ...`. Enlarge the BUTTON hit area to >=44px while keeping the `<Link>` icon at `size={14}`. Replace `h-6 w-6` with `min-h-11 min-w-11` (44px) — the existing `inline-flex items-center justify-center` keeps the 14px glyph centered, so the icon does NOT grow. Preserve the existing `aria-label="Copy link to this section"`, the `print:hidden`, and the existing `focus-visible:outline*` classes verbatim. Do not alter the Tooltip or copy logic.

2) apps/web/components/SiteHeader.tsx (~L33-37): the nav `<Link>` has no vertical padding. Add `inline-flex items-center min-h-11 py-2` to the existing className so each link is a >=44px tap target. KEEP the existing color/hover classes exactly: `text-[color:var(--color-text)] hover:text-[color:var(--color-accent)] underline-offset-4` and the `text-[14px]`. Do not change the gap or layout of the `<ul>`.

3) apps/web/components/archive/ArchiveList.tsx (L40-61): the two sort `<button>`s ("Newest first" / "Oldest first") are text-only. Add a min tap target to BOTH — e.g. `inline-flex items-center min-h-11` (plus a small `py-2` if needed) prepended to each button's className. The active/inactive color classes (the ternary referencing `--color-text` / `--color-text-muted`) MUST be preserved unchanged. Note these classNames are built via a ternary expression — add the sizing utilities so they apply in BOTH the active and inactive branches (e.g. wrap with a shared base class string, or add to both ternary arms identically).

4) apps/web/components/issue/IssueHero.tsx (~L95-103): the PDF download `<a>` is `inline-block ...` with no min-height. Change to give it a >=44px target: add `inline-flex items-center min-h-11` (replacing `inline-block`) so the tap target is 44px tall. Preserve `target`, `rel`, `download`, the `text-[color:var(--color-primary)]`, `underline underline-offset-2`, and `transition-opacity hover:opacity-75` classes unchanged.

Across all four: NO color value may be hardcoded — every color stays as the existing `var(--color-*)` references. No glyph/icon/font-size change. No behavior change.

Commit this fix alone: `fix(a11y): expand interactive controls to 44px touch targets (WCAG 2.5.5)`
  </action>
  <verify>
    <automated>grep -q "min-h-11" apps/web/components/AnchorCopyButton.tsx && grep -q "min-h-11" apps/web/components/SiteHeader.tsx && grep -q "min-h-11" apps/web/components/archive/ArchiveList.tsx && grep -q "min-h-11" apps/web/components/issue/IssueHero.tsx && grep -q "size={14}" apps/web/components/AnchorCopyButton.tsx && echo PASS</automated>
  </verify>
  <done>All four components have a >=44px (min-h-11/min-w-11) tap target; AnchorCopyButton icon stays size={14}; all existing aria-label, focus-visible, color (var(--color-*)), and behavior classes preserved; no hardcoded colors introduced.</done>
</task>

<task type="auto">
  <name>Task 3: Skip-to-content link (FIX 3) — own commit</name>
  <files>apps/web/app/layout.tsx, apps/web/app/globals.css</files>
  <action>
Add a "Skip to content" anchor as the FIRST focusable element inside `<body>` in apps/web/app/layout.tsx, using the sr-only → focus:not-sr-only pattern so it is visually hidden until keyboard-focused, then visible with a clear focus style. It targets the existing `<main id="main">` (layout.tsx L121).

Implementation in layout.tsx: immediately inside `<body ...>` (BEFORE `<ConvexClientProvider>`), insert:
```tsx
<a
  href="#main"
  className="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-50 focus:rounded focus:bg-[color:var(--color-bg)] focus:px-4 focus:py-2 focus:font-ui focus:text-[14px] focus:text-[color:var(--color-text)]"
>
  Skip to content
</a>
```
Notes:
- It MUST be the first child of `<body>` so it is the first thing keyboard users reach.
- The existing global `:focus-visible` ring (globals.css L87-90) applies automatically when the link is focused — do NOT add a manual outline; rely on that ring. The `focus:*` classes above handle only visibility, position, and a readable background so the visible text isn't lost against page content.
- All colors are `var(--color-*)` references — NO hardcoded colors.
- Copy is exactly "Skip to content" (sentence case, no period — matches Jesse-voice no-exclamation conventions).

Tailwind v4 sr-only check: confirm Tailwind core `sr-only` and `not-sr-only` utilities resolve in this build (they ship in core for `@import "tailwindcss"`). Quick check: run `pnpm --filter web build` is heavy — instead grep is not reliable for generated utilities, so reason as follows: Tailwind v4 core includes both utilities by default. If, and ONLY if, the executor finds they do not apply (verified by inspecting Tailwind output or a failing render), add a minimal fallback to the WCAG banner section of globals.css:
```css
.sr-only { position:absolute; width:1px; height:1px; padding:0; margin:-1px; overflow:hidden; clip:rect(0,0,0,0); white-space:nowrap; border:0; }
.focus\:not-sr-only:focus { position:static; width:auto; height:auto; padding:inherit; margin:inherit; overflow:visible; clip:auto; white-space:normal; }
```
Default to NOT adding the fallback (Tailwind core provides it); add it only if needed.

Commit this fix alone: `fix(a11y): add skip-to-content link as first focusable element (WCAG 2.4.1)`
  </action>
  <verify>
    <automated>grep -q 'href="#main"' apps/web/app/layout.tsx && grep -q 'Skip to content' apps/web/app/layout.tsx && grep -q 'sr-only' apps/web/app/layout.tsx && echo PASS</automated>
  </verify>
  <done>layout.tsx renders an `<a href="#main">Skip to content</a>` as the first child of `<body>`, hidden via sr-only and revealed on focus with a var(--color-*)-themed visible style; it targets the existing single `<main id="main">`; no hardcoded colors; the existing :focus-visible ring is relied upon (not duplicated).</done>
</task>

<task type="auto">
  <name>Task 4: Single &lt;main&gt; landmark reconciliation (FIX 4) — own commit</name>
  <files>apps/web/app/charities/page.tsx, apps/web/app/archive/page.tsx</files>
  <action>
Goal: exactly ONE `<main>` landmark per page. The layout (layout.tsx L121) already provides the sole `<main id="main">` wrapping all route children. Therefore route pages must NOT introduce a second `<main>` (that nests main-in-main = duplicate-landmark WCAG 1.3.1 violation).

Current state (verified during planning):
- about/page.tsx: uses `<article>` — CORRECT, leave unchanged (do not touch).
- charities/page.tsx (L46): uses `<div>` — already does NOT add a second main. Leave it as `<div>`. Do NOT change `<div>`→`<main>` (that would CREATE the very duplicate-landmark bug we are eliminating).
- archive/page.tsx (L35): uses `<main className="mx-auto max-w-[1100px] px-4 py-12 md:px-6 lg:px-8">` — this is a nested main inside the layout's `<main>` = EXISTING duplicate-landmark violation. FIX IT: change the top-level `<main ...>` to `<div ...>` (keep all className and children identical; change only the tag and its closing tag `</main>`→`</div>`).

So this task's net change is:
1. archive/page.tsx: `<main ...>` → `<div ...>` (open + close tags), classes unchanged.
2. charities/page.tsx: confirm it remains a `<div>` with no second main. If the audit notes asked to swap div→main, OVERRIDE that — the correct reconciliation (per about's established correct pattern and the single-landmark rule) is to keep charities as a `<div>` and instead remove archive's stray nested `<main>`. Add a brief one-line code comment in charities/page.tsx noting the single-main-lives-in-layout rationale so a future editor doesn't "fix" it back.

Result: every route (home, archive, charities, about, issue, shop, etc.) has exactly one `<main id="main">` — the one in layout.tsx — and the skip-link `#main` target (Task 3) resolves unambiguously.

Commit this fix alone: `fix(a11y): enforce single <main> landmark per page (WCAG 1.3.1)`
  </action>
  <verify>
    <automated>test "$(grep -c '<main' apps/web/app/archive/page.tsx)" = "0" && test "$(grep -c '<main' apps/web/app/charities/page.tsx)" = "0" && test "$(grep -c 'id="main"' apps/web/app/layout.tsx)" = "1" && echo PASS</automated>
  </verify>
  <done>archive/page.tsx no longer contains a `<main>` (now `<div>`); charities/page.tsx contains no `<main>` (remains `<div>` with a rationale comment); layout.tsx remains the single `<main id="main">`; about/page.tsx untouched. Exactly one main landmark per page.</done>
</task>

</tasks>

<verification>
After all four tasks:

1. Run the unit suite and confirm no regression:
   ```bash
   pnpm --filter web test:unit
   ```
   Expected: pass/fail counts match the pre-change baseline (per STATE.md Plan 10-03: 79 pass / 29 fail, where the 29 failures are Phase 8 Wave 0 sentinels that are OUT OF SCOPE). This plan must NOT change that count in either direction. If the count changes, investigate before committing the affected task.

2. Type check:
   ```bash
   pnpm --filter web typecheck
   ```
   Expected: exits 0 (no new type errors from markup edits).

3. Constraint sanity checks:
   - `git diff --name-only` MUST NOT include apps/web/lib/theme.ts or apps/web/components/issue/GameSlot.tsx.
   - No hardcoded hex/rgb colors added: `git diff apps/web | grep '^+' | grep -iE "#[0-9a-fA-F]{3,6}|rgb\(|hsl\("` returns nothing (all colors stay as var(--color-*)).

4. Single-landmark check: `grep -rc '<main' apps/web/app/**/page.tsx apps/web/app/layout.tsx` shows only layout.tsx with a main (id="main") — no route page declares its own `<main>`.
</verification>

<success_criteria>
- prefers-reduced-motion guard present in globals.css; touches only animation/transition/scroll; no color/layout/sizing/font properties; no existing rule modified.
- All four interactive controls (copy button, nav links, sort buttons, PDF link) have >=44px tap targets; AnchorCopyButton glyph still size={14}; all aria-labels, focus-visible, and var(--color-*) classes preserved.
- Skip-to-content `<a href="#main">` is the first focusable element in `<body>`, hidden until focus, themed via var(--color-*), relying on the existing :focus-visible ring.
- Exactly one `<main id="main">` landmark per page (layout owns it); archive's stray nested main removed; charities stays a div; about untouched.
- `pnpm --filter web test:unit` and `pnpm --filter web typecheck` show no regression vs baseline.
- HARD CONSTRAINTS honored: theme.ts and GameSlot.tsx untouched; zero hardcoded colors; markup/CSS-only (no behavior/data/color changes).
- Four atomic commits, one per fix.
</success_criteria>

<output>
After completion, create `.planning/quick/260520-0kt-wcag-aa-accessibility-conformance-pass-o/260520-0kt-SUMMARY.md`
</output>
