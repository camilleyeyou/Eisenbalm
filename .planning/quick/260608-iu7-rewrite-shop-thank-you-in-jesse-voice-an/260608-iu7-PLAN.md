---
phase: quick-260608-iu7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/shop/thank-you/page.tsx
  - apps/web/__tests__/thank-you-source.test.ts
autonomous: false
requirements: [CMR-03]
must_haves:
  truths:
    - "The /shop/thank-you page renders the locked Jesse-voice copy (eyebrow 'Order received', h1 'Your lip balm is on its way.')."
    - "The page names the current featured charity inline when one is published, and falls back to \"this week's featured charity\" when the Sanity read returns null or throws."
    - "The page never uses session_id for any lookup; the value is not read into a downstream variable."
    - "The CMR-03 tripwire passes with the REFINED contract: Sanity read allowed, Convex + Stripe lookups + session_id-lookup forbidden."
    - "pnpm --filter web typecheck and the four target test suites are green."
  artifacts:
    - path: "apps/web/app/shop/thank-you/page.tsx"
      provides: "Async server component fetching latest published charity name and rendering locked copy"
      contains: "QUERY_LATEST_CHARITY_NAME"
    - path: "apps/web/__tests__/thank-you-source.test.ts"
      provides: "Refined CMR-03 source-scan tripwire (Sanity allowed, Convex/Stripe/session_id-lookup banned)"
      contains: "session_id"
  key_links:
    - from: "apps/web/app/shop/thank-you/page.tsx"
      to: "sanityClient"
      via: "import { sanityClient } from '@/lib/sanity/client'"
      pattern: "from\\s+['\"]@/lib/sanity/client['\"]"
    - from: "apps/web/app/shop/thank-you/page.tsx"
      to: "weeklyIssue charity name"
      via: "sanityClient.fetch(QUERY_LATEST_CHARITY_NAME)"
      pattern: "sanityClient\\.fetch"
---

<objective>
Rewrite /shop/thank-you in Jesse's voice and name the week's featured charity inline.

Purpose: The current thank-you page is generic and does not name the charity the buyer just funded. Jesse's voice demands the page be dry, precise, and specific — naming the actual beneficiary makes the gesture concrete. CMR-03 (the no-DB-lookup tripwire) is refined, not removed: a server-side Sanity read of the current featured charity is permitted, but session_id lookups, Convex, and Stripe API calls remain forbidden.

Output: An async server component that fetches the latest published charity name (mirroring shop/page.tsx) and renders the locked copy, plus a refined source-scan test that encodes the new contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

# Jesse voice (from CLAUDE.md): dry, precise, absurdly serious. No winking, no irony signaling, no emoji.

<interfaces>
<!-- Reuse verbatim from apps/web/app/shop/page.tsx — same GROQ, same client, same try/catch shape. -->

From apps/web/lib/sanity/client.ts:
```typescript
export const sanityClient: SanityClient // useCdn: true, perspective: 'published'
```

From apps/web/app/shop/page.tsx (the pattern to mirror):
```typescript
export const revalidate = 60

const QUERY_LATEST_CHARITY_NAME = groq`
  *[_type == "weeklyIssue" && status == "published"]
  | order(issueNumber desc)[0] {
    "charityName": charity->name
  }
`

// in the component body:
let charityName: string | null = null
try {
  const result = await sanityClient.fetch<{ charityName: string | null } | null>(
    QUERY_LATEST_CHARITY_NAME,
  )
  charityName = result?.charityName ?? null
} catch {
  charityName = null
}
```

Current thank-you page styling to PRESERVE (className strings + CSS-var structure):
- eyebrow: `<p className="font-ui text-[14px] uppercase tracking-[0.1em] text-[color:var(--color-text-muted)]">`
- h1: `<h1 className="mt-3 font-display text-[28px] md:text-[36px] font-semibold leading-[1.15] text-[color:var(--color-text)]">`
- body: `<p className="mt-6 font-body text-[18px] leading-[1.65] text-[color:var(--color-text)]">`
- return link wrapper: `<p className="mt-8 font-body text-[16px] leading-[1.65]">` with `<Link href="/" className="text-[color:var(--color-accent)] underline-offset-4 hover:underline">`
- section wrapper: `<section className="mx-auto max-w-[860px] px-4 md:px-6 lg:px-8 py-16">`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite thank-you page as async server component with charity fetch + Jesse-voice copy</name>
  <files>apps/web/app/shop/thank-you/page.tsx</files>
  <action>
Rewrite apps/web/app/shop/thank-you/page.tsx as an async server component that fetches the current featured charity name and renders the locked copy (per locked decisions D-02, D-03, D-04).

1. Imports: keep `import type { Metadata } from 'next'` and `import Link from 'next/link'`. Add `import { groq } from 'next-sanity'` and `import { sanityClient } from '@/lib/sanity/client'` (mirror shop/page.tsx imports exactly).

2. Add `export const revalidate = 60` to mirror shop/page.tsx ISR caching, so the charity callout refreshes within 60s of a new issue publishing.

3. Keep the existing `metadata` export verbatim (title 'Thank you', description 'Order received.', robots noindex/nofollow).

4. Add the inline GROQ projection above the component, copied verbatim from shop/page.tsx:
   ```ts
   const QUERY_LATEST_CHARITY_NAME = groq`
     *[_type == "weeklyIssue" && status == "published"]
     | order(issueNumber desc)[0] {
       "charityName": charity->name
     }
   `
   ```

5. Replace the entire CMR-03 header docblock with a docblock documenting the REFINED contract: the page MAY do a server-side Sanity read of the current featured charity, but MUST NOT use session_id for any lookup, MUST NOT import/call Convex, and MUST NOT call the Stripe API. Note the source-scan tripwire path.

6. Component signature: `export default async function ThankYouPage()`. REMOVE the `PageProps` interface and the `searchParams` param entirely — session_id is not needed for types and dropping it is the cleanest way to guarantee no downstream use (per D-04). Do NOT `await searchParams` anywhere.

7. In the body, fetch the charity name with the try/catch → null pattern from shop/page.tsx (see <interfaces>).

8. Render the locked copy (D-03), PRESERVING the existing className strings / CSS-var structure (see <interfaces>):
   - eyebrow `<p>`: `Order received`
   - h1: `Your lip balm is on its way.`  (note: "on its way", not the old "on the way")
   - body `<p>`: `This week, every dollar of proceeds goes to {charityName ?? "this week's featured charity"} — not the margin, the proceeds, in full. A small purchase pointed at a serious end. Thank you for pointing it there.`  — interpolate charityName as a JSX expression; use the literal em dash `—`. Escape any apostrophes in JSX (e.g. `this week&apos;s` only appears inside the fallback string literal, which is a plain JS string and needs no JSX escaping; the body sentence has no raw apostrophes).
   - ADD a second, smaller/muted receipt line `<p>` BELOW the body and ABOVE the return link: `A receipt will follow by email.` Style it consistent with the page using a smaller muted treatment, e.g. `className="mt-4 font-body text-[14px] leading-[1.6] text-[color:var(--color-text-muted)]"`.
   - return link `<p>` + `<Link href="/">`: `Return to the latest issue.` (preserve existing classes)

Constraints: tracked-source only — touch ONLY this file in Task 1. No Convex, no Stripe, no email changes. Honor Jesse voice (no emoji, no winking). Do not read or use session_id.
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Eisenbalm && pnpm --filter web typecheck && pnpm --filter web test:unit -- thank-you-source.test.ts stripe-webhook.test.ts stripe-webhook-source.test.ts stripe-webhook-idempotency.test.ts</automated>
  </verify>
  <done>thank-you/page.tsx is an async server component (no searchParams param, no session_id reference). It imports sanityClient + groq, defines QUERY_LATEST_CHARITY_NAME, fetches charityName with try/catch → null, and renders the locked copy with charityName interpolation + the "this week's featured charity" fallback, the new receipt line, and the Return link. Existing className/CSS-var structure preserved. typecheck passes; all four target suites green (note: thank-you-source.test.ts requires Task 2's refinements to pass — both tasks land before the verify command is final-green).</done>
</task>

<task type="auto">
  <name>Task 2: Refine CMR-03 tripwire to the new contract</name>
  <files>apps/web/__tests__/thank-you-source.test.ts</files>
  <action>
Refine apps/web/__tests__/thank-you-source.test.ts to encode the REFINED CMR-03 contract (per locked decision D-04). The page MAY now do a server-side Sanity read; it must still NOT use session_id for lookups, Convex, or Stripe.

1. Update the header docblock to document the refined contract: the thank-you page MAY do a server-side Sanity read of the current featured charity (to name the beneficiary), but MUST NOT look anything up by session_id, MUST NOT touch Convex, and MUST NOT call the Stripe API. State that the webhook remains the authoritative fulfillment event.

2. KEEP the `describe` block and KEEP the "thank-you page file exists at the expected path" test (file-exists test).

3. REMOVE the Sanity-ban test entirely — drop the `it('does NOT import from @sanity/client or @/lib/sanity (no Sanity read)', ...)` block including its three assertions (`@sanity/client`, `@/lib/sanity`, `sanityClient.fetch`). A Sanity read is now permitted.

4. KEEP the Convex-ban test verbatim (`does NOT import convex modules`).

5. KEEP the Stripe-ban test verbatim (`does NOT call stripe.checkout.sessions.retrieve`).

6. The existing `does NOT call fetch()` test bans the bare `fetch(` token. A Sanity read uses `sanityClient.fetch(...)` which also matches `/\bfetch\s*\(/`. To keep a meaningful "no arbitrary HTTP" guard without breaking on the now-allowed Sanity `.fetch()`, REPLACE that assertion so it bans only a raw global `fetch(` not preceded by a `.` — e.g. assert `source` does not match `/(?<![.\w])fetch\s*\(/`. Keep the test's intent comment updated to "no arbitrary global fetch; sanityClient.fetch is allowed".

7. ADD a new test asserting the source does not use session_id in a lookup. Simplest robust form: assert the file body does not contain the substring `session_id` at all (Task 1 drops the param entirely, so this holds). Use `expect(source).not.toMatch(/session_id/)` with an `it('does NOT reference session_id (no session-based lookup)', ...)` block and a comment explaining that session_id lookups invite enumeration attacks and are forbidden.

Constraints: tracked-source only — touch ONLY this test file in Task 2.
  </action>
  <verify>
    <automated>cd /Users/user/Desktop/Eisenbalm && pnpm --filter web test:unit -- thank-you-source.test.ts</automated>
  </verify>
  <done>thank-you-source.test.ts no longer bans Sanity imports/fetch; still bans Convex + Stripe; bans raw global fetch( but allows sanityClient.fetch(); asserts the source contains no session_id; keeps the file-exists test; header comment documents the refined contract. Suite passes against the Task 1 page.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The /shop/thank-you page now fetches the latest published charity name via Sanity and renders the Jesse-voice copy ("Order received" / "Your lip balm is on its way." / proceeds-to-{charity} body / "A receipt will follow by email." / "Return to the latest issue."). The CMR-03 tripwire is refined to permit the Sanity read while still forbidding Convex, Stripe, and session_id lookups.</what-built>
  <how-to-verify>
    1. Deploy the change (or run the web app) so the live /shop/thank-you route is reachable.
    2. Visit /shop/thank-you (a trailing `?session_id=...` may be present from a real Stripe redirect — it must be ignored).
    3. Confirm the eyebrow reads "Order received", the h1 reads "Your lip balm is on its way.", and the body sentence names the current week's featured charity (the same name shown on /shop). If no issue is published, confirm it falls back to "this week's featured charity".
    4. Confirm the smaller muted line "A receipt will follow by email." appears below the body, and "Return to the latest issue." links to /.
    5. Confirm the page does not flicker (server-rendered, charity name already in the HTML) and the styling matches the rest of the site.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any copy/charity-name/styling issues.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter web typecheck` passes (no type errors from removed searchParams param or new fetch).
- `pnpm --filter web test:unit -- thank-you-source.test.ts stripe-webhook.test.ts stripe-webhook-source.test.ts stripe-webhook-idempotency.test.ts` — all four suites green.
- Source scan: thank-you/page.tsx imports `@/lib/sanity/client`, calls `sanityClient.fetch`, contains no `session_id`, no `convex`, no `stripe.checkout`.
</verification>

<success_criteria>
- thank-you/page.tsx renders the locked Jesse-voice copy with the live featured-charity name and a null-safe fallback.
- session_id is never used for a lookup (param dropped entirely).
- Refined CMR-03 tripwire passes and encodes the new contract (Sanity allowed; Convex/Stripe/session_id forbidden).
- Only the two tracked-source files changed; no Convex/email/Stripe machinery touched.
- Human verifies the live page names the charity and matches site styling.
</success_criteria>

<output>
After completion, create `.planning/quick/260608-iu7-rewrite-shop-thank-you-in-jesse-voice-an/260608-iu7-SUMMARY.md`
</output>
