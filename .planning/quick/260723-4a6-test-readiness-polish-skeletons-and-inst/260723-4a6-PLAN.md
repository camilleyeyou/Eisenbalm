---
phase: quick-260723-4a6
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [QUICK-260723-4a6]
files_modified:
  # ── Task 1: first impressions ──
  - apps/dispatch-control/components/ui/Skeleton.tsx
  - apps/dispatch-control/components/ui/DocumentTitle.tsx
  - apps/dispatch-control/app/icon.svg
  - apps/dispatch-control/app/layout.tsx
  - apps/dispatch-control/app/sign-in/[[...sign-in]]/page.tsx
  - apps/dispatch-control/app/(dashboard)/issues/loading.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/loading.tsx
  - apps/dispatch-control/app/(dashboard)/my-tasks/loading.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/loading.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/loading.tsx
  - apps/dispatch-control/app/(dashboard)/registry/loading.tsx
  - apps/dispatch-control/app/(dashboard)/finance/loading.tsx
  - apps/dispatch-control/app/(dashboard)/config/loading.tsx
  - apps/dispatch-control/app/(dashboard)/settings/loading.tsx
  - apps/dispatch-control/app/(dashboard)/eval-center/loading.tsx
  - apps/dispatch-control/app/(dashboard)/signal-desk/loading.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/WorkspaceOutline.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/ContextPanel.tsx
  # ── Task 2: palette + shortcuts + confirms ──
  - apps/dispatch-control/components/ui/ConfirmDialog.tsx
  - apps/dispatch-control/components/CommandPalette.tsx
  - apps/dispatch-control/app/(dashboard)/layout.tsx
  - apps/dispatch-control/components/Masthead.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RecoveryRail.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RerollButton.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/AgentPromptEditorView.tsx
  - apps/dispatch-control/components/inspector/InspectorFooter.tsx
  - apps/dispatch-control/__tests__/RecoveryRail.test.tsx
  - apps/dispatch-control/__tests__/InspectorFooter.test.tsx
  # ── Task 3: Gate-1 correctness ──
  - packages/pipeline/src/eisenbalm_pipeline/api/signoffs.py
  - packages/pipeline/tests/test_signoffs_endpoints.py
autonomous: true

must_haves:
  truths:
    - "Every dashboard navigation paints an instant layout-matched skeleton instead of a blank frame or a bare 'Loading…' string"
    - "Signing in shows a branded two-panel screen (ink wordmark band + Clerk box on rail bg), not a bare centered Clerk box on neutral-50"
    - "Every browser tab shows a route-specific title ('… — Dispatch Control') and the app shows a favicon"
    - "Pressing ⌘K / Ctrl+K anywhere in the dashboard opens a command palette that keyboard-navigates to routes, issues, and recent runs"
    - "A discoverable ⌘K chip in the masthead opens the same palette"
    - "Destructive / unsaved-change confirmations render as a branded in-app dialog, never window.confirm"
    - "On an issue workspace, number keys 1-5 jump between the five stages (only when no field is focused and no modifier is held)"
    - "A run paused at Gate 1 (awaiting-review + completedAt null) or a failed run cannot receive a sounds-human sign-off — the endpoint 409s run_not_reviewable"
    - "A run paused at Gate 1 shows a paused-not-failed recovery surface in Run Details (already shipped — regression-locked here)"
  artifacts:
    - path: "apps/dispatch-control/components/ui/Skeleton.tsx"
      provides: "Shimmer skeleton primitive + SkeletonLine/SkeletonCard composed helpers"
      min_lines: 25
    - path: "apps/dispatch-control/app/icon.svg"
      provides: "64x64 ink square + vermilion diagonal slash favicon (D/C wordmark identity)"
      contains: "svg"
    - path: "apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/loading.tsx"
      provides: "Workspace-frame skeleton (tab row + 3-column grid) painted on every stage switch"
      min_lines: 10
    - path: "apps/dispatch-control/components/ui/ConfirmDialog.tsx"
      provides: "1c-styled modal + promise-based useConfirm()/ConfirmProvider for swapping the 5 window.confirm sites"
      min_lines: 60
    - path: "apps/dispatch-control/components/CommandPalette.tsx"
      provides: "In-house ⌘K overlay + provider: routes/issues/recent-runs, keyboard nav, existing queries only"
      min_lines: 120
    - path: "apps/dispatch-control/app/sign-in/[[...sign-in]]/page.tsx"
      provides: "Branded two-panel sign-in with Clerk appearance mapped to 1c tokens"
      min_lines: 20
    - path: "packages/pipeline/src/eisenbalm_pipeline/api/signoffs.py"
      provides: "run_not_reviewable status guard in the sounds-human branch"
      contains: "run_not_reviewable"
  key_links:
    - from: "apps/dispatch-control/components/Masthead.tsx"
      to: "CommandPalette provider open()"
      via: "useCommandPalette() context consumed by the ⌘K chip"
      pattern: "useCommandPalette"
    - from: "apps/dispatch-control/components/CommandPalette.tsx"
      to: "api.issues.listForWorkspace + api.runs.listRecentForWorkspace"
      via: "useQuery (existing queries, no new Convex functions)"
      pattern: "listRecentForWorkspace"
    - from: "apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx"
      to: "stage hrefs (issueStoryHref…issueApprovalHref)"
      via: "window keydown 1-5 → router.push, guarded against typing/modifiers"
      pattern: "router\\.push"
    - from: "packages/pipeline/src/eisenbalm_pipeline/api/signoffs.py"
      to: "409 run_not_reviewable"
      via: "already-fetched run.status / run.completedAt predicate in the sounds-human branch"
      pattern: "run_not_reviewable"
---

<objective>
The operator's client is sending external testers. This plan closes the last first-impression
and correctness gaps before that: (1) instant-paint skeletons + branded sign-in + titles/favicon,
(2) one flagship ⌘K command palette plus styled confirm dialogs and stage keyboard shortcuts, and
(3) a server-side Gate-1 sign-off guard.

Purpose: make the console feel finished on first load, give it one memorable power-user interaction,
and stop a paused/failed run from receiving a bogus voice sign-off.
Output: a skeleton primitive + 11 `loading.tsx` segments, a branded sign-in, per-route titles + favicon,
a ⌘K palette + masthead chip, a 1c ConfirmDialog swapped into all 5 `window.confirm` sites, issue-stage
1-5 shortcuts, and a `run_not_reviewable` guard in `signoffs.py`.

VERIFIED RECON NOTE (read before touching Task 3): the recon's Tier-3 items (a)/(b) are PARTIALLY
already shipped by quick 260719-w6o. Specifically:
  - RunDetail.tsx:268 already computes `isPausedAtGate1 = run.status === 'awaiting-review' && run.completedAt == null`
    and line 363 already mounts `<RecoveryRail … isPausedAtGate1={true} />`; RecoveryRail.tsx:252-277 already
    renders the "Paused for your decision" (paused-not-failed) copy. Task 3b is therefore VERIFY + REGRESSION-LOCK,
    NOT re-implementation.
  - signoffs.py already has the F5 `no_sanity_issue` content-existence guard (lines 100-108, runs for BOTH kinds).
    It does NOT have a run-STATUS guard. Task 3a adds ONLY that status guard, in the sounds-human branch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@./CLAUDE.md

<!-- Design system & shell conventions (1c tokens, bracket-pixel Tailwind, 2px corners) live in
     the files below; every new file MUST carry a provenance header comment citing quick 260723-4a6. -->
@apps/dispatch-control/app/layout.tsx
@apps/dispatch-control/app/(dashboard)/layout.tsx
@apps/dispatch-control/components/Masthead.tsx
@apps/dispatch-control/components/onboarding/OnboardingProvider.tsx
@apps/dispatch-control/components/ui/HelpTip.tsx
@apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase. Use directly; no exploration. -->

Provider-in-server-layout precedent (mirror this EXACTLY for CommandPaletteProvider + ConfirmProvider):
  app/(dashboard)/layout.tsx is a Server Component that renders CLIENT providers
  (InspectorProvider → OnboardingProvider → shell div → MobileNavDrawer + Masthead + main).
  Adding two more client-provider wrappers KEEPS it a Server Component. Both new providers must wrap
  the shell `<div>` so BOTH the Masthead (⌘K chip) AND every route child (the 5 confirm sites) are inside them.
  OnboardingProvider.tsx is the canonical shape: createContext + useContext hook that throws if unmounted.

z-order ladder (verified — do NOT violate):
  HelpTip popover  = z-[60]  (portaled to document.body, position:fixed)
  inspector panel  = z-40
  drawer / tour    = z-50..z-[60]
  ConfirmDialog    = z-[70]   (NEW — above HelpTip)
  CommandPalette   = z-[75]   (NEW — top of stack)

Masthead right cluster (Masthead.tsx is already 'use client'):
  The ⌘K chip goes into the right cluster BEFORE `<div className="relative shrink-0">` (the My Tasks trigger),
  `hidden md:flex` (mobile uses the drawer). It calls `useCommandPalette().open()`.

Issue workspace stage tabs (issues/[issueNumber]/layout.tsx → FrameChrome):
  STAGE_TABS = [Story, Draft, Fact Check, Voice, Approval] with hrefFor = issueStoryHref, issueDraftHref,
  issueFactCheckHref, issueVoiceHref, issueApprovalHref (from lib/issueRouteResolver.ts, pure). Index i (0-4)
  maps to key `String(i+1)`. `n` (issueNumber) is in scope; `usePathname()`/`useRouter()` already imported here.

Palette data sources (EXISTING queries — NO new Convex functions):
  Issues: useQuery(api.issues.listForWorkspace, { workspace_id: DEFAULT_WORKSPACE_ID })
          → rows { issueNumber, published, held }; href = issueHref(n) = `/issues/${n}` (issueRouteResolver.ts)
  Runs:   useQuery(api.runs.listRecentForWorkspace, { workspace_id: DEFAULT_WORKSPACE_ID })
          → rows { runId, status, _id }; href = `/run-monitor/runs/${runId}` (RunsTable.tsx precedent)
  Static routes: reuse the canonical nav href list from components/AppSidebar.tsx (single source of truth) —
          do NOT hardcode a second list. Add "How to use" (/how-to-use) since it isn't a primary nav item.

The 5 window.confirm sites (exact call shapes):
  1. ReviewDeskRunView.tsx switchViewMode()  — `viewMode==='edit' && next!=='edit' && dirty[selectedSection] && !window.confirm('…unsaved…')`
  2. ReviewDeskRunView.tsx handleChipSelect() — `viewMode==='edit' && dirty[selectedSection] && !window.confirm('…switch sections…')`
  3. ReviewDeskRunView.tsx handleEditSection()— same dirty guard as #2
  4. RecoveryRail.tsx RestartAction.handleRestart() — pre-action confirm (publisher vs section copy)
  5. RerollButton.tsx handleReroll()          — pre-action confirm
  6. AgentPromptEditorView.tsx — TWO calls: L152 (agentKey-switch heads-up, MUST still fire even though nav already happened) and L169 requestStopEditing() (dirty guard)
  7. InspectorFooter.tsx RestartFooterAction.handleClick() — pre-action confirm
  (RecoveryRail + InspectorFooter already have __tests__ that drive confirm — update those to click the dialog button.)

signoffs.py — run object already fetched at top of record_sign_off (line 85):
  run = await _cc.convex_query(http, "pipelineRuns:byRunId", {"runId": run_id})
  run carries: status ('running'|'awaiting-review'|'complete'|'failed'), completedAt (ms | absent/None),
  sanityIssueId, issueNumber. The existing F5 guard (lines 100-108) 409s `no_sanity_issue` for BOTH kinds.
  Paused-at-Gate-1 (editor.py:383) writes status='awaiting-review' WITHOUT completedAt.
  Final review (publisher/__init__.py:225-226) writes status='awaiting-review' WITH completedAt + sanityIssueId.
  → predicate `status=='failed' OR (status=='awaiting-review' and not completedAt)` blocks pause/fail,
    allows the genuine final-review state (completedAt set).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: First impressions — skeleton primitive + instant-nav loading.tsx, branded sign-in, titles + favicon</name>
  <files>
    apps/dispatch-control/components/ui/Skeleton.tsx (new),
    apps/dispatch-control/components/ui/DocumentTitle.tsx (new),
    apps/dispatch-control/app/icon.svg (new),
    apps/dispatch-control/app/layout.tsx,
    apps/dispatch-control/app/sign-in/[[...sign-in]]/page.tsx,
    apps/dispatch-control/app/(dashboard)/{issues,issues/[issueNumber],my-tasks,run-monitor,prompt-lab,registry,finance,config,settings,eval-center,signal-desk}/loading.tsx (11 new),
    apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx,
    apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/WorkspaceOutline.tsx,
    apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/ContextPanel.tsx
  </files>
  <action>
Provenance: every NEW file gets a header comment citing `quick 260723-4a6`. Match 1c conventions exactly
(bracket tokens `bg-[color:var(--color-*)]`, `rounded-[2px]`, `font-[family-name:var(--font-*)]`).

1a. **Skeleton primitive** — `components/ui/Skeleton.tsx` (Server Component; no 'use client'):
   - `Skeleton({ className })`: a `<div>` with `animate-pulse rounded-[2px] bg-[color:var(--color-card-alt)]` (Tailwind
     default `animate-pulse` is available). Width/height/shape come entirely via `className` (e.g. `h-4 w-40`).
   - `SkeletonLine({ className })`: a Skeleton defaulting to `h-3 w-full` (one text-line block).
   - `SkeletonCard({ className })`: a bordered card `rounded-[2px] border border-[color:var(--color-faint)]
     bg-[color:var(--color-card)] p-4` containing 3 SkeletonLines of varied widths. Keep the whole file tiny (<60 lines).

1b. **loading.tsx for 11 route segments** (all Server Components rendering layout-matched skeletons):
   `(dashboard)/{issues, my-tasks, run-monitor, prompt-lab, registry, finance, config, settings, eval-center,
   signal-desk}/loading.tsx` + `(dashboard)/issues/[issueNumber]/loading.tsx`.
   - Each: a header SkeletonLine (`h-6 w-56`) + a few card/row blocks matching that page's shape (2-4 SkeletonCards
     or a stack of SkeletonLines). Keep each file small and shared-primitive-driven.
   - The `issues/[issueNumber]/loading.tsx` MUST mirror the workspace frame (fires on every stage switch): a
     "Back to Issues" line, an `Issue —` heading line, a horizontal ROW of 5 tab-shaped Skeletons (`h-11 w-28`),
     then a `grid grid-cols-1 lg:grid-cols-[232px_minmax(0,1fr)_320px] gap-6` of 3 SkeletonCard columns — so a
     stage switch paints the frame instantly instead of blanking.

1c. **Replace HIGH-TRAFFIC inline "Loading…" strings with skeletons** — ONLY these named sites (do NOT chase all ~30):
   - `ReviewDeskRunView.tsx`: the top `{loading && <p>Loading draft…</p>}` (≈L496-498) → a galley-shaped skeleton
     (a heading line + 4-6 SkeletonLines). Leave the two inner `draft ? … : <p>Loading draft…</p>` fallbacks as-is
     unless trivial. Preserve the never-blank convention: skeleton = loading; keep explicit empty-state TEXT for
     loaded-and-empty (e.g. "No tracked claims…", "Preview unavailable.") — do NOT skeletonize those.
   - `WorkspaceOutline.tsx`: its 'Loading outline…' branch → a short stack of SkeletonLines.
   - `ContextPanel.tsx`: its default/loading body → a couple of SkeletonLines.
   - Also swap the loading branches in these files IF a plain "Loading…"/"Loading runs…" string is present:
     FactCheckScreen (coverage 'Loading…'), DecisionRail loading lines, StoryBriefScreen, RunsTable
     ('Loading runs…' at RunsTable.tsx L72-77), ReviewQueue loading rows, MyTasksScreen. Locate each via a
     `grep -rn "Loading" <file>` before editing; if a file already renders a skeleton/spinner, leave it. These are
     client components — import Skeleton normally (it has no 'use client' and works in both).

1d. **Branded sign-in** — `app/sign-in/[[...sign-in]]/page.tsx`: two-panel layout, page bg
   `bg-[color:var(--color-rail)]` (NOT neutral-50). Left panel `hidden md:flex` = ink band
   (`bg-[color:var(--color-ink)]`) with the wordmark `DISPATCH<span className="text-[color:var(--color-vermilion)]">/</span>CONTROL`
   (font-ui, matching Masthead.tsx L258-260) + a one-line description: "The editorial console behind The Eisenbalm
   Dispatch — nine agents draft, one human approves." Right panel = the Clerk `<SignIn />` with an `appearance` prop
   mapping to 1c: `variables: { colorPrimary: 'var(--color-ink)' or cobalt, borderRadius: '2px',
   fontFamily: 'var(--font-ui)' }`. Keep it a Server Component (no 'use client' needed).

1e. **Titles + favicon**:
   - `app/layout.tsx` metadata → `{ title: { template: '%s — Dispatch Control', default: 'Dispatch Control' },
     description: <keep existing> }` (replace the current single `title` string).
   - `app/icon.svg` (new): a 64×64 SVG — ink `#17140e` square fill + a bold vermilion `#e8471d` diagonal slash band
     (a rotated `<rect>` or a `<path>`), nothing else. This matches the wordmark's D/C slash. Next.js serves
     `app/icon.svg` as the favicon automatically.
   - Per-route titles: create ONE tiny client helper `components/ui/DocumentTitle.tsx` — `'use client'`,
     `useEffect(() => { document.title = `${title} — Dispatch Control` }, [title])`, renders null. Use it for
     CLIENT-only pages/frames that cannot export `metadata`. For any route whose `page.tsx` (or a nearby server
     file) is a Server Component, prefer a static `export const metadata = { title: '…' }` instead of DocumentTitle.
   - Apply these titles (executor picks metadata-export vs DocumentTitle per file after a quick check of whether the
     file starts with 'use client'): Issues → "Issues"; My Tasks → "My Tasks"; Run Monitor → "Run Details";
     Prompt Lab → "Agent Instructions"; Registry → "Editorial Memory"; Eval Center → "Quality Tests";
     Signal Desk → "Signal Desk"; Finance → "Finance"; Config → "Configuration"; Settings → "Settings".
     (Nav display names come from lib/nomenclature.ts WORKBENCH_NAV_LABELS — reuse those constants where they exist.)
   - Issue workspace: in FrameChrome (issues/[issueNumber]/layout.tsx — a CLIENT component) mount `<DocumentTitle
     title={`Issue ${n} · ${currentStageSegment ? <Title-Case stage> : 'Overview'}`} />` so tabs read
     "Issue 12 · Draft — Dispatch Control". Derive the stage label from the existing `currentStageSegment` and the
     STAGE_TABS labels already in that file. (This file edit also happens in Task 2 for shortcuts — coordinate so
     both land; they touch different regions.)
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control build</automated>
  </verify>
  <done>
    11 loading.tsx segments exist; Skeleton.tsx + DocumentTitle.tsx + app/icon.svg exist; sign-in is two-panel on
    rail bg; root metadata uses the `%s — Dispatch Control` template; the named high-traffic "Loading…" strings now
    render skeletons; `pnpm --filter dispatch-control build` succeeds (favicon + metadata compile).
  </done>
</task>

<task type="auto">
  <name>Task 2: ⌘K command palette + masthead chip + stage 1-5 shortcuts + 1c ConfirmDialog swapped into all 5 confirm sites</name>
  <files>
    apps/dispatch-control/components/ui/ConfirmDialog.tsx (new),
    apps/dispatch-control/components/CommandPalette.tsx (new),
    apps/dispatch-control/app/(dashboard)/layout.tsx,
    apps/dispatch-control/components/Masthead.tsx,
    apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx,
    apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RecoveryRail.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RerollButton.tsx,
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/AgentPromptEditorView.tsx,
    apps/dispatch-control/components/inspector/InspectorFooter.tsx,
    apps/dispatch-control/__tests__/RecoveryRail.test.tsx,
    apps/dispatch-control/__tests__/InspectorFooter.test.tsx
  </files>
  <action>
Provenance header comment citing `quick 260723-4a6` on both new files. ZERO new npm deps. The
`(dashboard)/layout.tsx` MUST stay a Server Component (it only renders new client providers).

2a. **ConfirmDialog + promise-based useConfirm()** — `components/ui/ConfirmDialog.tsx` (`'use client'`):
   - Export a `ConfirmProvider` (createContext + `useConfirm()` hook that throws if unmounted — mirror
     OnboardingProvider.tsx's shape). `useConfirm()` returns `(opts: { title; body?; confirmLabel?; cancelLabel?;
     tone?: 'default'|'danger' }) => Promise<boolean>`. The provider holds the pending request + resolver in state
     and renders ONE dialog.
   - Dialog visuals: `fixed inset-0 z-[70]` with a `bg-[color:var(--color-ink)]/40` backdrop; centered card
     `rounded-[2px] border border-[color:var(--color-faint)] bg-[color:var(--color-card)]` with title (font-ui,
     uppercase-tracking), body text, and Cancel / Confirm buttons each `min-h-[44px]`. `role="dialog"`
     `aria-modal="true"` `aria-labelledby` the title id. On open: focus the Confirm button. Escape AND backdrop
     click resolve `false`; Confirm resolves `true`; Cancel resolves `false`. Danger tone → vermilion confirm button.
   - Mount `<ConfirmProvider>` in `(dashboard)/layout.tsx` wrapping the shell `<div>` (inside OnboardingProvider) so
     all route children can call `useConfirm()`.

2b. **CommandPalette + provider** — `components/CommandPalette.tsx` (`'use client'`, in-house, zero deps):
   - Export `CommandPaletteProvider` (context with `open()`/`close()`/`isOpen`; `useCommandPalette()` hook throws if
     unmounted) that ALSO renders `<CommandPalette />` once. Mount the provider in `(dashboard)/layout.tsx` wrapping
     the shell `<div>` (inside OnboardingProvider, sibling of ConfirmProvider) so the Masthead chip and all routes
     share it. A window `keydown` listener in the provider opens on `(e.metaKey || e.ctrlKey) && e.key === 'k'`
     (preventDefault) — this is the ONE listener allowed to fire with a modifier held.
   - Overlay: `fixed inset-0 z-[75]`; a click-outside backdrop; the panel `top-[15vh]` centered,
     `w-[min(640px,calc(100vw-32px))]`, 1c card. Top: an autofocused `<input>`. Below: results list, max ~10 visible,
     `scrollIntoView` on the active row. Keyboard: ArrowUp/ArrowDown move active index, Enter navigates
     (`router.push(href)` then `close()`), Escape closes. ARIA: input `role="combobox"` + `aria-activedescendant`;
     list `role="listbox"`; rows `role="option"`. Footer hint row: "↑↓ navigate · ↵ open · esc close".
   - Sources (all client-side, EXISTING queries — see <interfaces>):
     (1) static routes from AppSidebar's canonical nav list + "How to use" (/how-to-use);
     (2) issues via `api.issues.listForWorkspace` → label `Issue {n}` → issueHref(n); when the filter text is
         non-empty, also emit per-stage sub-entries `Issue {n} · Draft` etc. (5 stage hrefs) for the matched issue;
     (3) recent runs via `api.runs.listRecentForWorkspace` → label `Run {runId.slice(0,8)} · {status}` →
         `/run-monitor/runs/{runId}`.
   - Scoring: case-insensitive substring; `label.startsWith(q)` ranks above `label.includes(q)`; stable ordering;
     cap the rendered list (~10). Empty query shows the static routes + a few recent runs.

2c. **Masthead ⌘K chip** — `Masthead.tsx` (already 'use client'): add a slim chip button into the right cluster,
   placed BEFORE the `<div className="relative shrink-0">` My Tasks trigger, `hidden md:flex` (mobile uses the
   drawer). Style it like the existing readout chips (font-ui, 10.5px, uppercase, `rounded-[2px]`, min-h-[44px]) with
   a "⌘K" label. `onClick={() => useCommandPalette().open()}` (call the hook at component top, use the returned
   `open`). Do NOT convert any layout to a client component — CommandPaletteProvider (mounted above Masthead in the
   dashboard layout) supplies the context.

2d. **Stage 1-5 shortcuts** — FrameChrome in `issues/[issueNumber]/layout.tsx`: add a `useEffect` registering a
   window `keydown`. On a bare digit '1'-'5' (NO meta/ctrl/alt), and ONLY when
   `document.activeElement` is not an input/textarea/select/`[contenteditable]`, `router.push(STAGE_TABS[i-1]
   .hrefFor(n))`. Clean up the listener on unmount. Also add a subtle digit hint inside each stage tab label — a
   `hidden lg:inline` micro `<span>` showing the digit (`1`..`5`) before the label text, faint color
   (`text-[color:var(--color-faint)]`). Coordinate with Task 1e's DocumentTitle mount in this same file (different
   regions).

2e. **Swap all 5 window.confirm sites to useConfirm()** (preserve EXACT guard semantics — dirty-only guards stay
    dirty-only; pre-action confirms keep their copy):
   - `ReviewDeskRunView.tsx` — the 3 SYNCHRONOUS guards (switchViewMode, handleChipSelect, handleEditSection): make
     each handler `async` and `await confirm({ title:'Unsaved changes', body:<existing message>, tone:'danger',
     confirmLabel:'Leave anyway' })`; keep the `viewMode==='edit' && dirty[selectedSection]` precondition so confirm
     only fires when dirty; `if (!(await confirm(...))) return`. These are onClick handlers — async is fine
     (fire-and-forget); the post-confirm work (setViewMode / setSelectedSection + scrollIntoView / setEditFinding)
     runs after the await resolves true.
   - `RecoveryRail.tsx` (RestartAction.handleRestart), `RerollButton.tsx` (handleReroll), `InspectorFooter.tsx`
     (RestartFooterAction.handleClick), `AgentPromptEditorView.tsx` (requestStopEditing at L169): wrap the existing
     pre-action `window.confirm(...)` with `await confirm({...})` — same message text, tone:'danger' for
     destructive re-runs.
   - `AgentPromptEditorView.tsx` L152 (the agentKey-switch heads-up inside a useEffect): this one fires from an
     effect and must STILL surface even though the route already navigated. Replace the bare `window.confirm(...)`
     with a fire-and-forget `void confirm({ title:'Unsaved changes', body:'You have unsaved changes. Leave the
     editor?' })` — do NOT gate the subsequent reset on the result (preserve existing behavior: the heads-up fires,
     the reset proceeds regardless).
   - Tests: `RecoveryRail.test.tsx` and `InspectorFooter.test.tsx` currently mock `window.confirm`. Update them to
     wrap the render in `<ConfirmProvider>` and drive the flow by clicking the rendered dialog Confirm button
     instead of a `window.confirm` mock. Do NOT weaken assertions (still assert rerollAgent/publishManual is called
     on confirm and NOT called on cancel). If other __tests__ mock window.confirm for these components, update them
     the same way — grep `__tests__` for `confirm` and fix any that render the changed components.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test && pnpm --filter dispatch-control build</automated>
  </verify>
  <done>
    ⌘K/Ctrl+K opens the palette from anywhere; the masthead ⌘K chip opens the same palette; arrow keys + Enter
    navigate to routes/issues/runs; number keys 1-5 switch issue stages only when not typing; all 5 window.confirm
    sites render the 1c ConfirmDialog; no `window.confirm(` remains in the 4 component files (the AgentPromptEditor
    effect uses `confirm(...)` from the hook); updated tests pass; build + test green; no new npm deps; dashboard
    layout is still a Server Component.
  </done>
</task>

<task type="auto">
  <name>Task 3: Gate-1 correctness — signoffs.py run_not_reviewable status guard (+ tests); regression-lock the already-shipped paused-run recovery rail</name>
  <files>
    packages/pipeline/src/eisenbalm_pipeline/api/signoffs.py,
    packages/pipeline/tests/test_signoffs_endpoints.py
  </files>
  <action>
3a. **signoffs.py `run_not_reviewable` status guard** (sounds-human branch ONLY). The run is ALREADY fetched at the
   top of `record_sign_off` (line 85: `run = await _cc.convex_query(http, "pipelineRuns:byRunId", {"runId": run_id})`)
   — reuse it, no new Convex call. Inside the `elif body.kind == "sounds-human":` branch, insert the guard
   IMMEDIATELY AFTER the existing `forbidden_role` 403 block and BEFORE the open-voice-findings query, so the
   existing role-gate ordering is preserved:

     status = run.get("status")
     completed_at = run.get("completedAt")
     # §37.4(c): a run paused at Gate 1 (awaiting-review with no completedAt — editor.py writes
     # awaiting-review WITHOUT completedAt) or a failed run has no reviewable final draft to attest.
     # A genuine final-review run (publisher writes awaiting-review WITH completedAt) is unaffected.
     if status == "failed" or (status == "awaiting-review" and not completed_at):
         raise HTTPException(
             status_code=status_.HTTP_409_CONFLICT,
             detail={"reason": "run_not_reviewable",
                     "message": "This run is not in a reviewable state to sign off."},
         )

   NOTE the name clash: the local `status` variable shadows the imported `status` module used for
   `status.HTTP_409_CONFLICT`. AVOID it — either name the local `run_status` (preferred) or import the FastAPI
   status module under an alias. Do NOT leave `status.HTTP_*` referencing a shadowed local. Keep the F5
   `no_sanity_issue` guard (lines 100-108) exactly as-is — this is additive and does not replace it. Do NOT add
   this guard to the facts-cleared branch.

3b. **Tests** — `packages/pipeline/tests/test_signoffs_endpoints.py`:
   - The shared `_run()` helper (≈L38-39) currently returns `{"status": "awaiting-review", "sanityIssueId":
     "issue-42", "runId": run_id}` with NO `completedAt`. Under the new guard that is the paused-at-Gate-1 shape and
     would 409 the sounds-human SUCCESS test. FIX: add `"completedAt": 1_700_000_000_000` (any truthy ms value) to
     `_run()` so it represents a genuine final-review run. This keeps every facts-cleared test green (they ignore
     completedAt) and makes the sounds-human success path green.
   - ADD new cases asserting 409 `run_not_reviewable` for the sounds-human kind:
       (i) a paused run: `pipelineRuns:byRunId` returns `{"status":"awaiting-review","sanityIssueId":"issue-42",
           "completedAt": None, "runId": run_id}` (or completedAt absent) → POST {kind:"sounds-human"} → 409,
           `response.json()["detail"]["reason"] == "run_not_reviewable"`.
       (ii) a failed run: status "failed" (+ sanityIssueId present so it is NOT caught earlier by no_sanity_issue)
           → POST {kind:"sounds-human"} → 409 run_not_reviewable.
     Follow the existing harness EXACTLY (module-level TestClient over the signoffs router; monkeypatch
     `eisenbalm_pipeline.api.signoffs._cc.convex_query` / `.convex_mutation`; the local-dev path resolves the actor
     as Editor-in-chief so no auth headers are needed). Assert the mutation `signOffs:record` was NOT called in the
     409 cases.
   - Run the pipeline suite and require green: `cd packages/pipeline && uv run pytest tests/test_signoffs_endpoints.py -x`.

3c. **Regression-lock Task 3b's already-shipped paused-run recovery rail** (VERIFY, do NOT re-implement):
   `RunDetail.tsx:268` already computes `isPausedAtGate1 = run.status === 'awaiting-review' && run.completedAt == null`
   and L363 mounts `<RecoveryRail … isPausedAtGate1={true} />`; `RecoveryRail.tsx:252-277` already renders the
   "Paused for your decision" (paused-not-failed) copy + the Signal Desk restart link. Confirm this is intact after
   Task 2's ConfirmDialog swap in RecoveryRail.tsx (the swap must NOT disturb the paused branch). Check
   `__tests__/RecoveryRail.test.tsx` — if it does NOT already assert the `isPausedAtGate1` paused rendering (heading
   "Paused for your decision", NOT "What happened"), add ONE regression test that renders
   `<RecoveryRail runId agentRuns={[]} isPausedAtGate1={true} />` (wrapped in ConfirmProvider) and asserts the paused
   heading + the /signal-desk restart link appear. Do NOT modify RunDetail.tsx behavior.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/test_signoffs_endpoints.py -x</automated>
  </verify>
  <done>
    A POST sounds-human sign-off on a paused-at-Gate-1 run (awaiting-review, no completedAt) OR a failed run returns
    409 `{reason:"run_not_reviewable"}` and records nothing; the facts-cleared + sounds-human SUCCESS tests still pass
    (with `_run()` carrying completedAt); the pipeline signoffs suite is green; the no_sanity_issue F5 guard is
    untouched; the paused-run RecoveryRail mount + paused copy are verified intact and regression-locked.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter dispatch-control build` succeeds (strict compile — catches the Vercel/Linux-only bugs that vitest
  misses per RETROSPECTIVE guidance).
- `pnpm --filter dispatch-control test` — full suite green (1087 tests / 136 files baseline; updated confirm-driving
  tests pass).
- `cd packages/pipeline && uv run pytest tests/test_signoffs_endpoints.py -x` — green, including the 2 new
  run_not_reviewable 409 cases.
- No new npm dependencies introduced (grep the diff for package.json changes — there should be none in apps/dispatch-control).
- No new or changed Convex functions (the palette reuses api.issues.listForWorkspace + api.runs.listRecentForWorkspace only).
- `(dashboard)/layout.tsx` and all route layouts remain Server Components (only client PROVIDERS are added as children).
- Every `data-testid` preserved; z-order ladder respected (ConfirmDialog z-[70] < CommandPalette z-[75]; both above
  HelpTip z-[60] and inspector z-40).
- Keyboard listeners never fire while typing (input/textarea/select/contentEditable) or with meta/ctrl/alt held —
  except the ⌘K combo itself.
</verification>

<success_criteria>
- Every dashboard route paints an instant skeleton on navigation; the named high-traffic "Loading…" strings are skeletons.
- Sign-in is branded two-panel; every tab has a route-specific title; a favicon renders.
- ⌘K opens a working palette (routes + issues + recent runs) discoverable via a masthead chip; issue stages 1-5 are keyboard-navigable.
- All 5 window.confirm sites use the 1c ConfirmDialog; their tests drive the dialog, not window.confirm.
- A paused/failed run cannot receive a sounds-human sign-off (409 run_not_reviewable); the paused-run recovery rail is regression-locked.
- All three verification commands pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260723-4a6-test-readiness-polish-skeletons-and-inst/260723-4a6-SUMMARY.md`.
</output>
