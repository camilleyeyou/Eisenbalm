---
phase: quick-260710-k8y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/agentList.ts
  - apps/dispatch-control/app/(dashboard)/prompt-lab/page.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptsListClient.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/TestRunPanel.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/AssembledPreview.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptEditor.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptSaveDialog.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/VersionHistoryPanel.tsx
  - apps/dispatch-control/__tests__/PromptSaveDialog.test.tsx
  - .planning/quick/260710-k8y-implement-prompt-lab-nomenclature-propos/PROPOSAL.md
autonomous: true
requirements: [NOM-01, NOM-02, NOM-03, NOM-04, NOM-05]

must_haves:
  truths:
    - "The prompt list groups read House Rules, Job Descriptions, Section Briefs, Assignment Memos — in that top-to-bottom order — each with a one-line descriptor underneath its header."
    - "Each agent card shows a human title (e.g. 'Editor — Picks the Winner', 'Bonus Writer — Jingle') instead of the raw humanized slug ('Editor Gate1')."
    - "The test-run panel is titled 'Rehearsal' with modes 'Sample week' / 'Your own input' / 'Replay a real run' plus per-mode descriptors, and a 'Draft vs. live' compare action."
    - "The assembled preview disclosure reads 'What the agent sees' with the model-input descriptor."
    - "Saving a version reads 'Save draft as v{n}', REQUIRES a note (Confirm save is disabled until a non-whitespace note is typed), the activate control reads 'Make live' / 'Restore this version', and the active badge reads 'LIVE'."
    - "The unknown-variable warning tells the editor what to do: '{token} isn't supplied by the pipeline — remove it or ask your developer to wire it'."
    - "'pnpm --filter dispatch-control build' exits 0 and the dispatch-control vitest suite is green."
  artifacts:
    - path: "apps/dispatch-control/app/(dashboard)/prompt-lab/_components/agentList.ts"
      provides: "Renamed GROUP_LABELS + new GROUP_DESCRIPTORS + AGENT_DISPLAY_NAMES map + displayNameForAgentKey resolver"
      contains: "displayNameForAgentKey"
    - path: "apps/dispatch-control/app/(dashboard)/prompt-lab/page.tsx"
      provides: "GROUP_ORDER reordered to [asset, system, section-guidance, user-template]"
      contains: "GROUP_ORDER"
    - path: "apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptsListClient.tsx"
      provides: "Group descriptors rendered, display names in cards/search, 'Edits only' + 'edited since launch' microcopy"
    - path: "apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptSaveDialog.tsx"
      provides: "Required-note gate + 'Save draft as v{n}' heading"
      contains: "Save draft as"
    - path: "apps/dispatch-control/__tests__/PromptSaveDialog.test.tsx"
      provides: "Test proving Confirm save is blocked on empty note, enabled on non-empty"
      contains: "Confirm save"
  key_links:
    - from: "PromptsListClient.tsx"
      to: "agentList.ts (GROUP_DESCRIPTORS, displayNameForAgentKey)"
      via: "import + render beneath each group <h2> and in each card"
      pattern: "displayNameForAgentKey|GROUP_DESCRIPTORS"
    - from: "page.tsx GROUP_ORDER"
      to: "PromptsListClient section render order"
      via: "grouped keys passed in GROUP_ORDER"
      pattern: "GROUP_ORDER"
    - from: "PromptSaveDialog Confirm save button"
      to: "note.trim().length"
      via: "disabled binding + early-return in handleConfirm"
      pattern: "note\\.trim\\(\\)"
---

<objective>
Implement the Prompt Lab Nomenclature Proposal: a newsroom-register UI copy/microcopy
rename pass across the dispatch-control Prompt Lab, plus two small logic tweaks
(required save-note + next-version-number label + editor-facing unknown-variable
microcopy).

This is NOT a redesign. NO data-model / Convex / API_CONTRACTS / route-slug / enum /
payload-key changes. Slugs and mode enum keys stay exactly as they are — only
human-visible labels change. All target strings are copied verbatim from PROPOSAL.md
in this task directory.

Purpose: The Prompt Lab should speak the editor's language ("House Rules", "Rehearsal",
"Make live") instead of the system's implementation vocabulary ("Shared assets", "Test
run", "Activate"), so Andrew can operate it without a translation layer.

Output: 8 edited component/source files + 1 new component test + the committed
PROPOSAL.md provenance file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260710-k8y-implement-prompt-lab-nomenclature-propos/PROPOSAL.md

# The canonical rename map. COPY STRINGS VERBATIM from this file (em dashes "—" and
# apostrophes included). Where this plan quotes a string, it matches PROPOSAL.md.

<interfaces>
<!-- Verified this session. Executor: use these directly, no exploration needed. -->

Editable agent keys that receive a display-name override (from VariableRegistry.ts —
these are the ACTUAL slugs; note editor_gate1 has NO underscore, and bonus is split
into three concrete slugs, so map each one):
  editor_gate1, editor_final, scout, advocate, researcher, calibrator, game,
  bonus_jingle, bonus_spec_ad, bonus_big_budget, rubric, voice_constraints

agentList.ts current exports:
  type EditableAgentGroup = 'system' | 'user-template' | 'section-guidance' | 'asset'
  export const GROUP_LABELS: Record<EditableAgentGroup, string>
  export function humanizeAgentKey(key: string): string   // e.g. "Editor Gate1"

agentList.ts is imported ONLY by page.tsx and PromptsListClient.tsx (verified).
GROUP_LABELS is referenced ONLY in PromptsListClient.tsx (filter <select> + <h2>).
humanizeAgentKey is called ONLY in PromptsListClient.tsx (search haystack + card).

promptVersions Convex API already available (used elsewhere in these components):
  api.promptVersions.listForAgent  // newest-first rows: { version:number, content, note?, isActive, createdAt, ... }
  api.promptVersions.saveVersion   // (note? optional — passing a required non-empty string is NOT a contract change)

Established convex-mock test pattern (mirror for the new test):
  apps/dispatch-control/__tests__/AddCorrectionDialog.test.tsx
  → vi.mock('convex/react', () => ({ useMutation: vi.fn(), useQuery: vi.fn() }))
  → vi.mock('@convex/_generated/api', () => ({ api: { promptVersions: { saveVersion: '...', listForAgent: '...' } } }))

Confirmation checks (already done this session, re-confirm in verify):
  - No existing test asserts any OLD Prompt Lab UI string → renames break nothing.
  - docs/API_CONTRACTS.md references "prior-real"/"canned fixture" only as PROSE
    describing the D-04 modes + enum keys (prior/manual/fixture, UNCHANGED) — no UI
    label is documented, so nothing in the contract changes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group taxonomy, descriptors, agent display names, page order + list microcopy</name>
  <files>
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/agentList.ts
    apps/dispatch-control/app/(dashboard)/prompt-lab/page.tsx
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptsListClient.tsx
    .planning/quick/260710-k8y-implement-prompt-lab-nomenclature-propos/PROPOSAL.md (provenance — git add so it commits with this task)
  </files>
  <action>
    Implements NOM-01 (group rename + descriptors + order) and NOM-05 (agent display
    names) and the list-page badge/filter microcopy. Copy all strings VERBATIM from
    PROPOSAL.md (em dashes "—" included). For TS strings containing apostrophes
    (Week's, Judge's, week's) use DOUBLE-quoted strings to avoid escaping.

    A) agentList.ts:
    - Rename GROUP_LABELS values (keys UNCHANGED):
        system            → "Job Descriptions"
        user-template     → "Assignment Memos"
        section-guidance  → "Section Briefs"
        asset             → "House Rules"
    - ADD `export const GROUP_DESCRIPTORS: Record<EditableAgentGroup, string>` with the
      four one-liners from the PROPOSAL "four asset groups" table (verbatim):
        system            → "Who each agent is and the rules it never breaks. Edit these to change judgment and behavior."
        user-template     → "The note each agent gets every run, carrying that week's data. Plumbing — edit only to change what an agent is handed."
        section-guidance  → "The standing brief for each section of the issue: its job, its length, what it owns, what it must not do."
        asset             → "Law that many prompts inherit. Edit once, everyone obeys — the most powerful lever on this page."
    - ADD `export const AGENT_DISPLAY_NAMES: Record<string, string>` (slug → title,
      verbatim from PROPOSAL "Agent display names"; bonus_* expanded to the 3 real slugs):
        editor_gate1       → "Editor — Picks the Winner"
        editor_final       → "Editor — Publish Brief"
        scout              → "Scout — Finds Candidates"
        advocate           → "Advocate — Argues the Case"
        researcher         → "Researcher — Builds the Dossier"
        calibrator         → "Calibrator — Sets the Week's Style"
        game               → "Game Writer"
        bonus_jingle       → "Bonus Writer — Jingle"
        bonus_spec_ad      → "Bonus Writer — Spec Ad"
        bonus_big_budget   → "Bonus Writer — Big Budget"
        rubric             → "QA Judge's Rubric"
        voice_constraints  → "The Voice (House Style)"
    - ADD `export function displayNameForAgentKey(key: string): string` returning
      `AGENT_DISPLAY_NAMES[key] ?? humanizeAgentKey(key)`. Keep humanizeAgentKey exported
      and unchanged (it is the fallback for *_user, design, section-guidance keys).

    B) page.tsx: change GROUP_ORDER to `['asset', 'system', 'section-guidance', 'user-template']`
       (House Rules first → highest leverage; Assignment Memos last → plumbing). Type stays
       EditableAgentGroup[]. No other change.

    C) PromptsListClient.tsx:
    - Import GROUP_DESCRIPTORS and displayNameForAgentKey from './agentList' (GROUP_LABELS
      import stays).
    - Under each group `<h2>{GROUP_LABELS[group]}</h2>` (~line 176), render the descriptor
      beneath it, subtle/newsroom styling consistent with the page, e.g.:
        `<p className="text-xs text-neutral-500">{GROUP_DESCRIPTORS[group]}</p>`
    - Replace `humanizeAgentKey(key)` with `displayNameForAgentKey(key)` in BOTH the card
      title (~line 193) and the search haystack (~line 114: keep the raw `key` in the
      haystack too so slug search still works, i.e. `` `${key} ${displayNameForAgentKey(key)}` ``).
    - "Drift only" button label (~line 165) → "Edits only" (leave aria-pressed / driftOnly
      state and all logic UNCHANGED).
    - "edited since seed" badge (~line 197) → "edited since launch".
    - Reorder GROUP_FILTER_OPTIONS (~line 50) to match the new page order
      `['asset', 'system', 'section-guidance', 'asset'...]` → use
      `['asset', 'system', 'section-guidance', 'user-template']` for consistency (labels
      auto-resolve via GROUP_LABELS; this is cosmetic dropdown ordering only).

    D) git add the PROPOSAL.md provenance file so it is committed with this task.

    Do NOT touch groupForAgentKey, listEditableAgentKeys, drift logic, or any Convex call.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && pnpm --filter dispatch-control build && pnpm --filter dispatch-control test && grep -q "House Rules" "app/(dashboard)/prompt-lab/_components/agentList.ts" && grep -q "displayNameForAgentKey" "app/(dashboard)/prompt-lab/_components/agentList.ts" && grep -q "Editor — Picks the Winner" "app/(dashboard)/prompt-lab/_components/agentList.ts" && grep -q "Edits only" "app/(dashboard)/prompt-lab/_components/PromptsListClient.tsx" && grep -q "edited since launch" "app/(dashboard)/prompt-lab/_components/PromptsListClient.tsx" && grep -q "'asset', 'system', 'section-guidance', 'user-template'" "app/(dashboard)/prompt-lab/page.tsx" && ! grep -qi "prompt lab" ../../docs/API_CONTRACTS.md && echo "T1 OK"</automated>
  </verify>
  <done>
    Prompt list renders House Rules / Job Descriptions / Section Briefs / Assignment Memos
    (in that order), each with a descriptor line; cards show human titles via
    displayNameForAgentKey (falling back to humanizeAgentKey); "Edits only" + "edited since
    launch" microcopy present; build passes; vitest green; API_CONTRACTS unaffected;
    PROPOSAL.md staged for commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rehearsal (test-run) panel + assembled preview copy</name>
  <files>
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/TestRunPanel.tsx
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/AssembledPreview.tsx
  </files>
  <action>
    Implements NOM-03. Copy strings VERBATIM from the PROPOSAL "Test run panel" table.
    Enum values and payload keys stay UNCHANGED — this is label-only. InputMode
    ('prior' | 'manual' | 'fixture') and TestRunBody keys must NOT change.

    A) TestRunPanel.tsx:
    - h2 (~line 183) "Test run" → "Rehearsal".
    - Mode-label array (~lines 195-200) — keep the enum keys, change only labels:
        ['fixture', 'Sample week'], ['manual', 'Your own input'], ['prior', 'Replay a real run']
    - ADD a `MODE_DESCRIPTORS: Record<InputMode, string>` (verbatim from PROPOSAL) and
      render the active mode's descriptor as a subtle line beneath the mode radiogroup:
        fixture → "Runs the draft on a stored example input. Quick smoke test."
        manual  → "You supply the values — paste a real dossier or candidate list and see what the draft does with it."
        prior   → "The exact input this agent received in a past run. Same input, new prompt — any difference is your edit. The gold standard."
      (subtle styling, e.g. `text-[11px] text-neutral-400`.)
    - Reword the fixture inline paragraph (~lines 270-274) to drop the stale word "canned":
      "Uses the stored sample input for {agentKey} (no variables or prior run needed)."
      (keep the `<span className="font-mono">{agentKey}</span>`.)
    - Compare button label (~line 301) "Compare against active" → "Draft vs. live"
      (keep the "Comparing…" transient). Add its descriptor as a subtle caption near the
      button row: "Runs both on the same input, side by side. Answers: did my edit help?"
    - Coherence with "Draft vs. live" (labels ONLY — do NOT rename the `active` variable,
      the useQuery, or any state): ResultColumn side label "Active" → "Live" (~line 358);
      score-delta text "vs active" → "vs live" (~line 332) and "(draft … · active …)" →
      "(draft … · live …)" (~lines 335-336).
    - Leave handleRun / handleCompare / canRun / canCompare / scoring logic UNCHANGED.

    B) AssembledPreview.tsx:
    - <summary> text (~line 45) "Assembled preview (sample values)" → "What the agent sees".
    - Replace the helper <p> (~lines 48-51) with the PROPOSAL descriptor, preserving the
      "not a test-run" caveat:
      "The final assembled text — your prompt plus filled variables — exactly as the model
      receives it. Readability aid only — not a test-run."
    - Leave assembleWithSamples and all substitution logic UNCHANGED.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && pnpm --filter dispatch-control build && pnpm --filter dispatch-control test && grep -q ">Rehearsal<" "app/(dashboard)/prompt-lab/_components/TestRunPanel.tsx" && grep -q "Sample week" "app/(dashboard)/prompt-lab/_components/TestRunPanel.tsx" && grep -q "Replay a real run" "app/(dashboard)/prompt-lab/_components/TestRunPanel.tsx" && grep -q "Draft vs. live" "app/(dashboard)/prompt-lab/_components/TestRunPanel.tsx" && grep -q "What the agent sees" "app/(dashboard)/prompt-lab/_components/AssembledPreview.tsx" && ! grep -q "Canned fixture" "app/(dashboard)/prompt-lab/_components/TestRunPanel.tsx" && grep -qF "InputMode = 'prior' | 'manual' | 'fixture'" "app/(dashboard)/prompt-lab/_components/TestRunPanel.tsx" && echo "T2 OK"</automated>
  </verify>
  <done>
    Panel titled "Rehearsal"; three modes labeled Sample week / Your own input / Replay a
    real run with per-mode descriptors; compare action reads "Draft vs. live" with its
    descriptor; assembled preview reads "What the agent sees"; InputMode enum + payload keys
    intact; build passes; vitest green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Versioning controls copy + required-note gate + unknown-variable microcopy</name>
  <files>
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptSaveDialog.tsx
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptEditor.tsx
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/VersionHistoryPanel.tsx
    apps/dispatch-control/__tests__/PromptSaveDialog.test.tsx
  </files>
  <action>
    Implements NOM-02 (versioning copy) + NOM-04 (required note + unknown-var microcopy).
    Activate/override/rollback LOGIC must stay UNCHANGED — relabel only. Copy strings
    verbatim from PROPOSAL. Use double-quoted TS strings where apostrophes appear.

    A) PromptSaveDialog.tsx — heading "Save draft as v{n}" + REQUIRED note:
    - Import `useQuery` from 'convex/react' (alongside useMutation) and compute the next
      version number from the existing API:
        const versions = useQuery(api.promptVersions.listForAgent, { workspace_id: workspaceId, agentKey })
        const nextVersion = versions === undefined ? null : (versions.length > 0 ? Math.max(...versions.map(v => v.version)) + 1 : 1)
      Heading (~line 67) becomes: `nextVersion != null ? `Save draft as v${nextVersion}` : "Save draft as new version"`.
    - Make the note REQUIRED:
        const noteEmpty = note.trim().length === 0
      - Label span (~line 76) "Note (optional)" → "Note (required) — what changed and why".
      - In handleConfirm, early-return when noteEmpty; send `note: note.trim()` (no longer
        `|| undefined`, since it is guaranteed non-empty). saveVersion's `note` param stays
        optional in Convex — always sending a non-empty string is NOT a contract change.
      - Confirm save button (~line 91): `disabled={disabled || saving || noteEmpty}`.
      - Add an inline hint shown when noteEmpty, subtle, e.g.:
        `<p className="text-xs text-neutral-500">A note is required — record what changed and why.</p>`
      - Keep the "Confirm save" button LABEL unchanged (stable test selector).

    B) PromptEditor.tsx — button label + unknown-variable microcopy (NOM-04):
    - The "Save as new version" button (~line 103) → "Save draft as new version"
      (PromptEditor must NOT add a useQuery to compute v{n}; its top-level render runs in the
      smoke test without a Convex provider, and an unconditional hook would break it. The
      v{n} form lives in the dialog only. This satisfies PROPOSAL's stated fallback.)
    - Replace the unknown-variable warning banner contents (~lines 68-87) with per-token
      sentences using the exact template (token shown as `{name}`):
        for each unknown `name`: "{name} isn't supplied by the pipeline — remove it or ask your developer to wire it"
      Render as a list inside the existing red alert box (`role="alert"`), one line per token,
      the `{name}` portion in `font-mono`.
    - Button title (~lines 97-101) hasUnknown branch: replace
      "Resolve unknown variables before saving" with the same template applied to the first
      unknown token, e.g. `` `{${unknown[0]}} isn't supplied by the pipeline — remove it or ask your developer to wire it` ``.
    - Leave findUnknownVariables, canSave, and the save-disable gate logic UNCHANGED.

    C) VersionHistoryPanel.tsx — relabel only (activate/override/rollback logic UNCHANGED):
    - Active badge (~line 227) "Active" → "LIVE".
    - "Currently active" (~line 252) → "Currently live".
    - Activate button label (~lines 268-272): the non-rollback branch "Activate" → "Make live";
      the rollback branch "Rollback to this version" → "Restore this version". Keep the
      "Activating…" transient as "Making live…".
    - Button title tooltips (~lines 259-265): "Activate v{version}" → `Make live v${v.version}`;
      "Roll back to v{version}" → `Restore v${v.version}`.
    - Compare-selector "(active)" markers (~lines 329 & 347) → "(live)" for coherence with
      the LIVE badge.
    - Do NOT touch the eval-gate / override-with-reason block, the in-progress guard, the
      "Run evals for v{N}" producer, or any mutation call — labels only.

    D) NEW test apps/dispatch-control/__tests__/PromptSaveDialog.test.tsx (jsdom — *.test.tsx
    auto-matches jsdom). Mirror __tests__/AddCorrectionDialog.test.tsx mocking:
      vi.mock('convex/react', () => ({ useMutation: vi.fn(), useQuery: vi.fn() }))
      vi.mock('@convex/_generated/api', () => ({ api: { promptVersions: { saveVersion: 'promptVersions:saveVersion', listForAgent: 'promptVersions:listForAgent' } } }))
    In beforeEach: useMutation → a save spy (vi.fn(async () => 'v-id')); useQuery →
    return `[{ version: 1 }, { version: 2 }]` (so nextVersion = 3). Render
      <PromptSaveDialog workspaceId="eisenbalm" agentKey="game" content="..." onSaved={()=>{}} onCancel={()=>{}} />
    Assert:
      1. Heading shows "Save draft as v3".
      2. Confirm save button (getByRole button /confirm save/i) is DISABLED initially (empty note).
      3. After typing a non-empty note into the note input, the button is ENABLED.
      4. Setting the note to whitespace ("   ") disables it again.
      5. Clicking Confirm save with a real note calls the save spy once with
         note equal to the trimmed non-empty string.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && pnpm --filter dispatch-control build && pnpm --filter dispatch-control test && grep -q "Note (required) — what changed and why" "app/(dashboard)/prompt-lab/_components/PromptSaveDialog.tsx" && grep -q "Save draft as" "app/(dashboard)/prompt-lab/_components/PromptSaveDialog.tsx" && grep -q "noteEmpty" "app/(dashboard)/prompt-lab/_components/PromptSaveDialog.tsx" && grep -q "isn't supplied by the pipeline" "app/(dashboard)/prompt-lab/_components/PromptEditor.tsx" && ! grep -q "Resolve unknown variables before saving" "app/(dashboard)/prompt-lab/_components/PromptEditor.tsx" && grep -q "Make live" "app/(dashboard)/prompt-lab/_components/VersionHistoryPanel.tsx" && grep -q "Restore this version" "app/(dashboard)/prompt-lab/_components/VersionHistoryPanel.tsx" && grep -q ">LIVE<" "app/(dashboard)/prompt-lab/_components/VersionHistoryPanel.tsx" && test -f __tests__/PromptSaveDialog.test.tsx && echo "T3 OK"</automated>
  </verify>
  <done>
    Save dialog reads "Save draft as v{n}", note is required (Confirm save disabled until a
    non-whitespace note is entered) with an inline hint; the unknown-variable warning uses
    the actionable "{token} isn't supplied by the pipeline …" copy; version controls read
    "Make live" / "Restore this version" / "LIVE"; activation/override logic unchanged; new
    PromptSaveDialog test proves the required-note gate; build passes; vitest green.
  </done>
</task>

</tasks>

<verification>
- `pnpm --filter dispatch-control build` exits 0 (the REAL strict type-check gate — vitest
  does not type-check). Run after EVERY task.
- `pnpm --filter dispatch-control test` (vitest run) green after every task. No existing
  test asserts an old string, so renames must not introduce failures; the one new test
  (PromptSaveDialog) proves the required-note behavior.
- No new npm dependencies added.
- `docs/API_CONTRACTS.md` unchanged — copy-only work; mode enum keys (prior/manual/fixture),
  payload keys, route slugs, and Convex signatures all untouched.
- Grep sweep for stale old strings in the 8 edited files returns none of: "Test run",
  "Canned fixture", "Manual variables", "Prior-real input", "Compare against active",
  "Assembled preview", "Note (optional)", "Rollback to this version", "Drift only",
  "edited since seed", "Resolve unknown variables", "Agent system prompts", "User templates",
  "Section guidance", "Shared assets".
</verification>

<success_criteria>
- All PROPOSAL.md rename-map rows implemented verbatim across the Prompt Lab.
- Group order is House Rules → Job Descriptions → Section Briefs → Assignment Memos, each
  with a descriptor; agent cards show human display names.
- Rehearsal panel + assembled preview + versioning controls carry the new copy.
- Saving a version requires a note and labels the save as "Save draft as v{n}".
- Unknown-variable microcopy tells the editor what to do.
- Slugs, routes, enum keys, payload shapes, and Convex signatures unchanged.
- `pnpm --filter dispatch-control build` exits 0 and the vitest suite is green after each task.
- PROPOSAL.md committed as the provenance artifact.
</success_criteria>

<output>
After completion, create
`.planning/quick/260710-k8y-implement-prompt-lab-nomenclature-propos/260710-k8y-SUMMARY.md`
</output>
