---
phase: quick-260722-tv1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dispatch-control/components/AppSidebar.tsx
  - apps/dispatch-control/app/(dashboard)/issues/page.tsx
  - apps/dispatch-control/components/inspector/InspectorPanel.tsx
  - apps/dispatch-control/components/ui/HelpTip.tsx
  - apps/dispatch-control/components/AwaitingYouInbox.tsx
  - apps/dispatch-control/components/_PlaceholderScreen.tsx
  - apps/dispatch-control/app/(dashboard)/issues/_components/HeldIssueRow.tsx
  - apps/dispatch-control/app/globals.css
  - apps/dispatch-control/app/(dashboard)/voice-pass/[runId]/VoicePassRunView.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx
  - apps/dispatch-control/app/(dashboard)/issues/_components/WorkspaceOutline.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/DecisionRail.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SourceIndex.tsx
  - apps/dispatch-control/app/(dashboard)/story-brief/_components/StoryBriefScreen.tsx
  - apps/dispatch-control/app/(dashboard)/signal-desk/_components/SignalDeskScreen.tsx
  - apps/dispatch-control/components/galley/AnnotationMark.tsx
  - apps/dispatch-control/components/galley/PassageToolbar.tsx
  - apps/dispatch-control/components/decision-log/DecisionLog.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/layout.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/graph/page.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/page.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/page.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunsTable.tsx
  - apps/dispatch-control/app/(dashboard)/eval-center/_components/DriftScoreboard.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/VersionHistoryPanel.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/DiffViewer.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/IssueComments.tsx
autonomous: true
requirements: [LAYOUT-AUDIT-260722-TV1]
must_haves:
  truths:
    - "AppSidebar's bottom block (How to use + Signed-in-as role) is fully visible — no 52px clip inside the 100vh-52px shell row"
    - "Issues home root no longer overscrolls (~100px) or double-gutters (48px) inside main's p-6"
    - "InspectorPanel slide-over starts below the 52px Masthead — My Tasks / UserButton stay clickable while it is open"
    - "HelpTip 'Explain workspace stages' popover renders ABOVE the workspace sticky stage nav (z-30), not under it"
    - "Voice Pass galley gets a usable reading width in the narrow workspace canvas AND the wide standalone route — one container-query layout serves both mounts"
    - "WorkspaceOutline / DecisionRail / SourceIndex jump controls navigate to the Draft stage galley anchor when no galley is mounted on the current stage (Story/Fact Check/Approval) — no silent no-ops"
    - "Galley annotation popovers and the passage toolbar clamp to the viewport — no right-edge clip and no overlap of the 52px Masthead"
    - "run-monitor tab bar stays sticky while sub-routes scroll; runs tables are max-width capped and long lists (runs/drift/versions/comments) are client-capped with a Show-more toggle"
  artifacts:
    - path: "apps/dispatch-control/components/AppSidebar.tsx"
      provides: "aside uses h-full (not h-screen) so it fits the shell row"
      contains: "h-full"
    - path: "apps/dispatch-control/app/globals.css"
      provides: ".voice-canvas inline-size container + .voice-stage-row/.voice-stage-rail container-query rules"
      contains: "voice-stage-row"
    - path: "apps/dispatch-control/app/(dashboard)/issues/_components/WorkspaceOutline.tsx"
      provides: "jumpToSection falls back to issueDraftHref(n)+#anchor via useRouter/useParams when the galley anchor element is absent"
      contains: "issueDraftHref"
    - path: "apps/dispatch-control/components/galley/AnnotationMark.tsx"
      provides: "popover self-clamp useLayoutEffect shifting left when rect.right overflows the viewport"
      contains: "useLayoutEffect"
  key_links:
    - from: "apps/dispatch-control/app/(dashboard)/issues/_components/WorkspaceOutline.tsx"
      to: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx"
      via: "router.push(issueDraftHref(n)+'#galley-...') -> hash-scroll effect on draft load"
      pattern: "issueDraftHref|window\\.location\\.hash"
    - from: "apps/dispatch-control/app/(dashboard)/voice-pass/[runId]/VoicePassRunView.tsx"
      to: "apps/dispatch-control/app/globals.css"
      via: "voice-canvas / voice-stage-row / voice-stage-rail classes replace lg:flex-row / lg:w-[336px]"
      pattern: "voice-stage-row|voice-canvas"
---

<objective>
A 4-agent layout audit of dispatch-control (following quick 260722-n5r, which rebuilt the Issue Workspace frame) found ~23 verified layout/markup/CSS defects across the app shell, the Issue Workspace stages, and the run-monitor/ops surfaces. Every finding was confirmed at file:line. This plan closes all of them in three cohesive tasks.

Purpose: The app shell stops clipping and colliding; the Issue Workspace stages read at a usable width with working jump-nav; long ops lists stop stretching and running unbounded.
Output: 26 files updated — all changes are layout/markup/CSS only. NO Convex function changes, NO data-flow or sign-off-gate changes, NO run-monitor visual-token modernization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<verified_findings>
All findings below were verified at file:line by the audit (do NOT re-investigate). App shell (confirmed):
`(dashboard)/layout.tsx` = `<div class="flex h-screen flex-col overflow-hidden bg-[color:var(--color-rail)]">` → Masthead (52px) → `<div class="flex flex-1 overflow-hidden">` holding `<AppSidebar/>` + `<main class="flex-1 overflow-y-auto p-6">`. `<main>` is the ONLY scrollport (100vh−52px). The sidebar row is `overflow-hidden`, so any child taller than the row is clipped.

Confirmed patterns to match:
- Bracket-pixel Tailwind + `var(--color-*)` tokens throughout; `--radius: 0` (hard edges).
- Tailwind v4.3.0 is installed (`tailwindcss@^4.3.0`, `@tailwindcss/postcss`). globals.css already uses `@import "tailwindcss"`, `container-type: inline-size` (`.galley-root`), and `@supports (container-type: inline-size)` — the container-query CSS route is proven in THIS file.
- Galley section anchors are `galley-{sectionId}` DOM ids; globals.css already sets `[id^='galley-'] { scroll-margin-top: 88px }` for the sticky-nav offset.
- `lib/galley/sectionIdMap.ts` exports `galleyAnchorFor(sectionId)` → `galley-${id}` (theme → null, deliberation-conversation → `galley-deliberation`).
- `lib/issueRouteResolver.ts` exports `parseIssueNumber(param)` and `issueDraftHref(n)` → `/issues/${n}/draft`.
- The workspace frame derives its issueNumber via `useParams()` + `parseIssueNumber` (see `issues/[issueNumber]/layout.tsx`). WorkspaceOutline renders INSIDE that frame, so `useParams()` yields `{ issueNumber }` there.
- `DecisionRail` already receives an optional `issueNumber` prop (ApprovalStage passes it). `SourceIndex` currently receives only `runId`.
- ReviewDeskRunView (the Draft stage) already has a `deepLinkAppliedRef` one-shot effect pattern to mirror for the hash-scroll effect.

Test facts (verified via grep): NO test asserts `h-screen`/`w-[210px]` on the sidebar, NO test asserts `lg:w-[336px]`/`min-h-[70vh]`/`lg:flex-row` on Voice Pass, NO test asserts `min-h-screen` anywhere. IssueComments.test fixture = 2 comments (asserts 2 listitems, oldest-first). EvalCenter.test drift fixture = 2 points (`drift-point-scout_normal_week-0/-1`). All list caps use thresholds (20/50) far above test fixtures, so the caps do not fire in tests — provided the toggle button is NOT a `listitem` and the map index stays within the shown slice.
</verified_findings>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shell + page-root defects (7 files)</name>
  <files>apps/dispatch-control/components/AppSidebar.tsx, apps/dispatch-control/app/(dashboard)/issues/page.tsx, apps/dispatch-control/components/inspector/InspectorPanel.tsx, apps/dispatch-control/components/ui/HelpTip.tsx, apps/dispatch-control/components/AwaitingYouInbox.tsx, apps/dispatch-control/components/_PlaceholderScreen.tsx, apps/dispatch-control/app/(dashboard)/issues/_components/HeldIssueRow.tsx</files>
  <action>
Add a one-line file-header provenance comment `quick 260722-tv1` to each file's existing top doc-comment block where you make a change (match the existing comment style already present in each file).

1. AppSidebar.tsx (~L71): the `<aside>` uses `h-screen` inside the 100vh−52px `overflow-hidden` shell row → its `mt-auto` block (How to use + RoleIndicator "Signed in as …") is clipped. Change `h-screen` → `h-full` on the `<aside>` className. Change ONLY that token; leave `w-[210px] flex flex-col …` untouched.

2. issues/page.tsx (~L237): root is `<div className="min-h-screen bg-[color:var(--color-rail)] px-6 py-8 lg:px-8">` inside main's p-6 → ~100px overscroll + 48px double gutters. Replace with `<div className="min-h-full bg-[color:var(--color-rail)] py-2">` (match the 260722-n5r workspace-frame root pattern: `min-h-full`, no horizontal padding, small vertical breathing). KEEP the inner `<div className="mx-auto flex max-w-[1100px] flex-col gap-12">` exactly — the 1100px reading measure is deliberate.

3. issues/page.tsx (~L230, held list cap — item 7 part 2): `heldIssues` is currently uncapped (published is `.slice(0,5)`). In the Held render block (~L283-292), render only the first 8: iterate `heldIssues.slice(0, 8)` instead of `heldIssues`, and when `heldIssues.length > 8`, add ONE note line after the mapped rows: `<p className="font-[family-name:var(--font-ui)] text-[11px] text-[color:var(--color-ink-soft)]">+{heldIssues.length - 8} more held</p>`. Do not change the `heldIssues` filter itself.

4. InspectorPanel.tsx (~L516): the slide-over is `fixed right-0 top-0 z-40 h-full …` → it paints over the 52px Masthead. Change `top-0` → `top-[52px]` and `h-full` → `h-[calc(100%-52px)]` on that root `<div>` className. Leave `fixed right-0 z-40 … w-full max-w-[480px] …` untouched.

5. HelpTip.tsx (~L76): the popover span className contains `absolute top-full z-20 …` → it renders UNDER the workspace sticky stage nav (z-30). Change `z-20` → `z-40` (stays below modals/tour at z-50). Change ONLY that token.

6. AwaitingYouInbox.tsx (~L123): the dropdown root has fixed `w-[360px]`. Change `w-[360px]` → `w-[min(360px,calc(100vw-24px))]` so it never overflows a narrow viewport. Leave the rest of the className untouched.

7. _PlaceholderScreen.tsx (~L18): root uses viewport-sized `min-h-[60vh]` (it renders inside the workspace's sticky overflow-y-auto rails via ContextPanel, forcing pointless rail scroll). Change `min-h-[60vh]` → `min-h-[320px]`. Change ONLY that token.

8. HeldIssueRow.tsx (~L47-56): the free-text `heldReason` row can shove the Reopen button. On the inner text container `<div className="flex flex-wrap items-center gap-2">` (~L48) add `min-w-0`. On the reason `<span>` (~L53, the "Issue {n} — Held · {heldReason} · …" span) add `break-words`. Preserve all existing classes, the `data-testid`s (none here) and structure.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control build</automated>
  </verify>
  <done>Sidebar bottom block fits (h-full); Issues home uses min-h-full + no double gutter + held capped at 8 with +N note; InspectorPanel starts at top-[52px] h-[calc(100%-52px)]; HelpTip popover z-40; AwaitingYou dropdown width-clamped; PlaceholderScreen min-h-[320px]; HeldIssueRow text container min-w-0 + reason break-words. Build green.</done>
</task>

<task type="auto">
  <name>Task 2: Issue Workspace stage defects — voice width, dead jump-nav, overlay clamps, padding (11 files)</name>
  <files>apps/dispatch-control/app/globals.css, apps/dispatch-control/app/(dashboard)/voice-pass/[runId]/VoicePassRunView.tsx, apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx, apps/dispatch-control/app/(dashboard)/issues/_components/WorkspaceOutline.tsx, apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/DecisionRail.tsx, apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SourceIndex.tsx, apps/dispatch-control/app/(dashboard)/story-brief/_components/StoryBriefScreen.tsx, apps/dispatch-control/app/(dashboard)/signal-desk/_components/SignalDeskScreen.tsx, apps/dispatch-control/components/galley/AnnotationMark.tsx, apps/dispatch-control/components/galley/PassageToolbar.tsx, apps/dispatch-control/components/decision-log/DecisionLog.tsx</files>
  <action>
Add a `quick 260722-tv1` provenance note to each file's top doc-comment where you change it.

VOICE STARVATION (container-query, custom-CSS route — the safe default; do NOT use Tailwind v4 arbitrary container variants):

1. globals.css — append a new block (mirror the existing `.galley-root` container pattern; keep the ASCII/JSDoc comment style):
```css
/* quick 260722-tv1 — Voice Pass container-query layout: one layout serves
   both the narrow Issue-Workspace canvas mount AND the wide standalone
   /voice-pass/[runId] route. Column (rail full-width below galley) by
   default; row (336px fixed rail) once the CANVAS (not viewport) is >= 56rem.
   Replaces the viewport lg: breakpoint that starved the galley in the
   ~582px workspace canvas (the defect Draft was cured of in 260722-n5r). */
.voice-canvas {
  container-type: inline-size;
}
.voice-stage-row {
  display: flex;
  flex-direction: column;
}
.voice-stage-rail {
  width: 100%;
}
@container (min-width: 56rem) {
  .voice-stage-row {
    flex-direction: row;
  }
  .voice-stage-rail {
    width: 336px;
    flex-shrink: 0;
  }
}
```

2. VoicePassRunView.tsx:
   - Root (~L259) `<div className="flex min-h-[70vh] flex-col gap-4">` → `<div className="voice-canvas flex flex-col gap-4">` (DROP `min-h-[70vh]` viewport-sizing; ADD `voice-canvas` to establish the query container).
   - Stage row (~L299) `<div className="flex flex-1 flex-col gap-4 lg:flex-row">` → `<div className="voice-stage-row flex-1 gap-4">` (DROP `flex flex-col lg:flex-row`; the `.voice-stage-row` class owns display+direction so the container query can flip it; keep `flex-1 gap-4` as Tailwind).
   - Rail column (~L385) `<div className="w-full shrink-0 lg:w-[336px]">` → `<div className="voice-stage-rail shrink-0">` (DROP `w-full lg:w-[336px]`; the class owns width per container width).
   - Leave the LEFT galley column `<div className="flex flex-1 flex-col gap-3">` (~L301) untouched.

3. ReviewDeskRunView.tsx (~L465): root `<div className="flex min-h-[70vh] flex-col gap-4">` → `<div className="flex flex-1 flex-col gap-4">` (DROP `min-h-[70vh]`; keep the rest). Do NOT add a container class here — this stage is not container-query-dependent.

DEAD JUMP-NAV (route to the Draft-stage galley anchor when no galley is mounted on the current stage):

4. WorkspaceOutline.tsx: import `useParams` and `useRouter` from `next/navigation`, `parseIssueNumber` and `issueDraftHref` from `@/lib/issueRouteResolver`. In the component body add `const params = useParams()`, `const router = useRouter()`, and derive `const issueNumber = typeof params.issueNumber === 'string' ? parseIssueNumber(params.issueNumber) : null`. Convert `jumpToSection` from a module function into a call that closes over `router`/`issueNumber` (either inline it in the component or pass them as args). New behavior: `const anchor = galleyAnchorFor(sectionId); if (!anchor) return; const el = document.getElementById(anchor); if (el) { el.scrollIntoView({ behavior: 'smooth', block: 'start' }) } else if (issueNumber != null) { router.push(issueDraftHref(issueNumber) + '#' + anchor) }`. Preserve every `data-testid` (`outline-row-${section.id}` etc.).

5. ReviewDeskRunView.tsx (hash-scroll receiver): after `draft` loads, add a one-shot hash-scroll effect mirroring the existing `deepLinkAppliedRef` pattern. Add `const hashScrollAppliedRef = useRef(false)` and a `useEffect` depending on `[draft]`: `if (hashScrollAppliedRef.current) return; if (!draft) return; if (typeof window === 'undefined') return; const hash = window.location.hash; if (!hash.startsWith('#galley-')) return; hashScrollAppliedRef.current = true; document.getElementById(hash.slice(1))?.scrollIntoView({ block: 'start' })`. (The `[id^='galley-'] { scroll-margin-top: 88px }` rule already handles the sticky offset.) Add the eslint-disable-next-line react-hooks/exhaustive-deps comment as the sibling deep-link effect does.

6. DecisionRail.tsx (Approval stage — galley not mounted): import `useRouter` from `next/navigation` and `issueDraftHref` from `@/lib/issueRouteResolver`; add `const router = useRouter()`. In `handleTranscript` (~L306) and `jumpToFinding` (~L312): after computing the anchor, replace the bare `document.getElementById(anchor)?.scrollIntoView(...)` with: try the element first; if absent AND `issueNumber != null`, `router.push(issueDraftHref(issueNumber) + '#' + anchor)`. (`handleTranscript`'s anchor is the literal `'galley-deliberation'`.) When `issueNumber` is undefined (legacy standalone `/review-desk/[runId]`), keep today's attempt-scroll behavior. Pass `issueNumber` down to SourceIndex where it is mounted (find the `<SourceIndex runId={runId} />` mount in the Verification section and add `issueNumber={issueNumber}`).

7. SourceIndex.tsx: add an optional `issueNumber?: number` to `SourceIndexProps`; import `useRouter` from `next/navigation` and `issueDraftHref` from `@/lib/issueRouteResolver`; add `const router = useRouter()`. In `handleJump` (~L100): `const anchor = galleyAnchorId(sectionName); if (!anchor) return; const el = document.getElementById(anchor); if (el) { el.scrollIntoView({ behavior: 'smooth', block: 'start' }) } else if (issueNumber != null) { router.push(issueDraftHref(issueNumber) + '#' + anchor) }`. Preserve the `data-testid` and aria labels.

PADDING CONSISTENCY (stage canvases must not double main/frame padding):

8. StoryBriefScreen.tsx: READ the full file first. Drop the outer `p-6` from the state-branch ROOT wrappers so its content edge matches sibling stage canvases (which carry none): the empty root (~L136 `flex flex-col gap-4 p-6`), the loading root (~L194 `flex flex-col gap-3 p-6`), and the main root (~L219 `flex flex-col gap-6 p-6`). For the FAILED-branch root (~L154), that root is itself a bordered card (`border … bg-[color:var(--color-card)] p-6`) — its `p-6` is genuine internal card padding; KEEP it (do not strip padding that a bordered card depends on). Preserve every `data-testid` (`story-brief-empty`/`-error`/`-loading`).

9. SignalDeskScreen.tsx: it renders standalone at /signal-desk inside main's p-6, so its own `p-6` double-gutters. Drop `p-6` from the no-runs empty root (~L84 `<div className="p-6">` → `<div>`) and from the main root (~L99 `flex flex-col gap-6 p-6` → `flex flex-col gap-6`). Preserve structure.

GALLEY OVERLAY CLAMPS:

10. AnnotationMark.tsx (~L252 popover): add a ref + self-clamp so right-edge annotations don't clip (main's overflow forces overflow-x clip). Add `useLayoutEffect` to the existing `import { useEffect, useRef, useState } from 'react'` (→ add `useLayoutEffect`). Add `const popoverRef = useRef<HTMLSpanElement>(null)` and `const [popoverShift, setPopoverShift] = useState(0)`. Attach `ref={popoverRef}` to the `<span className="galley-popover" role="dialog">`. Add a `useLayoutEffect` keyed on `[open]`: `if (!open) { setPopoverShift(0); return } if (typeof window === 'undefined') return; const el = popoverRef.current; if (!el) return; const rect = el.getBoundingClientRect(); if (rect.width === 0) return; const overflow = rect.right - (window.innerWidth - 16); setPopoverShift(overflow > 0 ? -overflow : 0)`. Apply the shift via inline style ONLY when non-zero so the default CSS position is preserved: `style={popoverShift ? { left: popoverShift } : undefined}` on the popover span. (jsdom returns width 0 → no-ops safely; mirrors HelpTip's self-clamp.)

11. PassageToolbar.tsx (~L172-173): the toolbar is `fixed` and `style={{ top: Math.max(selection.top - 44, 8), left: selection.left }}`. (a) Floor top at 60 (52px masthead + 8) so it never overlaps the Masthead: `top: Math.max(selection.top - 44, 60)`. (b) Clamp left within the viewport using a conservative 360px width estimate: `left: Math.min(selection.left, Math.max(8, (typeof window !== 'undefined' ? window.innerWidth : 1024) - 368))`. (In jsdom selection.left is 0 → `Math.min(0, 656) = 0`, unchanged; in a real narrow viewport it pulls the toolbar left.) Leave the button rows untouched.

12. DecisionLog.tsx (~L185): the `<dl className="grid grid-cols-[auto_1fr] …">` value cells overflow on long runId/JSON snapshots. Add `min-w-0 break-words` to EACH value `<dd>` in that `<dl>` (the 1fr column cells at ~L187,189,191,193-196,198,200,202). Leave the `<dt>` cells and grid template untouched.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control build</automated>
  </verify>
  <done>globals.css has .voice-canvas/.voice-stage-row/.voice-stage-rail; Voice Pass uses them (no lg:w-[336px]/min-h-[70vh]); ReviewDeskRunView drops min-h-[70vh] and has a one-shot #galley- hash-scroll effect; WorkspaceOutline/DecisionRail/SourceIndex route to issueDraftHref(n)+#anchor when the galley anchor is absent; StoryBrief (except bordered error card) + SignalDesk drop outer p-6; AnnotationMark popover self-clamps; PassageToolbar top floored at 60 + left clamped; DecisionLog dd cells min-w-0 break-words. Build green.</done>
</task>

<task type="auto">
  <name>Task 3: run-monitor + ops hardening — sticky tabs, max-width, list caps (9 files)</name>
  <files>apps/dispatch-control/app/(dashboard)/run-monitor/layout.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/graph/page.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/runs/page.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/page.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunsTable.tsx, apps/dispatch-control/app/(dashboard)/eval-center/_components/DriftScoreboard.tsx, apps/dispatch-control/app/(dashboard)/prompt-lab/_components/VersionHistoryPanel.tsx, apps/dispatch-control/app/(dashboard)/prompt-lab/_components/DiffViewer.tsx, apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/_components/IssueComments.tsx</files>
  <action>
Add a `quick 260722-tv1` provenance note to each file's top doc-comment where you change it. Every list cap must render its toggle button OUTSIDE the `<ul>`/`<tbody>` (never as a listitem/row) and keep any `data-testid`/index stable within the shown slice.

1. run-monitor/layout.tsx (~L24): the tab-bar div `<div className="flex shrink-0 items-center gap-6 border-b border-[color:var(--color-faint)] px-1">` is not sticky. Add `sticky top-0 z-20 bg-[color:var(--color-rail)]` to that className (the dashboard shell bg is `var(--color-rail)` per `(dashboard)/layout.tsx:48`, so the sticky bar reads opaque against it). Keep `shrink-0` etc.

2. run-monitor/graph/page.tsx (~L26): `<div className="flex flex-col h-full -m-6">` → `<div className="flex flex-col h-full -mx-6">` (drop the vertical `-my-6` that pulls the header under the tab bar and adds ~24px overscroll; keep `-mx-6`, `h-full`, and the flex structure). Do NOT touch the AutoPublishBanner interplay (out of scope).

3. runs/page.tsx (~L26): `<div className="space-y-4">` → `<div className="mx-auto w-full max-w-[1600px] space-y-4">` (matches the workspace 1600px measure so tables don't stretch edge-to-edge on wide monitors).

4. runs/[runId]/page.tsx (~L27): `<div className="space-y-4">` → `<div className="mx-auto w-full max-w-[1600px] space-y-4">`.

5. RunsTable.tsx (client cap to latest 50): add `import { useState } from 'react'`. After `const runs = useQuery(...)` and the loading/empty guards, add `const [showAll, setShowAll] = useState(false)` and `const visibleRuns = showAll ? runs : runs.slice(0, 50)`. Map `visibleRuns` (not `runs`) in `<tbody>`. AFTER the `</table>` closing tag but INSIDE the outer `<div>`, when `runs.length > 50 && !showAll`, render a toggle: `<button type="button" onClick={() => setShowAll(true)} className="w-full border-t border-neutral-200 px-4 py-3 text-xs font-medium text-blue-600 hover:bg-neutral-50">Show all ({runs.length})</button>`. `runs` is already newest-first from the query — the first 50 are the most recent. Keep the existing row markup and the `View →` link.

6. DriftScoreboard.tsx (ScenarioDriftSeries): add `import { useState } from 'react'`. (a) Wrap the `<table className="mt-1 w-full min-w-[420px] …">` (~L83) in `<div className="overflow-x-auto">…</div>`. (b) Cap to the latest 20 rows: inside `ScenarioDriftSeries`, add `const [showAll, setShowAll] = useState(false)` and `const visibleRows = showAll ? rows : rows.slice(-20)` (rows are ascending oldest-first, so `slice(-20)` is the latest 20). Map `visibleRows` in `<tbody>` — KEEP the existing `data-testid={`drift-point-${scenario.id}-${i}`}` where `i` is the map index within `visibleRows` (test fixtures are 2 rows, so indices stay 0/1). When `rows.length > 20 && !showAll`, render below the table wrapper: `<button type="button" onClick={() => setShowAll(true)} className="mt-1 text-[11px] font-medium text-[color:var(--color-cobalt)] hover:underline">Show all ({rows.length})</button>`.

7. VersionHistoryPanel.tsx (~L237, cap latest 20): READ the surrounding code to confirm `versions` order and the existing `useState` import (it already holds `activating`/`overrideReason` state). Add a `const [showAllVersions, setShowAllVersions] = useState(false)`. Determine order: if `versions` is newest-first, show `versions.slice(0, 20)`; if oldest-first, show `versions.slice(-20)` — pick whichever yields the LATEST 20 (verify against how the list is ordered/rendered). Map the sliced array in the `<ul>`. When `versions.length > 20 && !showAllVersions`, render a toggle ABOVE or BELOW the `<ul>` (outside it): `<button type="button" onClick={() => setShowAllVersions(true)} className="text-xs font-medium text-blue-600 hover:underline">Show older ({versions.length})</button>`. Keep every `data-rollback-mount` intact within the shown slice.

8. DiffViewer.tsx (~L102): the split-diff root `<div className="grid grid-cols-2 divide-x divide-neutral-200 overflow-auto rounded border border-neutral-200 font-mono text-xs">` has `overflow-auto` but no max-height. Add `max-h-[480px]` to that className so vertical scroll engages instead of the diff running unbounded. Change ONLY that className.

9. IssueComments.tsx (~L78, cap latest 20): add `useState` to the existing React import if not already present, and `const [showAllComments, setShowAllComments] = useState(false)`. The fixture/query is oldest-first (verified). Compute `const visibleComments = showAllComments ? comments : comments.slice(-20)` and map `visibleComments` in the `<ul role="list">`. When `comments.length > 20 && !showAllComments`, render ABOVE the `<ul>` (NOT inside it, so it is not a `listitem`): `<button type="button" onClick={() => setShowAllComments(true)} className="self-start font-[family-name:var(--font-ui)] text-[12px] font-medium text-[color:var(--color-cobalt)] hover:underline">Show earlier comments ({comments.length - 20})</button>`. Chronological order within the shown slice is preserved (slice keeps order). With the 2-comment fixture, `slice(-20)` returns both in order and no toggle renders → `getAllByRole('listitem')` stays 2.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control build</automated>
  </verify>
  <done>run-monitor tab bar sticky (sticky top-0 z-20 bg rail); graph page uses -mx-6; both runs pages max-w-[1600px]; RunsTable caps to 50 with Show all toggle; DriftScoreboard series wrapped in overflow-x-auto + capped to 20 with per-scenario toggle; VersionHistoryPanel caps to 20 with Show older toggle; DiffViewer max-h-[480px]; IssueComments caps to latest 20 with Show earlier toggle above the list. Build green.</done>
</task>

</tasks>

<verification>
Full-suite gate (both must be green):
- `pnpm --filter dispatch-control build` (Next.js production build — catches type errors vitest misses, per project memory "run strict build before frontend phase done").
- `pnpm --filter dispatch-control test` (vitest). Update ONLY assertions genuinely invalidated by markup moves (e.g. a test asserting a moved `data-testid`/aria on a repositioned element). Do NOT weaken logic tests. Per the verified test facts, no existing assertion should break — if one does, confirm it is a pure markup assertion before touching it.

Spot-invariants (no automated harness — confirmed by the build + preserved-attribute rule):
- Every `data-testid` and aria attribute preserved and moved with its element.
- Mobile / `grid-cols-1` behavior unchanged (all changes are additive clamps or column-first container queries).
- No Convex function file touched; no sign-off-gate or data-flow logic changed.
</verification>

<success_criteria>
- All 23 audit findings closed across the 26 listed files, grouped into the 3 tasks.
- `pnpm --filter dispatch-control build` AND `pnpm --filter dispatch-control test` both green.
- No `convex/*.ts` changes; no run-monitor visual-token modernization; AppSidebar mobile collapse + MyTasks severity grouping left untouched (out of scope).
- Provenance comments citing `quick 260722-tv1` added to each changed file.
</success_criteria>

<output>
After completion, create `.planning/quick/260722-tv1-fix-app-wide-layout-audit-findings-sideb/260722-tv1-SUMMARY.md`.
</output>
