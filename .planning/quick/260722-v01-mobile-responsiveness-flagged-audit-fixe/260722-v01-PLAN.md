---
phase: quick-260722-v01
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [MOBILE-RESPONSIVE, AUDIT-1, AUDIT-2, AUDIT-3, AUDIT-4, AUDIT-5, AUDIT-6, AUDIT-7, AUDIT-8, AUDIT-9]
files_modified:
  # Task 1
  - apps/dispatch-control/components/SidebarNav.tsx            # NEW — extracted shared nav
  - apps/dispatch-control/components/AppSidebar.tsx
  - apps/dispatch-control/components/MobileNavDrawer.tsx        # NEW — off-canvas drawer + hamburger
  - apps/dispatch-control/components/Masthead.tsx
  - apps/dispatch-control/app/(dashboard)/layout.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/layout.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx
  # Task 2
  - apps/dispatch-control/app/(dashboard)/my-tasks/_components/MyTasksScreen.tsx
  - apps/dispatch-control/app/(dashboard)/story-brief/_components/LeadActions.tsx
  - apps/dispatch-control/app/(dashboard)/story-brief/_components/BriefFieldStrengthen.tsx
  - apps/dispatch-control/app/(dashboard)/story-brief/_components/StoryBriefScreen.tsx
  - apps/dispatch-control/components/ui/HelpTip.tsx
  - apps/dispatch-control/app/(dashboard)/registry/_components/RegistryTable.tsx
  - apps/dispatch-control/app/(dashboard)/finance/_components/IssueRevenueTable.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/_components/ClaimsChecklist.tsx  # DELETE
  - convex/runs.ts
  - convex/_generated/*                                        # if codegen changes on deploy
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunsTable.tsx
  - apps/dispatch-control/__tests__/LeadActions.test.tsx
  - apps/dispatch-control/__tests__/BriefFieldStrengthen.test.tsx
  - apps/dispatch-control/__tests__/MyTasksScreen.test.tsx
  - apps/dispatch-control/__tests__/StoryBriefScreen.test.tsx
  # Task 3 (class-token swaps only)
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/ReviewQueue.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/CostRollup.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RecoveryRail.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/BudgetAlertBanner.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunControlBar.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/CancelRunButton.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RerollButton.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/graph/_components/DriftStrip.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/graph/page.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/runs/[runId]/page.tsx

must_haves:
  truths:
    - "Below md width the left sidebar is hidden and a hamburger in the masthead opens the same nav as an off-canvas drawer"
    - "Below md the main content pads 16px (p-4), md+ keeps 24px (p-6)"
    - "The workspace stage tabs scroll horizontally in one row on phones instead of wrapping into a tall block"
    - "With the AutoPublishBanner active, the graph page sizes to the space under the banner instead of overflowing"
    - "My Tasks rows are grouped into Must fix / Review recommended / Information sections with counts"
    - "The Story stage shows exactly ONE consolidated Decision log, not one per lead/brief field"
    - "HelpTip popovers render in a body portal (fixed) and never clip inside overflow ancestors"
    - "Registry and Finance revenue tables cap at latest 50 / 24 with a Show-all toggle"
    - "Run Monitor surfaces use the var(--color-*) token system, matching the rest of the app"
  artifacts:
    - path: "apps/dispatch-control/components/SidebarNav.tsx"
      provides: "Shared nav content (NAV_GROUPS + NAV_PINNED + RoleIndicator) rendered by both desktop sidebar and mobile drawer"
    - path: "apps/dispatch-control/components/MobileNavDrawer.tsx"
      provides: "Self-contained hamburger trigger + off-canvas drawer + backdrop, mounted from the dashboard layout"
    - path: "convex/runs.ts"
      provides: "Additive listRecentForWorkspace query (server-side take(100))"
      contains: "listRecentForWorkspace"
  key_links:
    - from: "apps/dispatch-control/app/(dashboard)/layout.tsx"
      to: "MobileNavDrawer + AppSidebar (hidden md:flex)"
      via: "client child rendered in the server layout; SidebarNav shared by both"
      pattern: "MobileNavDrawer"
    - from: "apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunsTable.tsx"
      to: "api.runs.listRecentForWorkspace"
      via: "useQuery switch (only after successful convex deploy)"
      pattern: "listRecentForWorkspace"
---

<objective>
Close the "Flagged, NOT fixed" items from the 2026-07-22 dispatch-control layout
audit, make dispatch-control genuinely mobile-responsive, and unify the Run
Monitor surfaces onto the app's token system — reducing operator confusion and
making the whole console feel like one product.

Purpose: dispatch-control is unusable below tablet width (hard 210px sidebar,
no collapse) and carries several confirmed-but-deferred UX defects (flat task
list, ~11 duplicate Decision logs on Story, unpaginated tables, dead code, and
a Run Monitor still on raw neutral-* Tailwind). This plan fixes all of them.

Output: a responsive shell with an off-canvas mobile drawer, a height-chained
graph/banner layout, grouped tasks, one Story Decision log, a portaled HelpTip,
capped tables, an additive recent-runs Convex query, and a token-migrated Run
Monitor. Mobile behavior is verified by concrete class-level review (no browser).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ui-reviews/dispatch-control-20260722-LAYOUT-AUDIT.md

<shell_ground_truth>
`app/(dashboard)/layout.tsx` (Server Component — MUST stay server):
```
<div className="flex h-screen flex-col overflow-hidden bg-[color:var(--color-rail)]">
  <Masthead />                                      {/* 52px, 'use client' */}
  <div className="flex flex-1 overflow-hidden">
    <AppSidebar />                                  {/* fixed w-[210px], h-full */}
    <main className="flex-1 overflow-y-auto p-6">   {/* the ONLY scrollport */}
      <div className="mb-4 empty:mb-0"><AutoPublishBanner … /></div>
      {children}
    </main>
  </div>
</div>
<OnboardingTour />
```
`<main>` is the app's only scrollport (100vh − 52px) and already pads every page.
</shell_ground_truth>

<design_tokens>
Defined in `app/globals.css` (all exist): `--color-ink`, `--color-ink-soft`,
`--color-faint` (#8b8778), `--color-rail`, `--color-nav`, `--color-card`,
`--color-card-alt` (#fbfaf6), `--color-cobalt` (#253ad4), `--color-vermilion`
(#e8471d), `--color-marigold` (#f2b01e), `--color-marigold-text` (#9a6f04),
`--color-green` (#148a52). Fonts: `--font-ui`, `--font-display`, `--font-mono`.
Status-chip precedent (copy verbatim): `PayoutRow.tsx` uses
`bg-[color:var(--color-marigold)]/20 … text-[color:var(--color-marigold-text)]`.
</design_tokens>

<key_facts>
- Masthead.tsx is a client component. AppSidebar.tsx is a client component and
  currently owns NAV_GROUPS/NAV_PINNED mapping + RoleIndicator inside its `<aside>`.
- AppSidebar.test.tsx renders `<AppSidebar/>` and asserts the "Signed in as
  {role}" text + `role-indicator` testid — RoleIndicator MUST stay inside
  whatever AppSidebar renders (moving it into shared SidebarNav is fine; jsdom
  ignores the `hidden` class so display:none never hides it from queries).
- Masthead.test.tsx mocks ONLY `convex/react`, `@convex/_generated/api`,
  `@clerk/nextjs`. It does NOT mock `next/navigation` or `@/lib/role`. Therefore
  the mobile drawer MUST NOT be imported into Masthead.tsx (that would add
  usePathname/useRole imports and break the Masthead test). Mount the drawer in
  the dashboard LAYOUT instead; only add a className to Masthead.
- `deriveTasks`/`TaskSeverity` vocabulary (lib/derivedState.ts): three severities
  `'must-fix' | 'review-recommended' | 'information'`; SEVERITY_META labels are
  "Must fix" / "Review recommended" / "Information". Tasks are already sorted
  must-fix→review-recommended→information, then by stage.
- `DecisionLog` (components/decision-log/DecisionLog.tsx) takes `{ runId?,
  issueNumber? }` and subscribes to `auditLog.listDecisions`. It is currently
  mounted once inside `LeadActions` (per lead) AND once inside
  `BriefFieldStrengthen` (per brief field) — up to ~11 identical run-scoped logs
  on the Story page.
- `ClaimsChecklist.tsx` has ZERO real importers (only a comment mention in
  components/galley/ClaimMark.tsx) and NO dedicated test file. Its route
  (`run-monitor/runs/[runId]/review/page.tsx`) is redirect-only.
- `convex/runs.listForWorkspace` `.collect()`s every run; subscribers are
  RunsTable, ReviewQueue, CostRollup, DriftStrip. ONLY RunsTable may switch to
  the new query. `by_workspace` index tuple is `[workspace_id, _creationTime]`.
- AuditLogViewer.tsx is the pagination exemplar (`limit: 50` + "showing latest
  50" note). RunsTable.tsx is the client-cap exemplar (slice(0,50) + Show-all).
</key_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile responsiveness (drawer, padding, stage-tab scroll) + graph/banner height chain</name>
  <files>
    apps/dispatch-control/components/SidebarNav.tsx (NEW),
    apps/dispatch-control/components/AppSidebar.tsx,
    apps/dispatch-control/components/MobileNavDrawer.tsx (NEW),
    apps/dispatch-control/components/Masthead.tsx,
    apps/dispatch-control/app/(dashboard)/layout.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/layout.tsx,
    apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx
  </files>
  <action>
Add a file-header provenance comment citing `quick 260722-v01` to every file
touched below.

**1a — Extract shared nav, add responsive drawer (audit item 1).**

Create `components/SidebarNav.tsx` (`'use client'`): move the ENTIRE inner nav
markup out of AppSidebar — the `<nav aria-label="Main navigation">…</nav>` block
including the NAV_GROUPS mapping, the `NavLink` helper, the `NAV_PINNED` pinned
block, and `RoleIndicator`. Keep `usePathname`/`isActiveHref`/`useRole` usage
here. Export default `SidebarNav`. Preserve the `role-indicator` testid and
every class verbatim. Optional prop `onNavigate?: () => void` fired from each
`NavLink`'s onClick (the drawer passes a close handler; the desktop sidebar
passes nothing).

Rewrite `AppSidebar.tsx`: the `<aside>` becomes `hidden md:flex` (removed from
the flow below md; unchanged 210px in-flow at md+). Keep `h-full w-[210px]
flex-col border-r … bg-[color:var(--color-nav)] py-[14px]`. Its only child is
now `<SidebarNav />`. (AppSidebar.test.tsx still renders AppSidebar → SidebarNav
→ RoleIndicator, so it passes unchanged; `hidden` is a class, not removal.)

Create `components/MobileNavDrawer.tsx` (`'use client'`) — fully self-contained:
  - Local `useState` `open`. A hamburger `<button>`: `fixed left-[8px] top-0 z-[60]
    flex h-[52px] min-h-[44px] min-w-[44px] items-center justify-center
    text-[color:var(--color-masthead-text)] md:hidden` (sits over the ink
    masthead's left edge; hidden at md+). Use the lucide `Menu` icon, `size={20}`,
    `aria-hidden`. Button gets `aria-label="Open navigation"`,
    `aria-expanded={open}`, `aria-controls="mobile-nav-drawer"`.
  - When `open`: render a backdrop `<button aria-label="Close navigation"
    className="fixed inset-0 z-40 cursor-default md:hidden" onClick={close}/>` and
    a drawer `<aside id="mobile-nav-drawer" className="fixed left-0 top-[52px]
    z-50 h-[calc(100%-52px)] w-[240px] overflow-y-auto border-r
    border-[color:var(--color-ink)]/[.13] bg-[color:var(--color-nav)] py-[14px]
    md:hidden"><SidebarNav onNavigate={close} /></aside>`.
  - Escape closes (keydown listener on window while open). Route change closes:
    `useEffect(() => close(), [pathname])` with `pathname = usePathname()`.
    Focus the first link on open (query `#mobile-nav-drawer a` in a
    layout/effect, best-effort). SSR-safe (all rendering is client;
    `md:hidden` keeps it inert at md+).

Mount `<MobileNavDrawer />` in `app/(dashboard)/layout.tsx` — render it as the
FIRST child inside the outer `<div className="flex h-screen flex-col …">`,
BEFORE `<Masthead />`. Do NOT import it into Masthead (would break Masthead.test).
The layout stays a Server Component (it may render this client child).

Edit `Masthead.tsx` (className ONLY — no new imports): add mobile left-padding so
the fixed hamburger does not overlap the wordmark. Change the header class
`… px-[22px] …` → `… pl-[52px] pr-[22px] md:pl-[22px] …` (reserve 52px on the
left below md; unchanged at md+). No other Masthead change.

**1b — Main padding on mobile.** In `layout.tsx`, `<main>` `p-6` → `p-4 md:p-6`.

**1c — Workspace stage tabs horizontal scroll (mobile).** In
`issues/[issueNumber]/layout.tsx`, the sticky stage `<nav>` is `sticky top-0
z-30 flex flex-wrap gap-2 …`. Change to `… flex flex-wrap gap-2 max-lg:flex-nowrap
max-lg:overflow-x-auto …` (below lg: single scrollable row; lg+ unchanged wrap).
Each stage `<Link>` currently `flex min-h-[44px] min-w-[112px] …` → add
`shrink-0` so tabs don't compress. The sticky bg (`bg-[color:var(--color-rail)]`)
stays on the nav so it still covers scrolled content.

**1d — Graph × AutoPublishBanner height chain (audit item 4).**
In `layout.tsx`: `<main className="flex-1 overflow-y-auto p-4 md:p-6">` →
`<main className="flex flex-1 flex-col overflow-y-auto p-4 md:p-6">`, and the
banner wrapper `<div className="mb-4 empty:mb-0">` → `<div className="mb-4
shrink-0 empty:mb-0">`. In `run-monitor/layout.tsx`, the shell root
`<div className="flex h-full flex-col">` → `<div className="flex min-h-0 flex-1
flex-col">`. Net effect: the run-monitor shell (and the graph page's `h-full`
chain inside it) sizes to the height REMAINING under the banner instead of
overflowing by the banner's height.

Before finishing, code-review scan the direct page roots that become flex-column
items of `<main>` (MyTasksScreen `flex flex-col gap-4`, issues workspace
`min-h-full`, settings/finance/registry screen roots, run-monitor shell): confirm
each has a single root with its own gap/padding spacing and none relies on
block-flow margin-collapsing (flex items don't collapse margins — verify no root
uses a top/bottom margin expecting collapse). Document the scan result in the
SUMMARY.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control build</automated>
    <automated>pnpm --filter dispatch-control test -- AppSidebar Masthead</automated>
  </verify>
  <done>
    - `SidebarNav.tsx` exists; AppSidebar renders it and the aside is `hidden md:flex`; `role-indicator` testid preserved; AppSidebar.test green.
    - `MobileNavDrawer.tsx` exists: hamburger is `fixed left-[8px] top-0 z-[60] … md:hidden` with `aria-expanded`/`aria-controls="mobile-nav-drawer"`; open drawer is `fixed left-0 top-[52px] z-50 h-[calc(100%-52px)] w-[240px] … md:hidden` with a `fixed inset-0 z-40 … md:hidden` backdrop; Escape + backdrop-click + route-change all close.
    - Drawer mounted in `(dashboard)/layout.tsx` (NOT in Masthead); layout still a Server Component; Masthead.test green (only a className changed on the header).
    - Masthead header is `pl-[52px] pr-[22px] md:pl-[22px]`.
    - `<main>` is `flex flex-1 flex-col overflow-y-auto p-4 md:p-6`; banner wrapper has `shrink-0`; `run-monitor/layout.tsx` root is `flex min-h-0 flex-1 flex-col`.
    - Stage nav is `… flex flex-wrap gap-2 max-lg:flex-nowrap max-lg:overflow-x-auto`; each stage Link has `shrink-0`.
    - Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flagged functional/UX items (task grouping, Decision-log consolidation, HelpTip portal, table caps, dead-code delete, additive recent-runs query)</name>
  <files>
    apps/dispatch-control/app/(dashboard)/my-tasks/_components/MyTasksScreen.tsx,
    apps/dispatch-control/app/(dashboard)/story-brief/_components/LeadActions.tsx,
    apps/dispatch-control/app/(dashboard)/story-brief/_components/BriefFieldStrengthen.tsx,
    apps/dispatch-control/app/(dashboard)/story-brief/_components/StoryBriefScreen.tsx,
    apps/dispatch-control/components/ui/HelpTip.tsx,
    apps/dispatch-control/app/(dashboard)/registry/_components/RegistryTable.tsx,
    apps/dispatch-control/app/(dashboard)/finance/_components/IssueRevenueTable.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/_components/ClaimsChecklist.tsx (DELETE),
    convex/runs.ts,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunsTable.tsx,
    apps/dispatch-control/__tests__/LeadActions.test.tsx,
    apps/dispatch-control/__tests__/BriefFieldStrengthen.test.tsx,
    apps/dispatch-control/__tests__/MyTasksScreen.test.tsx,
    apps/dispatch-control/__tests__/StoryBriefScreen.test.tsx
  </files>
  <action>
Add/refresh file-header provenance comments citing `quick 260722-v01` on every
touched file.

**2a — My Tasks severity grouping (audit item 2).** In the PURE `MyTasksList`
(MyTasksScreen.tsx), after the existing `tasks.length === 0` empty-state guard,
extract the current per-task render body into a local `renderTask(task)` function
(byte-identical markup + testids). Then partition the `tasks` array (already
sorted by severity) into the three `TaskSeverity` buckets and render sections in
order: `must-fix`, `review-recommended`, `information`. For each NON-empty bucket
render a `<section>` with a single-element MICRO-LABEL header whose full text is
`{SEVERITY_META[sev].label} · {count}` (e.g. "Must fix · 3") — put the label and
count in ONE text node (no nested `<span>` reading exactly "Must fix"), so the
existing `getByText('Must fix')` SeverityBadge assertion stays unambiguous —
followed by a `<ul role="list" className="flex flex-col gap-3">` of that bucket's
`renderTask` rows. Omit empty buckets. MICRO_LABEL class (copy StoryBriefScreen's):
`font-[family-name:var(--font-ui)] text-xs font-semibold uppercase
tracking-[.09em] text-[color:var(--color-ink-soft)]`. Keep the empty state and
every row testid unchanged. Update MyTasksScreen.test.tsx ONLY if a new
assertion for the section headers is warranted (add one asserting a "Must fix ·
N" header renders); do not weaken existing row assertions.

**2b — Consolidate Story Decision log (audit item 6).** Remove the
`<DecisionLog runId={runId} />` mount and its `import DecisionLog` from BOTH
`LeadActions.tsx` (~L123 + L25 import) and `BriefFieldStrengthen.tsx` (~L163 +
L36 import). In `StoryBriefScreen.tsx`, mount ONE `<DecisionLog runId={effectiveRunId}
/>` at the BOTTOM of the main return (after the Brief `<section>`), inside a
small labeled block: a `<section aria-label="Decision log">` with an
`<h2 className={MICRO_LABEL}>Decision log</h2>` then `<DecisionLog
runId={effectiveRunId} />`. `effectiveRunId` is non-null in the main return path
(guarded above). An unfiltered run-scoped log is acceptable (DecisionLog already
scopes by runId). Update the tests: in LeadActions.test.tsx and
BriefFieldStrengthen.test.tsx, remove/relax the assertions that the shared
DecisionLog renders INSIDE those components (the removal/apply reason now
surfaces in the Story-level log, not per row) — keep the require/remove/apply
behavior assertions. If StoryBriefScreen.test.tsx needs it, add one assertion
that a single Decision-log region renders.

**2c — Portal HelpTip (audit item 7).** Rewrite the popover in `HelpTip.tsx` to
render via `createPortal(popover, document.body)` with `position: fixed`,
positioned from the trigger's `getBoundingClientRect()`: default below the
trigger (top = rect.bottom + 4, left = rect.left), clamped to the viewport left/
right with 8px margins (max-w 240px), and flipped above (bottom-anchored) if
there is no room below. Keep: `aria-describedby` wiring to the tip id when open;
hover (mouseenter/leave) + focus/blur + click-toggle + Escape behavior; the 44px
trigger (`min-h-[44px] min-w-[44px]`); `max-w-[240px]`; `role="tooltip"`. Raise
z to `z-[60]` (dialogs are z-50; a help icon inside a dialog may legitimately
sit above it). SSR-safe: only portal after mount (a `mounted` state set in
`useEffect`, or bail out of the portal until `typeof document !== 'undefined'` &&
mounted). jsdom returns zero-width rects — the positioning math must never throw
there (guard `rect.width === 0` like the current impl). HelpTip.test.tsx queries
by role/text/`document.getElementById` (all portal-transparent), so it should
pass unchanged — confirm, and update ONLY if a query breaks.

**2d — Table caps (audit item 5), copying the AuditLogViewer/RunsTable
exemplar.** In `RegistryTable.tsx`: after computing `filtered`, add
`const [showAll, setShowAll] = useState(false)` (call the hook unconditionally,
before any early return) and render `showAll ? filtered : filtered.slice(0, 50)`;
add a "showing latest 50 of {filtered.length}" note near the filter pills when
`filtered.length > 50 && !showAll`, and a full-width "Show all ({filtered.length})"
button below the table (mirror RunsTable's button). In `IssueRevenueTable.tsx`:
same pattern with a 24-row cap over `revenue.issues` (keep the Unattributed row
always visible, outside the cap); note "showing latest 24 of {N}". Client-side
only. (Task 3 later swaps RegistryTable's raw neutral-* tokens — leave those for
Task 3; here add only the cap logic.)

**2e — Delete dead ClaimsChecklist (audit item 8).** Confirm zero real importers
(only the comment in components/galley/ClaimMark.tsx — grep to reconfirm), then
`git rm` `run-monitor/runs/[runId]/review/_components/ClaimsChecklist.tsx`. No
dedicated test file exists (do not touch the sibling `PreviewIframe.tsx`).

**2f — Additive recent-runs Convex query (audit item 3) — ADDITIVE ONLY.**
In `convex/runs.ts`, add `export const listRecentForWorkspace = query({...})`:
same `by_workspace` index, `.order('desc').take(100)` (orders by the index's
implicit `_creationTime` desc, which ≈ `startedAt` desc for runs — document this
assumption in the docstring), then `.sort((a,b) => b.startedAt - a.startedAt)`
the taken rows before returning. Do NOT modify `listForWorkspace` or any other
query. Switch ONLY `RunsTable.tsx` from `api.runs.listForWorkspace` to
`api.runs.listRecentForWorkspace` (keep its existing `slice(0,50)` cap +
Show-all toggle working against the ≤100 rows). Do NOT touch ReviewQueue /
CostRollup / DriftStrip.

Then DEPLOY (project memory: committing convex/*.ts ≠ deployed — Phase 39 shipped
a prod 500 exactly this way): run `pnpm --filter @eisenbalm/convex dev:once` from
the repo root (creds in convex/.env.local; deployment dev:modest-magpie-797). If
codegen changes `convex/_generated/*`, commit those. If the deploy ERRORS on
credentials, keep the convex/runs.ts change committed but REVERT the RunsTable
switch back to `listForWorkspace`, and record the blocker in the SUMMARY — the UI
must NEVER reference an undeployed query.

**2g — Fix the Draft stage "Show preview" collapsed iframe (user-reported).**
`ReviewDeskRunView.tsx` mounts the preview as `<div className="min-h-[500px]
flex-1"><PreviewIframe …/></div>`. `PreviewIframe`'s internals are a `h-full`
chain (`relative h-full w-full` div + `h-full w-full` iframe), but `min-height`
+ unconstrained `flex-1` never yields a DEFINITE height, so `h-full` resolves
to auto and the iframe collapses to the UA default ~150px strip inside the
500px box. Fix the mount wrapper in `ReviewDeskRunView.tsx`: replace
`min-h-[500px] flex-1` with `h-[max(560px,calc(100vh-280px))] w-full border
border-[color:var(--color-faint)] bg-white` (definite height → the h-full chain
resolves; the border frames the preview against the rail background). Leave
`PreviewIframe.tsx`'s internals alone in this task (its `neutral-*` classes are
migrated in Task 3 — add it to Task 3's file list).
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test -- MyTasksScreen LeadActions BriefFieldStrengthen StoryBriefScreen HelpTip</automated>
    <automated>pnpm --filter dispatch-control build</automated>
  </verify>
  <done>
    - My Tasks renders up to three `<section>`s ("Must fix · N" / "Review recommended · N" / "Information · N") in that order; empty buckets omitted; every row testid preserved; `getByText('Must fix')` still resolves to the single SeverityBadge.
    - `DecisionLog` no longer imported/mounted in LeadActions or BriefFieldStrengthen; exactly ONE `<DecisionLog runId={effectiveRunId} />` in StoryBriefScreen under a "Decision log" heading; those three tests green.
    - HelpTip popover renders through `createPortal(document.body)` with `position: fixed`, `z-[60]`, viewport-clamped, flip-above fallback; SSR-safe; HelpTip.test green.
    - RegistryTable caps at latest 50 (+ Show-all + "showing latest 50 of N"); IssueRevenueTable caps at latest 24 issues (+ Show-all + note), Unattributed row unaffected.
    - `ClaimsChecklist.tsx` deleted; grep confirms no import breakage; build green.
    - Draft "Show preview" wrapper has a definite height (`h-[max(560px,calc(100vh-280px))]`) so the iframe's `h-full` chain resolves — no more collapsed ~150px strip.
    - `convex/runs.ts` has `listRecentForWorkspace` (take(100) + startedAt sort, documented assumption); `listForWorkspace` untouched. If deployed: RunsTable uses the new query, `pnpm --filter @eisenbalm/convex typecheck` green, `_generated` diffs committed. If deploy blocked: RunsTable reverted to `listForWorkspace` and blocker recorded in SUMMARY.
    - Full `pnpm --filter dispatch-control test` and `build` green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run Monitor visual unification (audit item 9) — class-token swaps only</name>
  <files>
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunsTable.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/ReviewQueue.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/CostRollup.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RecoveryRail.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/BudgetAlertBanner.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunControlBar.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/CancelRunButton.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RerollButton.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/graph/_components/DriftStrip.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/graph/page.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/layout.tsx,
    apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/runs/[runId]/page.tsx,
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/_components/PreviewIframe.tsx
  </files>
  <action>
STRUCTURAL changes are OUT of scope — this is a mechanical class-token swap that
brings Run Monitor onto the 1c `var(--color-*)` / bracket-pixel system used
across the issues/workspace surfaces. Preserve ALL data-testids, all behavior,
all layout structure. Add/refresh a `quick 260722-v01` provenance line on each
file. Reference `ReviewQueue`-vs-`IssueCard` and `AuditLogViewer` for the target
look.

Apply this mapping to every raw class in the listed files:
  - `text-neutral-900` → `text-[color:var(--color-ink)]`
  - `text-neutral-500/600/700`, `text-neutral-800` → `text-[color:var(--color-ink-soft)]`
  - `text-neutral-400` → `text-[color:var(--color-faint)]`
  - `border-neutral-100/200/300` → `border-[color:var(--color-faint)]`
    (for hairline dividers `divide-neutral-100` → `divide-[color:var(--color-ink)]/10`)
  - `bg-neutral-50/100` → `bg-[color:var(--color-card-alt)]`; a plain card
    surface `bg-white` → `bg-[color:var(--color-card)]` (per context)
  - status/severity chips:
      `bg-yellow-100 text-yellow-800` / `bg-amber-100 text-amber-800`
        → `bg-[color:var(--color-marigold)]/20 text-[color:var(--color-marigold-text)]`
        (copy PayoutRow.tsx's exact marigold chip)
      `bg-red-* text-red-*` → vermilion:
        `bg-[color:var(--color-vermilion)]/15 text-[color:var(--color-vermilion)]`
        (plain error text `text-red-600` → `text-[color:var(--color-vermilion)]`)
      `bg-green-* text-green-*` → `bg-[color:var(--color-green)]/15 text-[color:var(--color-green)]`
        (plain `text-green-800` → `text-[color:var(--color-green)]`)
      `bg-blue-* text-blue-*` (RunsTable/RunDetail "running" chip, "View →"
        links) → cobalt: chips `bg-[color:var(--color-cobalt)]/15
        text-[color:var(--color-cobalt)]`; links `text-[color:var(--color-cobalt)]
        hover:text-[color:var(--color-cobalt-dark)]`
  - `focus-visible:ring-neutral-900` → `focus-visible:ring-[color:var(--color-vermilion)]`
    (match the app's focus-ring convention)
  - typography: `font-medium text-sm` UI text → `font-[family-name:var(--font-ui)]
    text-[13px]`; `text-xs` UI text → `text-[11px]` (add `font-ui` where it's UI
    chrome text); headings `text-xl font-semibold` →
    `font-[family-name:var(--font-display)] text-[20px] font-semibold
    text-[color:var(--color-ink)]`; `text-base font-semibold` section headings →
    `font-[family-name:var(--font-display)] text-[15px] font-semibold
    text-[color:var(--color-ink)]`.

Update the STATUS_CLASSES maps in RunsTable.tsx and RunDetail.tsx (and any
`statusBadgeClass` helper) to the token values above. Migrate the specific
targets called out by the audit: `graph/page.tsx` header (`text-neutral-900` /
`text-neutral-500`), `issues/[issueNumber]/runs/[runId]/page.tsx` header +
back-link (`text-neutral-900` / `text-neutral-500`), and the
`run-monitor/layout.tsx` tab-label typography (`text-sm font-medium` →
`font-[family-name:var(--font-ui)] text-[13px]`; the cobalt active-underline is
already tokened — keep it).

Do NOT change any React structure, hooks, props, testids, or copy. After the
swap, run the suite; if a test asserts a specific legacy class (e.g.
`toHaveClass('bg-yellow-100')`), update ONLY that exact assertion to the new
token value — change no other assertion. (registryDoNotUse.test.ts and
DriftStrip.test.tsx were checked and assert behavior/text, not these classes.)
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test</automated>
    <automated>pnpm --filter dispatch-control build</automated>
  </verify>
  <done>
    - No raw `neutral-*`, `bg-yellow-*/red-*/green-*/blue-*`, or `text-sm/text-xs`-without-token remain in the listed Run Monitor files (grep clean per the mapping); all replaced with `var(--color-*)` / bracket-pixel equivalents.
    - STATUS_CLASSES in RunsTable + RunDetail use token chip classes; graph/page.tsx, issues run-detail page, and run-monitor layout tab labels migrated.
    - Every data-testid, hook, prop, and copy string preserved; zero structural change.
    - Full `pnpm --filter dispatch-control test` green (only exact legacy-class assertions, if any, updated to token values) and `build` green.
  </done>
</task>

</tasks>

<verification>
Run from repo root after all three tasks:
- `pnpm --filter dispatch-control build` — clean (project memory: vitest does NOT
  type-check; the build is the gate that caught prior Vercel/Linux-only bugs).
- `pnpm --filter dispatch-control test` — full suite green.
- If convex/runs.ts was deployed: `pnpm --filter @eisenbalm/convex typecheck` green
  and `convex/_generated/*` diffs committed.

Mobile behavior is verified by class-level code review (no browser). The
per-task `<done>` blocks carry the concrete class assertions
(`hidden md:flex`, `fixed top-[52px] z-50` drawer + `z-40` backdrop,
`p-4 md:p-6`, `max-lg:flex-nowrap max-lg:overflow-x-auto`, `flex min-h-0 flex-1
flex-col` run-monitor root, `createPortal(document.body)` + `z-[60]` HelpTip).
</verification>

<success_criteria>
- Below md: sidebar hidden, hamburger opens the same nav as an off-canvas
  drawer; main pads p-4; stage tabs scroll in one row.
- Graph page no longer overflows when the AutoPublishBanner is active.
- My Tasks grouped by severity; Story shows one Decision log; HelpTip portaled;
  Registry/Finance tables capped with Show-all; ClaimsChecklist deleted.
- `listRecentForWorkspace` added + deployed (or cleanly deferred with RunsTable
  reverted and blocker recorded); other subscribers untouched.
- Run Monitor surfaces on the token system; zero structural change; all testids
  preserved.
- `(dashboard)/layout.tsx` stays a Server Component; Masthead not imported by the
  drawer.
- `pnpm --filter dispatch-control build` + `test` green.
</success_criteria>

<output>
After completion, create `.planning/quick/260722-v01-mobile-responsiveness-flagged-audit-fixe/260722-v01-SUMMARY.md`.
Record in it: (1) the flex-column margin-collapse scan result (1d), (2) whether
the Convex deploy (2f) succeeded — and if not, that RunsTable was reverted to
`listForWorkspace` and why, (3) any test assertions that had to change.
</output>
