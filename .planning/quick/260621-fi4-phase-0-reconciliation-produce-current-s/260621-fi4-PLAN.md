---
phase: quick-260621-fi4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CURRENT_STATE.md
autonomous: true
requirements: [PHASE0-RECON]
must_haves:
  truths:
    - "A reader of docs/CURRENT_STATE.md can answer all five §0 questions with concrete file:line citations from the real codebase"
    - "The doc explicitly states where agent prompts live today (the single most important finding)"
    - "The doc flags where the Mission Control brief's assumed architecture diverges from reality"
    - "No production code was modified — the only new/changed file is docs/CURRENT_STATE.md"
  artifacts:
    - path: "docs/CURRENT_STATE.md"
      provides: "Phase 0 reconciliation report answering the five brief questions, grounded with file:line citations"
      min_lines: 120
      contains: "## Divergences from the Mission Control brief"
  key_links:
    - from: "docs/CURRENT_STATE.md"
      to: "packages/pipeline/src/eisenbalm_pipeline/prompts/"
      via: "citation of prompt-file location + lib/prompts.py loader"
      pattern: "prompts/.*\\.md|lib/prompts\\.py"
---

<objective>
Produce `docs/CURRENT_STATE.md` — a single read-only reconciliation report that answers the Mission Control dashboard brief's five Phase 0 questions against the ACTUAL Eisenbalm codebase, not the brief's assumptions. This is a HARD GATE: no dashboard code may be written until this document exists and is grounded in real file:line evidence.

Purpose: The dashboard's whole premise is externalizing agent prompts out of code into a DB. Phase 0 must establish ground truth — especially where prompts live today — so subsequent dashboard phases are scoped honestly.

Output: `docs/CURRENT_STATE.md` (alongside `CLAUDE_CODE_BRIEF.md` and `API_CONTRACTS.md`). NO production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/CLAUDE_CODE_BRIEF.md
@docs/API_CONTRACTS.md
@CLAUDE.md

# Reconciliation targets (verify these against reality — do not trust without reading)
# Prompts (Q1 — the single most important finding):
@packages/pipeline/src/eisenbalm_pipeline/lib/prompts.py
# Orchestration + trigger (Q2):
@packages/pipeline/src/eisenbalm_pipeline/graph/builder.py
@packages/pipeline/src/eisenbalm_pipeline/api/main.py
@packages/pipeline/src/eisenbalm_pipeline/api/runs.py
# Cost/logging capture (Q4) — recent commits da6e43d / 99d68b8 added real OpenRouter token+USD capture:
@packages/pipeline/src/eisenbalm_pipeline/lib/cost.py
@packages/pipeline/src/eisenbalm_pipeline/lib/openrouter_client.py

<facts>
<!-- Ground-truth surface map from planning discovery. Executor MUST verify each with its own reads before citing. -->
- Agent prompts are NOT hardcoded in agent .py files. They live as markdown files in
  `packages/pipeline/src/eisenbalm_pipeline/prompts/` (advocate.md, bonus-big-budget.md,
  bonus-jingle.md, bonus-spec-ad.md, calibrator.md, design.md, editor-final.md, editor.md,
  game.md, researcher.md, scout.md, + README.md) and are loaded at runtime by
  `lib/prompts.py::load_prompt()` which extracts bytes between `<!-- PROMPT START -->` /
  `<!-- PROMPT END -->` markers via importlib.resources. Voice constraints live separately in
  `lib/voice.py`; the QA rubric in `agents/qa/rubric.md`. NOTE: these are FILES IN CODE (shipped
  in the wheel), NOT a database — this is the central divergence from the dashboard's "prompts in a DB" premise.
- Pipeline orchestration: LangGraph graph assembled in `graph/builder.py`; agent nodes under
  `agents/` (calibrator, scout, advocate, editor, researcher, origin_story, problem, founder_bio,
  case_study, game, bonus, chronicler, design/, qa/, publisher/) wrapped by `agents/_wrapper.py`.
  FastAPI app in `api/main.py` with routes in `api/runs.py` (`/run/weekly`, `/run/{runId}/resume`)
  and `api/webhooks.py` (Sanity publish webhook). Trigger is an HTTP endpoint guarded by
  PIPELINE_TRIGGER_SECRET — verify whether any cron/GitHub Action/Vercel cron actually calls it,
  or whether it is manual-only.
- Generated content lands SPLIT: Sanity = canonical published content (`lib/sanity_client.py`);
  Convex = real-time deliberation events / votes / pitch log / runs (`lib/convex_client.py`,
  `convex/*.ts`); pipeline execution/checkpointer state = Postgres via
  `graph/checkpointer.py` (SUPABASE_POSTGRES_URL — per project memory this URL now points at
  Railway Postgres, NOT Supabase; the env var name is a misnomer — flag this).
- Cost/logging: `lib/cost.py` (CostRecorder, per-run AgentCost {tokens_in, tokens_out, usd,
  duration_ms}, CostCapExceeded) JSON-stringifies cost into Convex `pipelineRuns.cost` + Sanity
  `weeklyIssue.pipelineMetadata.cost`; durationMs into `pipelineRuns.durationMs`. Recent commits
  da6e43d / 99d68b8 wired REAL OpenRouter token+USD capture in `acomplete` (openrouter_client.py)
  — verify exactly what acomplete captures (real usage vs estimate), whether it is per-agent or
  per-run aggregate, and where it is persisted. STATE.md / project memory record this fix.
- Frontend auth: no auth/middleware/login/session files found under apps/web beyond the Stripe
  checkout route (`apps/web/app/api/checkout/create-session/route.ts`). Verify there is no
  `middleware.ts`, no auth provider, no protected routes — i.e. the existing frontend is public/
  unauthenticated. State this explicitly (an absence finding, not speculation).
- The Mission Control brief referenced in the task ("§0", "Phase 0 HARD GATE", "five questions")
  is a SEPARATE dashboard brief, NOT docs/CLAUDE_CODE_BRIEF.md. docs/CLAUDE_CODE_BRIEF.md is the
  original build brief (375 lines, no literal "§0"/"Phase 0" heading) and is the source of the
  "assumed architecture" to reconcile against.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the five answers against the real codebase</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/lib/prompts.py, packages/pipeline/src/eisenbalm_pipeline/graph/builder.py, packages/pipeline/src/eisenbalm_pipeline/api/runs.py, packages/pipeline/src/eisenbalm_pipeline/lib/cost.py, packages/pipeline/src/eisenbalm_pipeline/lib/openrouter_client.py</files>
  <action>
    READ-ONLY investigation. Do NOT modify any production file. For each of the five §0 questions, gather concrete `path:line` evidence by reading files and grepping. Treat every item in `<facts>` as a hypothesis to confirm or correct — cite line numbers you actually saw.

    Q1 — Where do agent prompts live? (MOST IMPORTANT)
    - Confirm prompts are markdown files under `packages/pipeline/src/eisenbalm_pipeline/prompts/` and loaded by `lib/prompts.py::load_prompt()`. Record the loader's resolution strategy and the START/END marker convention with line cites.
    - Grep one or two agent modules (e.g. `agents/scout.py`, `agents/calibrator.py`) to confirm they call `load_prompt(...)` rather than embedding the prompt inline. Cite the call site lines.
    - Note where voice constraints (`lib/voice.py`) and the QA rubric (`agents/qa/rubric.md`) live — these are part of the prompt surface.
    - State plainly: prompts are FILE-externalized in code (shipped in the wheel), NOT in any database. This is the load-bearing answer for the dashboard.

    Q2 — What orchestrates the pipeline and how is it triggered?
    - Confirm LangGraph assembly in `graph/builder.py` (cite the StateGraph build + node/edge wiring lines) and FastAPI in `api/main.py` + `api/runs.py` (cite the `/run/weekly` route + its trigger-secret guard).
    - Determine the trigger mechanism: grep the repo for any cron / GitHub Actions / Vercel cron / scheduler that calls `/run/weekly`. Check `.github/workflows/`, `railway.toml`, `vercel.json`, and `convex/crons.ts`. If nothing schedules it, state that the pipeline is triggered manually via the HTTP endpoint (cite the absence).

    Q3 — Where does generated content land?
    - Confirm the split: Sanity (canonical, `lib/sanity_client.py`), Convex (deliberation/runs, `lib/convex_client.py` + `convex/*.ts`), Postgres checkpointer (`graph/checkpointer.py`). Cite the write sites.
    - Verify the SUPABASE_POSTGRES_URL-points-at-Railway misnomer: check `graph/checkpointer.py` + `.env.example` for the env var, and note the project memory record. Cite what you find.

    Q4 — Per-run logging / cost capture?
    - Read `lib/cost.py` and `lib/openrouter_client.py`. Characterize EXACTLY what `acomplete` captures after commits da6e43d / 99d68b8: real OpenRouter usage (tokens) and USD vs estimate; per-call vs per-agent vs per-run aggregate; and where it is persisted (Convex `pipelineRuns.cost`/`durationMs`, Sanity `pipelineMetadata.cost`). Cite the capture lines in `acomplete` and the record/persist path in `cost.py`. Confirm it is NOT fire-and-forget.

    Q5 — Frontend auth story?
    - Grep `apps/web` for `middleware.ts`, auth providers, login/session, protected routes. Confirm whether any auth exists. The expected finding is that the only server route is the Stripe checkout route and there is no user auth — state this as an explicit absence with the evidence (what you searched, what you found / didn't find).

    Use `git log --oneline -5 -- packages/pipeline/src/eisenbalm_pipeline/lib/openrouter_client.py packages/pipeline/src/eisenbalm_pipeline/lib/cost.py` to confirm the da6e43d/99d68b8 commits touch these files. Keep all findings as notes for Task 2 — do not write the doc yet.
  </action>
  <verify>
    <automated>git status --porcelain | grep -v '^??' | grep -q . && echo "FAIL: tracked files modified during read-only investigation" || echo "OK: no tracked production files changed"</automated>
  </verify>
  <done>Each of the five questions has at least one confirmed `path:line` citation (or an explicit, evidenced "not found" for Q2 trigger and Q5 auth). No tracked files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/CURRENT_STATE.md</name>
  <files>docs/CURRENT_STATE.md</files>
  <action>
    Create `docs/CURRENT_STATE.md` using ONLY the verified findings from Task 1. Every claim must carry a `path:line` citation. Where something could not be found, say so explicitly ("Not found: searched X, Y, Z — no match") rather than speculating.

    Structure the document:

    1. `# Eisenbalm — Current State (Phase 0 Reconciliation)` with a one-paragraph preamble: this is the read-only ground-truth gate for the Mission Control dashboard; it reconciles the dashboard brief's assumptions against the real codebase; no code was changed.

    2. `## TL;DR` — a 5-row table: Question | Answer (one line) | Key citation. The Q1 row is the headline: prompts are file-externalized markdown loaded by lib/prompts.py, NOT in a DB.

    3. One `##` section per question, each with: the answer, the supporting `path:line` citations, and any nuance:
       - `## Q1 — Where agent prompts live` (lead with this; it is the most important finding for the dashboard). Cover prompts/*.md, lib/prompts.py loader + markers, agent call sites, voice.py, qa/rubric.md. End with the implication: moving prompts to a DB means replacing the file loader, not extracting hardcoded strings.
       - `## Q2 — Pipeline orchestration & trigger` — LangGraph + FastAPI + the trigger mechanism (or evidenced absence of a scheduler).
       - `## Q3 — Where generated content lands` — the Sanity / Convex / Postgres split, plus the SUPABASE_POSTGRES_URL→Railway misnomer.
       - `## Q4 — Per-run logging & cost capture` — exact characterization of acomplete's real token+USD capture (commits da6e43d/99d68b8) and its persistence targets; explicitly not fire-and-forget.
       - `## Q5 — Frontend auth` — the (likely absent) auth story, stated as an evidenced absence.

    4. `## Divergences from the Mission Control brief` — a bullet list flagging where the brief's assumed architecture differs from reality, so later dashboard phases scope honestly. At minimum: (a) prompts are files-in-code not a DB; (b) the env var SUPABASE_POSTGRES_URL points at Railway Postgres; (c) the trigger story (manual vs scheduled) as found; (d) auth must be built from zero if the dashboard needs it. Add any others surfaced during investigation.

    Keep it tight and skimmable (~120-200 lines). This is a reference doc for planners, not prose.
  </action>
  <verify>
    <automated>test -f docs/CURRENT_STATE.md && grep -q "## Divergences from the Mission Control brief" docs/CURRENT_STATE.md && grep -Eq "lib/prompts\.py|prompts/.*\.md" docs/CURRENT_STATE.md && grep -Eq "[A-Za-z_/]+\.(py|ts|tsx|md):[0-9]+" docs/CURRENT_STATE.md && echo OK</automated>
  </verify>
  <done>docs/CURRENT_STATE.md exists, answers all five questions with file:line citations, includes the Divergences section, and leads Q1 with the file-externalized-not-DB prompt finding. No other files changed.</done>
</task>

</tasks>

<verification>
- `git status --porcelain` shows only `docs/CURRENT_STATE.md` added (no production code modified).
- All five §0 questions answered with at least one concrete `path:line` citation each.
- Q1 explicitly states prompts are file-externalized markdown (lib/prompts.py + prompts/*.md), not a database.
- `## Divergences from the Mission Control brief` section present.
</verification>

<success_criteria>
- A planner reading docs/CURRENT_STATE.md can scope the dashboard phases without re-investigating: they know where prompts live, what orchestrates and triggers the pipeline, where content lands, what cost/logging exists, and the auth baseline.
- Every answer is grounded in real file:line evidence; absences are stated explicitly, not guessed.
- The reconciliation gate is satisfied with zero production code changes.
</success_criteria>

<output>
After completion, create `.planning/quick/260621-fi4-phase-0-reconciliation-produce-current-s/260621-fi4-SUMMARY.md`
</output>
