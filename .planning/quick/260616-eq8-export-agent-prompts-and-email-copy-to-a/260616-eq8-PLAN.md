---
phase: quick-260616-eq8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/client-editable/EISENBALM-EDITABLE-COPY.md
  - docs/client-editable/EISENBALM-EDITABLE-COPY.docx
  - scripts/build_editable_docx.py
autonomous: true
requirements: [EQ8-01]
must_haves:
  truths:
    - "A non-technical client can open one document and edit all 8 email copies + all 10 agent prompts in Jesse's voice"
    - "Each editable block is labeled with a machine-findable SOURCE: label that maps back to the exact source file"
    - "Every block lists the placeholders that must be preserved verbatim"
    - "Returned edits can be mapped back to the right file via stable delimiters"
    - "A .docx exists that the client uploads to Google Drive (opens as a Google Doc) and its content matches the Markdown master"
  artifacts:
    - path: "docs/client-editable/EISENBALM-EDITABLE-COPY.md"
      provides: "Committed Markdown master / source of truth: intro + PART 1 (8 emails + subjects + footer) + PART 2 (10 prompts) + EXCLUDED note"
      contains: "How to use this document"
    - path: "docs/client-editable/EISENBALM-EDITABLE-COPY.docx"
      provides: "Google-Docs-ready .docx generated from the .md"
    - path: "scripts/build_editable_docx.py"
      provides: "Re-runnable python-docx generator that parses the .md into a .docx"
  key_links:
    - from: "scripts/build_editable_docx.py"
      to: "docs/client-editable/EISENBALM-EDITABLE-COPY.md"
      via: "reads the .md, maps # / ## headings -> docx headings, paragraphs -> body"
      pattern: "EISENBALM-EDITABLE-COPY\\.md"
---

<objective>
Export every client-editable piece of English copy — the 8 transactional/marketing email templates (subject lines + body prose + footer) and the 10 LangGraph agent prompts — into ONE consolidated document in two matching formats: a committed Markdown master (`docs/client-editable/EISENBALM-EDITABLE-COPY.md`) and a generated `.docx` (`docs/client-editable/EISENBALM-EDITABLE-COPY.docx`) the non-technical client uploads to Google Drive to edit.

Purpose: Give the client a single, plain-language, Google-Docs-ready surface to revise copy in Jesse's voice and send back, without touching code or risking placeholder tokens.

Output: One Markdown master, one matching .docx, one re-runnable generator script.

EXPORT-ONLY / behavior-preserving: do NOT modify any prompt file, email template, `subjects.ts`, footer, or any other source. Only ADD files under `docs/client-editable/` and `scripts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

# Plain-voice precedent for the "How to use this document" intro and the EXCLUDED note:
@apps/studio/PROMPT_EDITING_GUIDE.md

# Prompt file structure (header token note + PROMPT START / PROMPT END markers):
@packages/pipeline/src/eisenbalm_pipeline/prompts/scout.md

# The 8 subject lines:
@packages/emails/src/subjects.ts

# Representative email template (prose inside <Heading>/<Text>, token interpolations):
@packages/emails/src/templates/OrderConfirmation.tsx

# Footer copy:
@packages/emails/src/layouts/Footer.tsx

<sources_to_read>
EMAIL TEMPLATES (read each to extract real prose + tokens — do NOT invent):
- packages/emails/src/templates/OrderConfirmation.tsx   (E1)
- packages/emails/src/templates/Shipping.tsx            (E2)
- packages/emails/src/templates/DeliveredEstimate.tsx   (E3)
- packages/emails/src/templates/TheRitual.tsx           (E4)
- packages/emails/src/templates/CharityReceipt.tsx      (E5)
- packages/emails/src/templates/ReviewAsk.tsx           (E6)
- packages/emails/src/templates/NewsletterOptin.tsx     (E7)
- packages/emails/src/templates/Replenishment.tsx       (E8)
- packages/emails/src/subjects.ts                       (subject lines, keyed 1..8)
- packages/emails/src/layouts/Footer.tsx                (footer copy + unsubscribe)

AGENT PROMPTS (read text between <!-- PROMPT START --> and <!-- PROMPT END -->; read the top comment for the token list; SKIP README.md):
- packages/pipeline/src/eisenbalm_pipeline/prompts/scout.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/advocate.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/researcher.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/calibrator.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/editor.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/editor-final.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/game.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/design.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/bonus-big-budget.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/bonus-jingle.md
- packages/pipeline/src/eisenbalm_pipeline/prompts/bonus-spec-ad.md
</sources_to_read>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Markdown master from real source content</name>
  <files>docs/client-editable/EISENBALM-EDITABLE-COPY.md</files>
  <action>
Create `docs/client-editable/EISENBALM-EDITABLE-COPY.md` (create the `docs/client-editable/` directory). This is the committed source of truth. EXPORT-ONLY: read all sources listed in `<sources_to_read>`; do NOT edit any of them.

READ FIRST: open and read every file in `<sources_to_read>` so you extract the actual prose and tokens. Do NOT invent or paraphrase copy — copy the real English.

Document structure (top → bottom):

1. TITLE + "How to use this document" intro (plain language, modeled on apps/studio/PROMPT_EDITING_GUIDE.md tone). It must tell the client:
   - Edit the wording only; do NOT change the ALL-CAPS section labels, the `SOURCE:` lines, or any `{placeholder}` in curly braces.
   - If unsure about anything, leave an inline question in the doc — don't guess.
   - Send the whole document back to us to implement (we map your edits back to the code).
   - Everything here is Jesse's voice: dry, precise, sincere, played completely straight — no winking, no irony signaling, no emoji. (Per CLAUDE.md.)

2. `## PART 1 — EMAIL COPY`
   For each of the 8 emails E1..E8, in this order (OrderConfirmation=E1, Shipping=E2, DeliveredEstimate=E3, TheRitual=E4, CharityReceipt=E5, ReviewAsk=E6, NewsletterOptin=E7, Replenishment=E8) emit a block with:
   - A `### E{n} — {Human Name}` heading.
   - A line exactly of the form `SOURCE: templates/<File>.tsx` (e.g. `SOURCE: templates/OrderConfirmation.tsx`). This label is machine-findable and stable.
   - `Subject:` followed by the editable subject line pulled from subjects.ts for that number (e.g. E1 → "Your order is confirmed.").
   - `Body:` followed by the editable body paragraphs — the prose inside the `<Heading>`/`<Text>` components, one paragraph per source paragraph, with `{token}` interpolations rendered as readable placeholders in curly braces (e.g. `{amountDisplay}`, `{firstName}`, the charity name/location conditional rendered as `{charity name, location}`, etc.). Preserve the real wording verbatim.
   - A final line: `Keep these placeholders exactly: …` listing every interpolation that appears in that email's subject or body (e.g. `{amountDisplay}`, `{charity name, location}`). If an email has none, write `Keep these placeholders exactly: (none)`.
   Then ONE `### Email footer` block with `SOURCE: layouts/Footer.tsx`, the editable footer prose (funded-charity line, CAN-SPAM postal line, unsubscribe link text), and its `Keep these placeholders exactly: …` line (charity name/location conditional, postal address, unsubscribe link).
   Use a clear horizontal-rule delimiter (`---`) between blocks so returned edits map cleanly back to each file.

3. `## PART 2 — AGENT PROMPTS`
   For each of the 10 prompt files (scout, advocate, researcher, calibrator, editor, editor-final, game, design, bonus-big-budget, bonus-jingle, bonus-spec-ad — SKIP README.md) emit a block with:
   - A `### {Human Name}` heading (e.g. "Scout", "Advocate", "Editor (final review)", "Bonus — big-budget ad").
   - A line exactly of the form `SOURCE: prompts/<file>.md` (e.g. `SOURCE: prompts/scout.md`).
   - The editable prose: the exact text found between `<!-- PROMPT START -->` and `<!-- PROMPT END -->` (verbatim; do NOT include the markers themselves or the header comment).
   - A `Keep these placeholders exactly: …` line listing the tokens named in that file's top comment (e.g. scout → `{featured_keys}`). If a file declares no tokens, write `Keep these placeholders exactly: (none)`.
   Use `---` delimiters between blocks.

4. `## EXCLUDED — managed in code (ask us to change)`
   A short note that the global Jesse voice (`voice.py`), the QA rubric, and the Chronicler are managed in code and are not editable here — "ask us to change." Model the wording on the "What is not in these files" section of PROMPT_EDITING_GUIDE.md.

Conventions: Jesse's voice throughout the intro/notes (dry, precise, sincere, no emoji). The 8 `SOURCE: templates/<File>.tsx` labels and 10 `SOURCE: prompts/<file>.md` labels must each appear exactly once and be byte-stable.
  </action>
  <verify>
    <automated>test -s docs/client-editable/EISENBALM-EDITABLE-COPY.md && grep -c '^SOURCE: templates/' docs/client-editable/EISENBALM-EDITABLE-COPY.md | grep -qx 8 && grep -c '^SOURCE: prompts/' docs/client-editable/EISENBALM-EDITABLE-COPY.md | grep -qx 10 && grep -q 'How to use this document' docs/client-editable/EISENBALM-EDITABLE-COPY.md && grep -qi 'EXCLUDED' docs/client-editable/EISENBALM-EDITABLE-COPY.md && grep -qi 'Keep these placeholders exactly' docs/client-editable/EISENBALM-EDITABLE-COPY.md && git status --porcelain packages/emails packages/pipeline | grep -q . && echo "FAIL: source modified" || echo "OK"</automated>
  </verify>
  <done>
`EISENBALM-EDITABLE-COPY.md` exists and is non-empty; contains the "How to use this document" intro, exactly 8 `SOURCE: templates/…tsx` labels, exactly 10 `SOURCE: prompts/….md` labels, at least one "Keep these placeholders exactly" line per block, and the EXCLUDED note. No source file under packages/emails or packages/pipeline is modified (export-only).
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate the matching .docx from the Markdown master via uv + python-docx</name>
  <files>scripts/build_editable_docx.py, docs/client-editable/EISENBALM-EDITABLE-COPY.docx</files>
  <action>
Create a small, re-runnable generator script `scripts/build_editable_docx.py` that reads `docs/client-editable/EISENBALM-EDITABLE-COPY.md` and writes `docs/client-editable/EISENBALM-EDITABLE-COPY.docx` so the .docx content matches the .md.

Parsing rules (keep simple and robust):
- A line starting with `# ` → docx Heading level 1.
- A line starting with `## ` → Heading level 2.
- A line starting with `### ` → Heading level 3.
- `---` (horizontal rule) → an empty paragraph (visual separator) — or skip; either is fine as long as block separation reads clearly.
- Every other non-blank line → a normal body paragraph (preserve the literal text, including `SOURCE:` lines, `{placeholders}`, and `Keep these placeholders exactly:` lines verbatim).
- Blank lines → paragraph breaks.
Do NOT attempt full Markdown rendering (no bold/italic/table parsing needed) — heading-level + paragraph mapping is sufficient and intended.

Run it with uv (pandoc is NOT installed; python-docx is the locked approach):

```
uv run --with python-docx python scripts/build_editable_docx.py
```

The script should resolve paths relative to the repo root (or accept the .md and .docx paths as argv with sensible defaults pointing at docs/client-editable/). It must be idempotent — re-running overwrites the .docx cleanly.

EXPORT-ONLY: the only new/changed files are `scripts/build_editable_docx.py` and `docs/client-editable/EISENBALM-EDITABLE-COPY.docx`. Do not touch any source under packages/.
  </action>
  <verify>
    <automated>uv run --with python-docx python scripts/build_editable_docx.py && test -s docs/client-editable/EISENBALM-EDITABLE-COPY.docx && unzip -l docs/client-editable/EISENBALM-EDITABLE-COPY.docx | grep -q word/document.xml && echo "OK"</automated>
  </verify>
  <done>
`scripts/build_editable_docx.py` exists and runs cleanly under `uv run --with python-docx`; `docs/client-editable/EISENBALM-EDITABLE-COPY.docx` exists, is non-empty, and is a valid docx (`unzip -l` shows `word/document.xml`). Its content mirrors the Markdown master (headings + paragraphs incl. all SOURCE labels and placeholder lines). No source under packages/ is modified.
  </done>
</task>

</tasks>

<verification>
- `docs/client-editable/EISENBALM-EDITABLE-COPY.md` exists, non-empty, contains: the "How to use this document" intro, exactly 8 `SOURCE: templates/…tsx` labels, exactly 10 `SOURCE: prompts/….md` labels, per-block "Keep these placeholders exactly: …" lines, and the EXCLUDED note.
- `docs/client-editable/EISENBALM-EDITABLE-COPY.docx` exists and is a valid docx: `unzip -l docs/client-editable/EISENBALM-EDITABLE-COPY.docx | grep word/document.xml`.
- `scripts/build_editable_docx.py` regenerates the .docx from the .md re-runnably via `uv run --with python-docx`.
- `git status --porcelain packages/` shows NO changes (export-only / behavior-preserving).
</verification>

<success_criteria>
- One consolidated, plain-language, Google-Docs-ready document exists in two matching formats (.md master + generated .docx).
- All editable email copy (8 emails: subjects + bodies + footer) and all 10 agent prompts are present, extracted verbatim from source, each under a stable `SOURCE:` label with a placeholder-preservation line.
- The client can edit wording only and send the whole doc back; edits map back to the right file via the SOURCE labels and delimiters.
- No source file was modified.
</success_criteria>

<output>
After completion, create `.planning/quick/260616-eq8-export-agent-prompts-and-email-copy-to-a/260616-eq8-SUMMARY.md`
</output>
