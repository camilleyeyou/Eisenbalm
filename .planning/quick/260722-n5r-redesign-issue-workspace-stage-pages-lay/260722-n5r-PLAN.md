---
phase: quick-260722-n5r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx
  - apps/dispatch-control/app/globals.css
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/approval/ApprovalStage.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SectionChipList.tsx
  - apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/fact-check/FactCheckScreen.tsx
autonomous: true
requirements: [ISSUE-WORKSPACE-STAGE-LAYOUT-REDESIGN]
must_haves:
  truths:
    - "Stage tabs stay pinned to the viewport top and remain clickable while any stage page is scrolled mid-content"
    - "The outline (left) and context (right) rails pin below the sticky tabs and scroll independently while the center canvas scrolls the page"
    - "Draft galley headlines render across the canvas width (no one-word-per-line wrap) — the galley fills the full center column, no nested left chip column in galley mode"
    - "Draft edit mode still exposes the section selector, as a horizontal wrap strip above the editor"
    - "Fact Check claims are grouped by section (EDITABLE_SECTIONS reading order, unknown -> trailing 'Other'); all-checked groups default collapsed, groups with must-fix/unchecked/changed default open"
    - "Approval decision rail is constrained to a readable measure, not stretched across the widened canvas"
    - "pnpm --filter dispatch-control build passes"
  artifacts:
    - path: "apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx"
      provides: "Widened 1600px frame, 232/1fr/320 grid with items-start, sticky stage-tab nav, sticky independently-scrolling outline + context rails"
      contains: "max-w-[1600px]"
    - path: "apps/dispatch-control/app/globals.css"
      provides: "container-type on .galley-root + container-query headline/h2 scale under @supports"
      contains: "container-type: inline-size"
    - path: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SectionChipList.tsx"
      provides: "orientation prop ('vertical' default | 'horizontal')"
      contains: "orientation"
    - path: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx"
      provides: "Removed nested lg:w-64 column; chip strip rendered horizontal in edit mode only; galley full width"
    - path: "apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/fact-check/FactCheckScreen.tsx"
      provides: "Section-grouped collapsible claim list preserving filters/selection/testids"
    - path: "apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/approval/ApprovalStage.tsx"
      provides: "max-w readability wrapper around DecisionRail"
      contains: "max-w-"
  key_links:
    - from: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx"
      to: "SectionChipList orientation=horizontal"
      via: "rendered only when viewMode === 'edit'"
      pattern: "orientation=\"horizontal\""
    - from: "apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/fact-check/FactCheckScreen.tsx"
      to: "EDITABLE_SECTIONS ordering"
      via: "group filteredRows by normalized galley section id"
      pattern: "EDITABLE_SECTIONS"
---

<objective>
The operator finds the Issue Workspace stage pages (Draft / Fact Check / Voice / Approval) overwhelming and the scrolling "weird". Root causes (confirmed by code reading, treat as ground truth):

1. Column arithmetic starves the canvas. The frame (`layout.tsx`) is `max-w-[1200px]` with `lg:grid-cols-[220px_1fr_300px]` + two `gap-6`s -> ~630px canvas. Draft then nests its OWN `lg:w-64` SectionChipList inside the canvas -> galley ~360px wide.
2. Viewport-scaled display type in a narrow column. `.galley-headline` is `clamp(30px, 4vw, 52px)` — 52px inside a ~360px column wraps one word per line.
3. Duplicate navigation on Draft: `WorkspaceOutline` (frame left) and `SectionChipList` (canvas left) render the SAME 9 sections side-by-side.
4. No sticky regions anywhere — one page-level scroll; tabs, outline, context all scroll out of view.
5. Fact Check renders every claim (132 in the live run) as one flat `<ul>` — an unpaginated wall.
6. Approval's DecisionRail stretches full canvas width.

Fix, surgically (NOT a rewrite): widen + stabilize the frame with sticky tabs and sticky rails; give the galley a container-query type scale; de-duplicate Draft's navigation; group Fact Check claims into collapsible per-section sections; constrain Approval's rail.

Purpose: The stage workspace reads as a calm magazine galley with always-reachable navigation, not a cramped wall of viewport-scaled type.
Output: Six edited files; `pnpm --filter dispatch-control build` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx
@apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx
@apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SectionChipList.tsx
@apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/fact-check/FactCheckScreen.tsx
@apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/approval/ApprovalStage.tsx
@apps/dispatch-control/app/globals.css

<verified_facts>
CONFIRMED by reading (do NOT re-investigate):
- The `<SectionChipList>` COMPONENT (default export) is imported by EXACTLY ONE file: `ReviewDeskRunView.tsx`. `WorkspaceOutline.tsx` imports ONLY the named `EDITABLE_SECTIONS` export from that module — changing the component's default rendering cannot affect the outline.
- `.galley-root` is the class on `Galley.tsx`'s outer `<div ref={containerRef}>` (Galley.tsx:332). `.galley-headline` and `.galley-h2` are its descendants (Galley.tsx:353+, GallerySection.tsx). So `container-type: inline-size` on `.galley-root` + `cqi` units on those descendants resolves against the galley's own width.
- claim_checks `sectionName` uses the camelCase galley-id vocabulary (`originStory`, `problemStatement`, `founderBio`, `caseStudy`, `bonus`, `game`) — it matches `EDITABLE_SECTIONS[].id` directly (verified in `packages/pipeline/tests/test_claims_extractor.py` + `test_factcheck_endpoints.py`). `qaSectionToGalleyId` (`lib/galley/sectionIdMap.ts`) is available as a defensive fallback for any legacy snake_case rows.
- `deriveClaimChipState(...)` (from `@/components/provenance/ClaimProvenanceCard`) is ALREADY imported and used per-row in FactCheckScreen; it returns `'checked' | 'must-fix' | 'changed' | 'review-recommended' | 'unchecked'`. Reuse it for group counts + collapse defaults — do NOT invent a second classifier.
- Voice Pass (`VoicePassRunView.tsx`) is INTENTIONALLY out of scope: widening the frame + the galley container query already relax its galley automatically. Do NOT edit it.
- The frame's outer scroll surface bg token is `var(--color-rail)` (the layout root `bg-[color:var(--color-rail)]`).
</verified_facts>

<constraints>
- Do NOT change data flow, Convex queries/subscriptions, sign-off gates, or any server code. This is layout/markup/CSS only.
- Do NOT modify run-monitor surfaces or VoicePassRunView.
- Preserve ALL `data-testid` attributes. When markup moves, the testid moves with the element (fact-check-row-*, chip-count, outline-row-*, workspace-tab-*, cost-vs-budget).
- Match existing code style: bracket-pixel Tailwind (`px-[9px]`, `lg:w-[336px]`), `var(--color-*)` tokens, file-header provenance comments referencing the quick id.
- Sticky ONLY at `lg:` — the `grid-cols-1` mobile fallback must stay a plain vertical stack.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen &amp; stabilize the frame — sticky tabs, sticky rails, container-query galley scale, Approval measure</name>
  <files>apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/layout.tsx, apps/dispatch-control/app/globals.css, apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/approval/ApprovalStage.tsx</files>
  <action>
    Three coupled edits — they must ship together so the widened canvas and the retuned type scale look right in one pass. Add/extend the file-header provenance comment in each touched file noting `quick 260722-n5r` (frame widen + sticky rails / galley container-query scale / approval measure).

    A. `layout.tsx` (inside `FrameChrome`):
    - Widen the inner wrapper: `mx-auto flex max-w-[1200px] flex-col gap-6` -> `max-w-[1600px]` (keep everything else on that div).
    - Make the stage-tab `<nav aria-label="Workspace stages">` sticky so stage switching is always reachable mid-scroll. Add to its className, PRESERVING the existing `flex flex-wrap gap-2 border-b border-[color:var(--color-faint)] pb-3`: `sticky top-0 z-30 bg-[color:var(--color-rail)] pt-2`. (The `pt-2` + bg keep scrolled content from bleeding through the top edge.) Do not touch the tab `<Link>`s themselves.
    - Widen + stabilize the grid. Change `grid grid-cols-1 gap-6 lg:grid-cols-[220px_1fr_300px]` -> `grid grid-cols-1 gap-6 lg:grid-cols-[232px_minmax(0,1fr)_320px] lg:items-start`. (`minmax(0,1fr)` lets the canvas shrink so long galley content/code never blows the track out; `items-start` is REQUIRED for the sticky children below to travel.)
    - Wrap the two side rails (NOT the center `min-w-0` canvas div) each in a sticky, independently-scrolling shell. Replace `<WorkspaceOutline />` with:
      `<div className="lg:sticky lg:top-[72px] lg:max-h-[calc(100vh-88px)] lg:overflow-y-auto"><WorkspaceOutline /></div>`
      and replace `<ContextPanel title="Context">{panelContent}</ContextPanel>` with:
      `<div className="lg:sticky lg:top-[72px] lg:max-h-[calc(100vh-88px)] lg:overflow-y-auto"><ContextPanel title="Context">{panelContent}</ContextPanel></div>`
      (72px clears the sticky nav which sits at top:0 and is ~56px tall; the calc leaves an 88px gutter.) The center canvas div (`<div className="min-w-0">…{children}…</div>`) is UNCHANGED — it stays the tall, page-scrolling column. Leave `IssueComments` and `WorkspaceControls` exactly where they are (below the grid — they are NOT rails).

    B. `globals.css` (the D-04 galley type scale block, ~L172-212):
    - Add `container-type: inline-size;` to the existing `.galley-root { background: var(--background); }` rule (keep the background line).
    - Keep the existing viewport-based `.galley-headline` (`clamp(30px, 4vw, 52px)`) and `.galley-h2` (`28px`) rules as the fallback. Append an override AFTER them, guarded so it only applies where container units resolve:
      ```
      @supports (container-type: inline-size) {
        .galley-headline { font-size: clamp(26px, 5.5cqi, 44px); }
        .galley-h2 { font-size: clamp(20px, 3cqi, 26px); }
      }
      ```
      Leave `.galley-deck`, `.galley-body`, `.galley-pullquote` UNCHANGED.

    C. `ApprovalStage.tsx`:
    - Wrap the returned `<DecisionRail .../>` in a readability measure: `return (<div className="max-w-[760px]"><DecisionRail runId={runId} issueNumber={issueNumber} held={held} /></div>)`. Do not change the props or the `useWorkspaceState()` read.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control typecheck</automated>
  </verify>
  <done>Frame is `max-w-[1600px]` with the `232px/minmax(0,1fr)/320px` + `items-start` grid; the stage-tab nav is `sticky top-0`; both side rails are `lg:sticky lg:top-[72px]` with independent overflow; `.galley-root` has `container-type: inline-size` and headline/h2 have `@supports`-guarded `cqi` scales; DecisionRail is wrapped in `max-w-[760px]`. typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: De-duplicate Draft navigation — drop the nested chip column, add orientation prop, edit-mode horizontal strip</name>
  <files>apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SectionChipList.tsx, apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx</files>
  <action>
    Extend the file-header comment in both files noting `quick 260722-n5r` (Draft nav de-duplication).

    A. `SectionChipList.tsx` — add an orientation prop, default preserving today's behavior (only ReviewDeskRunView consumes this component, verified, so this is safe):
    - Add to `SectionChipListProps`: `orientation?: 'vertical' | 'horizontal'`.
    - Destructure with default `orientation = 'vertical'`.
    - The `<nav aria-label="Editable sections">` className becomes conditional: vertical (default) keeps `flex flex-col gap-1`; horizontal uses `flex flex-wrap gap-1.5`. The per-chip `<button>` classes are UNCHANGED — in a flex-wrap row they shrink to content and the label+badge `justify-between` still reads correctly. Keep every existing testid/aria attribute (`chip-count`, `data-unresolved`, `aria-current`).

    B. `ReviewDeskRunView.tsx` — collapse the two-column canvas into one full-width column and move the chip strip into edit mode only:
    - Change the body wrapper `<div className="flex flex-1 flex-col gap-4 lg:flex-row">` -> `<div className="flex flex-1 flex-col gap-4">`.
    - DELETE the entire `{/* LEFT — section-chip jump-nav / section selector */}` block (the `<div className="w-full shrink-0 lg:w-64"><SectionChipList .../></div>`). In galley mode WorkspaceOutline (frame left rail) already provides jump-nav + per-section state, so this is pure duplication.
    - The former RIGHT column `<div className="flex flex-1 flex-col gap-3">` is now the sole, full-width canvas child — keep it as-is.
    - Inside it, render the chip list as the section SELECTOR ONLY in edit mode, as a horizontal strip directly BELOW the header row (the `<div className="flex items-center justify-between gap-2">` with the title + Edit/Preview/Provenance buttons) and ABOVE the `viewMode === 'edit'` editor body. Add:
      ```
      {viewMode === 'edit' && (
        <SectionChipList
          orientation="horizontal"
          sections={EDITABLE_SECTIONS}
          selected={selectedSection}
          onSelect={handleChipSelect}
          dirty={dirty}
          counts={chipCounts}
        />
      )}
      ```
    - Leave `handleChipSelect`, `chipCounts`, galley/editor/iframe bodies, the revise/related-facts panels, and all data wiring UNTOUCHED.

    NOTE on tests: galley mode no longer renders `SectionChipList`. If a vitest test asserts the vertical chip column is present in galley (default) mode, update ONLY that assertion to reflect the new galley-mode DOM (WorkspaceOutline owns galley jump-nav; the chip strip is edit-mode-only). Do not broaden scope. Run `pnpm --filter dispatch-control test` to surface any such test.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control typecheck</automated>
  </verify>
  <done>`SectionChipList` accepts `orientation` (default 'vertical'); `ReviewDeskRunView` renders no chip column in galley mode (galley fills the canvas) and renders the horizontal chip strip above the editor in edit mode only; all data wiring and testids preserved; typecheck passes and any impacted galley-mode test is updated.</done>
</task>

<task type="auto">
  <name>Task 3: Group Fact Check claims into collapsible per-section sections</name>
  <files>apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/fact-check/FactCheckScreen.tsx</files>
  <action>
    Extend the file-header comment noting `quick 260722-n5r` (claim table grouped by section, collapsible). Only the claim-LIST render (the `<section aria-label="Claims">` block ~L444-510) changes — the summary, the filter chips, `applyFilters`, selection state, panel publishing, and every action handler stay EXACTLY as they are.

    1. Import `EDITABLE_SECTIONS` from `../../../review-desk/[runId]/_components/SectionChipList` and `qaSectionToGalleyId` from `@/lib/galley/sectionIdMap` (verify the relative depth compiles from the fact-check dir; adjust `../` count if tsc complains).

    2. Add a helper to normalize a row to a group key: given `row.sectionName`, return the matching `EDITABLE_SECTIONS` id by (a) direct id match, else (b) `qaSectionToGalleyId(sectionName)` match, else `null`. Rows with a `null`/missing/unknown key go in a trailing group keyed `'other'` labelled "Other".

    3. Build ordered groups from `filteredRows`: iterate `EDITABLE_SECTIONS` in order, emitting a group per section that has >=1 matching filtered row (skip empty groups), then append the "Other" group last if it has rows. Each group = `{ key, label, rows }`.

    4. Add local collapse state that respects a computed default but honors user toggles — e.g. `const [collapseOverrides, setCollapseOverrides] = useState<Record<string, boolean>>({})`. For each group compute `defaultCollapsed = group.rows.every(r => deriveClaimChipState({ text: r.text, status: r.status, importance: r.importance, sourceUrl: r.sourceUrl, changedSinceCheck: r.changedSinceCheck }) === 'checked')` (all-checked -> collapsed; ANY must-fix/unchecked/changed/review-recommended -> open). Effective collapsed = `collapseOverrides[key] ?? defaultCollapsed`. Toggling sets `collapseOverrides[key]` to the negation of the current effective value.

    5. Render: replace the single `<ul>` with one block per group. Each group gets a header `<button>` (collapse toggle, `aria-expanded`, keyboard-focusable, min-h target consistent with existing controls) showing the section label + counts computed from that group's rows via `deriveClaimChipState`: `"{rows.length} claims · {mustFix} must fix · {unchecked} unchecked"` (mustFix = chip 'must-fix', unchecked = chip 'unchecked'). When expanded, render the group's rows using the EXISTING per-row `<li><button data-testid={`fact-check-row-${row.claimIndex}`} …>` markup verbatim (same chip mark logic, same `selectClaim`, same selected styling, same MICRO_LABEL rows) inside a per-group `<ul>`. Keep the outer `filteredRows.length === 0` "No claims match the active filters." empty state unchanged (render it when there are no groups).

    Do NOT change: `FACT_CHECK_FILTERS`/`applyFilters`, `selectClaim`/`selectedClaimIndex`, the context-panel `useEffect`, or any action handler. Grouping is purely a reorganization of the list render.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control build</automated>
  </verify>
  <done>Fact Check renders claims as collapsible per-section groups in EDITABLE_SECTIONS order with a trailing "Other"; all-checked groups start collapsed and problem groups start open; group headers show label + counts; filters/selection/panel/testids unchanged; `pnpm --filter dispatch-control build` passes.</done>
</task>

</tasks>

<verification>
- `pnpm --filter dispatch-control build` is green (the authoritative gate — project memory: vitest does not type-check; the strict Next build is what catches Vercel/Linux-only failures).
- `pnpm --filter dispatch-control test` passes (update only assertions genuinely invalidated by the galley-mode chip removal; no logic tests should change).
- `git diff --name-only` lists ONLY the six files in `files_modified` — no server/Convex/run-monitor/VoicePassRunView changes.
- Manual read-through: the stage nav has `sticky top-0`; both rails are `lg:sticky lg:top-[72px]`; `.galley-root` has `container-type: inline-size`; ReviewDeskRunView renders `SectionChipList` only under `viewMode === 'edit'`; FactCheckScreen preserves every `fact-check-row-*` testid.
</verification>

<success_criteria>
- The four stage pages read as a wide, calm galley: canvas ~1000px on Draft, headlines cap at 44px and wrap normally, no duplicate section list in galley mode.
- Stage tabs stay pinned and clickable; outline + context rails stay visible and scroll independently while the canvas scrolls.
- Fact Check's 132-claim run is navigable via collapsible per-section groups instead of one flat wall.
- Approval's decision rail sits at a readable measure, not stretched.
- Mobile (`grid-cols-1`) remains a sane vertical stack (sticky only at `lg:`).
- No data flow, sign-off gate, Convex, or server behavior changed.
</success_criteria>

<notes>
- Sticky-offset numbers (72px rail top / 88px calc gutter) are tuned to the ~56px sticky nav; if the nav's rendered height differs materially at `lg:`, nudge the two offsets together (keep rail `top` &gt; nav height, and `max-h` = `100vh - (top + 16)`), but do not restructure.
- Do NOT run `node gsd-tools.cjs state record-session` for this quick task — hand-edit STATE if a status note is needed (project convention for quick tasks on this repo's multi-milestone ROADMAP).
</notes>

<output>
After completion, create `.planning/quick/260722-n5r-redesign-issue-workspace-stage-pages-lay/260722-n5r-SUMMARY.md`
</output>
