---
phase: quick-260724-i5n
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [QUICK-260724-i5n]
files_modified:
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/storyOutline.ts
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/useReviewedSections.ts
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryDeskGrid.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryFocusView.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryOutlineTab.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx
  - apps/dispatch-control/components/galley/Galley.tsx
  - apps/dispatch-control/__tests__/storyOutline.test.ts
autonomous: true

must_haves:
  truths:
    - "The Draft stage lands on a 3-up card grid of the 9 editable sections (not the all-sections galley), with status-colored top rules and an 'N of 9 reviewed' progress header"
    - "Opening a card shows a per-story focus view with folder-style OUTLINE and DRAFT tabs, crumbs (back-to-desk + Story X of 9 + prev/next), and footer prev/Next-unreviewed nav"
    - "The Outline tab is derived from the draft's block structure — lede + h2 beats with per-beat word counts and finding dots — plus a side rail of Brief fields, claim counts, and open findings"
    - "The Draft tab renders exactly ONE section's galley prose reusing the existing Galley annotation/claim rendering (not a fork)"
    - "'Mark reviewed' is a localStorage-only nav aid keyed by runId+section; it changes no publish gate and leaves the Approval stage / DecisionRail untouched"
    - "Selected story + active tab survive refresh via ?story=&tab= searchParams, and a legacy #galley-<id> deep link (DecisionRail/WorkspaceOutline) lands on that story's Draft tab"
    - "Edit mode (SectionEditorPanel), annotation accept/dismiss, provenance toggle, and revise/related-facts panels all still work inside the new flow"
  artifacts:
    - path: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/storyOutline.ts"
      provides: "Pure deriveStoryOutline(blocks, resolved, unresolved) + countWords/sectionExcerpt helpers"
      exports: ["deriveStoryOutline", "countWords"]
    - path: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/useReviewedSections.ts"
      provides: "localStorage reviewed-state hook keyed by runId (LD-5)"
      exports: ["useReviewedSections"]
    - path: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryDeskGrid.tsx"
      provides: "Cards-variant desk grid + reviewed progress header (mockup 02)"
      min_lines: 90
    - path: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryFocusView.tsx"
      provides: "Per-story focus view: crumbs, header, OUTLINE/DRAFT tabs, footer nav, edit mode (mockups 03/04)"
      min_lines: 120
    - path: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryOutlineTab.tsx"
      provides: "Derived outline beats + Brief/claims/findings side rail (mockup 03)"
      min_lines: 60
    - path: "apps/dispatch-control/__tests__/storyOutline.test.ts"
      provides: "Unit coverage for lede/beat split, word counts, block-position finding dots"
      contains: "deriveStoryOutline"
  key_links:
    - from: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx"
      to: "?story=&tab= searchParams"
      via: "useSearchParams drives desk-vs-story + active tab, survives refresh"
      pattern: "useSearchParams"
    - from: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/StoryFocusView.tsx"
      to: "components/galley/Galley.tsx (single-section)"
      via: "sections={[sectionId]} scoping prop reuses annotation system"
      pattern: "sections="
    - from: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx"
      to: "legacy #galley-<id> hash"
      via: "hashchange listener rewrites to ?story=<id>&tab=draft"
      pattern: "hashchange"
---

<objective>
Replace the Draft stage's all-sections galley landing in `apps/dispatch-control`
with the approved CARDS variant: a Story Desk card grid of the 9 editable
sections, and a per-story focus view with Outline/Draft tabs. All content is
DERIVED from the draft the console already loads — no new pipeline shapes, no
new Convex tables.

Purpose: give the editor a "read them one at a time" review surface instead of a
single scrolling galley, without touching the publish gate, the annotation
system, or the section editor.

Output: a desk↔story flow driven entirely by URL searchParams, mounted by the
one live consumer of `ReviewDeskRunView` (the Draft stage — both legacy routes
`/review-desk/[runId]` and `/issues/[n]/review` already redirect there, so LD-6
"both consumers move together" is satisfied by editing this one component).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260724-i5n-implement-the-review-desk-redesign-cards/mockups/02-story-desk-cards.html
@.planning/quick/260724-i5n-implement-the-review-desk-redesign-cards/mockups/03-story-outline-tab.html
@.planning/quick/260724-i5n-implement-the-review-desk-redesign-cards/mockups/04-story-draft-tab.html
@apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx
@apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SectionChipList.tsx
@apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SectionEditorPanel.tsx
@apps/dispatch-control/components/galley/Galley.tsx
@apps/dispatch-control/lib/contentPatchClient.ts
@apps/dispatch-control/lib/galley/spanResolver.ts
@apps/dispatch-control/lib/galley/sectionIdMap.ts
@apps/dispatch-control/app/(dashboard)/issues/_components/WorkspaceStateProvider.tsx
@apps/dispatch-control/app/globals.css

<locked_decisions_recap>
LD-1 Desk = CARDS variant (mockup 02): 3-up grid, status-colored top rules
  (reviewed=green, must-fix=vermilion, review=marigold, clean default = nav→cobalt
  on hover), kicker + headline + 2-line excerpt + footer (status chip, severity
  dots, word count), "✓ Reviewed" tick top-right, header "N of 9 reviewed" meter.
LD-2 Story focus view (mockups 03/04): crumbs (back-to-desk, "Story X of 9",
  prev/next steppers), header (kicker, headline, meta line, "Mark reviewed" ghost
  + "Edit story" primary), folder OUTLINE/DRAFT tabs (Draft tab shows open-finding
  count badge), footer prev + "Next unreviewed".
LD-3 Outline tab DERIVED from draft blocks: lede = paragraphs before first h2;
  beats = h2 blocks + first paragraph lead line; per-beat word counts + finding
  dots (map findings to beats by block position). Side rail: Brief fields
  (central claim / reader effect / known risks) from ws.brief, claim counts
  (sourced/unsourced), open findings with jump links. Caption: "The outline is
  read from the draft's structure — edit in the Draft tab."
LD-4 Draft tab renders ONE section's galley prose reusing the existing Galley
  annotation/claim rendering — do NOT fork the annotation system.
LD-5 "Mark reviewed" = client nav aid only: localStorage keyed by runId (+section
  id). NOT a publish gate. Approval stage / DecisionRail unchanged.
LD-6 Both consumers of ReviewDeskRunView move together (the two legacy routes are
  pure redirects into the Draft stage — see objective).
LD-7 Keep 1c tokens + hard-edged styling (radius 0–2px). Structured sections
  (game/theme/podcast/deliberation) show "Structured"/"Transcript" instead of a
  word count and may fall back to the existing structured editors/preview in the
  Draft tab.
LD-8 Preserve: edit via SectionEditorPanel (its save/revision-mismatch harness),
  annotation accept/dismiss, provenance toggle (if cheap), revise/related-facts
  panels, and deep links (DecisionRail/DraftPanelContent "jump to finding" must
  land on a story's Draft tab).
</locked_decisions_recap>

<interfaces>
<!-- Extracted from the codebase — executor should use these directly. -->

The 9 editable sections (camelCase ids, reading order), from SectionChipList.tsx:
```ts
export const EDITABLE_SECTIONS: { id: string; label: string }[] = [
  { id: 'originStory', label: 'Origin Story' },
  { id: 'problemStatement', label: 'Problem' },
  { id: 'founderBio', label: 'Founder Bio' },
  { id: 'caseStudy', label: 'Case Study' },
  { id: 'bonus', label: 'Bonus' },
  { id: 'game', label: 'Game' },
  { id: 'deliberation-conversation', label: 'Deliberation' },
  { id: 'podcast', label: 'Podcast' },
  { id: 'theme', label: 'Theme' },
]
```

Draft shapes (contentPatchClient.ts):
```ts
interface ContentBlock { type: 'paragraph' | 'h2' | 'h3' | 'blockquote'; text: string }
interface DraftSection { headline?: string; blocks: ContentBlock[]; lossy: boolean }
interface DraftResponse {
  revisionId: string
  sections: Record<string, DraftSection>   // keyed by the 4 long-read ids
  theme: Record<string, any>; game: Record<string, any>; bonus: Record<string, any>
  bonusType: 'specAd' | 'bigBudget' | 'jingle'
  podcast: Record<string, any>; conversation: { speaker: string; text: string }[]
}
```

Span resolver (spanResolver.ts) — the ONE resolution both galley + chip counts use:
```ts
resolveSectionFindings(blocks, findings, sectionId): { resolved: ResolvedAnnotation[]; unresolved: UnresolvedFinding[] }
// ResolvedAnnotation carries blockIndex/start/end/severity; UnresolvedFinding carries blockIndexHint? via finding, severity, reason.
```

Section-id bridge (sectionIdMap.ts): `qaSectionToGalleyId(qaName) -> galleyId|null`,
`galleyAnchorFor(sectionId) -> 'galley-<id>' | 'galley-deliberation' | null (theme)`.

Axis scope (axisPartition.ts): `FACTUAL_AXES: ReadonlySet<string>` — the Draft galley
already scopes open findings to this; reuse it so desk/story/outline counts agree
with what the single-section galley actually lights.

Brief (WorkspaceStateProvider — `useWorkspaceState().brief`, Doc<'briefs'> | null | undefined):
```ts
{ premise, currentPeg, centralClaim, readerEffect, knownRisks, voiceIntention, updatedAt }
```

Galley props (Galley.tsx) already accepted: runId, draft, revisionId, reloadDraft,
onEditSection(sectionId, findingId?), onInspect?, onRevise?, onRelatedFacts?,
showProvenance?, includeAxes?, onUnsourcedClaimClick?. Reader order is fixed
(4 long-reads → game → bonus → podcast → deliberation).

1c tokens (globals.css): --color-ink/-soft, --color-faint, --color-rail,
--color-card/-alt, --color-cobalt, --color-vermilion, --color-marigold,
--color-marigold-text, --color-green, --color-nav; --font-display/-body/-ui/-mono.
Do NOT invent tokens; the mockups' hex values already equal these.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure outline derivation + reviewed-state hook (tested)</name>
  <files>
    apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/storyOutline.ts,
    apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/useReviewedSections.ts,
    apps/dispatch-control/__tests__/storyOutline.test.ts
  </files>
  <behavior>
    deriveStoryOutline:
    - Test: blocks with 2 opening paragraphs then an h2 → lede beat spans block 0..1
      (kind 'lede', wordCount = words in blocks 0+1), first beat starts at the h2.
    - Test: each h2 opens a beat whose range is [h2Index .. nextH2Index); beat.title
      = h2 text, beat.lead = first following paragraph's lead sentence, beat.wordCount
      = summed words across the beat's blocks.
    - Test: a resolved finding at blockIndex N attaches a severity dot ('err' for
      'error', 'warn' for 'warning', 'info') to the beat whose range contains N; a
      finding in the lede range attaches to the lede.
    - Test: blocks with NO h2 → a single lede beat, no beats[].
    - Test: countWords('  two   words ') === 2; countWords('') === 0.
    useReviewedSections (jsdom-safe): SSR-safe init (guards window), returns
    { reviewed:Set, isReviewed, toggle, count }; toggle persists to
    localStorage['reviewDesk:reviewed:<runId>'] as a JSON id array. (Covered
    indirectly — its logic is trivial; the required RED/GREEN unit test is for
    deriveStoryOutline/countWords, which are pure and cheap.)
  </behavior>
  <action>
    Create `storyOutline.ts` (pure, no React/DOM imports — import types only from
    `@/lib/contentPatchClient` and `@/lib/galley/spanResolver`):

    - `export function countWords(text: string): number` — trim, split on /\s+/,
      drop empties.
    - `export function firstSentence(text: string, max = 160): string` — up to the
      first '.', '!', or '?' (inclusive) or `max` chars, whichever is shorter.
    - `export interface OutlineBeat { kind: 'lede' | 'beat'; label: string; lead:
      string; wordCount: number; dots: Array<'err' | 'warn' | 'info'>; blockStart:
      number; blockEnd: number }` (blockEnd exclusive).
    - `export function deriveStoryOutline(blocks: ContentBlock[], resolved:
      ResolvedAnnotation[], unresolved: UnresolvedFinding[]): { lede: OutlineBeat |
      null; beats: OutlineBeat[] }`:
        * Partition blocks into groups: the leading run before the first `h2` is the
          lede (if non-empty); each subsequent `h2` starts a new group running to the
          next `h2` (or end). `h3`/`blockquote`/`paragraph` all count toward the
          current group's word total.
        * lede.label = firstSentence(first paragraph's text) (fallback '' if none);
          lede.lead = first paragraph text after the lead sentence, or the paragraph
          itself if short. beat.label = the h2 text; beat.lead = firstSentence(first
          paragraph in the group).
        * dots: for each resolved annotation, place its severity dot on the group
          whose [blockStart, blockEnd) contains `resolved.blockIndex`. For an
          unresolved finding that carries a valid `blockIndexHint` in range, do the
          same; unresolved findings with no usable block are NOT placed as beat dots
          (they still surface in the side-rail list — the caller owns that). Map
          'error'→'err', 'warning'→'warn', 'info'→'info'.

    Create `useReviewedSections.ts` (`'use client'`): a hook
    `useReviewedSections(runId: string)` backed by
    `localStorage['reviewDesk:reviewed:' + runId]` (JSON string[] of section ids).
    SSR-safe lazy init (`typeof window === 'undefined'` → empty). Return
    `{ reviewed: Set<string>, isReviewed(id), toggle(id), count }`. `toggle` updates
    state AND writes localStorage. Keep it dependency-light (no Convex).

    Create the vitest file `__tests__/storyOutline.test.ts` covering the behavior
    cases above (pure functions — Node env, no jsdom/mocks needed).
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test -- storyOutline</automated>
  </verify>
  <done>deriveStoryOutline + countWords/firstSentence exported and green; useReviewedSections compiles and reads/writes the runId-scoped localStorage key.</done>
</task>

<task type="auto">
  <name>Task 2: Desk grid + story focus view + single-section galley + orchestrator rewire</name>
  <files>
    apps/dispatch-control/components/galley/Galley.tsx,
    apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryDeskGrid.tsx,
    apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryOutlineTab.tsx,
    apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/StoryFocusView.tsx,
    apps/dispatch-control/app/(dashboard)/review-desk/[runId]/ReviewDeskRunView.tsx
  </files>
  <action>
    A) `components/galley/Galley.tsx` (LD-4 — reuse, don't fork): add ONE optional
    additive prop `sections?: ReadonlyArray<string>`. When provided, render ONLY the
    sections whose id ∈ the set (gate each render branch: the LONG_READ_SECTIONS
    `.map` filters by membership; the `<GalleryGameSlot>` renders only if `game` ∈
    set; every `bonus` branch only if `bonus` ∈ set; the podcast `<section>` only if
    `podcast` ∈ set; the deliberation `<section>` only if `deliberation-conversation`
    ∈ set). Undefined = render everything (BACK-COMPAT — Voice Pass and any full
    render are unchanged). Keep the single `<PassageToolbar>` mount and ALL existing
    wiring (onEditSection/onInspect/onRevise/onRelatedFacts/showProvenance/
    includeAxes/onUnsourcedClaimClick) intact. Do not otherwise change resolution.

    B) `StoryDeskGrid.tsx` (`'use client'`, presentational — mockup 02): props
    `{ draft, chipCounts, reviewedIds, onOpen(sectionId), issueNumber }`. Render the
    header ("The Draft Desk" title, italic sub, right-aligned tally "N of 9 reviewed
    · X must fix · Y in review" + a meter `<i>` whose width = reviewed/9, cap "Review
    progress"). Render a 3-col grid (`grid-template-columns:repeat(3,1fr)` on lg, 1
    col on mobile) of 9 cards, one per EDITABLE_SECTIONS entry, each a `<button>`
    calling `onOpen(section.id)`:
      * Top rule color (LD-1): reviewed → green; else error>0 → vermilion; else
        warning>0 → marigold; else default nav border → cobalt on hover.
      * kicker (section.label) + numeral (01..09) + headline
        (`draft.sections[id]?.headline` for long-reads, else a sensible per-section
        title: bonus/game headline, "Podcast"/"Deliberation"/"Theme").
      * 2-line excerpt (`-webkit-line-clamp:2`) from the section's first paragraph
        (long-reads/specAd bonus) — use a small local excerpt helper; structured
        sections show a short descriptor.
      * footer: status chip (Clean/Review/Must fix from chipCounts), severity dots
        (error=vermilion, warning=marigold, from chipCounts), and word count
        ("N words") for prose sections OR "Structured"/"Transcript" for
        game/theme(Structured) and podcast(Transcript) per LD-7.
      * "✓ Reviewed" tick top-right when reviewed.
    Use ONLY 1c tokens + `rounded-[2px]`/hard edges. Reuse the mockup's exact class
    intent but as Tailwind arbitrary values / inline styles against the tokens.

    C) `StoryOutlineTab.tsx` (`'use client'`, mockup 03): props `{ sectionId,
    blocks, resolved, unresolved, brief, sourcedCount, unsourcedCount, openFindings,
    onJumpToFinding() }`. Call `deriveStoryOutline(blocks, resolved, unresolved)`;
    render the two-column grid: LEFT = "The shape of the piece" cap + lede/beat rows
    (numeral or "—", label as `<h3>` with a "Lede" tag on the lede, lead line, and a
    right column with `wordCount` "w" + severity dots), ending with the derived
    caption "The outline is read from the draft's structure — edit in the Draft tab."
    RIGHT side rail: a Brief panel (central claim / reader effect / known risks from
    `brief`; render an honest "No brief yet" line when `brief` is null/undefined — never
    blank), a "Claims in this story" panel (sourced/unsourced big numbers), and an
    "Open findings · N" panel listing each open finding with a "Jump to line" link
    that calls `onJumpToFinding` (which switches the parent to the Draft tab and
    scrolls the galley section into view). Structured sections (no prose blocks) show
    an honest "This section has no prose outline — see the Draft tab." message.

    D) `StoryFocusView.tsx` (`'use client'`, mockups 03/04): props `{ runId, draft,
    sectionId, activeTab, onTab(tab), onBack(), onNav(sectionId), reviewed,
    onToggleReviewed(), chipCounts, resolved, unresolved, openFindings, sourcedCount,
    unsourcedCount, brief, editing, onEdit(), onExitEdit(), onDirtyChange, ...galley
    wiring props }`. Layout:
      * crumbs row: "← Draft desk" (onBack), "Story X of 9" (index+1 of 9), prev/next
        steppers labeled with neighbor section labels (onNav).
      * story-head: kicker + headline (h1) + meta line (word count · "N must fix" ·
        warnings · "reviewed"/"unreviewed" from chipCounts + reviewed) + actions:
        "✓ Mark reviewed"/"Reviewed" ghost button (onToggleReviewed) + "Edit
        story"/"Done editing" primary button (onEdit/onExitEdit).
      * folder tabs: OUTLINE + DRAFT (Draft carries a vermilion count badge = open
        finding count for this section). Clicking calls onTab; the active tab is
        `activeTab`.
      * OUTLINE panel → `<StoryOutlineTab .../>`.
      * DRAFT panel → when `editing`, render `<SectionEditorPanel runId section=
        {sectionId} draft onDirtyChange focusFindingId findingReason/>` (preserve LD-8
        save/revision-mismatch harness). When not editing: if the section is one the
        galley can render, mount `<Galley ... sections={[sectionId]} />` (single
        section — LD-4, reusing annotation/claim/provenance rendering); for `theme`
        (galley never renders it) show a small structured summary of the theme values
        + an "Edit story to change the theme" note (LD-7 fallback).
      * footer-nav: prev section link + "Next unreviewed: <label>" link (computed by
        the parent, passed in or derived here from reviewed set + section order).
    Presentational — the parent owns URL/state; this component only calls the passed
    callbacks. Keep the provenance toggle here (cheap) if `showProvenance`/
    `onToggleProvenance` are passed.

    E) `ReviewDeskRunView.tsx` — the orchestrator (keep the file name/exports; the
    Draft page imports it unchanged). Keep: the draft load + `reloadDraft`, the
    `qaCorrections`/`claimChecks` subscriptions, the FACTUAL_AXES-scoped `chipCounts`
    memo, the `revisePassage`/`relatedFacts` panels, and `showProvenance` state.
    Replace the `viewMode` galley/edit/iframe body with a URL-driven desk↔story
    switch:
      * Read `useSearchParams()`: `story` (section id | null), `tab` ('outline' |
        'draft', default 'outline'), and the existing `edit` deep-link param.
      * `story` null → render `<StoryDeskGrid onOpen={(id)=> push ?story=id&tab=draft}
        ... />` (opening a card lands on the Draft tab, matching the reviewer flow).
      * `story` set → render `<StoryFocusView>` with all wiring; `onTab` does
        `router.replace('?story=<id>&tab=<t>')`, `onNav`/`onBack` use `router.push`.
      * Reviewed state via `useReviewedSections(runId)`; "Next unreviewed" = first
        section (in EDITABLE_SECTIONS order after current) not in the reviewed set.
      * Deep links (LD-8): keep the existing `?edit=<sectionId>[&finding=]` one-shot —
        translate it to `?story=<id>&tab=draft` + `editing=true` (enter
        SectionEditorPanel for that section, finding reason visible). Replace the old
        `#galley-*` one-shot scroll receiver with a `hashchange`-and-mount listener:
        when `location.hash` matches `#galley-<id>` (or `#galley-deliberation`), map
        it back to a section id and `router.replace('?story=<id>&tab=draft')`, then
        clear the hash (so a repeat click to the same section re-fires). This is what
        makes DecisionRail/WorkspaceOutline "jump to finding" land on the story's
        Draft tab.
      * Guard leaving a dirty editor: when `editing` && the section is dirty, the
        crumbs/steppers/back/tab-switch use the existing `useConfirm` "Unsaved
        changes" pattern before navigating (reuse the current dialog copy).
      * Brief comes from `useWorkspaceState().brief` (this component only ever mounts
        inside the workspace frame). Import `useWorkspaceState`.
    Do NOT touch DecisionRail, DraftPanelContent, WorkspaceOutline, the Approval
    stage, or any publish gate.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test -- Galley SectionChipList</automated>
  </verify>
  <done>Draft stage shows the card grid by default; opening a card shows the story view with working Outline/Draft tabs; ?story/?tab survive refresh; a #galley-<id> hash lands on the story's Draft tab; edit/annotation/provenance/revise all still function; existing Galley + SectionChipList tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite + strict production build</name>
  <files>apps/dispatch-control/__tests__/storyOutline.test.ts</files>
  <action>
    Run the affected dispatch-control test suite and confirm no regressions in the
    components this plan touches or relies on (Galley, SectionChipList,
    WorkspaceOutline, DraftNotGenerated, review-desk-editors, sectionIdMap). Fix any
    fallout from the desk↔story rewire (e.g. a test that assumed the old page-level
    `viewMode` chrome — update the assertion to the new flow, do not weaken it).

    Then run the STRICT production build (vitest does NOT type-check — this repo has
    shipped latent type bugs that only failed on Vercel/Linux, see MEMORY
    "run-strict-build-before-frontend-phase-done"). Resolve every type error the
    build surfaces before declaring done — especially around the new `sections?`
    Galley prop, the searchParams string→union narrowing, and the `useWorkspaceState`
    import path.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test && pnpm --filter dispatch-control build</automated>
  </verify>
  <done>The dispatch-control vitest suite passes and `next build` completes with zero type/lint errors.</done>
</task>

</tasks>

<verification>
- `pnpm --filter dispatch-control test` — full suite green (including the new storyOutline test and the untouched Galley/SectionChipList/WorkspaceOutline suites).
- `pnpm --filter dispatch-control build` — strict Next.js production build passes (type + lint).
- Manual smoke (executor, dev server): Draft stage lands on the 9-card grid; opening a card opens the story Draft tab; Outline tab shows derived beats + Brief/claims/findings; "Mark reviewed" toggles the tick and progress meter and persists across refresh; refresh preserves ?story/?tab; from the Approval stage, a DecisionRail "jump to finding" lands on that story's Draft tab with the annotation visible; "Edit story" opens SectionEditorPanel and saving still works.
</verification>

<success_criteria>
- Draft stage default view is the CARDS desk (LD-1), not the all-sections galley.
- Per-story focus view with folder Outline/Draft tabs, crumbs, steppers, and footer nav (LD-2).
- Outline is DERIVED (lede + h2 beats + word counts + block-positioned finding dots) with the Brief/claims/findings side rail and the mandated caption (LD-3).
- Draft tab reuses the existing Galley annotation/claim rendering for a single section — no fork (LD-4).
- "Mark reviewed" is localStorage-only, changes no gate, Approval/DecisionRail untouched (LD-5).
- The one live consumer (Draft stage) carries the new flow; legacy routes still redirect into it (LD-6).
- 1c tokens + hard edges only; structured sections show Structured/Transcript (LD-7).
- Edit mode, annotation accept/dismiss, provenance toggle, revise/related-facts, and deep links all preserved; ?story=&tab= survive refresh; #galley-<id> deep links land on a story's Draft tab (LD-8).
- Strict production build passes.
</success_criteria>

<output>
After completion, create `.planning/quick/260724-i5n-implement-the-review-desk-redesign-cards/260724-i5n-SUMMARY.md`
</output>
