---
phase: quick-260724-lp1
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [UI-UNIFORM, ROUTE-DRAFT]
files_modified:
  - apps/dispatch-control/lib/derivedState.ts
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/ReviewQueue.tsx
  - apps/dispatch-control/__tests__/derivedState.test.ts
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunsTable.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/layout.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunControlBar.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx
  - apps/dispatch-control/app/(dashboard)/registry/page.tsx
  - apps/dispatch-control/app/(dashboard)/registry/_components/RegistryTable.tsx
  - apps/dispatch-control/app/(dashboard)/registry/_components/CharityStatusBadge.tsx
  - apps/dispatch-control/app/(dashboard)/registry/_components/CoverageStrip.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/page.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptsListClient.tsx
  - apps/dispatch-control/app/(dashboard)/prompt-lab/_components/AgentPromptEditorView.tsx

must_haves:
  truths:
    - "The Run Monitor awaiting-review card 'Review →' CTA navigates to /issues/{n}/draft (Story Desk), not /issues/{n}/approval."
    - "The 'Clear the facts' (signoff-facts) task CTA navigates to /issues/{n}/draft, not /issues/{n}/approval."
    - "Run Monitor, Prompt Lab, and Registry render in the uniform vocabulary: hard edges (no rounded-lg cards / rounded-full pills except 8px status dots), 1c tokens only (no neutral/gray/slate), square mono-caps status chips, mono-caps table headers, kicker→display-h1→italic-dek page heads."
    - "The full dispatch-control vitest suite passes AND `next build` succeeds."
  artifacts:
    - path: "apps/dispatch-control/lib/derivedState.ts"
      provides: "signoff-facts task CTA now points at issueDraftHref"
      contains: "issueDraftHref"
    - path: "apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/ReviewQueue.tsx"
      provides: "awaiting-review 'Review →' CTA now points at issueDraftHref + uniform status-rule card"
      contains: "issueDraftHref"
    - path: "apps/dispatch-control/app/(dashboard)/registry/_components/RegistryTable.tsx"
      provides: "hard-edged uniform table + square filter/status chips"
    - path: "apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptsListClient.tsx"
      provides: "uniform agent-card grid (status top-rule cards, square Stock/Edited chips)"
  key_links:
    - from: "lib/derivedState.ts (signoff-facts href)"
      to: "issueDraftHref"
      via: "const href = fallbackHref ?? issueDraftHref(n as number)"
      pattern: "issueDraftHref\\(n as number\\)"
    - from: "ReviewQueue.tsx AwaitingReviewRow"
      to: "issueDraftHref"
      via: "href = pRun?.issueNumber ? issueDraftHref(pRun.issueNumber) : <legacy fallback>"
      pattern: "issueDraftHref\\(pRun\\.issueNumber\\)"
---

<objective>
Uniform visual pass on the three off-system dispatch-control workbench surfaces
(Run Monitor, Prompt Lab, Registry) so they match the approved Claude Design
"Uniform System Sheet" (mockup 08) and their per-page mockups (05/06/07), PLUS
route the two "Review"-labeled CTAs to the Draft stage (Story Desk) instead of
the Approval stage.

Purpose: These three surfaces are the only workbench pages still wearing generic
neutral Tailwind (text-neutral-*, rounded-lg cards, rounded-full pills). This
brings them onto the house 1c-token / hard-edge system so the whole app reads as
one product. The routing fix sends reviewers into the Draft/Story Desk stage
(where the work happens) rather than dead-ending them at the Approval gate.

Output: Restyled (className/markup-only) Run Monitor, Prompt Lab, and Registry
surfaces; two CTA hrefs flipped to issueDraftHref; one test assertion updated;
green vitest suite + green `next build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260724-lp1-uniform-visual-pass-on-run-monitor-promp/mockups/08-system-sheet.html
@.planning/quick/260724-lp1-uniform-visual-pass-on-run-monitor-promp/mockups/05-run-monitor.html
@.planning/quick/260724-lp1-uniform-visual-pass-on-run-monitor-promp/mockups/06-prompt-lab.html
@.planning/quick/260724-lp1-uniform-visual-pass-on-run-monitor-promp/mockups/07-registry.html
@apps/dispatch-control/app/globals.css

<house_rules>
This is a RESTYLE, not a rearchitecture (locked decision 2). className/markup
changes ONLY. Zero behavior/data/handler/query changes. Same components, same
props, same test IDs, same aria labels, same handlers, same Convex queries.

The uniform vocabulary (mockup 08, the System Sheet):
- Hard edges everywhere. Radius 0–2px. Eliminate `rounded-lg` cards and
  `rounded-full` pills. The ONLY circles allowed are small (≈8px) status dots.
  When you see `rounded-full`, decide: is it a small status dot (keep it round)
  or a text pill/badge (make it a square chip)?
- 1c tokens only. Replace EVERY `text-neutral-*` / `bg-neutral-*` /
  `border-neutral-*` / `text-gray-*` / `bg-gray-*` / `text-slate-*` with a 1c
  token. There must be zero neutral/gray/slate class hits left in the three
  restyled subtrees when you're done.
- Four fonts, four jobs: Newsreader (`--font-display`) = headlines; Lora italic
  (`--font-body`) = deks/excerpts/descriptions; Space Grotesk (`--font-ui`) =
  UI; IBM Plex Mono caps (`--font-mono`) = kickers, labels, numerals.
- Status is never colour alone: chip label + colour, or dot + count.
- Every page head: mono-caps cobalt kicker ("System Workbench") → Newsreader
  display h1 (~32px) → Lora italic dek.
</house_rules>

<token_map>
globals.css exposes these `--color-*` tokens (used in-code as
`[color:var(--color-NAME)]`) and font vars (`font-[family-name:var(--font-NAME)]`):

Colours: ink, ink-soft, faint, paper, nav, rail, card, card-alt, cobalt,
cobalt-dark, vermilion, marigold, marigold-text, green, masthead-text,
masthead-muted.  Fonts: display, body, ui, mono.

There is NO `--color-border` token in this app — the established 1px border is
`border-[color:var(--color-faint)]` (or `border-[color:var(--color-ink)]/10` for
row dividers). Reuse those; do not invent a border token.

Neutral → 1c substitution guide:
  text-neutral-900 / -800 / -700  → text-[color:var(--color-ink)]
  text-neutral-600 / -500         → text-[color:var(--color-ink-soft)]  (or -faint for meta)
  bg-neutral-50 / -100            → bg-[color:var(--color-card-alt)]
  bg-neutral-900                  → bg-[color:var(--color-ink)]
  border-neutral-200 / -300       → border-[color:var(--color-faint)]

Uniform SQUARE STATUS CHIP (replaces every rounded-full text pill):
  inline-block border px-2 py-[3px]
  font-[family-name:var(--font-mono)] text-[9.5px] tracking-[.09em] uppercase
  + per-status border colour + tinted fill:
    clean/complete/done/vetted → green   border + bg green/8, text green
    review/awaiting/candidate  → marigold border + bg marigold/10, text marigold-text
    running/info/featured      → cobalt  border + bg cobalt/6,  text cobalt
    failed/blocked/must-fix    → SOLID vermilion fill, white text  (only solid chip allowed)
    neutral/stock              → faint border + bg card-alt, text ink-soft

Uniform TABLE (mockups 05 & 07 use identical CSS):
  wrapper: bg card, 1px faint border, NO rounded-lg.
  th: font-mono text-[9.5px] tracking-[.12em] uppercase text-faint, font-medium,
      left-aligned, 1px faint bottom border.
  td: text-[12.5px]; numerals (cost/duration/dates) use
      font-mono text-[11px] text-ink-soft; 1px faint bottom border; last row no border.
  row hover: hover:bg-[color:var(--color-card-alt)]. No zebra stripes.

Uniform STATUS-RULE CARD (mockup 05 q-card / 06 agent / 08 mini-card):
  bg card, 1px faint border, 3px top border carrying status
  (nav grey when neutral → cobalt on hover; marigold when "held"/"edited";
  green when clean; vermilion when failing).
  Inside, in order: mono kicker → Newsreader h3 headline → mono meta (or Lora
  italic description for agent cards).

Uniform PAGE HEAD:
  <span> mono-caps cobalt kicker "System Workbench"
  <h1>  font-display ~32px font-semibold text-ink  (keep the existing
        {WORKBENCH_NAV_LABELS.x} content — do NOT hardcode the label string,
        nomenclature.test.ts asserts these constants)
  <p>   font-body italic text-[14px] text-ink-soft dek
</token_map>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip the two "Review" CTAs to the Draft stage + update the one test</name>
  <files>
    apps/dispatch-control/lib/derivedState.ts
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/ReviewQueue.tsx
    apps/dispatch-control/__tests__/derivedState.test.ts
  </files>
  <action>
    Locked decision 1: EXACTLY two "Review"-labeled CTAs change target from
    issueApprovalHref → issueDraftHref. Change the href target ONLY — do NOT
    touch the task's `where`/`stage`/`title`/`sev`, the signoff-voice task, the
    ReviewQueue loading fallback, the lastVisitedStage redirect, the MyTasks
    "Go to Approval" empty-state, the CommandPalette "Approval" entry, or any
    DecisionRail behavior.

    1) `lib/derivedState.ts` — the signoff-facts CTA (around line 523):
       - Change `const href = fallbackHref ?? issueApprovalHref(n as number)`
         to `const href = fallbackHref ?? issueDraftHref(n as number)`.
         (`issueDraftHref` is ALREADY imported at the top of the file — do not
         add a duplicate import.)
       - `issueApprovalHref` is used ONLY on that line in this file, so it
         becomes an unused import → REMOVE `issueApprovalHref` from the
         `from './issueRouteResolver'` import block, or the strict `next build`
         (Task 3 gate) will fail on the unused import. Keep the other imports
         (issueDraftHref, issueVoiceHref, issueFactCheckHref, issueStoryHref).
       - Update the now-stale code comment just above it (currently "the facts
         sign-off task deep-links to Approval, not Draft") to state it now
         deep-links to the Draft/Story Desk stage per quick 260724-lp1.
       - Leave `where: 'Approval'` and `stage: 5` as-is (data/label unchanged;
         the task's semantic home is still the Approval sign-off — only the
         navigation target moves to the Draft stage where the fixing happens).
       - Do NOT touch the signoff-voice branch (it uses issueVoiceHref and must
         stay `/voice`).

    2) `run-monitor/runs/_components/ReviewQueue.tsx`:
       - Change the import on ~line 25 from `issueApprovalHref` to
         `issueDraftHref`.
       - In `AwaitingReviewRow`, change
         `href = pRun?.issueNumber ? issueApprovalHref(pRun.issueNumber) : <legacy>`
         to use `issueDraftHref(pRun.issueNumber)`. Leave the legacy run-keyed
         loading fallback string (`/run-monitor/runs/${...}/review`) untouched —
         it is a transient loading state and the legacy redirect is out of scope.
       - Update the file's header docstring so it reflects the CTA now targeting
         the Draft (Story Desk) stage rather than Approval.
       - (Styling of this component happens in Task 2 — this task is href-only.)

    3) `__tests__/derivedState.test.ts` — the ONLY test asserting the old href
       (around lines 658–662):
       - Change `expect(factsTask?.primary.href).toBe('/issues/7/approval')` to
         `.toBe('/issues/7/draft')`.
       - Update that test's title/comment (currently "deep-links to
         /issues/7/approval, not /draft") to describe the new Draft/Story Desk
         target.
       - Do NOT touch the adjacent claim-task test (asserts `/issues/7/fact-check`)
         or the signoff-voice test (asserts `/issues/7/voice`) — both stay.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test -- derivedState</automated>
  </verify>
  <done>
    signoff-facts and the Run Monitor awaiting-review CTA both resolve to
    /issues/{n}/draft; issueApprovalHref no longer imported in derivedState.ts;
    the derivedState test asserts /issues/7/draft and the targeted vitest run is
    green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle Run Monitor + Registry to the uniform system (className/markup only)</name>
  <files>
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunsTable.tsx
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/ReviewQueue.tsx
    apps/dispatch-control/app/(dashboard)/run-monitor/layout.tsx
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunControlBar.tsx
    apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx
    apps/dispatch-control/app/(dashboard)/registry/page.tsx
    apps/dispatch-control/app/(dashboard)/registry/_components/RegistryTable.tsx
    apps/dispatch-control/app/(dashboard)/registry/_components/CharityStatusBadge.tsx
    apps/dispatch-control/app/(dashboard)/registry/_components/CoverageStrip.tsx
    (plus any remaining stray neutral/gray/rounded hits in the run-monitor +
     registry _components subtrees — RecoveryRail, CostRollup, CancelRunButton,
     AgentNode, WriterExpansion, BudgetAlertBanner, AddCharityDialog,
     CorrectionsList, AddCorrectionDialog, AddCharityDialogTrigger — token/radius
     hygiene only)
  </files>
  <action>
    Apply the token_map + house_rules to the Run Monitor and Registry surfaces.
    These two surfaces share the SAME uniform table pattern (mockups 05 & 07),
    so treat them together for consistency. Reference mockup 05 for Run Monitor,
    mockup 07 for Registry. Real data/props/queries/handlers stay; only classes
    and small markup wrappers change. Preserve every testid, aria-label,
    focus-visible:ring-2 pattern, and min-h-[44px]/min-w-[44px] hit target.

    RUN MONITOR:
    - `RunsTable.tsx`: drop `rounded-lg` on the wrapper (hard-edged card, 1px
      faint border); make `<th>` mono-caps 9.5px letterspaced faint; make the
      cost/duration/started cells mono numerals (font-mono text-[11px]
      text-ink-soft); convert the `rounded-full` status pill (uses
      STATUS_CLASSES) into the uniform square chip — rewrite STATUS_CLASSES to
      the bordered+tinted variants (running→cobalt, done→green,
      awaiting-review→marigold, failed→SOLID vermilion, cancelled→neutral) and
      UPPERCASE the label text; keep the empty-state but square it off; keep the
      "Show all" button (square it). Do not change the query or the 50-row cap.
    - `ReviewQueue.tsx`: convert the `rounded-lg` marigold list cards into the
      mockup-05 status-rule "q-card" (bg card, 1px faint border, 3px marigold
      top border; inside: mono kicker → Newsreader h3 → mono meta → hard-edged
      ink CTA). Convert the `rounded-full` "Awaiting Review" pill → square chip.
      Square off the ink "Review →" button (`rounded-md`→hard edge) but KEEP its
      href (already fixed in Task 1), min-h-[44px], and focus ring. Square off
      the loading + empty-state wrappers. Keep the AwaitingReviewRow structure
      and its byRunId query.
    - `layout.tsx` (Runs/Graph tab shell): already cobalt-underline on the 1c
      system — confirm it matches the mockup underline-tab pattern (mono-caps
      labels, 3px cobalt bottom border on active). Convert the tab label from
      `--font-ui` to mono-caps if it reads better against mockup 05's tab strip;
      otherwise leave. No structural/handler change.
    - `RunControlBar.tsx`: OPTIONAL light touch — you MAY add the mono-caps
      cobalt "System Workbench" kicker above the existing `<h1>Runs</h1>`
      (markup-only). Do NOT alter the Trigger Run handler, confirm flow, or the
      button's focus ring. Swap any stray neutral classes to 1c tokens.
    - `RunDetail.tsx`: GEOMETRY SQUARE-OFF ONLY. It already uses 1c colour
      tokens — do not recolour. Convert each `rounded-lg` card → hard edge
      (radius 0–2px) and each `rounded-full` → decide dot-vs-pill: keep small
      (~8px) status DOTS round, convert text pills/badges → square chips. Swap
      the two stray neutral hits to 1c tokens. Preserve action names (asserted
      by runDetailActionNames.test.ts) and all testids.
    - Hygiene the remaining run-monitor _components (RecoveryRail, CostRollup,
      CancelRunButton, AgentNode, WriterExpansion, BudgetAlertBanner): swap any
      neutral/gray classes → 1c tokens and square off rounded-lg/rounded-full
      text pills. No behavior change.

    REGISTRY:
    - `registry/page.tsx`: replace the plain
      `<h1 className="text-neutral-900">{WORKBENCH_NAV_LABELS.registry}</h1>` +
      the flex header with the uniform PAGE HEAD (kicker → Newsreader h1 → Lora
      italic dek), keeping the Add-charity trigger on the right (mockup 07
      shows a primary "+ Add charity" button, right-aligned). Keep the
      {WORKBENCH_NAV_LABELS.registry} constant as the h1 text.
    - `CoverageStrip.tsx`: restyle to the mockup-07 coverage card (bg card, 1px
      faint border, 3px cobalt top border; mono labels + Newsreader numerals;
      the meter is a 5px nav-track bar with green fill + mono caption). Swap
      neutral→1c, square off radii. Keep the self-fetch behavior.
    - `RegistryTable.tsx` (the big one, ~47 neutral/rounded hits): apply the
      uniform TABLE pattern (hard edges, mono-caps faint th, mono numerals,
      card-alt hover); convert the `rounded-full` FILTER pills (~line 156) to
      square filter chips (1px border; active = cobalt fill, white text, per
      mockup 07 `.f-chip`); convert the `rounded-lg` inner cards/wrappers to
      hard edges; swap every remaining neutral class → 1c token. Preserve the
      org name (Newsreader) + sub-line (Lora italic) cell treatment from mockup
      07. Keep all filter state, testids, and Convex queries.
    - `CharityStatusBadge.tsx`: this is the registry status chip — convert the
      three `rounded-full` variants into the uniform square chip (featured→
      cobalt, vetted→green, candidate→marigold, blocked→SOLID vermilion). This
      is the shared badge RegistryTable renders per row.
    - Hygiene the registry dialogs/lists (AddCharityDialog, CorrectionsList,
      AddCorrectionDialog, AddCharityDialogTrigger): neutral→1c token swap +
      radius square-off; no behavior/handler change.

    When done, there must be ZERO `text-neutral-*/bg-neutral-*/border-neutral-*/
    text-gray-*/bg-gray-*/text-slate-*` hits and ZERO `rounded-lg/rounded-md/
    rounded-xl/rounded-2xl` (and no `rounded-full` text pills — only dots) left
    in the run-monitor and registry subtrees.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test && echo "NEUTRAL_LEFT:" && (grep -rEl 'text-neutral-|bg-neutral-|border-neutral-|text-gray-|bg-gray-|text-slate-' apps/dispatch-control/app/*/run-monitor apps/dispatch-control/app/*/registry --include='*.tsx' 2>/dev/null || echo none)</automated>
  </verify>
  <done>
    Run Monitor + Registry render in the uniform vocabulary (hard-edged tables,
    square status/filter chips, status-rule cards, kicker/h1/dek Registry head);
    no neutral/gray/slate or rounded-lg/full-pill classes remain in either
    subtree; the full vitest suite is still green (no test asserts an old class).
  </done>
</task>

<task type="auto">
  <name>Task 3: Restyle Prompt Lab to the uniform system + FULL verification gate</name>
  <files>
    apps/dispatch-control/app/(dashboard)/prompt-lab/page.tsx
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/PromptsListClient.tsx
    apps/dispatch-control/app/(dashboard)/prompt-lab/_components/AgentPromptEditorView.tsx
    (plus token/radius hygiene across the rest of the prompt-lab _components
     subtree: VersionHistoryPanel, DiffViewer, PromptEditor, PromptSaveDialog,
     VariableChips, AssembledPreview, PromptMarkerExport, TestRunPanel,
     EvalDrawer, _CodeMirrorInner)
  </files>
  <action>
    Apply the token_map + house_rules to the Prompt Lab surface. Reference
    mockup 06. RESTYLE ONLY — same components/props/queries/handlers; preserve
    every testid (PromptEditor.test.tsx and PromptSaveDialog.test.tsx exist —
    keep their roles/testids), aria-label, focus ring, and min-44px hit target.

    PRIMARY VISIBLE CHROME (match mockup 06 structure):
    - `prompt-lab/page.tsx`: replace the neutral `<h1>` + `<p class="text-
      neutral-500">` head with the uniform PAGE HEAD (kicker "System Workbench"
      → Newsreader h1 → Lora italic dek). Keep the {WORKBENCH_NAV_LABELS.prompt_lab}
      constant as the h1 text (nomenclature.test.ts asserts it).
    - `PromptsListClient.tsx` (agent card grid, ~25 hits): convert each prompt
      card into the mockup-06 status-rule "agent" card — bg card, 1px faint
      border, 3px top border (nav grey → cobalt on hover; marigold when the
      prompt is edited/overridden); inside, in order: mono kicker → Newsreader
      h3 name → Lora italic description → footer row with a square Stock/Edited
      chip + mono version meta ("active v{N} · updated {date}"). Convert the
      grouped section captions to mono-caps faint. Keep the single
      listActiveForWorkspace subscription and all links.
    - `AgentPromptEditorView.tsx` (editor card zone, ~23 hits): restyle to the
      mockup-06 editor card — white card + 1px faint border; editor-head (mono
      kicker + Newsreader h3 + hard-edged ghost/primary buttons); code pane on
      card-alt with mono font; the version/meta rail as mono rows. Square off all
      radii; swap neutral→1c. Do not change the save/test/history handlers.

    HYGIENE SWEEP (rest of the subtree — token + radius only, no restructure):
    - VersionHistoryPanel, DiffViewer, PromptEditor, PromptSaveDialog,
      VariableChips, AssembledPreview, PromptMarkerExport, TestRunPanel,
      EvalDrawer, _CodeMirrorInner: replace every neutral/gray/slate class → 1c
      token; square off rounded-lg/rounded-md cards; convert rounded-full text
      pills → square chips (keep small status dots round). These deeper
      drawers/dialogs are not depicted in the mockup — do NOT restructure their
      layout, just bring them onto the token/edge system. No behavior change.

    When done, there must be ZERO neutral/gray/slate class hits and ZERO
    rounded-lg/md/xl/2xl (and no rounded-full text pills) left in the prompt-lab
    subtree.

    FINAL VERIFICATION (HARD GATE — this is the last task):
    Run the full dispatch-control vitest suite AND the strict production build.
    vitest does NOT type-check — the `next build` is the real gate and MUST pass
    (Phase 27 shipped latent bugs that only failed on Vercel/Linux because the
    build was skipped). If the build surfaces an unused-import / type error from
    any of the three tasks, fix it before declaring done.
  </action>
  <verify>
    <automated>pnpm --filter dispatch-control test && pnpm --filter dispatch-control build</automated>
  </verify>
  <done>
    Prompt Lab renders in the uniform vocabulary (kicker/h1/dek head, status-rule
    agent grid, hard-edged editor card, square Stock/Edited chips); no
    neutral/gray/slate or rounded-lg/full-pill classes remain in the prompt-lab
    subtree; the FULL vitest suite passes AND `pnpm --filter dispatch-control
    build` completes with no type/lint errors.
  </done>
</task>

</tasks>

<verification>
- Routing: `deriveTasks` signoff-facts href === `/issues/{n}/draft`; ReviewQueue
  AwaitingReviewRow resolves `issueDraftHref(pRun.issueNumber)`; derivedState
  test asserts `/issues/7/draft`. signoff-voice still `/voice`, claim still
  `/fact-check`, MyTasks empty-state still `/approval` (untouched).
- Uniform pass: `grep -rE 'text-neutral-|bg-neutral-|border-neutral-|text-gray-|bg-gray-|text-slate-'`
  over the run-monitor, prompt-lab, and registry subtrees returns nothing; no
  `rounded-lg/md/xl/2xl`; `rounded-full` only on ≈8px status dots.
- Gate: `pnpm --filter dispatch-control test` green AND `pnpm --filter
  dispatch-control build` green.
</verification>

<success_criteria>
- Both "Review" CTAs (signoff-facts task + Run Monitor awaiting-review card)
  navigate to the Draft/Story Desk stage; exactly those two changed, nothing
  else in the routing surface.
- Run Monitor, Prompt Lab, and Registry match the approved mockups' uniform
  vocabulary: hard edges, 1c tokens only, square mono-caps status chips,
  mono-caps table headers, kicker→display-h1→italic-dek page heads.
- No behavior/data/handler/query/testid changes anywhere.
- Full vitest suite passes; strict `next build` passes.
</success_criteria>

<output>
After completion, create
`.planning/quick/260724-lp1-uniform-visual-pass-on-run-monitor-promp/260724-lp1-SUMMARY.md`.

In the summary, note (locked decision 4 follow-up): whether the Eval Center
subtree shares any components restyled here, and if Eval Center still uses
neutral/gray classes, flag it as a follow-up (do NOT restyle it in this task).
Also note any prompt-lab deep drawers (TestRunPanel/EvalDrawer) that received
token hygiene only vs. full mockup treatment.
</output>
