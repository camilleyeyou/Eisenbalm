---
phase: quick-260719-w6o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pipeline/src/eisenbalm_pipeline/api/signoffs.py
  - packages/pipeline/tests/test_signoffs_endpoints.py
  - apps/dispatch-control/lib/derivedState.ts
  - apps/dispatch-control/__tests__/derivedState.test.ts
  - "apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/approval/page.tsx"
  - "apps/dispatch-control/app/(dashboard)/story-brief/_components/OrgOptionSlate.tsx"
  - "apps/dispatch-control/app/(dashboard)/story-brief/_components/NeedsYourDecisionCard.tsx"
  - apps/dispatch-control/__tests__/OrgOptions.test.tsx
  - apps/dispatch-control/__tests__/NeedsYourDecision.test.tsx
  - "apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx"
  - "apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RecoveryRail.tsx"
  - apps/dispatch-control/__tests__/RecoveryRail.test.tsx
autonomous: true
requirements: [PAUSED-GATE1-FIRST-CLASS]
must_haves:
  truths:
    - "A sign-off POST on a run with no sanityIssueId (failed OR paused-at-Gate-1 — neither has content) 409s {reason:'no_sanity_issue'} and records nothing; a genuinely reviewable run (sanityIssueId set) is unaffected"
    - "My Tasks for a paused-at-Gate-1 run shows exactly one story-decision task (not two false 'Must fix' sign-off cards pointing at Approval)"
    - "The Approval route for a paused-at-Gate-1 run renders a 'paused at the story decision' panel linking to the Story stage, never mounts DecisionRail"
    - "OrgOptionSlate and NeedsYourDecisionCard render a killed candidate AS killed with its killReason — never 'No concern flagged.' / '—'"
    - "Run Monitor mounts a reachable restart/adjudicate affordance (RecoveryRail paused branch → /signal-desk) for a genuinely paused run, unhardcoding the dead nomenclature editor_gate_1 'live' branch"
  artifacts:
    - path: "apps/dispatch-control/lib/derivedState.ts"
      provides: "exported isPausedAtGate1(i) predicate; deriveStoryStage reuses it; deriveTasks emits one gate-1 task (not sign-off tasks) for a paused run"
      contains: "export function isPausedAtGate1"
    - path: "packages/pipeline/src/eisenbalm_pipeline/api/signoffs.py"
      provides: "no_sanity_issue content guard mirrored from content.py::_resolve_sanity_id, covering failed + paused runs"
      contains: "no_sanity_issue"
    - path: "apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/approval/page.tsx"
      provides: "PausedAtGate1Panel server branch (status==='awaiting-review' && completedAt==null)"
      contains: "awaiting-review"
    - path: "apps/dispatch-control/app/(dashboard)/story-brief/_components/OrgOptionSlate.tsx"
      provides: "killed/killReason render (BriefOrgCard pattern) + killReason-preferring Main concern"
      contains: "killReason"
    - path: "apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RecoveryRail.tsx"
      provides: "paused-at-Gate-1 branch reusing RestartAction editor_gate_1 (isPausedAtGate1=true)"
      contains: "isPausedAtGate1"
  key_links:
    - from: "apps/dispatch-control/lib/derivedState.ts::deriveTasks"
      to: "apps/dispatch-control/lib/derivedState.ts::isPausedAtGate1"
      via: "shared predicate gates sign-off tasks + emits the gate-1 decision task"
      pattern: "isPausedAtGate1"
    - from: "apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx"
      to: "apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RecoveryRail.tsx"
      via: "mount when run.status==='awaiting-review' && run.completedAt==null, passing isPausedAtGate1={true}"
      pattern: "isPausedAtGate1"
    - from: "packages/pipeline/src/eisenbalm_pipeline/api/signoffs.py::record_sign_off"
      to: "run.sanityIssueId"
      via: "409 no_sanity_issue guard after the 404 run lookup, before the kind branches"
      pattern: "sanityIssueId"
---

<objective>
Close the third incarnation of the "run masquerades as reviewable" class: a run paused at the Gate-1 interrupt (`status === 'awaiting-review' && completedAt == null` — the all-candidates-killed D-14 branch AND the low-confidence Gate-1 branch, byte-identical shapes per `editor.py:378-398`/`526-538`) is a DIFFERENT status than `'failed'`, so it shares none of yesterday's failed-run guards (quick 260718-51o). The 2026-07-18 re-audit traced five linked defects to source; this plan fixes all of them, plus the server hole they exploit.

Make paused-at-Gate-1 first-class across dispatch-control, mirroring 260718-51o's approach: a shared predicate (not scattered booleans), an honest terminal/decision surface instead of a dead "clear to publish" screen, killed candidates shown AS killed, a reachable recovery affordance, and a single server-side content guard that closes the sign-off hole at the source for BOTH failed and paused runs.

Purpose: An operator hitting a paused run must be told "pick the charity first," must see killed orgs as killed, must have a working restart path, and must NOT be able to record a `sounds-human` sign-off against a contentless run.
Output: Server content guard (RED pytest), a shared `isPausedAtGate1` predicate + paused-aware My Tasks (RED vitest), an honest Approval panel + killed/killReason on both pickers (RED component tests), and a reachable Run Monitor recovery affordance (RED component test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/ui-reviews/dispatch-control-20260718-UI-REVIEW.md

<interfaces>
<!-- Extracted verbatim from the codebase — executor uses these directly, no re-discovery. -->

THE shared predicate already exists (duplicated) inside deriveStoryStage — lib/derivedState.ts:180:
  const pausedAtGate1 = i.runStatus === 'awaiting-review' && i.runCompletedAt == null
`DerivationInputs` already carries `runStatus` (line 125) and `runCompletedAt` (line 137).
A genuinely reviewable final-Andrew-gate run is `awaiting-review` with completedAt SET — so this
predicate is FALSE for it (the completedAt==null clause is the discriminator; test at
derivedState.test.ts:262 pins this). Gate-1 is pre-content: no draft, no claims, no qaFindings.

deriveTasks (lib/derivedState.ts:429) — CURRENT flow:
  if (i.runId === null) return []                              // 430
  if (i.runStatus === 'failed') return []                      // 435 (260718-51o)
  ... qaFindings loop (empty for paused) ... claimRows loop (empty for paused) ...
  const runningOrDone = i.runId !== null && i.runStatus !== 'running'   // 486 — TRUE for 'awaiting-review'
  if (runningOrDone && i.signOffs !== undefined) { push signoff-facts + signoff-voice }  // F1 BUG

issueStoryHref(n) EXISTS — lib/issueRouteResolver.ts:44 → `/issues/${n}/story`. NOT yet imported
into derivedState.ts (which imports issueDraftHref/issueVoiceHref/issueFactCheckHref/issueApprovalHref
from './issueRouteResolver' at lines 18-23 — add issueStoryHref there).

deriveStageStates (line 237) already renders paused as Story 'needs-you' via deriveStoryStage:181 —
StageStrip shows the vermilion "Needs you · 1" correctly. DO NOT touch deriveStageStates/deriveStoryStage
except to swap deriveStoryStage's inline `pausedAtGate1` const for the new exported helper (identical logic).

approval/page.tsx (server component) — the run row is already resolved server-side:
  const run = await new ConvexHttpClient(url).query(api.pipelineRuns.byIssueNumber, { issueNumber: n })
  if (!run) redirect(issueHref(n))
  if (run.status === 'failed') return <FailedRunPanel ... />   // 94-96 — MIRROR this for paused
  return (<><ApprovalPanelPublisher /><ApprovalStage runId={run.runId} issueNumber={n} /></>)
The pipelineRuns row (convex/schema.ts:70/77) carries `completedAt?: number` and `sanityIssueId?: string`.
So `run.status`, `run.completedAt`, `run.runId` are all in scope. `issueHref`+`parseIssueNumber` already
imported from '@/lib/issueRouteResolver'; add `issueStoryHref`. FailedRunPanel (page.tsx:41-79) is the
exact styling template (rounded-[2px], var(--color-vermilion)/--color-card/--font-display/--font-ui, min-h-[44px] links).

OrgOptionSlate.tsx — `verification` object ALREADY typed with `killed: boolean` / `killReason?: string`
(VerificationRecordRow, lines 45-57) and ALREADY fetched (line 144-148). Render code shows verification
date (175-201) + Main concern `{candidate.primaryConcern || '—'}` (236-241) but NEVER killed/killReason.
BriefOrgCard.tsx:93-101 + 113-115 is the REFERENCE render pattern (killed banner + killReason-preferring
Main concern), one file away.

NeedsYourDecisionCard.tsx — its LOCAL VerificationRecordRow type (lines 35-43) does NOT declare
killed/killReason (must ADD them; the runtime Convex row DOES carry them — verificationRecords schema
convex/schema.ts:572-574 `killed: v.boolean()`, `killReason: v.optional(v.string())`). Risk column
line 196: `{candidate.primaryConcern || 'No concern flagged.'}` — the false string. `findVerification`
(lines 52-62) already resolves the record per candidate.

RunDetail.tsx — `run = useQuery(api.runs.byRunId, {runId})`; the `runs` table (convex/schema.ts:254-256)
has `status` + `completedAt?`. RecoveryRail is mounted ONLY at line 314 for `run.status === 'failed'`
with `isPausedAtGate1={false}` hardcoded (line 322). AgentRunRow[] = `agentRuns` (line 214).

RecoveryRail.tsx — `restartAvailabilityFor('editor_gate_1', { isPausedAtGate1: true }) === 'live'`
(nomenclature.ts:226). Inside, RestartAction's editor_gate_1 branch (RecoveryRail.tsx:141-153) renders a
`<Link href="/signal-desk">` — the reachable adjudication path. `findFailedAgentKey` returns null when no
agent row is 'failed'; the `if (!failedAgentKey)` block (lines 236-247) currently renders the
"no specific step is recorded as failed" fallback. Existing test RecoveryRail.test.tsx:204-207 renders
`<RecoveryRail agentRuns={rows('editor_gate_1')} isPausedAtGate1 />` (an ARTIFICIAL failed editor_gate_1
row) and asserts the /signal-desk link — so the paused branch MUST be nested inside `if (!failedAgentKey)`
to leave that test's failed-row path untouched.

signoffs.py record_sign_off (line 54) — after the 404 run lookup (85-90) it goes straight to the
`if body.kind == "facts-cleared":` branch (92). content.py::_resolve_sanity_id (content.py:84-92) is the
guard-to-mirror: `sanity_id = run.get("sanityIssueId"); if not sanity_id: raise 409 {reason:"no_sanity_issue"}`.
test_signoffs_endpoints.py `_run()` helper (line 38-39) returns `sanityIssueId: "issue-42"` — so ALL
existing tests stay green; the local-dev claims sentinel (`sub == "local-dev-operator"`) satisfies the
role gate in the test harness (proven by test_sounds_human_ungated).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden the sign-off endpoint — 409 no_sanity_issue for contentless runs (F5, pipeline, RED → GREEN)</name>
  <files>packages/pipeline/tests/test_signoffs_endpoints.py, packages/pipeline/src/eisenbalm_pipeline/api/signoffs.py</files>
  <behavior>
    New test in test_signoffs_endpoints.py (mirror the existing MagicMock/monkeypatch harness; the
    local-dev claims sentinel already satisfies the role gate — see test_sounds_human_ungated). MUST
    fail against the current signoffs.py (RED — currently returns 200):

    - test_sign_off_409_no_sanity_issue: mock `pipelineRuns:byRunId` → `{"status":"awaiting-review","runId":"run-abc"}`
      (NO sanityIssueId key — the paused-at-Gate-1 AND failed-early shape). POST {kind:"sounds-human"}
      → expect 409 with detail["reason"] == "no_sanity_issue"; assert signOffs:record was NOT called.
      (This reproduces the 2026-07-18 production incident where sounds-human 200'd on a contentless
      run. Pre-fix: 200 → FAILS. Post-fix: 409 → PASSES.)

    Do NOT modify any existing test — they all use `_run()` which sets sanityIssueId="issue-42", so
    they must stay green (proves the guard does NOT over-gate a genuinely reviewable run).
  </behavior>
  <action>
    Write the test above FIRST and confirm RED. Then edit signoffs.py record_sign_off: insert a guard
    IMMEDIATELY AFTER the 404 run-lookup block (after line 90, before `if body.kind == "facts-cleared":`),
    mirroring content.py::_resolve_sanity_id (content.py:84-92) verbatim in shape:

      # quick 260719-w6o (F5) — a run with no Sanity issue has produced no
      # reviewable content: it failed early, or paused at Gate 1 before any
      # draft was written (editor.py:378-398 / 526-538). No sign-off can
      # attest content that does not exist. ONE guard covers BOTH the failed
      # and Gate-1-paused shapes without enumerating status values, closing
      # the same class as quick 260718-51o at the source. A genuinely
      # reviewable run always carries sanityIssueId (written at draft time —
      # content.py's edit routes already require it), so this never over-gates.
      sanity_id = run.get("sanityIssueId")
      if not sanity_id:
          raise HTTPException(
              status_code=status.HTTP_409_CONFLICT,
              detail={
                  "reason": "no_sanity_issue",
                  "message": "This run has produced no reviewable content to sign off.",
              },
          )

    Reuse `no_sanity_issue` (not a new reason) — it is the codebase's established contentless-run
    contract (review.py:82/196, content.py:69). Do NOT touch the two-sign-off gate logic, the
    facts-cleared/sounds-human prerequisite branches, or the role gate.
  </action>
  <verify>
    <automated>cd packages/pipeline && python -m pytest tests/test_signoffs_endpoints.py -x -q</automated>
  </verify>
  <done>The new test fails pre-guard (200) and passes post-guard (409 no_sanity_issue); ALL pre-existing test_signoffs_endpoints.py tests still pass. Note in SUMMARY: pipeline change needs a Railway deploy to take effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared isPausedAtGate1 predicate + paused-aware My Tasks (F1, RED → GREEN)</name>
  <files>apps/dispatch-control/__tests__/derivedState.test.ts, apps/dispatch-control/lib/derivedState.ts</files>
  <behavior>
    New tests in derivedState.test.ts (use the existing `baseInputs` helper). MUST fail against the
    current derivedState.ts (RED):

    - deriveTasks, paused-at-Gate-1 run: `deriveTasks(baseInputs({ runStatus: 'awaiting-review', runCompletedAt: undefined, signOffs: {} }))`
      → expect exactly ONE task; expect `tasks[0].stage === 1` and `tasks[0].primary.href` includes `/story`;
      expect NO task with id 'signoff-facts' or 'signoff-voice'.
      (Pre-fix: 2 signoff tasks, no gate-1 task → FAILS. Post-fix: 1 gate-1 task → PASSES.)
    - deriveTasks, genuinely-reviewable run UNCHANGED (regression guard): a run with
      `runStatus: 'awaiting-review'` but `runCompletedAt: 1_700_000_000_000` and `signOffs: {}` still
      emits the two sign-off tasks (this is the final-Andrew-gate run, NOT paused-at-Gate-1) —
      assert the result contains ids 'signoff-facts' and 'signoff-voice'. (Passes pre AND post — proves
      the completedAt==null discriminator; if it fails pre-fix the test is mis-specified.)
    - isPausedAtGate1 unit: `expect(isPausedAtGate1(baseInputs({ runStatus: 'awaiting-review', runCompletedAt: undefined }))).toBe(true)`;
      `expect(isPausedAtGate1(baseInputs({ runStatus: 'awaiting-review', runCompletedAt: 123 }))).toBe(false)`;
      `expect(isPausedAtGate1(baseInputs({ runStatus: 'running' }))).toBe(false)`.
      (Import `isPausedAtGate1` in the test's import block.)
  </behavior>
  <action>
    Write the tests FIRST and confirm RED. Then edit lib/derivedState.ts:

    1. Add `issueStoryHref` to the './issueRouteResolver' import (lines 18-23).

    2. Export the shared predicate (place it just above deriveStoryStage, ~line 161):
         // quick 260719-w6o (F1) — THE single paused-at-Gate-1 predicate
         // (§37.4(c)): status==='awaiting-review' && completedAt==null. Both the
         // all-candidates-killed and low-confidence Gate-1 interrupts share this
         // shape (editor.py:378-398 / 526-538). A finished run at the final Andrew
         // gate is 'awaiting-review' with completedAt SET, so this is FALSE for it.
         export function isPausedAtGate1(i: DerivationInputs): boolean {
           return i.runStatus === 'awaiting-review' && i.runCompletedAt == null
         }

    3. In deriveStoryStage (line 180), REPLACE the inline const with the helper (identical logic):
         const pausedAtGate1 = isPausedAtGate1(i)
       (Leave everything else in deriveStoryStage/deriveStageStates untouched — StageStrip already
        renders the correct 'needs-you' from here.)

    4. In deriveTasks, insert a branch IMMEDIATELY AFTER the failed guard (`if (i.runStatus === 'failed') return []`,
       line 435), returning exactly ONE story-decision task so My Tasks keeps the operator's pointer
       (deriveStoryStage's StageStrip 'needs-you' has no My-Tasks counterpart otherwise). A paused run
       has no draft/claims/findings, so an early return is complete and correct:
         // quick 260719-w6o (F1) — a run paused at Gate 1 has NOT chosen a charity:
         // it has no draft/claims/findings, and its sign-off buttons can only
         // mislead (deriveTasks line 486's runningOrDone was TRUE for 'awaiting-review',
         // emitting two false 'Must fix' sign-off tasks pointing at a dead Approval
         // screen — the audit's single most misleading signal). Emit ONE task: the
         // story decision itself, so My Tasks still says what's needed.
         if (isPausedAtGate1(i)) {
           const href = i.issueNumber === null ? '/issues' : issueStoryHref(i.issueNumber)
           return [
             {
               id: 'gate1-decision',
               sev: 'must-fix',
               title: 'Choose the charity to continue',
               where: 'Story',
               why: 'The run is paused for your decision — the agents could not confidently choose a charity.',
               primary: { label: 'Decide', href },
               stage: 1,
               openedAt: i.runStartedAt,
             },
           ]
         }

    Do NOT touch verify_candidates, the two-sign-off gate, or any Convex function.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && NEXT_PUBLIC_CONVEX_URL=https://modest-magpie-797.convex.cloud npx vitest run __tests__/derivedState.test.ts</automated>
  </verify>
  <done>New paused-run tests fail pre-edit and pass post-edit; the genuinely-reviewable regression test passes both pre and post; all pre-existing derivedState tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Honest story-decision surfaces — Approval paused panel + killed/killReason on both pickers (F2 + F3, RED → GREEN)</name>
  <files>apps/dispatch-control/__tests__/OrgOptions.test.tsx, apps/dispatch-control/__tests__/NeedsYourDecision.test.tsx, apps/dispatch-control/app/(dashboard)/story-brief/_components/OrgOptionSlate.tsx, apps/dispatch-control/app/(dashboard)/story-brief/_components/NeedsYourDecisionCard.tsx, apps/dispatch-control/app/(dashboard)/issues/[issueNumber]/approval/page.tsx</files>
  <behavior>
    Add RED component tests (mirror each file's existing mock harness — OrgOptions.test.tsx mocks
    convex/react useQuery + WorkspaceStateProvider; NeedsYourDecision.test.tsx passes plain props):

    - OrgOptions.test.tsx: seed a candidate whose matching verification record has
      `killed: true, killReason: 'domain parked; no registration'`. Assert the rendered card contains
      the killReason text AND does NOT show '—' as its Main concern. (Pre-fix: neither killed nor
      killReason is rendered → FAILS. Post-fix → PASSES.)
    - NeedsYourDecision.test.tsx: pass a `verificationRecords` entry with `killed: true, killReason: 'failed obscurity check'`
      for one of the top-two candidates (with no `primaryConcern`). Assert the Risk cell shows the
      killReason and does NOT read 'No concern flagged.'. (Pre-fix → FAILS. Post-fix → PASSES.)

    (approval/page.tsx is a Server Component doing a live ConvexHttpClient query — NOT unit-tested in
    this codebase, exactly as the FailedRunPanel was in 260718-51o; covered by the build gate + the
    manual-verification note. No test for it.)
  </behavior>
  <action>
    Write the two component tests FIRST and confirm RED. Then:

    (a) OrgOptionSlate.tsx — after the verification-date block (ends line 201), add a killed banner
        mirroring BriefOrgCard.tsx:93-101 (guard `verification?.killed`):
          {verification?.killed && (
            <p data-testid={`org-killed-${candidate.charityName}`} className="mt-1.5 text-[13px] font-semibold text-[color:var(--color-marigold-text)]">
              Verification flagged a concern: {verification.killReason || 'unspecified'}.
            </p>
          )}
        And change the Main concern value (line 240) to prefer killReason when killed (BriefOrgCard:113-115):
          {verification?.killed ? (verification.killReason || candidate.primaryConcern || '—') : (candidate.primaryConcern || '—')}

    (b) NeedsYourDecisionCard.tsx — add `killed?: boolean` and `killReason?: string` to the LOCAL
        VerificationRecordRow type (lines 35-43; the runtime Convex row already carries them). Change
        the Risk cell (line 196) to prefer the kill reason when killed:
          {verification?.killed ? `Verification killed this candidate: ${verification.killReason || 'unspecified'}` : (candidate.primaryConcern || 'No concern flagged.')}

    (c) approval/page.tsx — add `issueStoryHref` to the '@/lib/issueRouteResolver' import (line 25).
        Add a PausedAtGate1Panel component (clone FailedRunPanel:41-79 styling exactly — same tokens,
        AlertTriangle icon, rounded-[2px], min-h-[44px] links) with copy: heading "Paused at the story
        decision", body "This run paused before choosing a charity — pick the story first, then the
        rest of the pipeline runs.", a primary `<Link href={issueStoryHref(n)}>` labeled
        "Go to the story decision", and a secondary `<Link href="/signal-desk">` labeled "Open Signal
        Desk". Mount it with a branch placed IMMEDIATELY AFTER the existing failed branch (page.tsx:94-96),
        BEFORE the ApprovalStage return:
          if (run.status === 'awaiting-review' && run.completedAt == null) {
            return <PausedAtGate1Panel issueNumber={n} runId={run.runId} />
          }
        It MUST NOT mount ApprovalStage / ApprovalPanelPublisher / DecisionRail. (run.completedAt is on
        the pipelineRuns row — convex/schema.ts:70. A finished run has completedAt set, so this branch
        never catches a genuinely reviewable run.)

    Do NOT touch verify_candidates, the two-sign-off gate, DecisionRail, or any Convex function.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && NEXT_PUBLIC_CONVEX_URL=https://modest-magpie-797.convex.cloud npx vitest run __tests__/OrgOptions.test.tsx __tests__/NeedsYourDecision.test.tsx && pnpm --filter dispatch-control build</automated>
  </verify>
  <done>Both new component tests fail pre-edit and pass post-edit; `pnpm --filter dispatch-control build` exits 0 (proves the NeedsYourDecisionCard type change + approval/page.tsx branch type-check — vitest cannot catch this); the Approval route for a paused run returns the panel, never DecisionRail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Reachable Run Monitor recovery for a paused run — unhardcode the dead nomenclature branch (F4, RED → GREEN)</name>
  <files>apps/dispatch-control/__tests__/RecoveryRail.test.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RecoveryRail.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/RunDetail.tsx</files>
  <behavior>
    Add a RED test in RecoveryRail.test.tsx (mirror its existing harness — Clerk + pipelineControlClient
    mocked): render `<RecoveryRail runId="run-1" agentRuns={[]} isPausedAtGate1 />` (empty agentRuns → no
    failed agent, the REAL paused shape, distinct from the existing artificial `rows('editor_gate_1')`
    test at line 204). Assert a `/signal-desk` link renders AND paused-specific copy is present (e.g.
    a "Paused" heading). (Pre-fix: `!failedAgentKey` renders the "no specific step is recorded as
    failed" fallback with NO /signal-desk link → FAILS. Post-fix → PASSES.)

    Leave the existing RecoveryRail.test.tsx:204-207 assertion (artificial failed editor_gate_1 row +
    isPausedAtGate1) untouched and still passing — the new branch must NOT catch it.
  </behavior>
  <action>
    Write the test FIRST and confirm RED. Then:

    (a) RecoveryRail.tsx — NEST a paused branch INSIDE the existing `if (!failedAgentKey) {` block
        (lines 236-247), so a run with a real failed agent (incl. the existing test's artificial
        editor_gate_1 failed row) is unaffected:
          if (!failedAgentKey) {
            if (isPausedAtGate1) {
              return (
                <div className="space-y-4 rounded-lg border border-[color:var(--color-vermilion)] bg-white p-5">
                  <section>
                    <h3 className="text-xs font-semibold uppercase tracking-[.06em] text-[color:var(--color-vermilion)]">Paused for your decision</h3>
                    <p className="mt-1 text-sm text-neutral-700">This run paused at the story decision — the agents could not confidently choose a charity. Choose one to resume; completed steps are reused, not re-paid.</p>
                  </section>
                  <section>
                    <h3 className="text-xs font-semibold uppercase tracking-[.06em] text-neutral-500">Recommended recovery</h3>
                    <div className="mt-2 flex flex-wrap items-start gap-4">
                      <RestartAction runId={runId} agentKey="editor_gate_1" isPausedAtGate1={true} />
                    </div>
                  </section>
                </div>
              )
            }
            return ( ...existing "no specific step is recorded as failed" fallback, UNCHANGED... )
          }
        This reuses RestartAction's editor_gate_1 branch (RecoveryRail.tsx:141-153 → `<Link href="/signal-desk">`),
        making restartAvailabilityFor('editor_gate_1',{isPausedAtGate1:true})==='live' (nomenclature.ts:226)
        reachable in the shipped app for the first time. RestartAction is already defined in this file.

    (b) RunDetail.tsx — compute the predicate from the `runs`-table row (has status + completedAt,
        convex/schema.ts:254-256) and mount RecoveryRail for a genuinely paused run, alongside the
        existing failed mount (lines 314-324):
          const isPausedAtGate1 = run.status === 'awaiting-review' && run.completedAt == null
          ...
          {isPausedAtGate1 && (
            <RecoveryRail
              runId={runId}
              agentRuns={agentRuns as AgentRunRow[]}
              isPausedAtGate1={true}
            />
          )}
        Keep the existing `run.status === 'failed'` mount (with its hardcoded isPausedAtGate1={false})
        exactly as-is — failed and paused are mutually exclusive.

    Do NOT touch nomenclature.ts, RestartAction's internals, or any Convex function.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && NEXT_PUBLIC_CONVEX_URL=https://modest-magpie-797.convex.cloud npx vitest run __tests__/RecoveryRail.test.tsx && pnpm --filter dispatch-control build</automated>
  </verify>
  <done>The new empty-agentRuns paused test fails pre-edit and passes post-edit; the existing artificial-failed-row test at line 204 still passes; `pnpm --filter dispatch-control build` exits 0; RunDetail mounts RecoveryRail for a paused run.</done>
</task>

</tasks>

<verification>
- RED gate (hard, all 4 tasks): each task's new test(s) fail against the unmodified code. If any passes pre-fix, it is not exercising the change — fix the test before implementing. The one exception is Task 2's "genuinely-reviewable regression" test, which is designed to pass both pre and post (it pins the completedAt==null discriminator — it must NOT fail pre-fix).
- Full-suite gates (hard): Task 1 → the FULL `pytest tests/test_signoffs_endpoints.py` stays green (proves no over-gating of reviewable runs). Tasks 2-4 → `pnpm --filter dispatch-control build` exits 0 (PROJECT LESSON: vitest does NOT type-check — the NeedsYourDecisionCard type change, the approval/page.tsx branch, and RunDetail's new mount are exactly the kind of change that only fails at the type layer). Run the full vitest suite once at the end (`npx vitest run`) to confirm no regressions across the app.
- Manual verification (server/client components not unit-testable here): against paused run `b106e87a` (or any awaiting-review && completedAt==null) — (a) My Tasks shows ONE "Choose the charity" card, not two "Must fix" sign-off cards; (b) `/issues/{n}/approval` shows the paused panel with a Story-stage link, not DecisionRail's "clear to publish"; (c) OrgOptionSlate / NeedsYourDecisionCard show killed orgs with their killReason, not "No concern flagged." / "—"; (d) `/run-monitor/runs/{runId}` shows the paused recovery rail with a working /signal-desk link; (e) a sounds-human sign-off POST against the run 409s no_sanity_issue (after Railway deploy).
</verification>

<success_criteria>
- signoffs.py 409s {reason:"no_sanity_issue"} for any run without sanityIssueId (failed OR paused), records nothing; every existing signoffs test stays green.
- isPausedAtGate1 is exported and reused by deriveStoryStage; deriveTasks emits one gate-1 task (not sign-off tasks) for a paused run and is unchanged for a genuinely reviewable run.
- Approval renders a paused panel (Story-stage link) for a paused run and never mounts DecisionRail.
- OrgOptionSlate and NeedsYourDecisionCard render killed candidates AS killed with killReason.
- RunDetail mounts a reachable RecoveryRail (/signal-desk adjudication) for a paused run; the dead nomenclature editor_gate_1 'live' branch is now reachable.
- `pnpm --filter dispatch-control build` exits 0; full vitest + full signoffs pytest green; no two-sign-off-gate / verify_candidates / convex/*.ts code changed.
</success_criteria>

<scope_notes>
- ONE shared predicate (`isPausedAtGate1` in derivedState) is the DerivationInputs consumer; approval/page.tsx and RunDetail use the same predicate shape inline against the run row (they have run objects, not DerivationInputs) — this is intentional, not duplication worth a cross-boundary helper.
- deriveStageStates/StageStrip already handle the paused Story 'needs-you' correctly (Phase 47 Pitfall 3) — Task 2 only swaps deriveStoryStage's inline const for the exported helper; it does NOT re-derive stages.
- The Task 1 pipeline change requires a Railway deploy to take effect in production (note in SUMMARY). The frontend tasks require Vercel deploy but no Convex sync (no convex/*.ts touched).
- Per constraints: do NOT run `gsd-tools state record-session` (hand-edit STATE.md if needed).
</scope_notes>

<output>
After completion, create `.planning/quick/260719-w6o-make-paused-at-gate-1-a-first-class-ui-s/260719-w6o-SUMMARY.md`
</output>
