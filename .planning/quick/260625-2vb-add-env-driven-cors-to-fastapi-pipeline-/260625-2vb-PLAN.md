---
phase: quick-260625-2vb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pipeline/src/eisenbalm_pipeline/api/main.py
  - packages/pipeline/tests/api/test_cors.py
  - packages/pipeline/.env.example
  - packages/pipeline/README.md
  - apps/dispatch-control/DEPLOY.md
autonomous: true
requirements: [CORS-01]
must_haves:
  truths:
    - "Pipeline responds to a browser CORS preflight from an allowed origin with the matching access-control-allow-origin header"
    - "A non-listed origin does NOT receive access-control-allow-origin echoing it"
    - "Allowed origins are read from DASHBOARD_ALLOWED_ORIGINS env var, defaulting to http://localhost:3000"
    - "An operator can discover DASHBOARD_ALLOWED_ORIGINS from pipeline env docs and DEPLOY.md"
  artifacts:
    - path: "packages/pipeline/src/eisenbalm_pipeline/api/main.py"
      provides: "CORSMiddleware wired with env-driven origins"
      contains: "CORSMiddleware"
    - path: "packages/pipeline/tests/api/test_cors.py"
      provides: "Preflight allow + non-listed-origin deny tests"
      contains: "access-control-allow-origin"
  key_links:
    - from: "DASHBOARD_ALLOWED_ORIGINS env var"
      to: "CORSMiddleware allow_origins"
      via: "os.environ parse in main.py"
      pattern: "DASHBOARD_ALLOWED_ORIGINS"
---

<objective>
Add env-driven CORS to the FastAPI pipeline so the dispatch-control dashboard (a
browser app on a different Vercel origin) can call the pipeline's
`/agents/{key}/test-run`, `/agents/{key}/score`, `/pipeline/run`, `/runs/*`, and
`/review` endpoints with its Clerk `Authorization: Bearer` token.

Purpose: cross-origin browser calls from dispatch-control currently fail the CORS
preflight; this unblocks the prompt test-run, scoring, run-control, and review
features in production.
Output: CORSMiddleware on the app (env-driven, credentialed), a pytest proving
preflight allow + non-listed-origin deny, and env documentation for ops.

Constraint: code + tests + docs ONLY. Do NOT push or deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@packages/pipeline/src/eisenbalm_pipeline/api/main.py
@packages/pipeline/tests/api/test_score.py
@packages/pipeline/.env.example
@packages/pipeline/README.md
@apps/dispatch-control/DEPLOY.md

<interfaces>
<!-- App factory (main.py line 142): app = FastAPI(...) followed by include_router calls. -->
<!-- CORSMiddleware MUST be added AFTER `app = FastAPI(...)` and BEFORE the include_router calls. -->
<!-- Starlette CORSMiddleware import path: from fastapi.middleware.cors import CORSMiddleware -->

Established test pattern (from tests/api/test_score.py):
```python
from fastapi import FastAPI
from httpx import ASGITransport, AsyncClient
from eisenbalm_pipeline.api import agents as agents_module
# async tests, monkeypatch.setenv("EISENBALM_STUB_MODE", "true")
```

NOTE: test_score.py builds a *bare* app with only one router. For CORS we must
test against the REAL composed app `eisenbalm_pipeline.api.main.app` so the
middleware is present. Use fastapi.testclient.TestClient (sync) per the task —
TestClient runs lifespan; degraded boot (graph=None, no Postgres) is fine because
the test suite already runs with EISENBALM_STUB_MODE=1 and CORS works regardless
of lifespan state. A real mounted path is needed for preflight: use a POST route
that exists, e.g. `/agents/scout/score`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire env-driven CORSMiddleware into the pipeline app</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/api/main.py</files>
  <action>
    Add `from fastapi.middleware.cors import CORSMiddleware` to the imports.

    Immediately after `app = FastAPI(lifespan=lifespan, title="Eisenbalm Pipeline")`
    (line 142) and BEFORE the first `app.include_router(...)` call, add the CORS
    middleware driven by the DASHBOARD_ALLOWED_ORIGINS env var:

    - Read `os.environ.get("DASHBOARD_ALLOWED_ORIGINS", "")`.
    - Split on commas, `.strip()` each entry, drop empty strings.
    - If the resulting list is empty (unset or all-blank), default to
      `["http://localhost:3000"]`.
    - Call `app.add_middleware(CORSMiddleware, allow_origins=<list>,
      allow_credentials=True, allow_methods=["*"], allow_headers=["*"])`.

    Add a short comment explaining that allow_credentials=True forbids a wildcard
    "*" origin (Starlette will not echo "*" when credentials are allowed), so
    origins MUST be an explicit list — and that the dashboard sends an
    `Authorization: Bearer` Clerk token, hence allow_headers=["*"].

    Do NOT change the lifespan, the routers, or any other behavior. Keep the
    `os` import already present at line 24.
  </action>
  <verify>
    <automated>cd packages/pipeline && EISENBALM_STUB_MODE=1 uv run python -c "from eisenbalm_pipeline.api.main import app; print([m.cls.__name__ for m in app.user_middleware])"</automated>
  </verify>
  <done>main.py imports CORSMiddleware and registers it with allow_origins parsed from DASHBOARD_ALLOWED_ORIGINS (default ["http://localhost:3000"]), allow_credentials=True, allow_methods=["*"], allow_headers=["*"], placed after the FastAPI() call and before include_router calls.</done>
</task>

<task type="auto">
  <name>Task 2: Add CORS preflight allow + non-listed-origin deny tests</name>
  <files>packages/pipeline/tests/api/test_cors.py</files>
  <action>
    Create `packages/pipeline/tests/api/test_cors.py` using
    `fastapi.testclient.TestClient` against the real composed app
    `eisenbalm_pipeline.api.main.app` (import it at module top).

    Mirror the TestClient + import conventions in tests/api/test_score.py
    (module docstring explaining intent; rely on EISENBALM_STUB_MODE which the
    suite already sets; no real infra needed — CORS works under degraded boot).

    Write two tests against a real mounted endpoint path (use `/agents/scout/score`,
    a POST route that exists):

    Test 1 — preflight from an allowed origin is echoed back:
      - Send an OPTIONS request to `/agents/scout/score` with headers
        `Origin: http://localhost:3000` and
        `Access-Control-Request-Method: POST`.
      - Assert the response has an `access-control-allow-origin` header equal to
        `http://localhost:3000` (case-insensitive header lookup; TestClient
        normalizes header names to lowercase).

    Test 2 — a non-listed origin is NOT echoed:
      - Send the same OPTIONS preflight but with
        `Origin: https://evil.example.com`.
      - Assert the response's `access-control-allow-origin` header (if present at
        all) does NOT equal `https://evil.example.com`. (Starlette omits the
        header entirely for disallowed origins, so assert
        `resp.headers.get("access-control-allow-origin") != "https://evil.example.com"`.)

    Use `with TestClient(app) as client:` so lifespan runs (degraded boot is
    expected and fine). Keep the tests synchronous (TestClient is sync) — do not
    add async/ASGITransport here.
  </action>
  <verify>
    <automated>cd packages/pipeline && EISENBALM_STUB_MODE=1 uv run pytest tests/api/test_cors.py -x -q</automated>
  </verify>
  <done>test_cors.py exists; both tests pass — preflight from http://localhost:3000 returns matching access-control-allow-origin, and https://evil.example.com does not receive that origin in the header.</done>
</task>

<task type="auto">
  <name>Task 3: Document DASHBOARD_ALLOWED_ORIGINS in pipeline env docs and DEPLOY.md</name>
  <files>packages/pipeline/.env.example, packages/pipeline/README.md, apps/dispatch-control/DEPLOY.md</files>
  <action>
    1. `packages/pipeline/.env.example` — add a `DASHBOARD_ALLOWED_ORIGINS` entry
       following the file's existing commented-block style (a `# ─── … ───` style
       section header is optional; a short comment + the var is enough). Document:
       comma-separated list of browser origins allowed to call the pipeline from
       dispatch-control; defaults to `http://localhost:3000` when unset; in Railway
       set it to the dispatch-control Vercel domain(s). Example value:
       `DASHBOARD_ALLOWED_ORIGINS=http://localhost:3000`

    2. `packages/pipeline/README.md` — add a row to the env-var table (the
       `| Variable | Purpose / Where to get it |` table, lines ~39-50) for
       `DASHBOARD_ALLOWED_ORIGINS`: "Comma-separated browser origins permitted to
       make cross-origin (CORS) calls from the dispatch-control dashboard
       (test-run, scoring, run-control, review). Defaults to `http://localhost:3000`
       when unset. In Railway, set to the dispatch-control Vercel domain(s)."
       Match the existing table's markdown row format exactly.

    3. `apps/dispatch-control/DEPLOY.md` — add a note that the **Railway pipeline
       service** (not the dispatch-control Vercel project) must set
       `DASHBOARD_ALLOWED_ORIGINS` to the dispatch-control Vercel domain
       (comma-separated if more than one origin) for the prompt test-run, scoring,
       run-control, and review features to work from the browser. Place it near the
       existing `NEXT_PUBLIC_PIPELINE_URL` reference (the pipeline-URL row in the env
       table around line 48) so the two pipeline-related settings sit together —
       e.g. a short callout/note paragraph below the env-vars table, since
       DASHBOARD_ALLOWED_ORIGINS belongs to a different service and shouldn't be
       confused with the dispatch-control Vercel env vars in that table.
  </action>
  <verify>
    <automated>grep -l DASHBOARD_ALLOWED_ORIGINS packages/pipeline/.env.example packages/pipeline/README.md apps/dispatch-control/DEPLOY.md</automated>
  </verify>
  <done>DASHBOARD_ALLOWED_ORIGINS appears in all three docs: .env.example (commented entry with default), README env table (new row), and DEPLOY.md (note that the Railway pipeline service must set it to the dispatch-control Vercel domain).</done>
</task>

</tasks>

<verification>
- `EISENBALM_STUB_MODE=1 uv run pytest tests/api/test_cors.py -x -q` passes (run from packages/pipeline).
- Existing suite still green: `EISENBALM_STUB_MODE=1 uv run pytest tests/api -q` (CORS middleware is additive, no behavior change to existing routes).
- `grep DASHBOARD_ALLOWED_ORIGINS` finds the var in main.py + all three doc files.
- No `git push` / deploy performed.
</verification>

<success_criteria>
- CORSMiddleware is registered on the pipeline app, env-driven via
  DASHBOARD_ALLOWED_ORIGINS, defaulting to ["http://localhost:3000"], with
  allow_credentials=True, allow_methods=["*"], allow_headers=["*"] (no wildcard
  origin, so credentialed Authorization Bearer requests are honored).
- A browser preflight from an allowed origin is echoed; a non-listed origin is not.
- DASHBOARD_ALLOWED_ORIGINS is documented in .env.example, README, and DEPLOY.md.
- Code + tests + docs only — nothing pushed or deployed.
</success_criteria>

<output>
After completion, create `.planning/quick/260625-2vb-add-env-driven-cors-to-fastapi-pipeline-/260625-2vb-SUMMARY.md`
</output>
