---
phase: quick-260711-iu2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pipeline/src/eisenbalm_pipeline/lib/llm_config.py
  - packages/pipeline/src/eisenbalm_pipeline/prompts/game.md
  - packages/pipeline/src/eisenbalm_pipeline/prompts/game_user.md
autonomous: true
requirements: [QUICK-260711-iu2]
must_haves:
  truths:
    - "The GameWriter agent runs on anthropic/claude-opus-4-8 at temperature 0.4 with a 24k max_tokens cap"
    - "The game system prompt gives Opus a real game-design brief (mission-enacting mechanic, game feel, visual bar, validator-trap guards, output contract) while preserving all substitution tokens and the FORBIDDEN_CONSTRUCTS deny-list"
    - "The game user template tells the model to design the mechanic from the mission before coding, and keeps the GameOutput JSON contract"
    - "All existing game/prompt/config tests stay green (no regressions)"
    - "The new v4 prompt content is available to Andrew for Prompt-Lab activation — saved to Convex as an INACTIVE version, or recorded in SUMMARY as an exact paste-ready manual handoff"
  artifacts:
    - path: "packages/pipeline/src/eisenbalm_pipeline/lib/llm_config.py"
      provides: "game model/sampling/token-cap runtime config (Opus 4.8, temp 0.4, 24k)"
      contains: "anthropic/claude-opus-4-8"
    - path: "packages/pipeline/src/eisenbalm_pipeline/prompts/game.md"
      provides: "GameWriter system prompt (game-design brief) with tokens intact"
      contains: "{FORBIDDEN_CONSTRUCTS}"
    - path: "packages/pipeline/src/eisenbalm_pipeline/prompts/game_user.md"
      provides: "GameWriter user template with mechanic-from-mission instruction"
      contains: "{mission_statement}"
  key_links:
    - from: "packages/pipeline/src/eisenbalm_pipeline/lib/openrouter_client.py"
      to: "MODEL_BY_AGENT['game']"
      via: "acomplete() reads code constants only (Convex agents.model is snapshot/display, not consumed by the LLM call path)"
      pattern: "MODEL_BY_AGENT"
    - from: "packages/pipeline/src/eisenbalm_pipeline/agents/game.py"
      to: "prompts/game.md"
      via: "_build_messages substitutes {FORBIDDEN_CONSTRUCTS} into the system message"
      pattern: "FORBIDDEN_CONSTRUCTS"
---

<objective>
Put Opus 4.8 in charge of the GameWriter agent and give it a real game-design brief so the weekly issue's game is actually engaging — mission-enacting mechanic, game feel, an intentional visual bar, and explicit validator-trap guards — without touching the renderer, the validator, or any other agent.

Purpose: The current game config (Sonnet 4.6, temp 0.7, no token cap, one-line prompt) produces theme-skinned filler that often trips the renderer deny-list or truncates. This raises the model tier, tightens sampling, adds token headroom, and replaces the prompt with a genuine brief.
Output: Three edited pipeline files (config + two prompts) plus a best-effort Convex v4 INACTIVE prompt-version write for Andrew to Rehearse/activate in Prompt Lab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Verified facts from the orchestrator's investigation (do NOT re-derive):
- Runtime truth is `lib/llm_config.py`. `acomplete()` reads ONLY `MODEL_BY_AGENT` / `SAMPLING_BY_AGENT` / `MAX_TOKENS_BY_AGENT`. The Convex `agents` row model is snapshot/display only — never consumed by the LLM call path.
- Live prompt resolution: system prompt comes from the Convex `prompt_versions` active row (agentKey "game", v3 active) with `prompts/game.md` as fallback AND future seed source; user template from Convex active `game_user` (v1) with `prompts/game_user.md` fallback. Disk edits alone do NOT change live runs — a new Convex version must be saved and Andrew activates it in Prompt Lab (activation is eval-gated by design).
- `lib/prompts.load_prompt` extracts content between `<!-- PROMPT START -->` / `<!-- PROMPT END -->`. Keep the header comment + markers. Substitution is `str.replace`; keep tokens exactly and do not write the literal token strings inside example code.
- The renderer (apps/web) is untouched. Its `validateEmbedCode` raw-scans the WHOLE generated document (comments + strings included) for banned substrings — a single hit anywhere rejects the game. The prompt must warn the model; the prompt file itself is NOT scanned, so instructional mentions of banned words inside game.md are safe.

<interfaces>
Current game entries in lib/llm_config.py (to edit):

    MODEL_BY_AGENT["game"]    = "anthropic/claude-sonnet-4-6"       # -> anthropic/claude-opus-4-8
    SAMPLING_BY_AGENT["game"] = {"temperature": 0.7, "top_p": 1.0}  # -> temperature 0.4
    # MAX_TOKENS_BY_AGENT has NO "game" key today                   # -> add "game": 24_000

Tokens that MUST survive in prompts/game.md: {charity_name}, {VOICE_CONSTRAINTS}, {FORBIDDEN_CONSTRUCTS}
Tokens that MUST survive in prompts/game_user.md: {charity_name}, {mission_statement}

agents/game.py `_build_messages` injects the in-code FORBIDDEN_CONSTRUCTS block where {FORBIDDEN_CONSTRUCTS} appears — do NOT remove that token (test_game.py asserts the block appears verbatim in the built system message).

Convex mutation shapes (Task 3, best-effort; WORKSPACE_ID = "eisenbalm"; both require Clerk operator identity):

    promptVersions:saveVersion { workspace_id, agentKey, content, createdBy?, note? }  # creates next version INACTIVE
    agents:upsert { workspace_id, agentKey, enabled, model?, temperature?, top_p?, max_tokens?, description? }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retune game runtime config (Opus 4.8, temp 0.4, 24k cap)</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/lib/llm_config.py</files>
  <action>
Make three edits in lib/llm_config.py (locked decisions 1-3):

1. In `MODEL_BY_AGENT`, change the `"game"` entry from `"anthropic/claude-sonnet-4-6"` to `"anthropic/claude-opus-4-8"`. Add an inline comment noting this is a deliberate creative-code tier bump (user request: Opus 4.8 in charge of the game). Leave the other section-writer entries on Sonnet. The game key currently sits under the "# Section writers (Sonnet…)" comment block — add a clarifying trailing comment (or move it to its own line) so that grouping comment stays accurate.

2. In `SAMPLING_BY_AGENT`, change the `"game"` entry temperature from `0.7` to `0.4` (keep `"top_p": 1.0`). Add a brief comment: code correctness matters more than prose variety; value is inert if OpenRouter drops sampling for Opus-tier. Do NOT remove the game sampling key (sending temperature to Opus via OpenRouter is proven safe here — OpenRouter normalizes/drops unsupported params).

3. In `MAX_TOKENS_BY_AGENT`, ADD a new `"game": 24_000` entry with a comment explaining a full HTML game as a JSON-escaped string needs headroom (truncation -> schema retry -> broken games). Keep the existing `scout`/`researcher` entries.

Do NOT change any other agent's model, sampling, or token cap.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run python -c "from eisenbalm_pipeline.lib.llm_config import MODEL_BY_AGENT as MO, SAMPLING_BY_AGENT as S, MAX_TOKENS_BY_AGENT as MT; assert MO['game']=='anthropic/claude-opus-4-8', MO['game']; assert S['game']['temperature']==0.4, S['game']; assert S['game'].get('top_p')==1.0; assert MT['game']==24000, MT.get('game'); assert MO['origin_story']=='anthropic/claude-sonnet-4-6'; print('config ok')"</automated>
  </verify>
  <done>llm_config.py game entry = Opus 4.8, temperature 0.4 (top_p 1.0), max_tokens 24000; no other agent changed; the one-liner prints "config ok".</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the GameWriter system + user prompts</name>
  <files>packages/pipeline/src/eisenbalm_pipeline/prompts/game.md, packages/pipeline/src/eisenbalm_pipeline/prompts/game_user.md</files>
  <action>
Replace the body of `prompts/game.md` (locked decision 4). Keep the existing editorial header comment and the PROMPT START / PROMPT END markers exactly. Keep the three tokens {charity_name}, {VOICE_CONSTRAINTS}, {FORBIDDEN_CONSTRUCTS}. Write the file to EXACTLY this content (fenced block below is the file content, not including this sentence):

~~~markdown
<!--
  Eisenbalm Dispatch — GameWriter system prompt
  ⚠️  DO NOT DELETE any of these tokens — the pipeline fills them in automatically:
      {charity_name}       — the charity name for this issue
      {VOICE_CONSTRAINTS}  — the full Jesse voice rules block
      {FORBIDDEN_CONSTRUCTS} — the security deny-list of forbidden HTML/JS constructs
  You can move tokens around, but deleting any one of them will break the game output.
  Edit only the prose between PROMPT START and PROMPT END.
-->
<!-- PROMPT START -->
You are the GameWriter for The Eisenbalm Dispatch. You design one small, genuinely fun browser game that lives on a magazine page. The reader is not a captive user — they are flipping through an issue, and your game has to earn the ~90 seconds it asks for. Make something a person would choose to finish and then mention to a friend.

THE GAME MUST ENACT THE MISSION
- One mechanic, learnable in 5 seconds with no instructions. The player figures it out by doing.
- The mechanic must *be* what {charity_name} does — the player performs, in miniature, the charity's actual work. A river-cleanup charity has you pulling debris from a current; it does NOT have you catching generic falling blocks with the charity's name pasted on top. Reject theme-skins on generic games: if you could swap in any other charity without changing the mechanic, start over.
- Design the mechanic FROM the mission before you write a single line of code.

WINNABLE, AND THE WIN MEANS SOMETHING
- A reachable win state within 90 seconds of play. Target 60–90 seconds start to finish.
- Also a lose state (or a countdown timer) — stakes make the win worth reaching.
- The win screen states one true fact about {charity_name}, drawn from the mission text. Not flavor — a real detail the reader now knows.
- Always offer a restart control after a win or a loss.

GAME FEEL (this is what separates fun from a tech demo)
- Every input produces immediate visual feedback — motion, a color change, a number ticking. Nothing the player does should feel ignored.
- Show progress or score at all times, so the player always knows how close they are.
- Ramp difficulty slightly as play continues; the last ten seconds should feel harder than the first ten.
- Motion is smooth (CSS transitions or requestAnimationFrame) — never a stutter of instant jumps.

VISUAL BAR (no "AI slop")
- Choose an intentional, cohesive palette and typography that suit the charity's subject. No default system-gray boxes, no unstyled buttons, no Times New Roman.
- Set your own page background — the frame behind you is plain white, so paint it.
- Fill the space: look deliberate from 320px up to 1180px wide and at least ~420px tall. Stay playable and legible at 380px width.
- Both keyboard and touch must work. Touch targets are at least 44px.

VALIDATOR TRAPS (read this twice — a single stray character can kill the whole game)
Before your game is published, the page re-scans your ENTIRE document as raw text — comments and strings included — for forbidden character sequences. One hit anywhere replaces your game with an error box. So:
- Never write the character sequence "top." — not even in a DOM read. Reading an element's top edge as rect.top is fine; chaining rect.top.toFixed(0) is NOT (it contains "top."). Copy it to a variable first: const y = rect.top; then use y.toFixed(0).
- Never write "parent." — avoid chains that put a dot right after the word "parent", and never reference the parent frame at all.
- Never write any of these anywhere, including comments: fetch(, XMLHttpRequest, document.cookie, localStorage, eval(, import(, window.parent, window.top, document.domain.
- No external anything: no external <script> with a src, no external <link> stylesheet, no web fonts, no CDN, no network calls. If it is not inline in your one document, it does not exist.

OUTPUT CONTRACT
- Emit ONE complete HTML document: <!doctype html> through the closing html tag.
- All CSS in a single inline <style>. All JS in a single inline <script> with no src attribute.
- No console errors. No placeholder TODOs, no "insert art here". It must run exactly as returned.
- Before you emit, trace one full playthrough in your head: load → learn the mechanic → play → hit the win (and the lose) → read the fact → restart. If any step is unclear or unreachable, fix it before returning.

VOICE CONSTRAINTS (apply to the headline and every word of in-game copy):
{VOICE_CONSTRAINTS}

{FORBIDDEN_CONSTRUCTS}
<!-- PROMPT END -->
~~~

Then update `prompts/game_user.md` (locked decision 5): keep the header comment, markers, both tokens, and the GameOutput JSON contract; add one line telling the model to design the mechanic from the mission before writing code. Write the file to EXACTLY this content:

~~~markdown
<!--
  Eisenbalm Dispatch — Game USER-message template
  ⚠️  DO NOT DELETE the {charity_name} / {mission_statement} tokens — the pipeline
      fills them with the winning charity's name + mission statement.
  Edit only the prose between PROMPT START and PROMPT END.
-->
<!-- PROMPT START -->
CHARITY: {charity_name}
MISSION: {mission_statement}

Design the core mechanic from the mission above BEFORE writing any code — the player should perform, in miniature, what this charity does.

Return JSON GameOutput with: headline (game title), description (50-100 word plain-text summary for accessibility), embedCode (complete self-contained HTML document including inline <style> and inline <script> — no external dependencies of any kind).
<!-- PROMPT END -->
~~~

Notes for the executor:
- The two ~~~ fences are plan delimiters — do NOT write them into the files. Each file starts at the `<!--` header and ends at the `<!-- PROMPT END -->` line.
- Do NOT touch the in-code FORBIDDEN_CONSTRUCTS constant in agents/game.py — it is injected via the {FORBIDDEN_CONSTRUCTS} token and asserted verbatim by test_game.py.
- The instructional mentions of "top.", "parent.", localStorage, fetch(, etc. inside game.md are intentional and safe: the renderer validator scans generated GAME output, never this prompt file.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run python -c "from eisenbalm_pipeline.lib.prompts import load_prompt as L; g=L('game'); u=L('game_user'); assert all(t in g for t in ('{charity_name}','{VOICE_CONSTRAINTS}','{FORBIDDEN_CONSTRUCTS}')), 'missing system token'; assert all(t in u for t in ('{charity_name}','{mission_statement}')), 'missing user token'; assert 'mechanic' in g.lower() and 'validator' in g.lower(); assert g.strip() and u.strip(); print('prompts ok')" && uv run pytest tests/agents/test_game.py tests/test_package_data_prompts.py "tests/test_prompt_version_seeds.py::test_user_template_seed_byte_equivalence[game_user]" -q</automated>
  </verify>
  <done>game.md and game_user.md rewritten with all tokens intact and PROMPT START/END markers preserved; the token-presence one-liner prints "prompts ok"; test_game.py, test_package_data_prompts.py, and the game_user byte-equivalence test all pass.</done>
</task>

<task type="auto">
  <name>Task 3: Best-effort Convex v4 prompt-version write (else manual handoff)</name>
  <files>(no repo files — Convex data writes only; SUMMARY handoff on failure)</files>
  <action>
Save the new prompt content to Convex as INACTIVE versions so Andrew can Rehearse/activate them in Prompt Lab (locked decision 6). This is BEST-EFFORT — the mutations require Clerk operator identity and may reject the CLI; do NOT fight auth failures.

Steps:
1. From the repo `convex/` directory, extract the EXACT content `load_prompt()` returns (i.e. the text between the PROMPT START/END markers, as the pipeline consumes it — NOT the raw file with header/markers). Get it with:
   `cd packages/pipeline && uv run python -c "from eisenbalm_pipeline.lib.prompts import load_prompt; print(load_prompt('game'))"` and again for `game_user`. Capture each to a scratch file to build the JSON args.

2. Attempt the three writes (agentKey "game" system prompt, agentKey "game_user" user template, then agents:upsert for dashboard display):
   - `cd convex && npx convex run promptVersions:saveVersion '{"workspace_id":"eisenbalm","agentKey":"game","content":"<load_prompt(game) output>","note":"v4: Opus 4.8 game-design brief — game feel, visual bar, validator-trap guards (quick-260711-iu2)"}'`
   - `cd convex && npx convex run promptVersions:saveVersion '{"workspace_id":"eisenbalm","agentKey":"game_user","content":"<load_prompt(game_user) output>","note":"v4: mechanic-from-mission instruction (quick-260711-iu2)"}'`
   - `cd convex && npx convex run agents:upsert '{"workspace_id":"eisenbalm","agentKey":"game","enabled":true,"model":"anthropic/claude-opus-4-8","temperature":0.4,"top_p":1.0,"max_tokens":24000}'`
   If `npx convex run` supports an `--identity '{"subject":"quick-260711-iu2"}'` flag for the operator gate, include it. Use the dev deployment (dev:modest-magpie-797) admin key that `npx convex run` uses by default.

3. saveVersion intentionally creates the next version INACTIVE — that is the desired end state (Andrew reviews/Rehearses/activates in Prompt Lab; activation is eval-gated). Do NOT attempt to activate.

4. If ANY mutation throws Unauthorized / the CLI rejects `--identity` / auth blocks the write: STOP fighting it. Record in the SUMMARY a "Manual follow-up for Andrew" section containing, verbatim and paste-ready: (a) the exact `load_prompt('game')` output to paste into Prompt Lab as the new game system version, (b) the exact `load_prompt('game_user')` output for the game_user version, and (c) a note to set the game agent's model to anthropic/claude-opus-4-8 in the dashboard. This manual-handoff outcome is an acceptable success state.

Do NOT modify any Convex function code (`convex/*.ts`) — data writes only, no deploy.
  </action>
  <verify>
    <automated>cd convex && (npx convex run promptVersions:listVersions '{"workspace_id":"eisenbalm","agentKey":"game"}' 2>/dev/null | grep -qi "quick-260711-iu2" && echo "convex v4 saved") || echo "convex write blocked — MUST be recorded as manual handoff in SUMMARY"</automated>
  </verify>
  <done>Either: a new INACTIVE game (and game_user) prompt version tagged "quick-260711-iu2" exists in Convex AND the game agent row shows model anthropic/claude-opus-4-8; OR the write was auth-blocked and the SUMMARY contains a paste-ready manual-handoff section with the exact load_prompt outputs for both agentKeys plus the model-change note.</done>
</task>

</tasks>

<verification>
Full regression before declaring done:

    cd packages/pipeline && uv run pytest tests/ -x -q

Focused re-check of the assets touched:

    cd packages/pipeline && uv run pytest tests/agents/test_game.py tests/test_package_data_prompts.py tests/test_prompt_version_seeds.py tests/lib/test_config_loader.py -q

Expected: all pass. No test binds "game" to a specific model string (verified during planning), so the Opus 4.8 bump does not break the mocked real-mode or model_versions assertions.
</verification>

<success_criteria>
- lib/llm_config.py: game = anthropic/claude-opus-4-8, temperature 0.4 (top_p 1.0), max_tokens 24000; every other agent unchanged.
- prompts/game.md: game-design brief (mission-enacting mechanic, winnable-with-meaning, game feel, visual bar, validator traps, output contract) with {charity_name}, {VOICE_CONSTRAINTS}, {FORBIDDEN_CONSTRUCTS} tokens and PROMPT START/END markers intact.
- prompts/game_user.md: mechanic-from-mission line added; {charity_name}, {mission_statement} tokens and GameOutput JSON contract intact.
- Full pipeline test suite green (no regressions).
- Convex v4 INACTIVE versions saved for game + game_user (and game agent model display updated), OR an exact paste-ready manual-handoff recorded in SUMMARY for Andrew.
- apps/web, the game validator, GameSlot, Convex function code, and all other agents are untouched.
</success_criteria>

<output>
After completion, create `.planning/quick/260711-iu2-game-section-quality-opus-4-8-gamewriter/260711-iu2-SUMMARY.md`.
If Task 3's Convex write was auth-blocked, the SUMMARY MUST include a "Manual follow-up for Andrew" section with the verbatim load_prompt('game') and load_prompt('game_user') outputs to paste into Prompt Lab, plus the game-model change note.
</output>
