---
quick_id: 260718-00i
type: quick
title: Finish Phase 34 studio-bypass-retirement — collapse legacy publish surface
tasks: 3
wave: 1
autonomous: true
files_modified:
  - packages/pipeline/src/eisenbalm_pipeline/api/control.py
  - packages/pipeline/tests/test_publish_bridge.py
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SchedulePublishDialog.tsx
  - apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/DecisionRail.tsx
  - apps/dispatch-control/lib/issueRouteResolver.ts
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/page.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/_components/ReviewDecisionPanel.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/_components/SchedulePublishDialog.tsx
  - apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/ReviewQueue.tsx
  - apps/dispatch-control/__tests__/publishNoTypedConfirm.test.ts
  - apps/dispatch-control/lib/reviewClient.ts
source_audit: .planning/ui-reviews/dispatch-control-20260717-UI-REVIEW.md

must_haves:
  truths:
    - "POST /issues/{run_id}/publish-manual returns 409 missing_signoffs when either sign-off is absent, and never schedules _run_publisher in that case."
    - "The operator can Schedule-for-later from the Approval stage (DecisionRail), gated identically to Publish."
    - "Visiting /run-monitor/runs/[runId]/review server-redirects to /issues/[n]/approval (falls back to /issues when the runId is unresolvable)."
    - "The 'Review →' CTA in the Awaiting Review queue leads to the Approval stage, not the retired panel."
    - "No dangling references to ReviewDecisionPanel remain; dispatch-control build passes."
  artifacts:
    - path: "packages/pipeline/src/eisenbalm_pipeline/api/control.py"
      provides: "Two-sign-off gate on publish_manual (mirrors review.py:118-133)"
      contains: "signOffs:activeByRunId"
    - path: "packages/pipeline/tests/test_publish_bridge.py"
      provides: "RED test asserting 409 missing_signoffs on ungated publish-manual"
      contains: "missing_signoffs"
    - path: "apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SchedulePublishDialog.tsx"
      provides: "Ported schedule dialog co-located with DecisionRail"
    - path: "apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/page.tsx"
      provides: "Server-side redirect to Approval stage"
      contains: "legacyRedirectTarget"
  key_links:
    - from: "control.py::publish_manual"
      to: "signOffs:activeByRunId"
      via: "_cc.convex_query before scheduling _run_publisher"
      pattern: "signOffs:activeByRunId"
    - from: "DecisionRail.tsx Actions"
      to: "scheduleIssue → POST /issues/{runId}/schedule"
      via: "ported SchedulePublishDialog, gated by publishDisabled"
    - from: "run-monitor/.../review/page.tsx"
      to: "/issues/[n]/approval"
      via: "legacyRedirectTarget('approval', pRun?.issueNumber)"
      pattern: "legacyRedirectTarget\\('approval'"
---

<objective>
Finish the Phase 34 "studio-bypass-retirement" that was left half-done. Phase 34
built the new two-sign-off publish gate and the correct Approval surface
(DecisionRail) but never retired the OLD publish surface. A whole-dashboard UI
audit (source_audit above) scored Experience Design 1/4 and traced a live
production outage: three weekly issues stuck in `awaiting-review` since
2026-07-08, zero sign-offs ever recorded, one operator who clicked Publish 3x
against an unresolvable 409 and gave up.

Three confirmed, file:line-traced defects — all fixed here, exactly as the
audit and the (LOCKED) user decisions specify. Nothing outside these three
fixes is touched.

Purpose: unblock the stuck issues, close a second (independently-found) ungated
publish path, and preserve the scheduling capability the retirement would
otherwise delete.
Output: a gated `publish-manual` endpoint (+ RED test), scheduling ported onto
the correct surface, and the legacy `/run-monitor/runs/[runId]/review` panel
collapsed into a redirect.

TASK ORDER IS LOAD-BEARING (sequential, one wave, atomic commit per task):
  Task 1 (backend security gate) FIRST — independent; a frontend problem must
    never be able to block the security fix.
  Task 2 (port schedule dialog) BEFORE Task 3 (retire) — so Task 3's deletion
    of the legacy panel can never strand the scheduling capability. This is a
    deliberate re-order of the orchestrator's suggested split (port-before-
    retire guarantees no capability-loss window and keeps every commit
    self-consistent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ui-reviews/dispatch-control-20260717-UI-REVIEW.md
@./CLAUDE.md

<interfaces>
<!-- Everything below was extracted from the current tree during planning.
     The executor should use these directly — no re-discovery needed. Line
     anchors are "current tree" and WILL shift as edits land; anchor on the
     quoted code, not the number. -->

REFERENCE GATE — packages/pipeline/src/eisenbalm_pipeline/api/review.py:118-133
(publish_issue). This is the canonical two-sign-off 409 to copy for Task 1:
```python
active = await _cc.convex_query(
    http, "signOffs:activeByRunId", {"runId": run_id}
) or {}
missing = [k for k in ("facts-cleared", "sounds-human") if k not in active]
if missing:
    raise HTTPException(
        status_code=status.HTTP_409_CONFLICT,
        detail={
            "reason": "missing_signoffs",
            "message": (
                "Both sign-offs (Facts cleared + Sounds human) are "
                "required before publishing."
            ),
            "missing": missing,
        },
    )
```

UNGATED PATH — control.py::publish_manual (~855-925). Current body (elided):
resolve `http`/`actor_id` → local-import `groq_query`/`_run_publisher` →
`groq_query(QUERY_ISSUE_BY_RUN_ID)` → `if not issue or not issue.get("_id"):
raise 404` (~893-897) → `_emit_audit(action="publisher.manual_restart")`
(~899-906) → `asyncio.create_task(_run_publisher(...))` (~908-916) → return.
control.py ALREADY imports `import eisenbalm_pipeline.lib.convex_client as _cc`
(line 35) and uses `_cc.convex_query(http, ...)` throughout; `asyncio`,
`HTTPException`, `status` are already imported. The gate is a drop-in.

BACKEND TEST HARNESS — packages/pipeline/tests/test_publish_bridge.py already
provides: `_build_app()` (mounts control_router, sets app.state.convex_http =
MagicMock(), background_tasks=set()), `_set_role_claims(monkeypatch,
role=..., sub=...)`, `_wire_publisher(monkeypatch, issue={...})` (returns
`fake_groq, pub_spy`), and `AUTH_HEADER`. The convex-query monkeypatch idiom
(from tests/test_review_endpoints.py) is:
```python
async def mock_convex_query(http, path, args):
    if path == "signOffs:activeByRunId":
        return {}                       # no active sign-offs
    return None
monkeypatch.setattr("eisenbalm_pipeline.api.control._cc.convex_query", mock_convex_query)
```
Both-active shape: `{"facts-cleared": {"actorId": "u", "signedAt": 1},
"sounds-human": {"actorId": "u", "signedAt": 1}}`.

CORRECT SURFACE — DecisionRail.tsx Actions section (~517-618). `publishDisabled`
is computed INSIDE the Actions IIFE (~521):
```ts
const publishDisabled =
  blockers.length > 0 || !factsActive || !humanActive || !!held || busy
```
DecisionRail already receives `runId`, `issueNumber?`, `held?` props and holds
`factsActive`/`humanActive` (from signOffs.activeByRunId) + `busy` state.

REDIRECT PATTERN TO MIRROR VERBATIM — voice-pass/[runId]/page.tsx and
review-desk/[runId]/page.tsx both do: `const { runId } = await params` →
`new ConvexHttpClient(url).query(api.pipelineRuns.byRunId, { runId })` →
`redirect(legacyRedirectTarget(<surface>, pRun?.issueNumber ?? null))`, with
`export const dynamic = 'force-dynamic'`.

ROUTING HELPER — lib/issueRouteResolver.ts::legacyRedirectTarget currently:
```ts
export function legacyRedirectTarget(
  surface: 'review' | 'voice',
  issueNumber: number | null | undefined,
): string {
  if (typeof issueNumber !== 'number') return '/issues'
  return surface === 'review' ? issueReviewHref(issueNumber) : issueVoiceHref(issueNumber)
}
```
`issueApprovalHref(n)` => `/issues/${n}/approval` (already exported).
`issueReviewHref` => `/issues/${n}/review` (the galley stage — NOT the publish
decision; do not map the legacy publish panel to it).

REVIEWQUEUE DATA — ReviewQueue.tsx reads `api.runs.listForWorkspace` and filters
to status "awaiting-review". CONFIRMED: `runs` table rows have NO `issueNumber`
field (schema.ts). To link to `issueApprovalHref(n)` the queue must resolve
issueNumber per-row via the PUBLIC `api.pipelineRuns.byRunId` query (returns the
full pipelineRuns row incl. `issueNumber`).

TRIPWIRE THAT WILL BREAK ON DELETION — __tests__/publishNoTypedConfirm.test.ts
hard-references ReviewDecisionPanel.tsx: `REVIEW_DECISION_PANEL` const (~49-52),
its entry in `PUBLISH_SURFACE_FILES` (~54), the "all three files exist" sanity
test (~70-74), and two dedicated `it()` blocks (~88-92 "has NO <input>", ~103-107
"gates on allSignedOff"). Its header comment (~22-24) explicitly EXCLUDES
"SchedulePublishDialog's date/time field" from scope — which is WHY the ported
schedule dialog must stay a SEPARATE component, never inlined into DecisionRail.tsx.

IMPORTER CONFIRMATION (grep, planning-time):
- ReviewDecisionPanel: imported ONLY by run-monitor/.../review/page.tsx (being
  gutted) + referenced by publishNoTypedConfirm.test.ts. No other importers.
- legacy SchedulePublishDialog (run-monitor/.../review/_components): imported
  ONLY by ReviewDecisionPanel (being deleted). Safe to delete after Task 2.
- ClaimsChecklist.tsx / PreviewIframe.tsx (same legacy dir): have external/test
  references — DO NOT delete them (out of scope; orphaned-but-harmless).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the two-sign-off gate to POST /issues/{run_id}/publish-manual (RED test first)</name>
  <files>packages/pipeline/tests/test_publish_bridge.py, packages/pipeline/src/eisenbalm_pipeline/api/control.py</files>
  <behavior>
    - RED (must fail against CURRENT ungated handler): with an Editor-in-chief
      claim and NO active sign-offs (signOffs:activeByRunId → {}),
      POST /issues/{run}/publish-manual returns 409 {reason:"missing_signoffs",
      missing:["facts-cleared","sounds-human"]} and NEVER schedules _run_publisher
      (pub_spy not called) and NEVER audits (publisher.manual_restart not fired).
    - GREEN companion: with BOTH sign-offs active, the endpoint reaches the
      existing 200 path (issue resolved, audit fired, _run_publisher scheduled).
    - Unchanged: 404 when no Sanity issue exists (gate sits AFTER the 404 check,
      so the existing 404 test needs no sign-off wiring).
  </behavior>
  <action>
    STEP 1 — Write the RED test BEFORE touching control.py. In
    packages/pipeline/tests/test_publish_bridge.py add
    `test_publish_manual_requires_both_signoffs(monkeypatch)`:
      - `_set_role_claims(monkeypatch, role="Editor-in-chief", sub="user_editor")`
      - `fake_groq, pub_spy = _wire_publisher(monkeypatch, issue={"_id":"issue-99","issueNumber":99})`
        (wiring groq to resolve an issue proves the CURRENT code would 200/schedule
        the publisher — that is what makes the assertion RED today).
      - monkeypatch a `mock_convex_query(http, path, args)` that returns `{}` for
        path `"signOffs:activeByRunId"` (else None) onto
        `"eisenbalm_pipeline.api.control._cc.convex_query"`.
      - spy `_emit_audit` on the control module (async fn appending to a list),
        matching the existing test_editor_404 pattern.
      - POST `/issues/run-nosignoff-001/publish-manual` with `AUTH_HEADER`.
      - Assert: status 409; `detail["reason"] == "missing_signoffs"`;
        `set(detail["missing"]) == {"facts-cleared","sounds-human"}`;
        `pub_spy.assert_not_called()`; audit list empty.

    STEP 2 — HARD RED GATE. Run ONLY the new test against the UNMODIFIED handler:
      `cd packages/pipeline && uv run pytest tests/test_publish_bridge.py::test_publish_manual_requires_both_signoffs -q`
    It MUST FAIL (current handler returns 200 / schedules publisher — no gate).
    If it PASSES here, STOP: the test is not exercising the gate — fix the test,
    do not proceed. Do NOT weaken it to pass.

    STEP 3 — Apply the gate in control.py::publish_manual. Insert the two-sign-off
    409 (copy review.py:118-133 verbatim — same reason/message/`missing` shape)
    AFTER the `if not issue or not issue.get("_id"): raise 404` block and BEFORE
    the `_emit_audit(...)` call. Placement rationale: after the 404 so a
    nonexistent issue still 404s (existing test unaffected); before audit+schedule
    so a blocked publish is neither audited as a restart nor schedules
    _run_publisher ("nothing silent" — we never audit an action that 409s). Use
    the already-imported `_cc` and `HTTPException`/`status`:
    ```python
    active = await _cc.convex_query(
        http, "signOffs:activeByRunId", {"runId": run_id}
    ) or {}
    missing = [k for k in ("facts-cleared", "sounds-human") if k not in active]
    if missing:
        raise HTTPException(
            status_code=status.HTTP_409_CONFLICT,
            detail={
                "reason": "missing_signoffs",
                "message": (
                    "Both sign-offs (Facts cleared + Sounds human) are "
                    "required before publishing."
                ),
                "missing": missing,
            },
        )
    ```

    STEP 4 — Repair the two existing tests that now traverse the gate so they
    still exercise the 200/normal path (add a `mock_convex_query` returning BOTH
    sign-offs for `signOffs:activeByRunId`, patched onto
    `eisenbalm_pipeline.api.control._cc.convex_query`):
      - `test_editor_invokes_publisher_and_audits` (keep all 200 assertions).
      - `test_sentinel_regression_resolves_editor` (keep the `!= 403` assertion;
        wire both sign-offs so it honestly reaches the normal path).
    `test_editor_404_when_no_sanity_issue` needs NO change (gate is after the 404).

    Do NOT change review.py, signoffs.py, webhooks.py, or any Convex function.
    Do NOT deploy: pushing to master auto-deploys the pipeline to Railway /
    dev:modest-magpie-797 — flag that as a HUMAN follow-up; the executor does not
    deploy.
  </action>
  <verify>
    <automated>cd packages/pipeline && uv run pytest tests/test_publish_bridge.py tests/test_review_endpoints.py -q && uv run pytest tests/ -q</automated>
  </verify>
  <done>The RED test failed against the pre-fix handler and passes after; the full pipeline suite is green; publish-manual 409s missing_signoffs (with a `missing` list) and skips _run_publisher when either sign-off is absent, matching review.py's gate exactly. Commit atomically: `fix(pipeline): gate publish-manual on two sign-offs (closes ungated publish path)`.</done>
</task>

<task type="auto">
  <name>Task 2: Port SchedulePublishDialog onto the Approval surface (DecisionRail)</name>
  <files>apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SchedulePublishDialog.tsx, apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/DecisionRail.tsx</files>
  <action>
    USER DECISION (LOCKED): PORT scheduling, do not drop it. `scheduleIssue`
    (reviewClient) and `POST /issues/{run_id}/schedule` (review.py) STAY as-is.

    STEP 1 — Create the co-located dialog
    apps/dispatch-control/app/(dashboard)/review-desk/[runId]/_components/SchedulePublishDialog.tsx
    by copying the legacy dialog VERBATIM from
    run-monitor/runs/[runId]/review/_components/SchedulePublishDialog.tsx (it
    already imports `scheduleIssue, ReviewApiError` from '@/lib/reviewClient' and
    keeps the `schedule_in_past` → "Choose a time in the future." branch). Keep it
    a SEPARATE component — do NOT inline its `<input type="datetime-local">` into
    DecisionRail.tsx (the publishNoTypedConfirm tripwire scans DecisionRail.tsx for
    `<input\b` and would false-positive; a `<SchedulePublishDialog .../>` reference
    does not match). Optional, low-risk polish: swap the dialog's neutral-* Tailwind
    classes for the rail's `var(--color-*)` tokens so it matches the surface; keep
    all copy and behavior identical.

    STEP 2 — Wire it into DecisionRail.tsx:
      - Add `import SchedulePublishDialog from './SchedulePublishDialog'`.
      - Add component-level state: `const [showSchedule, setShowSchedule] = useState(false)`.
      - Inside the existing Actions IIFE fragment (where `publishDisabled` is in
        scope), immediately AFTER the PublishPreviewDialog block, add a secondary
        "Schedule for later" button `disabled={publishDisabled}` that opens the
        dialog, and render `<SchedulePublishDialog runId={runId}
        onScheduled={(at) => { setShowSchedule(false); setActionMessage(\`Scheduled
        for ${new Date(at).toLocaleString()}.\`) }} onCancel={() =>
        setShowSchedule(false)} />` when `showSchedule`.
      - GATE IDENTICALLY TO PUBLISH: reuse the SAME `publishDisabled` derivation
        (`blockers.length > 0 || !factsActive || !humanActive || !!held || busy`)
        so Schedule and Publish enable/disable together. This is the strict reading
        of "gated identically to Publish": the server's schedule_issue enforces the
        same missing_signoffs gate, and an open error finding (blocker) also blocks
        the facts-cleared sign-off, so including blockers is consistent. Style the
        button as a secondary control (border, not the filled ink primary) so
        Publish stays the single primary CTA.

    Touch nothing else in DecisionRail (blockers/readiness/sign-off logic unchanged).
  </action>
  <verify>
    <automated>cd apps/dispatch-control && pnpm --filter dispatch-control build && pnpm --filter dispatch-control test</automated>
  </verify>
  <done>DecisionRail (Approval stage AND the standalone /review-desk mount) shows a "Schedule for later" action gated identically to Publish, opening the ported dialog that calls scheduleIssue → POST /issues/{runId}/schedule. `pnpm --filter dispatch-control build` passes (vitest does NOT type-check — build is the gate) and vitest passes, including publishNoTypedConfirm (DecisionRail.tsx still has no `<input>`). Commit atomically: `feat(dispatch-control): port schedule-for-later onto the Approval decision rail`.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the legacy /run-monitor/runs/[runId]/review publish surface</name>
  <files>apps/dispatch-control/lib/issueRouteResolver.ts, apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/page.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/_components/ReviewDecisionPanel.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/runs/[runId]/review/_components/SchedulePublishDialog.tsx, apps/dispatch-control/app/(dashboard)/run-monitor/runs/_components/ReviewQueue.tsx, apps/dispatch-control/__tests__/publishNoTypedConfirm.test.ts, apps/dispatch-control/lib/reviewClient.ts</files>
  <action>
    STEP 1 — Extend the routing helper (DECISION: extend, don't call
    issueApprovalHref directly). In lib/issueRouteResolver.ts widen
    `legacyRedirectTarget`'s `surface` union to `'review' | 'voice' | 'approval'`
    and map `'approval'` → `issueApprovalHref(issueNumber)`, keeping the
    `typeof issueNumber !== 'number' → '/issues'` fallback. Update its JSDoc.
    Rationale: the retired panel is a DECISION/publish panel whose true
    replacement is the Approval stage (mounts DecisionRail), NOT the galley
    `issueReviewHref` stage; centralizing it in the helper preserves the single
    "never a run-keyed URL / fall back to /issues" invariant the helper exists to
    guarantee — consistent with the review-desk and voice-pass redirects.

    STEP 2 — Gut run-monitor/runs/[runId]/review/page.tsx into a server redirect,
    mirroring voice-pass/[runId]/page.tsx VERBATIM (drop ALL of: PreviewIframe /
    ClaimsChecklist / ReviewDecisionPanel imports, the cost lookup, previewToken,
    workspace resolution, the two-column layout). Final file:
    `export const dynamic = 'force-dynamic'`; await params → resolve
    `api.pipelineRuns.byRunId` via ConvexHttpClient →
    `redirect(legacyRedirectTarget('approval', pRun?.issueNumber ?? null))`.

    STEP 3 — Delete the two now-orphaned legacy components (grep-confirmed no other
    importers): run-monitor/runs/[runId]/review/_components/ReviewDecisionPanel.tsx
    and .../SchedulePublishDialog.tsx. DO NOT delete ClaimsChecklist.tsx or
    PreviewIframe.tsx (they have external/test references — leaving them orphaned is
    harmless and out of scope). Before deleting, re-grep to confirm zero importers:
    `grep -rn "ReviewDecisionPanel" apps/dispatch-control --include='*.tsx' --include='*.ts'`
    must return nothing except the test edit in STEP 5.

    STEP 4 — Repoint the Awaiting Review CTA in
    run-monitor/runs/_components/ReviewQueue.tsx. Since `runs.listForWorkspace`
    rows carry NO issueNumber, extract a per-row child component (e.g.
    `AwaitingReviewRow({ run })`) defined in the same file that calls
    `const pRun = useQuery(api.pipelineRuns.byRunId, { runId: run.runId })` and
    computes `const href = pRun?.issueNumber ? issueApprovalHref(pRun.issueNumber)
    : \`/run-monitor/runs/${encodeURIComponent(run.runId)}/review\``. Change the
    `.map(run => ...)` to render `<AwaitingReviewRow key={run._id} run={run} />`,
    moving the existing card markup into the child. The `<Link>` now targets
    `href` (Approval when resolved; the now-redirecting legacy route as a safe
    loading/unresolved fallback — never a dead end). Import `issueApprovalHref`
    from '@/lib/issueRouteResolver'. Keep the "Review →" label (relabel optional).

    STEP 5 — Fix the tripwire that references the deleted panel:
    __tests__/publishNoTypedConfirm.test.ts. Remove the `REVIEW_DECISION_PANEL`
    const and its entry in `PUBLISH_SURFACE_FILES`, and delete the two
    ReviewDecisionPanel-specific `it()` blocks ("has NO <input>" and "gates on
    allSignedOff"). Keep the DecisionRail + PublishPreviewDialog assertions and the
    registry cross-check; trim the header comment's ReviewDecisionPanel mention.
    The tripwire's purpose survives for the two remaining publish surfaces.

    STEP 6 — De-slop the stale client copy (audit Copywriting finding) in
    lib/reviewClient.ts: update the top-of-file endpoint comment, the `publishIssue`
    JSDoc, and the `scheduleIssue` JSDoc so they document the current
    `missing_signoffs` contract (both sign-offs required) instead of the
    pre-Phase-34 `claims_not_signed_off` copy. Doc-only — no behavior change; do
    NOT alter the `_reviewFetch` reason-parsing or any function signature.
  </action>
  <verify>
    <automated>cd apps/dispatch-control && grep -rn "ReviewDecisionPanel" app components __tests__ lib | grep -v "no matches" ; pnpm --filter dispatch-control build && pnpm --filter dispatch-control test</automated>
  </verify>
  <done>/run-monitor/runs/[runId]/review is a server redirect to /issues/[n]/approval (falls back to /issues when runId is unresolved); ReviewDecisionPanel.tsx and the legacy SchedulePublishDialog.tsx are deleted with zero dangling references (grep clean); the Awaiting Review "Review →" CTA resolves to issueApprovalHref via per-row pipelineRuns.byRunId (safe fallback to the now-redirecting legacy route while loading); publishNoTypedConfirm is trimmed to the two surviving surfaces and passes; reviewClient docstrings reflect the missing_signoffs contract. `pnpm --filter dispatch-control build` passes (the hard gate) and vitest passes. Commit atomically: `fix(dispatch-control): retire legacy run-review publish panel — redirect to Approval`.</done>
</task>

</tasks>

<verification>
- Backend RED discipline honored: test_publish_manual_requires_both_signoffs FAILED against the pre-fix handler, PASSES after the gate. Full pipeline suite green (`uv run pytest tests/ -q`).
- Scheduling capability lives on DecisionRail BEFORE the legacy panel is deleted (Task 2 precedes Task 3) — no capability-loss window.
- No dangling ReviewDecisionPanel references anywhere (grep clean).
- `pnpm --filter dispatch-control build` passes after every frontend task (vitest does not type-check — build is the authoritative gate for Tasks 2 & 3).
- Redirect never returns a run-keyed URL (goes through legacyRedirectTarget; falls back to /issues when issueNumber is unresolvable).
</verification>

<success_criteria>
- publish-manual returns 409 missing_signoffs and skips _run_publisher when either sign-off is absent (mirrors review.py:118-133); a RED test proves it and the full pipeline suite is green.
- The Approval stage (DecisionRail) offers Schedule-for-later gated identically to Publish; POST /issues/{runId}/schedule and reviewClient.scheduleIssue are unchanged.
- Visiting the legacy /run-monitor/runs/[runId]/review server-redirects to /issues/[n]/approval; the Awaiting Review CTA leads to Approval, not the retired panel.
- ReviewDecisionPanel.tsx + legacy SchedulePublishDialog.tsx deleted; publishNoTypedConfirm trimmed and green; reviewClient docstrings corrected.
- Three atomic commits (one per task). Pipeline deploy to Railway / dev:modest-magpie-797 on push is a flagged HUMAN follow-up — the executor does not deploy.
</success_criteria>

<output>
After completion, create `.planning/quick/260718-00i-finish-phase-34-retirement-collapse-lega/260718-00i-SUMMARY.md`.
</output>
