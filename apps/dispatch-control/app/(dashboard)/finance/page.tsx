/**
 * Finance — Phase 27 (Stripe reconciliation, model_pricing table, usage projections).
 * Placeholder: renders section name + phase label until Phase 27 builds the real view.
 */
export default function FinancePage() {
  return (
    <div className="space-y-2">
      <h1 className="text-xl font-semibold text-neutral-900">Finance</h1>
      <p className="text-sm text-neutral-500">
        Stripe reconciliation, AI cost tracking, and model pricing — coming in Phase 27.
      </p>
    </div>
  )
}
