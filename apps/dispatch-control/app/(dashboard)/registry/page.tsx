/**
 * Registry — Phase 26 (review gate, charity registry, auto_publish, claims gate).
 * Placeholder: renders section name + phase label until Phase 26 builds the real view.
 */
export default function RegistryPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-xl font-semibold text-neutral-900">Registry</h1>
      <p className="text-sm text-neutral-500">
        Charity registry, review gate, and Scout dedup controls — coming in Phase 26.
      </p>
    </div>
  )
}
