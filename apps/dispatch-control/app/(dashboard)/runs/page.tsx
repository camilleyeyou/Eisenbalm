/**
 * Runs — Phase 23 (read-only dashboard views, real run data, agent_runs table).
 * Placeholder: renders section name + phase label until Phase 23 builds the real view.
 */
export default function RunsPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-xl font-semibold text-neutral-900">Runs</h1>
      <p className="text-sm text-neutral-500">
        Pipeline run history and live status — coming in Phase 23.
      </p>
    </div>
  )
}
