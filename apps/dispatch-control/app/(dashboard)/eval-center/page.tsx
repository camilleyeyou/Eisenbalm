/**
 * Eval Center — placeholder (Phase 30 D-02). Real screen lands in Phase 38
 * (golden scenarios, scoreboard time-series, drift detection).
 */
import PlaceholderScreen from '@/components/_PlaceholderScreen'

export default function EvalCenterPage() {
  return (
    <PlaceholderScreen
      title="Eval Center"
      phase="Phase 38"
      blurb="Improve the machine: golden scenarios, scoreboard time-series, drift detection."
    />
  )
}
