/**
 * Config — Phase 22 (config externalization, pipeline_config table, schedule/cost-cap toggles).
 * Placeholder: renders section name + phase label until Phase 22 builds the real view.
 */
export default function ConfigPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-xl font-semibold text-neutral-900">Config</h1>
      <p className="text-sm text-neutral-500">
        Pipeline configuration, scheduling, and cost-cap settings — coming in Phase 22.
      </p>
    </div>
  )
}
