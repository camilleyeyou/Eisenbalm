/**
 * Voice Pass — placeholder (Phase 30 D-02). Real screen lands in Phase 36
 * (machine-tells lit over clean prose, house-voice rewrites, sign-off).
 */
import PlaceholderScreen from '@/components/_PlaceholderScreen'

export default function VoicePassPage() {
  return (
    <PlaceholderScreen
      title="Voice Pass"
      phase="Phase 36"
      blurb="De-slop it: machine-tells lit over clean prose, house-voice rewrites, Sounds-human sign-off."
    />
  )
}
