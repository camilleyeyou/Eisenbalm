'use client'
/**
 * Stub — the full collapsed resolved-findings list with the Reopen action
 * (D-04) lands in Plan 33-05 Task 2. This placeholder exists so
 * DecisionRail's import resolves while Task 1 ships standalone.
 */
export default function ResolvedFindingsList(_props: { runId: string }) {
  return null
}
