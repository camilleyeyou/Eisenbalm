/**
 * Prompts — Phase 24 (prompt editing, versioning, diff, rollback; prompt_versions table).
 * Placeholder: renders section name + phase label until Phase 24 builds the real view.
 */
export default function PromptsPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-xl font-semibold text-neutral-900">Prompts</h1>
      <p className="text-sm text-neutral-500">
        Versioned prompt editor for each agent — coming in Phase 24.
      </p>
    </div>
  )
}
