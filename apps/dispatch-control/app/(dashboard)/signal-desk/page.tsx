/**
 * Signal Desk — placeholder (Phase 30 D-02). Real screen lands in Phase 37
 * (candidate slate, Gate 1 decision panel, interrupt adjudication).
 */
import PlaceholderScreen from '@/components/_PlaceholderScreen'

export default function SignalDeskPage() {
  return (
    <PlaceholderScreen
      title="Signal Desk"
      phase="Phase 37"
      blurb="Steer discovery: candidate slate, Gate 1 decision panel, interrupt adjudication."
    />
  )
}
