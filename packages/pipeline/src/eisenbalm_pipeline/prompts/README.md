# Agent Prompts

These are the canonical prompt files. They ship with the package automatically (same mechanism as `agents/qa/rubric.md`) — no extra packaging config needed.

**Editing instructions for Andrew:** see `apps/studio/PROMPT_EDITING_GUIDE.md`.

**One rule:** never delete or rename `{tokens}` — they are filled in by the pipeline at run time.
